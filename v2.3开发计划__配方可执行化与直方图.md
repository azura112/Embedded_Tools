# v2.3 开发计划__配方可执行化与直方图

> 起草日期：2026-09-12 ｜ 基线：v2.2（tag `v2.2` → commit `6ed9a83`，Release 已发布）｜ 状态：✅ 已交付归档（v2.3.0，2026-09-12；P4 两项全做，biquad 记 v2.4 候选，见交付文档）
> 上一版本：[v2.2] 31 模块 / 398 用例 × 2 几何 / selftest 20 上板 + PID 闭环真机走单 / CI 七 job 全绿（renode-smoke 首次真 0xFF）
> 输入：v2.2 遗留（① 板上 PID demo 为工程私有，交付明示"若进库归 examples/ 评估" ② API_GUIDE 11.x 配方均为纸面无载体 ③ et_sched 耗时统计只有 last/max，缺分布视图）；v3-candidates 池不变

---

## 1. 版本定位

- **版本号 v2.3**：冻结契约下纯增量 MINOR——新模块 et_hist + 三例自检式示例 + 架构文档，无破坏性变更。
- **本版主题**：**把"纸面配方"变成"可运行证据"**——API_GUIDE 11.x 的三个核心配方（闭环整定 / kv 备份恢复 / 升级流程）落地为 host 可跑、CI 守护、输出自检的示例；补上可观测性最后一块拼图（直方图分布）。
- **不变式**（继续遵守）：零动态内存、多实例句柄化、core/algorithm 层零硬件依赖、ISR-safe 范围显式标注、MINOR 只追加（`apidump --diff` 纯增门持续生效）。

## 2. v2.2 现状盘点

| 项 | 状态 |
|---|---|
| 模块 | 31 个，398 用例 × 2 几何（1K 399 / test-tab），覆盖 97.0%，docsync 210（含自指断言），sizecheck 3/3，CI 七 job 全绿 |
| 板面 | selftest 20/20 上板、PID 闭环真机走单（三组参数与 host 仿真逐值一致）、升级链回归全过 |
| 配方缺口 | API_GUIDE 11.10/11.11 等配方只有文字——用户要自己拼装才能验证"配方真的能跑"；examples/ 仅三个 demo（posix/stm32f103/stm32g474），无"配方级"示例 |
| 可观测缺口 | `et_stats` 有 min/max/均值/方差，`et_sched_task_stats` 有 last/max——**无分布视图**（"任务耗时集中还是长尾"答不了） |
| 治理 | apidump 基线 = v2.1（v2.3 P0 按滚动规则前滚至 v2.2）；checklist 第 5 条已放宽 |

## 3. 开发范围

### P0 治理常规（约 0.5 天）

| # | 任务 | 验收标准 |
|---|---|---|
| 1 | **apidump 基线滚动**：`--snapshot docs/API_INVENTORY_v2.2.md`（384 项比较单元），`--diff` 默认基线切至 v2.2，v2.1 归档保留 | 对 v2.2 基线 0/0 绿（发版点）；故意破坏仍红 |
| 2 | 版本宏升至 2.3.0 | 发布时与 tag 核对一致 |

### P1 `et_hist` —— algorithm/ 定容直方图（第 32 模块，约 1 天）

可观测性收尾：分布视图，与 et_stats（点估计）、et_sched_task_stats（单任务 last/max）互补。

```c
bool et_hist_init(et_hist_t *h, uint32_t *bins, uint32_t bin_count,
                  int32_t lo, int32_t hi);       /* [lo,hi] 等宽分 bin, bins 由调用方持有 */
void et_hist_push(et_hist_t *h, int32_t v);      /* O(1) */
uint32_t et_hist_count(const et_hist_t *h);      /* 总计数 */
uint32_t et_hist_bin(const et_hist_t *h, uint32_t i);
uint32_t et_hist_under / et_hist_over(const et_hist_t *h);  /* 越界计数 */
int32_t et_hist_percentile(const et_hist_t *h, uint8_t pct); /* 粗估(线性插值), 99=尾延迟 */
void et_hist_clear(et_hist_t *h);
```

**默认决议**（评审时间盒 0.5 天）：区间 `[lo, hi]` 等宽 `bin_count` 桶，**下含上含**（v 落在 hi 恰好入末桶——嵌入式判稳常用闭区间直觉）；`bin_count` ≥ 1 且 ≤ 255（uint8 索引）；越界进 under/over 计数器不丢弃；percentile 用桶内线性插值粗估（文档声明非精确分位数）；全部 uint32 计数、零分配；🏠MAIN 单上下文。

- 配方：`et_sched_task_stats` 采样喂 `et_hist` → `AT+TASKHIST` 式诊断（API_GUIDE 11.12），99 分位看长尾任务；
- 单测 ≥ 12 例：边界精确落入（lo/hi/桶界）、等宽计算、under/over、百分位插值手算序列、全零/单样本、clear、多实例、bin_count 边界（1/255/256 拒绝）、init 校验（lo≥hi/NULL）

### P2 配方可执行化 —— examples/ 三例（★ 核心，约 2 人日）

**自检式示例**：每个示例跑完输出"PASS/FAIL"判定（内置断言），`make ex` 一键全跑，CI 常设——配方的正确性从此由 CI 守护而非由文档维护者保证。

| # | 示例 | 配方载体 | 自检内容 |
|---|---|---|---|
| 1 | `examples/ex_pid_loop.c` | 11.10 闭环整定（v2.2 板上 demo 的 host 版收编） | 模拟对象 10ms 节拍闭环 3s：超调/settle 与预计算值区间断言；et_stats 判稳（方差阈值）+ et_hist 记录控制量分布 |
| 2 | `examples/ex_kv_backup.c` | 11.11 kv 备份恢复 | 虚拟 flash 上写 N 个 key → `kv_iter` 导出 → `xmodem_tx` 回环发送 → 接收端重灌新库 → 逐 key 比对一致 |
| 3 | `examples/ex_upgrade_flow.c` | 升级流程（xmodem + bootctl 组合） | 镜像经 xmodem 回环写入 → verify/stage → 模拟重启（re-init）→ attempt/confirm 与超次回滚双路径全走 |

- Makefile 新目标 `make ex`（三例顺序跑，任一 FAIL 即红）；CI 常设步骤；
- 每例头部注释 = 该配方的"可运行版"说明，API_GUIDE 配方节互链（"本配方可执行载体：examples/ex_*.c"）；
- 示例仅用公开 API + host port（不碰工程私有代码），作为 API 用法的外部视角检验；
- docsync 断言：三例文件存在 + `make ex` 在 Makefile + API_GUIDE 互链三处。

**本版固定三例**（防蔓延），新配方候选一律记 v2.4。

### P3 `docs/architecture.md`（约 0.5 天）

- 分层图（ASCII）+ 数据流（ISR→ringbuf→event→sched 主循环；测量→filter→PID→spwm）；
- **模块选型表**：按场景查模块（"要消尖峰→medfilt"、"要抗饱和控制→pid"、"要掉电参数→kv"）——31 模块后的导航需求；
- 与 getting-started（上手）分工：architecture 管"是什么/怎么选"，getting-started 管"怎么跑起来"。

### P4 可选（时间盒 0.5 天，不阻塞发布）

| # | 任务 | 说明 |
|---|---|---|
| 1 | bench v2.3 行 | `hist push` / `percentile` 耗时 |
| 2 | 双精度 biquad 评估 | 记 **v2.4 候选**（定点 Q15 双二阶，滤波组深化）——只评估立项条件，不实现 |

## 4. 交付标准（DoD）

1. 沿用 v2.2 全部 DoD（docsync 全绿 / 双几何 / docbuild / sizecheck / `--diff` 纯增门 / 量化声明附环境注记）；
2. **三示例全部自检通过并进 CI**：`make ex` 绿是发布硬性项；示例断言只对**确定性数值结果**（定点仿真无抖动），不得对耗时断言（`clock()` 粒度教训沿袭）；
3. 基线滚动生效：`--diff` 默认对 v2.2 基线；
4. 数字回刷：examples 数量、`make ex` 目标、docsync 计数、API_GUIDE 互链四处同步；
5. et_hist 的区间/百分位语义在头文件与 API_GUIDE 双处一致。

## 5. 里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 |
|---|---|---|
| M1 | P0 基线滚动 + 2.3.0 | D+0.5 |
| M2 | `et_hist` 合入 | D+1.5 |
| M3 | 三示例合入 + `make ex` + CI 绿 | D+3 |
| M4 | architecture.md + bench + 文档同步 | D+3.5 |
| M5 | 全量回归 → **tag v2.3** | D+4 |

## 6. 明确不做（Non-goals）

- 不做破坏性变更（v3 候选四项维持需求触发）；不做浮点（含浮点 biquad——定点评估记 v2.4）；
- **selftest 维持 20 套件**（medfilt/map/smap 沿 v2.2 决议由 host 单测覆盖，不为扩充而扩充）；
- 不做真实被控对象硬件、不做 FOC/自动整定、不做 i2c/spi/RTOS/动态内存/通配/多行编辑（永久边界，连续十一版）；
- 示例不做交互式（全部"跑完自检退出"，无人值守 CI 友好）；不做配置文件驱动（Makefile 硬目标）。

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 示例断言过紧导致 CI 间歇红 | 门禁疲劳 | 断言只对定点数值（host 确定性，无时钟抖动）；耗时类仅打印不断言（v1.7 DCE 教训的外推） |
| 示例随 API 演进腐化 | "可运行证据"变陈旧谎言 | 每例编译+运行进 CI 常规（每次 push 都验）；docsync 登记；示例只用公开 API（升级即编译错，天然暴露） |
| `et_hist` 区间/百分位语义争议 | 返工 | 预填决议 + 时间盒 0.5 天；percentile 声明"粗估"不做精确承诺 |
| 三示例工程量膨胀（各自引出小需求） | 时间盒失守 | 固定三例清单（Non-goals 明示）；示例内发现的新 API 需求记 v2.4 候选不现场加 |
| 板档期（本版无板依赖项） | — | 本版全部项无板上依赖——刻意选择，给板侧留喘息；PID demo 收编为 host 版即板侧成果复用 |

---

> 维护约定：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v2.3 后本文档归档不再改动。
