# v2.8 开发交付：冻结面基线滚动与门空隙收口

> 交付日期：2026-09-26 ｜ 版本 **2.8.0**（`0x020800`）｜ 分支 `dev/v2.8` → `main`
> 计划：`v2.8开发计划__冻结面基线滚动与门空隙收口.md`（2026-09-26，起草日实测基线 497 用例 / docsync 304 / 模块 34）
> 快照：`snap-2.8-start` = `e2100c4`（= G0 前置门提交，见 §3 偏差 1 的命名说明）

---

## 1. 交付摘要（三行以内）

治理版：冻结面基线滚动清偿（v2.6 归档补齐 + 发版点归档 v2.7 + 默认基线两轮滚动均 0/0 + 计划模板第⑥项根治）+ 门空隙收口（docref 清单逐 SHA 可达断言与白名单式判定、apidump 抗行尾契约化与双守卫、README 基线表述全量装门并升单一来源）。解析边界复评落档（`CO-7` 分支 B 论证维持、`CO-10` 裁决维持现状，代价矩阵入 API_GUIDE 5.7）；**库内代码零改动**，`apidump --diff` 两轮基线读数均 **新增 0 / 修改删除 0**。

## 2. AC 自查表（逐条 AC-1…AC-18，标注对应任务）

| AC | 结论 | 证据（文件 / 函数 / 命令 / 实测输出） |
|---|---|---|
| **AC-1**（G0-1） | **通过** | v2.7 交付文档（提交 `e2100c4`）：`grep -c "838edb1"` = **1**（仅 §4 修订注记的历史引述，`v2.7开发交付…md:101`）；§4 清单 = `e7c79f7`；§2 AC-20 与 §7.1 各含"v2.5"与"v2.6 归档缺失"，"对 v2.6 基线" = **0**；§7.3 含 `pass=25 fail=8` 与"基线即绿（回归钉）"标注、"pass=23 fail=10" = **0**；分支计数 **5612**（2 处）；实机记录 §15.3 表头含"会话累计值"；`v2.7评审记录__*.md` 已跟踪。**提交前** `sh tools/docref.sh` → **OK**（21/1288/104 逐值一致，时点证据在案）；**提交后** docref 转红 = `CO-6(v2.7)` 活体样本（§7.3） |
| **AC-2**（P0-1，HC-2） | **通过** | `docs/API_INVENTORY_v2.6.md` 在案（433 项 / 23200 B / **sha256 `cf57fb4b9df2d25f247d70f603ed143a639b3e2b5379267c333c9c52cc8e9f9e`**）；生成命令 = `git worktree add --detach build/wt-v26 v2.6` → 拷入**当前** `tools/apidump.sh` → `sh tools/apidump.sh --snapshot docs/API_INVENTORY_v2.6.md`（worktree 内，环境注记 §7.6）；与 v2.5 归档行尾归一比对 = **仅 `- \`ET_VERSION_MINOR 5\`` / `+ \`ET_VERSION_MINOR 6\`` 一行差**，item 级（`--diff` 判据）**新增 0 / 修改删除 0**；`git diff --stat snap-2.8-start..HEAD -- docs/API_INVENTORY_v2.[0-5].md` = **空**（v2.0~v2.5 归档零改动）；评审可按记载独立复现（sha256 对应 LF 规范形态，`git show v2.8:docs/API_INVENTORY_v2.6.md` 校验） |
| **AC-3**（P0-2，HC-1） | **通过** | P0-2 阶段 `grep -n "API_INVENTORY_v2.6.md" tools/apidump.sh` 命中默认基线行（当时 `:218`，P1-4 守卫插入后漂移至 `:227`，见 §3 偏差 8）；`sh tools/apidump.sh --diff` 对 v2.6 基线 → **新增 0 / 修改删除 0，exit 0**（15.7 s，§7.4 第一轮）；docsync 基线断言同批后（P3-4 起为单一来源动态断言）`grep -c "API_INVENTORY_v2.5" tools/docsync.sh` = **0**；v2.5 归档只读保留（git 零改动，见 AC-2） |
| **AC-4**（P0-3，CO-5） | **通过** | `grep -c "对 v2.0 基线" README.md` = **0**（`README.md:192` 改为"最近已发布 MINOR 的冻结基线（当前 `docs/API_INVENTORY_v2.7.md`）"——先 v2.6、发版点随 P0-5 同批滚至 v2.7）；docsync 交叉断言扩展为三重判定（逐行核对 + "API 冻结机检"行专守 + 行数下限 2，`tools/docsync.sh` "基线单一来源"前段）；**自证可红双形态**：红 A（改回无形态旧表述"对 v2.0 基线"）→ `FAIL … API 冻结机检行基线名 [缺失/无当前基线形态]`、exit 1；红 B（带形态但旧值 `v2.0.md`）→ `FAIL … 不符 1 行`、exit 1；还原 → 绿（§7.3） |
| **AC-5**（P0-4，HC-12） | **通过** | `docs/开发计划模板.md` §7 = **六项**（⑥"基线滚动"附 v2.6/v2.7 双版反例）；`docs/评审记录模板.md` 增 §3.2"基线滚动复核项"；v2.8 计划文末合规声明本即为六项（起草期先行遵守）；README checklist #10 同批"六项"（docsync 断言"核验清单六项"守护） |
| **AC-6**（P0-5，HC-1） | **通过** | `grep ET_VERSION et_config.h` → `0x020800` 注释与 `MINOR 8`；发版点 `docs/API_INVENTORY_v2.7.md` 在案（433 项 / 23200 B / **sha256 `3c8b158b0eabd5c245a7ad219b6dfa67b7604ad34f5adc6bd388695d5d336912`**，worktree 于 `tag v2.7` + 当前工具生成）；与 v2.6 归档比对 = 仅 `ET_VERSION_MINOR 6→7` 一行；`--diff` 对 **v2.7 基线** → **0/0，exit 0**（14.9 s，§7.4 第二轮）；`CHANGELOG.md` 含 `[2.8.0]` 节 |
| **AC-7**（P1-1，HC-3） | **通过** | `tools/docref.sh` 新增 §4 清单逐 SHA 断言（`git merge-base --is-ancestor <sha> <END>`；对本文档 §4 的 10 个 SHA 校验通过，见 §4）；**自证可红（真实形态）**：独立 worktree 检出 G0 前 v2.7 文档（清单含悬空 `838edb1`）→ `FAIL —— §4 清单含记录终点(v2.7)不可达的 SHA: 838edb1(终点不可达)`（§7.3）；终态本文档上的"临时写入悬空 SHA → 红 → 还原 → 绿"复验在 §7.3 附 |
| **AC-8**（P1-2，HC-3） | **通过** | 三场景红绿矩阵（独立 worktree `build/wt-ac8` 构造，冻结终点 `..v2.7`，证据全文见 §7.3）：**场景 A**（仅良性提交）→ `OK` + `提示 —— <END>(v2.7) 后有 1 个提交未计入 diffstat(清单)`；**场景 B**（触及 CHANGELOG 未登记）→ `FAIL —— <END>..HEAD 存在未经修订登记的实质变更`、exit 1；**场景 C**（触及交付文档无登记）→ FAIL、exit 1，（含修订登记 G0-1 型：R1 修订 + R2 纯登记行）→ `OK` + 提示、exit 0 |
| **AC-9**（P1-3，HC-3/HC-4） | **通过** | `item_lines()` 含显式 `tr -d '\r'`（`tools/apidump.sh:190-193`）；CRLF/LF 双副本真函数验证：**433/433 条目，diff 空**（命令 §7.3）；`--check`/`--diff` 对 v2.7 终态判定与修复前一致（0/0 exit 0 复跑在案）；红机制实证 = 管道链 od 定位（`\r` 在管道中存在而本机 sed 恒剥离）+ awk 同构正则实验（模式空间带尾部 `\r` → `$` 锚定 NO-MATCH = 非剥离实现下 0 条目） |
| **AC-10**（P1-4，HC-3） | **通过** | ① TMPD 回退告警在码（`tools/apidump.sh` Windows 分支 else 支）；红证据：`build/` 置为文件 → `WARN —— 仓库内临时目录 build/.apidump.1777 创建失败, 回退 mktemp -d…`；② 条目数守卫在码（默认生成路径）；红证据：空 HDRS 构造（六目录 + et_config.h + port/ 移出）→ `FAIL —— 生成条目数 0 < 参照清单 433 项的一半, 拒绝写入`、exit 1、现有清单 **sha256 未变**（`03214545…` 前后一致）；还原后正常路径 → `已生成 docs/API_INVENTORY.md (433 项)` exit 0 |
| **AC-11**（P2-1，CO-7） | **通过（分支 B）** | 二选一采**分支 B（论证维持）**：`docs/API_GUIDE.md` 5.7"解析边界裁决"段含**收益/风险/判据三要素**（收益 = 畸形主站即时 0x03；风险 = 重开板上认证过的成帧路径且本版无板级复验 + 大 bc 投机等长角例 + 新 crc_err 口径；判据 = HC-5 四前提 + 33 例矩阵零回退 + 板上 A/B 复验）；§8 与 `docs/v3-candidates.md`「v2.8 复评记录」登记"**维持 + 记 v3 候选**"；库内代码零改动（`et_modbus.c` 未动），33 例矩阵全绿 |
| **AC-12**（P2-2，CO-10） | **通过** | `docs/API_GUIDE.md` 5.7 含读帧 8 字节窗口**代价矩阵与裁决结论**：现状（整帧 8B 丢弃；受害形态 = 前导 `[本站地址][读 fc]` 2 字节伪造，主站超时重发可恢复，无静默错误）vs 替代（逐字节重同步 → 坏帧残字节可重组假帧 → 从站误执行写，风险更高）；**裁决 = 维持现状**；未翻转行为（无 HC-5 论证义务） |
| **AC-13**（P3-1，CO-11） | **通过** | `docs/API_GUIDE.md` 8.4 覆盖边界含"**板上 log 套件只验修饰面生效性（字符数），不校字符内容；内容级断言由 host 30 例承担**（`test/test_log.c` 实测 30 例）"；钩子半评估结论（必选 port 钩子 = 破坏既有 port 契约；可选 sink = 新公开面且价值低）记 §8 与 `v3-candidates.md`，**本版不实现** |
| **AC-14**（P3-2，CO-12） | **不做（显式声明）** | 板侧 `\n` 残留清理**未做**：评审已认可"保留证据原样"为合法选择；本版主线无板侧任务，板侧工程维持 v2.7 同步态（g474 README v2.8 行已注明"未重烧"）。声明同步于 §8 与 `v3-candidates.md`；触发 = 下一次板侧走单窗口（清理后须复跑确认 `disc=0` 基线并改写实机记录 §15.4 登记） |
| **AC-15**（P3-3/P3-4） | **通过（P3-4 做 / P3-3 显式不做）** | **P3-4 已做**（D-9）：docsync 基线断言改**单一来源取值**——基线名只存 `apidump.sh` BASE 一处，动态断言校验"默认基线归档存在且为自动生成 + 等于版本号最大归档"；红证据 = BASE 改回 v2.5 → `FAIL … ≠ 最新归档`、还原 → 绿（§7.3）；**P3-3 不做**（`CO-15(v2.6)` 第三次顺延）：触发条件（跨版性能判别力的实际诉求）仍未现，声明见 §8 + `v3-candidates.md`，记 v2.9 |
| **AC-16**（全版） | **通过（四门实测）＋ CI 推送后核验** | `sh tools/docsync.sh` → **`pass=306 fail=0`**（自指计数一致：本文档"本版 docsync：**306/306**"行 + architecture 机制门行 306；306 = 305 + 交付文档本体进入"覆盖率行"循环 +1）；`sh tools/apidump.sh --check` → exit 0；`--diff` 对 **v2.7 基线** → **0/0 exit 0**（14.9 s）；`sh tools/sizecheck.sh` → **pass=3 fail=0**（v2.8 表行 23880/24/740、36340/24/3016、24256/28/740 逐值实测）；`sh tools/docref.sh` → **OK**（白名单式；§4 与实测逐值一致）。**CI**：本提交即交付终态，推送 `main` + `tag v2.8` 后由 GitHub Actions 核验（`gh run list` / release.yml verify = 全量测试 + renode smoke `SELFTEST: 22/22`）——按"不实测不声称"纪律本行**不预写 run 号**，评审以 Actions 页核验（本地全量已在 §7.2 等价覆盖） |
| **AC-17**（全版，HC-4） | **通过** | `mingw32-make test` / `test-g4` = **497** ALL PASS；`test-tab` = **504**；`test-1k` = **498**（各 `grep -oE "pass=[0-9]+" 求和` 实测）；`mingw32-make ex` **五例 PASS**；`WERROR=1 test` / `WERROR=1 ex` 零诊断（严格匹配编译诊断 0 行；test 尾部唯一 grep 命中为用例名 `d_on_error_sp_kick` 假阳性）；覆盖率 **行 97.2%（10573/10872）、函数 99.3%（1066/1074）、分支 57.4%（5611/9778）**（`mingw32-make coverage-build` + `gcovr --print-summary`；较 v2.7 记录 10575/10877、5612/9780 总数 −5/−2 而三百分比不变，见 §5.2b）；`make test-asan`/`test-asan-1k` **CI 侧常设**（本机不可执行，维持声明，**未声称本机已跑**）；模块数维持 **34**（无模块增删，README 结构树与 apidump 清单一致） |
| **AC-18**（HC-11） | **通过** | 三落点逐条一致（差集为空）：计划 §1.1 ↔ `docs/v3-candidates.md`「v2.8 复评记录」↔ 本文 §8——含 `CO-7` 分支 B 结论、`CO-10` 维持现状结论、`CO-11`（明示半做/钩子半不实现）、`CO-12`（不做声明）、`CO-15(v2.6)`（第三次顺延）、D-9（已做）、`CO-11(v2.5)`（再次暂缓）的处置措辞三处同源 |

## 3. 偏差记录（与计划的每一处出入）

| # | 内容 | 原因 | 影响 |
|---|---|---|---|
| 1 | 快照 tag 命名用 **`snap-2.8-start`** 而非执行指令模板中的 `vX.Y-base` | 计划 §6 明文规定（不带 `v` 前缀；`v*` 命中 `release.yml` 已由 run `35378057777` 实测）；计划优先于通用模板 | 无；复核 `gh run list`/`gh release list` 无新 run/新增（§7.5） |
| 2 | G0-1 第⑤处修订（CO-13）落在 **`移植stm32实机记录.md` §15.3**，计划"涉及文件"列的是 v2.7 交付文档 | 交付文档无 §15 章节，§15.3 实际位于实机记录（交付文档仅引用之）；按 CO-13 意图修订真正落点 | 无；同一提交内完成，AC-1 核验以"§15.3 表头含累计值"通过 |
| 3 | G0-1 同提交入仓了 **`v2.8开发计划__*.md` 本体** | 计划涉及文件仅列交付文档与评审记录；但各版计划/评审记录均入库（`git ls-files` 实证），评审需要计划作 diff 基准 | 无；+1 文件入 G0 提交 |
| 4 | CO-4 的 5618→5612 实际改了 **2 处**（§2 AC-21 与 §7 复现表），计划表述"一行修订" | 该数字在交付文档出现两处，漏一处即留不一致 | 无；CO-4 意图（计数更正）完整达成 |
| 5 | P1-2 机制选型：**"修订登记 @SHA + 纯登记提交豁免 + 良性文件白名单"**（可裁量项 4 机制甲的 SHA 显式变体），未实现机制乙的 `--amend` 参数 | 甲路径无需运行时参数、行为可由 AC-8 矩阵完整判定；乙的显式推进动作与登记机制功能重叠 | 无；AC-8 三场景 + 反滞后底线 + G0 型可放行三前提全部满足 |
| 6 | AC-7 红证据主体采用**真实形态**（G0 前 v2.7 文档的悬空 `838edb1` 在独立 worktree 被新断言拦下）而非人为注入；终态文档上的"注入→红→还原→绿"作为补充复验 | 真实形态比注入更有说服力（正是 CO-1 的原始现场） | 无；两种形态均满足 HC-3 |
| 7 | P0-5b 提交（`ffb58aa`）时**漏了 README 两处基线名同批回刷**（v2.6→v2.7），docsync 基线名门随后拦截，随终态提交补齐 | 执行顺序疏忽；**门按设计工作**的实证（HC-7 同批义务由机检兜住） | 窗口极短（一个提交）且终态绿；如实登记 |
| 8 | AC-3 所引 `apidump.sh:218` 行号在终态为 `:227`（P1-4 守卫代码插入所致） | 行号随插入代码漂移 | 无；AC-3 核验的是"默认基线行命中"，行号非判据本体 |
| 9 | 计划环境注记的耗时（apidump 评审实测 5m35s/5m25s、docsync ~10 分钟）与本版实测（**--diff 15.7s/14.9s、docsync ~27s**）差约 20 倍 | 工具链/机器状态差异（v2.7 时代读数）；不影响任何判据，仅影响等待窗口预估 | 无；本版读数以实测为准 |
| 10 | 执行指令模板的快照复核项（"打完快照后复核无 run/无 Release"）在快照推送后执行时，`gh run list` 最新 3 条均为 v2.7 历史run | 属计划 §6 的既有复核动作，非偏离 | 无（列此备查） |

## 4. commit 范围

**记录范围定义**（HC-5）：本节记录 **`snap-2.8-start`（= G0 前置门提交 `e2100c4`，含 v2.7 五处随轮修订与评审记录/计划入仓）`..v2.8`** 的全部提交；**范围终点冻结为 tag 符号 `v2.8`**（v2.8 P1-2 起白名单式判定的推荐声明形态：终点 = 交付终态提交，`docref` 对终点存在性/等于 HEAD/数字一致三重校验即覆盖本提交）。数字以 `git diff --stat snap-2.8-start..v2.8` 的**实测**为准。

```
ffb58aa v2.8 P0-5(发版点): v2.7 归档 + 默认基线切 v2.7 + 体积表/机制门行回刷
0b6952a v2.8: v3-candidates 增「v2.8 复评记录」节 (HC-11 第二落点, 13 CO + D-9/CO-15(v2.6)/CO-11(v2.5) 逐条处置)
359ddff v2.8 P3-4 (D-9): docsync 基线断言改单一来源取值(滚动规则动态断言)
14f9bfe v2.8 P2-1/P2-2/P3-1: 解析边界裁决落档 5.7 + 板上 log 冒烟边界明示 8.4
26b75fb v2.8 P1-4: apidump 双守卫 (CO-9(v2.7)) —— TMPD 回退告警 + 生成条目数守卫
2f131cb v2.8 P1-3: item_lines 显式 tr -d '\r' 抗行尾 (CO-8(v2.7), 副作用升级为契约)
45fa4ba v2.8 P1-1/P1-2: docref 清单逐 SHA 可达性断言 + END==HEAD 迭代为白名单式判定
05fb0a3 v2.8 P0-4/P0-5(前半): 计划模板 §7 扩六项 + 版本宏 2.8.0 + CHANGELOG [2.8.0]
c3fe737 v2.8 P0-3: README 基线表述动态化 + docsync 交叉断言全量扩展 (CO-5(v2.7))
136b1bc v2.8 P0-1/P0-2: v2.6 冻结基线归档 + 默认基线切 v2.6 (CO-2(v2.7) 滞留清偿)
```

**清单不含本文档定稿提交自身**（自指不可预写）：其内容 = 本文档新增 + README 基线名/交付链接回刷 + CHANGELOG 数字定稿，由记录范围终点的 tag 符号 `v2.8` 与 docref 的终点/数字校验覆盖；G0 前置门提交 `e2100c4` = `snap-2.8-start` 本身（范围基点，其内容见 §2 AC-1）。

`git diff --stat snap-2.8-start..v2.8` = **17 files changed, 1941 insertions(+), 40 deletions(-)**

**分支与 tag（HC-9）**：本版库内/工具改动在 **`dev/v2.8`**，交付前 ff 合入 `main`；**`tag v2.8` = 交付终态 `HEAD`**，在下一版代码合入 `main` 之前定着并推送（`tag v2.8` → `release.yml`：验证门 = 全量单测 + renode smoke → Release 四附件）。

**CI**：本提交即终态，推送后以 `gh run list --branch main` 与 `gh run view <run> --json jobs` 核验七 job（unit-tests×2 / coverage / cross×2 / docbuild / renode smoke `SELFTEST: 22/22`）；本版**不把 run 号预写或回填入文档**——v2.7 的"回填 + amend"正是悬空提交（`838edb1`/`23c0d3f`）的成因（`CO-1(v2.7)`），自本版起以"终态推送 + Actions 在线核验"消除该形态。

## 5. 未完成项与新发现问题

### 5.1 未完成项（挂账，附理由与触发条件）

| 项 | 状态 | 理由 / 触发条件 |
|---|---|---|
| P3-2（`CO-12(v2.7)` 板侧 `\n` 清理） | **不做（显式声明）** | 评审认可保留证据原样；本版主线无板侧任务。触发 = 下一次板侧走单窗口（清理后复跑确认 `disc=0` 基线并改写实机记录 §15.4 登记） |
| P3-3（`CO-15(v2.6)` bench 相对比值化） | **第三次顺延** | 触发条件（出现"需要跨版性能回归判别力"的实际诉求）仍未现；口径改造会使历史读数失去可比性。记 v2.9 |
| `CO-11(v2.5)`（USART2 独立口/共享口地址不符静默的板侧验证） | **再次暂缓** | 硬件阻塞同前：bench 仅一路 USB-TTL。触发 = 第二路 USB-TTL 或双板对连 |
| `CO-7(v2.7)` 分支 A（双假设定长实现） | **记 v3 候选** | 评估结论 = 维持 v2.7 行为（§2 AC-11 三要素）；实现须 HC-5 四前提 + 板上 A/B 复验 |
| `CO-11(v2.7)` 钩子半（port.h 捕获钩子） | **不实现（评估完毕）** | 必选钩子 = 破坏既有 port 契约（三处 port 同步改）；可选 sink = 新公开面且价值低。触发 = 板上内容级 log 断言的实际诉求 |

### 5.2 新发现问题（供评审与下一版计划使用）

- **a. 工具门耗时与历史注记差约 20 倍**：本版实测 `apidump --diff` 15.7/14.9 s、`docsync` ~27 s，而计划环境注记载评审实测 5m35s/5m25s 与 ~10 分钟。不影响判据，建议 v2.9 计划注记按新值校准。
- **b. 覆盖率总数微移**：10573/10872 行、5611/9778 分支（v2.7 记录 10575/10877、5612/9780），三百分比不变（97.2/99.3/57.4）；与 sizecheck 的 text −4 B 同源（版本宏常量折叠级差异），非回归。
- **c. 一次瞬态门不一致**：P0-5b 后首次 `docsync` 内嵌 `apidump --check` 报清单漂移（宏计数头差异），未改任何文件直接复跑即绿，其后连续多次全绿。疑 Windows 文件可见性/AV 延迟，无代码影响；如再现建议在 `--check` 比对前加显式重读。
- **d. docsync 计数语义注记**：P3-4 后"基线"三字面量断言并为 1 条动态断言，pass 计数不再与"检查面"线性对应；评审解读 `pass=306` 时以断言分节注释为准。
- **e. 工作树常驻未跟踪项**：`.workbuddy/`、`.zcodeignore`（非本版范围，未处置、未入仓）。

## 6. 遗留风险

- **docref 白名单按文件名模式识别"良性提交"**：未来新增记录类文件若不匹配模式（如改名评审记录文件）会误红——失败方向为"红"（fail-safe），修正 = 扩模式或走修订登记；修订登记本身依赖作者自律（自指门的固有边界），评审方仍以 `git diff <END>..HEAD` 为最终权威。
- **apidump 条目数守卫阈值 ½**：当前参照 433 项、触发线 216 项，对合法生成有约 2 倍裕量；若未来公开面大幅精简至半数以下，须按可裁量项 6 重新校准阈值（守卫拒写时现有清单不被触碰，风险有界）。
- **`CO-7`/`CO-10` 裁决的时效性**：基于"畸形主站占比低 + 恢复需板级复验"的当前分析；若出现翻转性证据（如畸形主站真实占比数据、或板级复验窗口常态化），v3 分支 A 路径（双假设定长 + HC-5 四前提）已备妥，属**受控的已知路径**而非未知风险。
- **板侧版本宏滞后**：板侧工程未随本版重烧（零库内改动，有意为之），板上 `ET_VERSION_STRING` 仍报 2.7.0；下版板侧同步时一并消除，期间勿以板上版本号判别库态。

## 7. 机制自证与门读数

### 7.1 四门读数（本版终态）

> 本版 docsync：**306/306**

| 门 | 命令 | 结果 |
|---|---|---|
| 文档同步 | `sh tools/docsync.sh` | **`pass=306 fail=0`**（exit 0；~27 s） |
| 冻结面 | `sh tools/apidump.sh --check` / `--diff` | check **一致（exit 0）**；diff 对 v2.7 基线 **新增 0 项 / 修改删除 0 项（exit 0；14.9 s）** |
| 体积 | `sh tools/sizecheck.sh` | **pass=3 fail=0**（v2.8 行 23880/24/740、36340/24/3016、24256/28/740 逐值 = docbuild 实测） |
| §4 自指 | `sh tools/docref.sh` | **OK**（白名单式；清单 10 SHA 逐个 `merge-base --is-ancestor` 通过；diffstat 与本文 §4 声明逐值一致） |

### 7.2 全量回归读数

| 项 | 命令 | 结果 |
|---|---|---|
| 单元（默认 / G4） | `mingw32-make test` / `test-g4` | **497 / 497** ALL PASS（fail=0） |
| Tab 形态 / 1K 变体 | `mingw32-make test-tab` / `test-1k` | **504 / 498** ALL PASS |
| 配方载体 | `mingw32-make ex` | **五例 PASS**（pid_loop / kv_backup / upgrade_flow / modbus_slave / modbus_master） |
| 零警告 | `mingw32-make WERROR=1 test` / `WERROR=1 ex` | 编译诊断 **0 行**（ex 同）；test 尾部 grep 命中为用例名 `d_on_error_sp_kick` 假阳性 |
| 覆盖率 | `mingw32-make coverage-build` + `gcovr --print-summary` | 行 **97.2%**（10573/10872）、函数 **99.3%**（1066/1074）、分支 **57.4%**（5611/9778）——≥85% 门通过 |
| 内存门 | `make test-asan` / `test-asan-1k` | **CI 侧常设**；本机无 WSL/MSYS2、MinGW 无 libasan，**未声称本机已跑**（本版库内代码零改动，ASan 覆盖面与 v2.7 一致） |
| 模块数 | README 结构树 / apidump 清单 | **34**（无增删） |

### 7.3 机制自证表（HC-3：门须可红，逐项"先红后绿"）

| 门改动 | 红证据（构造 → 输出） | 还原 → 绿 |
|---|---|---|
| **P0-3** README 基线装门（三重判定） | 红 A：冻结机检行改回无形态旧表述 → `FAIL README 基线名与 apidump 默认基线不一致: 提取行 1 行(下限 2), 不符 0 行, API 冻结机检行基线名 [缺失/无当前基线形态]` exit 1；红 B：带形态旧值 `v2.0.md` → `FAIL … 不符 1 行, API 冻结机检行基线名 [docs/API_INVENTORY_v2.0.md]` exit 1 | 还原后 `ok README 基线名逐行核对一致 (2 行 ↔ apidump 默认 …)` |
| **P1-1** docref 清单可达性 | **真实形态**：独立 worktree 检出 G0 前 v2.7 文档（清单含悬空 `838edb1`）→ `FAIL —— §4 清单含记录终点(v2.7)不可达的 SHA: 838edb1(终点不可达)`；**终态复验**：本文档 §4 临时写入 `838edb1 demo` 行 → FAIL 并列出 → 移除 → OK（本次交付会话内执行） | 清单修正/移除后逐 SHA 校验通过 |
| **P1-2** docref 白名单式判定 | 场景 B：CHANGELOG 未登记提交 → `FAIL —— <END>..HEAD 存在未经修订登记的实质变更: temp: AC-8 场景B 构造(触及 CHANGELOG 未登记)` exit 1；场景 C-无登记：交付文档未登记提交 → 同型 FAIL exit 1 | 场景 A：良性提交 → `OK` + `提示 —— <END>(v2.7) 后有 1 个提交未计入 diffstat(清单)` exit 0；场景 C-含登记（R1 修订 + R2 纯登记行 `修订登记 @<R1-sha>`）→ `OK` + 提示 exit 0（AC-8 矩阵，独立 worktree `build/wt-ac8` 构造，终点冻结 `..v2.7`） |
| **P1-3** item_lines 抗行尾 | 机制实证：管道链 `od -c` 定位（`- \`abc\`\r` 的 `\r` 在管道中存在、本机 sed 恒剥离 = N-2 所述隐式依赖的直接实证）+ awk 同构正则实验：模式空间带尾部 `\r` → `$` 锚定 **NO-MATCH**（= 非剥离实现下 item_lines 0 条目 → `--diff` 误红），无 `\r` → MATCH | 修复后 CRLF/LF 双副本真函数 **433/433 条目 diff 空**（`sed 's/$/\r/' docs/API_INVENTORY_v2.6.md > build/inv_crlf.md` → `item_lines` 两副本排序比对）；`--diff` 复跑 0/0 |
| **P1-4** apidump 双守卫 | ① `build/` 置为普通文件 → `WARN —— 仓库内临时目录 build/.apidump.1777 创建失败, 回退 mktemp -d; …(CO-9(v2.7))`；② 空 HDRS 构造（core/algorithm/sys/protocol/drivers/debug/storage + et_config.h + port/ 移出）→ `FAIL —— 生成条目数 0 < 参照清单(docs/API_INVENTORY.md) 433 项的一半, 拒绝写入` exit 1，**现有清单 sha256 前后一致**（未被覆盖） | 头文件还原 → `apidump: 已生成 docs/API_INVENTORY.md (433 项)` exit 0 |
| **P3-4** 基线单一来源 | `apidump.sh` BASE 改回 v2.5 → `FAIL [基线单一来源] apidump 默认基线 docs/API_INVENTORY_v2.5.md ≠ 最新归档 docs/API_INVENTORY_v2.6.md (基线滚动规则未执行)` exit 1 | BASE 还原 → `ok 基线单一来源: apidump 默认基线 = 最新归档 …` |

**G0 提交的 CO-6 活体样本**：G0-1 提交（`e2100c4`）落地后、P1-2 落地前，`sh tools/docref.sh` 实测 `FAIL —— §4 声明与实测不符 (自指滞后)`（声明 21/1288/104 vs 实测 23/1692/104）——即 v2.7 评审所述"提交后任何提交使门红"的现场；P1-2 的冻结终点 + 白名单机制为本版对其的根治，未来轮次的同型场景走场景 C-含登记路径放行。

### 7.4 HC-1 两轮基线读数

| 轮 | 基线 | 命令 | 读数 |
|---|---|---|---|
| 第一轮（P0-2，`dev/v2.8` 中段） | `docs/API_INVENTORY_v2.6.md` | `sh tools/apidump.sh --diff` | **新增 0 / 修改删除 0，exit 0**（15.7 s） |
| 第二轮（P0-5 发版点） | `docs/API_INVENTORY_v2.7.md` | `sh tools/apidump.sh --diff` | **新增 0 / 修改删除 0，exit 0**（14.9 s） |

### 7.5 发布物（tag 与 Release）

- 快照 `snap-2.8-start`（`e2100c4`）推送后复核：`gh run list --limit 3` 最新为 v2.7 历史 run（无新 run）、`gh release list` 最新为 v2.7（无新增）——`snap-*` 无副作用再证。
- `tag v2.8` = 交付终态 `HEAD`（本提交），推送 → `release.yml`（验证门 = 全量单测 + renode smoke `SELFTEST: 22/22`）→ ARM ELF/BIN 四附件；CI 七 job 以 `gh run list --branch main` 核验（不回填 run 号入本文档，见 §4 CI 段）。

### 7.6 归档件生成环境注记（HC-2）

- 命令形态（v2.6 与 v2.7 归档同法，仅 tag 与目标文件名不同）：
  `git worktree add --detach build/wt-v26 v2.6` → `cp tools/apidump.sh build/wt-v26/tools/` → `cd build/wt-v26 && sh tools/apidump.sh --snapshot docs/API_INVENTORY_v2.6.md` → `sha256sum docs/API_INVENTORY_v2.6.md` → 拷回主树 `docs/` → `git worktree remove --force build/wt-v26`。
- 环境：Windows 10 / Git Bash（MSYS），`apidump.sh` 自锁 `LC_ALL=C`；生成产物为 **LF** 规范形态，sha256 以该形态为准（工作树因 `core.autocrlf=true` 检出为 CRLF，复现用 `git show <ref>:docs/API_INVENTORY_v2.6.md` 或行尾归一后比对）。
- 与上一版归档的逐行差异说明：v2.6↔v2.5 与 v2.7↔v2.6 均为**仅 `- \`ET_VERSION_MINOR n\`` / `+ \`ET_VERSION_MINOR n+1\`` 一行差**（item 级 0 增改）——v2.6/v2.7 两版公开面零变化的事实由本版归档双重机检确认。

## 8. CO 处置与候选池一致性（HC-11 三落点并置）

第一落点 = v2.8 计划 §1.1；第二落点 = `docs/v3-candidates.md`「v2.8 复评记录」；第三落点 = 本节。逐条措辞三处一致，差集为空：

| CO | 项 | v2.8 处置 | 触发条件 / 备注 |
|---|---|---|---|
| **CO-1(v2.7)** | §4 清单错指悬空提交 + docref 不核清单 | **已处置两半**：文本半 = G0-1（清单改 `e7c79f7` + 悬空注记）；装门半 = P1-1（清单逐 SHA 可达断言，红证据 = G0 前 v2.7 文档 `838edb1` 被当场拦下） | 门空隙（HC-5 残留面）已封死 |
| **CO-2(v2.7)** | 冻结面基线滞留 v2.5 跨两版 | **已清偿**：P0-1 补归档 v2.6（0 增改）+ P0-2 基线切 v2.6 + P0-5 发版点归档 v2.7 并再切 + P0-4 模板 §7 第⑥项（结构性根治） | 此后各版按六项起草 |
| **CO-3(v2.7)** | §7.3 复跑预期数字不准 | **已修**（G0-1）：实测值 `pass=25 fail=8` + 两例回归钉"基线即绿"标注 | 文本 |
| **CO-4(v2.7)** | 覆盖率分支计数 5618≠5612 | **已修**（G0-1，两处） | 文本 |
| **CO-5(v2.7)** | README:192"对 v2.0 基线"同族漏刷 | **已修并装门**（P0-3）：动态表述 + docsync 三重判定（自证可红双形态）；P3-4 再升单一来源 | 全库基线表述有门 |
| **CO-6(v2.7)** | docref `END==HEAD` 设计性红 | **已迭代**（P1-2）：白名单式判定（良性放行+提示；CHANGELOG/交付文档未登记必红；`修订登记 @<sha>` 放行 G0 型修订；AC-8 三场景矩阵在案；符号 `HEAD` 历史文档保持原严格语义） | |
| **CO-7(v2.7)** | `bc`/`qty` 不自洽 `0x10` 帧不回异常 0x03 | **分支 B（论证维持）**：收益/风险/判据三要素在 API_GUIDE 5.7；§8 与 v3-candidates 登记"维持 + 记 v3 候选" | **v3 候选**：双假设定长，须 HC-5 四前提 + 板上 A/B 复验 |
| **CO-8(v2.7)** | `item_lines` 依赖 sed 剥 CR 隐式行为 | **已修**（P1-3）：显式 `tr -d '\r'` 契约化；CRLF/LF 双副本 433/433 同集 | |
| **CO-9(v2.7)** | apidump TMPD 静默回退 + 无参数生成无守卫 | **已修**（P1-4）：回退告警 + 条目数守卫（<参照/2 拒绝写入），双红证据 | |
| **CO-10(v2.7)** | 从站读帧 8 字节窗口 | **裁决维持现状**（P2-2）：代价矩阵落 API_GUIDE 5.7（受害形态有界可恢复 vs 替代方案"误写"风险更高） | 除非翻转性分析，不再重复评估 |
| **CO-11(v2.7)** | 板上 log 套件不校字符内容 | **明示半已做**（P3-1：8.4 边界明示，内容级由 host 30 例承担）；**钩子半不实现**（必选钩子 = 破坏 port 契约；可选 sink = 新公开面价值低） | v3 破坏性窗口候选（评估过，不主动排期）；触发 = 板上内容级 log 断言诉求 |
| **CO-12(v2.7)** | 板侧 demo 命令行 `\n` 残留 | **本版不做（显式声明）**：评审认可"保留证据原样"为合法选择；本版主线无板侧任务 | 触发 = 下一次板侧走单窗口（清理后复跑 `disc=0` 基线并改写 §15.4 登记） |
| **CO-13(v2.7)** | §15.3 MBSLAVE 读数口径 | **已修**（G0-1）：实机记录 §15.3 表头注明"会话累计值，结论以增量对比为准" | 文本 |
| **D-9** | docsync 字面量断言改单一来源 | **已做**（P3-4）：基线断言升为"滚动规则"动态断言（基线名单一来源 = apidump BASE；自证可红） | P0-5 起切基线 docsync 零改动 |
| `CO-15(v2.6)` | bench 相对比值化（跨版第三次顺延） | **本版不做（显式声明）**：触发条件仍未现；口径改造使历史读数失去可比性 | 触发同前；记 v2.9 |
| `CO-11(v2.5)` | USART2 独立口/共享口地址不符静默的板侧验证 | **再次暂缓**（重新声明，不得静默消失） | 硬件阻塞同前：bench 仅一路 USB-TTL；触发 = 第二路 USB-TTL 或双板对连 |

**v2.8 新增破坏性候选：无。** 本版为治理版（归档/门/文档裁决），库内代码零改动（`CO-7` 采分支 B，`et_modbus.c` 未动）；契约面 `apidump --diff` 两轮基线读数均为**新增 0 项 / 修改删除 0 项**；语义面无新增登记项（v2.7 的 `crc_err`/`discarded` 口径维持）。
