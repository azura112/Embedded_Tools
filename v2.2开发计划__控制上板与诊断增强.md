# v2.2 开发计划__控制上板与诊断增强

> 起草日期：2026-09-11 ｜ 基线：v2.1（tag `v2.1` → commit `410b96b`，Release 已发布）｜ 状态：✅ 已交付归档（v2.2.0，2026-09-12；P4 未触发降级条款，P5 三项全做，见交付文档）
> 上一版本：[v2.1] 30 模块 / 382 用例 × 2 几何 / 冻结面纯增 27/0 机检通过 / 板侧零足迹（新模块被 GC）
> 输入：v2.1 遗留（① checklist 第 5 条分隔符过时 ② pid/stats/bytes 未进 selftest——发布期为保板端零变化的有意决策，v2.2 解除）③ 板上闭环缺真实使用 ④ apidump 基线滚动机制待首次执行；`et_task_t` 字段追加将是冻结附则"人工登记"流程的首次实战

---

## 1. 版本定位

- **版本号 v2.2**：冻结契约下的纯增量 MINOR——新模块 + 增量 API + 板上激活，无破坏性变更。
- **本版主题**：**让 v2.1 的控制链路真正上板**——pid/stats/bytes 进板上自测、shell 触发闭环 demo 在真机跑通"测量→滤波→控制→输出"，配一块缺失的滤波拼图（中值滤波）与调度可观测性（任务耗时统计）。
- **不变式**（继续遵守）：零动态内存、多实例句柄化、core/algorithm 层零硬件依赖、ISR-safe 范围显式标注、MINOR 只追加（`apidump --diff` 纯增门持续生效）。

## 2. v2.1 现状盘点

| 项 | 状态 |
|---|---|
| 模块 | 30 个，382 用例 × 2 几何（1K 383 / test-tab 8），覆盖 96.9%，docsync 186（含自指断言），sizecheck 3/3，apidump 冻结门在位 |
| 新模块状态 | et_pid/et_stats/et_bytes：host 单测全绿、**板端被 `--gc-sections` 丢弃（零足迹）**、未进 selftest——"有库无用"状态 |
| 板面 | CubeMX 已同步 v2.1，AT+SELFTEST 17/17（+2 SKIP）不变 |
| 遗留 | ① checklist 第 5 条仍写"全角冒号"，与 v2.0 起实际 `__` 惯例不符（v2.1 发布时已发现未改）② apidump 基线仍指 v2.0（`API_INVENTORY_v2.0.md`），需滚动机制 ③ 冻结附则"结构体字段追加须人工登记"流程零实战 |
| 候选池 | `docs/v3-candidates.md` 四项破坏性候选（需求触发才评，本版不碰） |

## 3. 开发范围

### P0 治理小件与基线滚动（约 0.5 天）

| # | 任务 | 验收标准 |
|---|---|---|
| 1 | **apidump 基线滚动**：`--snapshot docs/API_INVENTORY_v2.1.md` 固化 v2.1 基线；`--diff` 默认基线切至最新快照（规则：基线 = 最近已发布 MINOR），v2.0 归档保留 | `--diff` 对 v2.1 基线 0/0 绿；故意破坏仍红（自证沿用） |
| 2 | checklist 第 5 条更新：交付文档命名"全角冒号或 `__` 分隔符均可（v2.0 起实际惯例）"；docsync 如有冒号硬编码同步放宽 | docsync 全绿；自证不回归 |
| 3 | 版本宏升至 2.2.0 | 发布时与 tag 核对一致 |

### P1 板上自测扩充：selftest 17→20 套件（约 1 天，含板侧同步）

把"PC 全测、板上 GC"变为"板上冒烟"：

- **三套件进 `et_selftest`**（纯逻辑冒烟子集）：`pid`（固定增益阶跃 5 步，输出落入预计算区间）、`stats`（10 样本 min/max/mean 对拍）、`bytes`（BE/LE 往返 + 越界拒绝各 1）；
- **数字回刷全链清单**（P1 验收硬项，逐处核对）：`AT+SELFTEST` 计数（17→20）、smoke.sh 断言 grep（`SELFTEST: 17/17` → `20/20`）、实机记录、port README 体积表（selftest 变体增大）、交付文档；
- 板侧同步 + 上板复验：`AT+SELFTEST 20/20`（+2 SKIP）+ 既有升级链/SELFSTOR 回归不破；
- CI Renode smoke 断言同步（本机无 Renode 则以 CI run 为准，沿既有政策）。

### P2 板上闭环 demo：PID × 模拟被控对象（约 1 天，G474 上板）

真机验证"测量→滤波→控制→输出"组合链路（无真实被控对象，用软件一阶对象代替，文档明示）：

- shell 命令组：`AT+PIDSET <sp> <kp> <ki> <kd>` / `AT+PIDRUN <ms>` / `AT+PIDOUT`；
- 链路：10ms sched 节拍 → 模拟对象 `y += k(u−y)`（k 可设）→ `et_stats` 记录超调与稳态方差 → `AT+PIDOUT` 读摘要（超调 % / 稳定时间 / 方差）；
- **只做真机记录，不进 Renode smoke 断言**（沿"仿真不测时序"政策）；
- 实机记录新增 §10：三组参数（保守/激进/积分饱和对照）响应摘要——顺带验证 et_pid 抗饱和在真机时序下的行为；
- 该 demo 同时是 `API_GUIDE 11.10` 整定配方的板上实例（文档互链）。

### P3 `et_medfilt` —— algorithm/ 中值滤波（第 31 模块，约 1 天）

ADC 尖峰去除标配，与 movavg/lpf1/slew 并列：

```c
bool et_medfilt_init(et_medfilt_t *f, int32_t *win, uint32_t win_len);  /* 窗口奇数 3~15 */
int32_t et_medfilt_push(et_medfilt_t *f, int32_t v);                    /* 返回当前窗口中值 */
void et_medfilt_reset(et_medfilt_t *f);
```

- **默认决议**：窗口强制奇数（偶数 init 拒绝——避免"取上/下中位"歧义）；窗满前输出当前已入样本的奇数位中值（文档化语义）；实现用小窗口插入排序（≤15 元素 O(n²) 可接受，零分配）；
- 配方：尖峰场景 `medfilt → lpf1` 两级；与 movavg 对比表（尖峰抑制 vs 相位滞后）；
- 单测 ≥ 10 例：脉冲单点抑制（大尖峰不影响输出）、恒定输入、阶跃延迟窗/2、窗满前后语义、reset、多实例、init 校验（偶数/超长/NULL）

### P4 `et_sched` 任务耗时统计（增量 API，时间盒 1 天，可降级）

调度健康可观测：每任务 last/max 执行时长，板上诊断抖动/超时任务的第一手数据。

```c
void et_sched_task_stats(const et_task_t *t, uint32_t *last_ms, uint32_t *max_ms);  /* 只读 */
void et_sched_task_stats_reset(et_task_t *t);
```

- 实现：poll_once 内注入 `now` 前后差（不引入新时基依赖）；**`et_task_t` 追加字段 → 走 API_STABILITY 附则人工登记流程（冻结附则首次实战）**：`--diff` 保持绿 + 交付文档登记区填"字段/理由/破坏半径（调用方重编译即兼容）"；
- 单测 ≥ 6 例：耗时计量、max 保持、reset、未运行任务 0、多任务独立；
- **降级条款**：若登记流程或 ABI 影响评估超时间盒，整体挪 v2.3 并记录（不硬塞）。

### P5 可选（时间盒合计 0.5 天，不阻塞发布）

| # | 任务 | 说明 |
|---|---|---|
| 1 | kv 参数备份/恢复配方（纯文档） | `kv_iter` + `xmodem_tx` + shell 组合的导出/恢复流程——不动库代码 |
| 2 | bench v2.2 行 | medfilt push / sched stats 开销 |
| 3 | CRC16-MODBUS 查表 | `ET_CRC_TABLE` 第三表（256×2B），bench 对比入册 |

## 4. 交付标准（DoD）

1. 沿用 v2.1 全部 DoD（docsync 全绿 / 双几何 / docbuild 单源 / sizecheck / 量化声明附环境注记 / `--diff` 纯增门）；
2. **数字回刷全链**：selftest 17→20 的每一处引用（smoke.sh / 实机记录 / port README 体积表 / 交付文档 / API_GUIDE 平台表）同提交回刷——v1.9 教训的标准应用；
3. **基线滚动生效**：`--diff` 默认对 v2.1 基线，v2.0 归档只读保留；
4. 板上闭环记录 §10 落数（真机，含三组参数摘要）；selftest 扩充的上板复验记录；
5. `et_task_t` 字段追加（若 P4 保留）：附则人工登记完成 + `--diff` 绿 + 破坏半径声明。

## 5. 里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 |
|---|---|---|
| M1 | P0 基线滚动 + checklist 修正 + 2.2.0 | D+0.5 |
| M2 | P1 selftest 扩充 + 板同步 + smoke/数字全链回刷 | D+1.5 |
| M3 | P3 medfilt 合入 | D+2.5 |
| M4 | P2 板上闭环 demo + P4 时间盒（或降级） | D+3.5 |
| M5 | 全量回归 + 板面终验 → **tag v2.2** | D+4 |

## 6. 明确不做（Non-goals）

- 不做破坏性变更（v3 候选四项维持需求触发）；不做浮点 PID/统计（维持）；
- 不做真实被控对象硬件接入（模拟对象已足验证链路）；不做 FOC/自动整定；
- 不做 i2c/spi 抽象、RTOS、安全启动、动态内存、多行编辑/Tab 以外的 shell 增强（永久边界，连续九版）；
- smap 通配、u32 全键域维持 v2.0 终局关闭；medfilt 不做自适应窗口/加权中值。

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| selftest 计数变化牵动 ≥5 处数字 | 再现"数字滞后" | P1 验收硬项 = 回刷清单逐处核对（本版 DoD #2）；smoke 断言改动与 CI 联动验证 |
| 模拟被控对象时序在 Renode 失真 | 仿真结果误导 | 闭环 demo 只做真机记录，不进 smoke 断言（政策沿袭）；Renode 仅回归既有 selftest 断言 |
| `et_task_t` 追加字段是冻结附则首次实战 | 流程不熟导致冻结面误伤 | 严格按 API_STABILITY §5：登记区填项 + `--diff` 绿双证；时间盒到期即降级挪 v2.3 |
| medfilt 窗满前语义歧义 | 用户误用 | 预填决议文档化（奇数位中值）+ 用例钉死边界 |
| 板档期 | M2/M4 上板项受阻 | 拆分：库内改动（selftest 扩充代码/smoke 脚本）先行合入并标注"待板复验"；上板记录挂账沿既有模式，不阻塞 tag 以外的其他开发 |

---

> 维护约定：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v2.2 后本文档归档不再改动。
