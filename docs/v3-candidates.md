# v3.0 候选清单(v2.0 冻结审计遗留,破坏性窗口专用)

> 纪律:本清单是"触碰既有签名"项的**唯一去向**(v2.0 P1-3);攒到 v3 评审窗口
> 一次性处置。每项须给出破坏半径评估与迁移方案草案才可进 v3 计划。

## 来自 v2.0 API 冻结审计(docs/API_STABILITY.md §2)

| # | 项 | 破坏面 | 备注 |
|---|---|---|---|
| 1 | 回调 `user` 参数位置统一(现两派并存:首参派 sink/led/smap_visit/report 等 vs 末参派 atcmd/map_visit/list_visit/key_event 等) | 全部 20 个回调 typedef + 用户实现 | 建议统一 user **首参**(与句柄首参规约一致);迁移=机械替换 |
| 2 | `et_map_foreach` 回调形状与 `et_smap_foreach` 统一(槽指针 vs key+val 快照) | 2 个 visit 类型 | 若统一为快照+改值指针,`et_map` 的"改键置墓碑"删除路径需另给 `et_map_del_at` 类 API |
| 3 | `et_ringbuf_read/write` 与 `et_queue_put/get` 动词统一 | ringbuf 3 函数族 | 纯改名;评估收益低,默认**维持不改**,仅记录 |
| 4 | `et_stimer_poll` vs `et_sched_poll_once` 命名收敛 | 1 个改名 | 同 3 |

## 来自功能候选评估(v1.9 §5.4 / v2.0 P2 决议)

| # | 项 | 状态 |
|---|---|---|
| 5 | smap 通配/模式查找 | v2.0 决议:需求未证实,**关闭**;真需求出现再入表 |
| 6 | u32 全键域容器(键 0 可用) | v2.0 终局:由应用侧 `key+1` 偏移覆盖(FAQ),**关闭不立项** |
| 7 | shell 路径补全/多行编辑/历史搜索 | Tab 命令名补全 v2.0 已交付(`ET_SHELL_TAB`);其余**维持永久排除**,除非需求信号改变边界 |
| 8 | 定点 Q15 双二阶(biquad)滤波器 | **维持排队**(v2.5 复评完成, 2026-09-19): 立项条件 = 真实 IIR 型频响需求(陷波/高通截止/峰值 EQ)且 medfilt+lpf1 两级链不够用。**下次触发 = 用户提交具体频响指标**(截止频率/阻带衰减/采样率)。**判定依据**: ① 库内两个 IIR 形态需求(ADC 去噪/微分前滤波)均已被 et_lpf1 覆盖, 无二阶频响诉求; ② 截至 v2.5 复评时点仍无任何真实用例记录(示例/板上走单/问题记录中均未出现陷波/高通场景); ③ 条件未触发则不消耗版本容量(Non-goals)。实现预案: 直接 IIA 转置型 + int64 累加 + 系数 Q15, 语义与 et_lpf1 同族, **不做浮点变体** |

## v2.5 复评记录 (2026-09-19)

> 来源: `v2.5开发计划__Modbus主站补齐与日志规格解析加固.md` §1.1 CO 处置表。本仓历史上从未产出
> `v<X>.Y评审记录__*.md`, 故 v2.5 的 CO 清单由 v2.4 交付文档 §8 挂账 / §5 已知限制 / §1 过程自省
> 与 `移植stm32实机记录.md` §11 逐条提取(CO-1…CO-11)。v2.5 起评审记录载体建立
> (`docs/评审记录模板.md`), 后续版本按模板产出。

| CO | 项 | v2.5 复评结论 | 触发条件 / 备注 |
|---|---|---|---|
| CO-2 | USART2 独立串口(生产建议, 免分流歧义) | **暂缓**(维持) | 硬件阻塞: bench 仅一路 USB-TTL(CH343→COM12)。触发 = 获得第二路 USB-TTL 或双板对连; 届时单独立项 |
| CO-3 | Modbus 主站模式(轮询调度协同) | **纳入 v2.5 并已交付**(第 34 模块 `et_modbus_master`) | **边界**: 只做**单事务**状态机(一次一个在途请求), **不含多从站轮询表 / 不含调度器 / 不内部取时基** —— 调度归 `et_sched` + 应用(API_GUIDE 11.14 给出协作范式) |
| CO-4 | 定点 biquad(复评触发=具体频响指标) | **维持排队**(复评不通过) | 见上表 #8 行 |
| CO-5 | 单向/双向 32 位寄存器组合的**库级封装** | **拒绝(库级)**(维持) | 与 v2.4 既定原则一致: 库只管协议与访问控制, **值域语义属应用钩子**; 文档已覆盖约定(API_GUIDE 11.13 注③ 高低字顺序 + 11.13 补最小说明), 不新增实现 |
| CO-6 | 四项破坏性候选(回调 user 位置 / foreach 形状 / 动词统一 / 命名收敛) | **维持 v3 窗口** | 见本文件 §来自 v2.0 冻结审计 #1~#4; v2.5 为 MINOR, 不触碰 |
| CO-7 | selftest 套件 20 → 21 | **纳入 v2.5 P3**(时间盒 0.5 天) | 回刷清单为硬条件(HC-9); 与板档期冲突则不做并显式声明"维持 20" |
| CO-8 | `et_modbus_tick()` 静默路径应答调用侧漏 flush(文档范式未修) | **纳入 v2.5 并已修**(P1-10) | API_GUIDE 5.7 示例循环改为"tick 后复查并发送"; 正文明确**应答有两条产生路径**; `ex_modbus_slave` 增 tick 路径断言 |
| CO-9 | `et_log` 不支持域宽/补零: `%02x` 输出字面量**并错位消费后续实参** | **纳入 v2.5 并已修**(P1-1/P1-2, P0 ★) | 缺陷清偿: 完整吃掉规格序列 + 实参消费与 C 语义一致 + 域宽/补零/左对齐/精度子集 |
| CO-11 | 共享串口分流下"发往其它从站地址的帧"地址不符静默无法在板上验证 | **暂缓**(与 CO-2 同源) | v2.5 主站走单改用 **PC 从站仿真器**(`modbus_master.py --slave`), 不依赖该路径, 故不阻塞 |

CO-1(tag `v2.4` 待执行)与 CO-10(`tools/modbus_master.py` 自身三处 bug)在 v2.5 起草前**已关闭**
(v2.4 当轮修复并与 C 库组帧跨实现逐字节一致 `11 03 00 00 00 04 46 99`)。

## v2.6 复评记录 (2026-09-22)

> 来源: `v2.6开发计划__解析边界清偿与检出能力补强.md` §1.1(承接 `v2.5评审记录__*.md` 的
> `CO-1(v2.5)`…`CO-14(v2.5)`)。本节的**第三落点** = 同版交付文档 §8 的三处措辞并置对账
> (v2.5 缺该落点即评审不通过, v2.6 交付文档 §8 已补齐并三处可比对)。

| CO | 项 | v2.6 处置 | 触发条件 / 备注 |
|---|---|---|---|
| **CO-9(v2.5)** | `data()` 结果缓冲有效窗口是隐含契约(指向内部 RX 缓冲) | **拒绝入库 v2.6 → 维持 v3 窗口** | 改 `read_into(m,…, dst, cap)` 触碰既有签名, 属破坏性窗口(本文件首行纪律); v2.6 为 MINOR 不触碰。窗口已在 API_GUIDE 5.8 明示 |
| **CO-11(v2.5)** | USART2 独立串口(生产建议) / 共享串口分流下"发往其它从站地址的帧"地址不符静默的**板侧验证** | **再次暂缓(重新声明, 不得静默消失)** | 硬件阻塞同前: bench 仅一路 USB-TTL(CH343→COM12), 无法验证独立口。**触发 = 获得第二路 USB-TTL 或双板对连**; 届时单独立项(含 v2.4 `CO-2/CO-11` 原项的板侧验证) |
| #8 biquad | 定点 Q15 双二阶滤波器 | **维持排队** | 触发同前 = 用户提交具体频响指标(截止频率/阻带衰减/采样率); v2.6 期无新信号 |
| 32 位寄存器组合 | 单向/双向 32 位寄存器组合的**库级封装** | **维持拒绝(库级)** | 与 v2.4/v2.5 结论一致: 库只管协议与访问控制, 值域语义属应用钩子(API_GUIDE 11.13 已给约定) |

**v2.6 新增破坏性候选: 无。** 本版全部改动为内部实现修复(`CO-6` 主站定长合理性校验、
`CO-7` `%c` 域宽生效、`CO-8` `j/t/L` 消费)与治理门(`CO-12` 计划模板、`CO-13` host 零警告门 +
ASan 1K 变体、`CO-5` `§4` 自指门、`CO-3/CO-4` 跨文档计数门), 契约面预期
`apidump --diff` = **新增 0 项 / 修改删除 0 项**。唯一的既有语义收紧是
`et_modbus_master_stats_t.crc_err`(由"计入未成帧字节"改为"只计与在途事务严格同形的候选帧"),
属**行为修正**(字段未改, 签名未动), 已在交付文档 §2 登记区与 §3 偏差记录说明。
