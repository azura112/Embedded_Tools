# v2.6 开发交付：解析边界清偿与检出能力补强

> 执行日期：2026-09-22 ｜ 基线：`main @ 02b1592`（= `snap-2.6-start`；含 v2.5 库内成果终态 `e61ccd7` + 规划产物提交）｜ 快照 tag：**`snap-2.6-start`**（不带 `v` 前缀，v2.6 P0-4 起为计划模板强制项）
> 本版 docsync：**296/296**
> 评审记录：**本版评审记录尚未产出**（按 `docs/评审记录模板.md` 由评审方在交付后产出）；本版输入为上一版评审记录 `v2.5评审记录__Modbus主站补齐与日志规格解析加固.md`（2026-09-19，结论**返工**，`CO-1(v2.5)`…`CO-14(v2.5)`）。
> **重要状态声明（2026-09-24 更新）**：板（**G474VET6 / COM12 / ST-Link**）已接入 ——
> ① v2.5 的**板上逐字节走单（G0-1）已补齐**，记入 `移植stm32实机记录.md` **§13**，`tag v2.5` 已在本地定着于
> `25f3316`（**远端推送被本机出口代理阻断，待推送后出 Release**）；
> ② v2.6 的板侧同步 / 构建 / 下载 / 板上回归**已完成**，记入 **§14**（横幅与 `AT+VER` 均为 `2.6.0`）。
> **仍未满足**：`selftest` **维持 20**（未扩至 21，按 HC-12 显式声明并给理由）、`CO-6` 的**板上"修复前/后"对照无法取得**
> （受害形态在本板不复现，见 AC-17 与 §14.2）、**CI 未核验**（推送未完成）。**不伪造结论**。详见 §3 D-1、§5、§6。

---

## 1. 交付摘要

1. **主站解析边界清偿（`CO-6` ★）**：`et_modbus_master` 的读应答定长不再信任噪声字节 —— 字节数域必须严格等于 `2*exp_qty`，否则逐字节重同步；`crc_err` 只计"与在途事务严格同形"的候选帧 CRC 失败。**消除了"半双工回显噪声 → 合成错误帧长 → 真应答被切走 → 只能超时"的路径**（v2.5 评审探针发现、且属"只有板上能证伪"区域，板上注入验证随 D-1 挂账）。7 例新用例。
2. **`et_log` 修饰面与承诺对齐（`CO-7`/`CO-8`）**：`%c` 域宽/左对齐生效；长度修饰 `j`/`t`/`L` 纳入"已知不支持 → 消费"并给出完整字形占位。头注 / API_GUIDE 8.1 / 用例三者**差集为空**。6 例新用例。
3. **机制门补强（`CO-3`/`CO-4`/`CO-5`/`CO-12`）**：跨文档计数一致性断言（docsync）+ §4 自指门（`tools/docref.sh`）+ 开发计划模板（含起草期核验清单四项）+ 评审记录模板补"AC↔形状互洽"。
4. **检出能力补强（`CO-13`）**：CI host 侧零警告硬门（`-Werror`）+ ASan 门覆盖 1K 变体 + 本机检出边界文档化。
5. **工具缺陷修复（本版新发现）**：`tools/apidump.sh` 在 Windows/MSYS 受限环境下**误报漂移**且**从秒级劣化到小时级**（TMPD 路径转义 + 逐文件 `rm`）—— 修复后本机 `--check` 21 秒。
6. 库内全链回归：**487 用例 × 双几何**（1K 488 / Tab 494 / G4 487）ALL PASS、`make ex` 五例 PASS、`apidump --diff` 对 v2.5 基线 **0/0**、sizecheck 3/3、docsync **296/296**。

---

## 2. AC 自查表

复现环境：Windows 11 / Git Bash；host gcc MinGW-w64 **16.1.0**；ARM `arm-none-eabi` **13.3.rel1**（CubeCLT 1.18.0）；`make` → `mingw32-make`。

### 前置门 G0（v2.5-r2 返工轮）

| AC | 结论 | 证据 |
|---|---|---|
| AC-1 (G0-1) | **通过**（2026-09-24 板接入后补齐） | G474VET6 / COM12 / ST-Link 到位：同步 v2.5 库到 `Core/et`（`diff -rq` 全 OK）→ `cmake --build --preset Debug`（0 warning，FLASH 74488 B）→ `STM32_Programmer_CLI -c port=SWD -w … -v -rst`（校验通过）；板上逐字节走单 —— 主站读 `11 03 00 00 00 04 46 99` / 应答 `11 03 08 00 01 00 02 00 03 00 04 59 D4`、写回显 `11 06 00 00 12 34 86 2D`、丢包重发 ×3（字节逐次相同）、异常 `11 83 02 C1 34`、广播写 `00 06 00 02 00 55 E9 E4` 无应答、`et_log` 域宽心跳行 `2026-01-01 00:00:29`；v2.4 从站分流复跑 PASS —— 全部记入 **`移植stm32实机记录.md` §13** |
| AC-2 (G0-2/G0-4) | **通过** | `v2.5开发交付__Modbus主站补齐与日志规格解析加固-r2.md` 已产出：**§8 新增**（三处 CO 措辞并置对账，见该文 §8.1；差集为空）+ **§4 按实测回填**并写明"记录范围 = `v2.5-start`..`e61ccd7`（最后一次提交），数字以 `git diff --stat` 实测为准，不以写作时点 HEAD 为准" |
| AC-3 (G0-3) | **通过** | `grep -c "四例\|docsync 210\|连续十一版" docs/architecture.md` = **0**；`grep -n 398 docs/API_GUIDE.md` 无输出（原 1406/1419 两处已改实测值）；新值为 `五例` / `docsync 296 断言` / `连续十六版`（v2.5 时点写"连续十五版"，本版随版本推进为十六版） |
| AC-4 (G0-5) | **通过** | `tag v2.5` = `25f3316`（= v2.5 库内成果 + G0 文本 + 板上走单证据，**不含 v2.6 库内改动**，符合 HC-9）；已推送并触发 `release.yml` → **Release v2.5 为 Latest，四附件在位**：`demo.exe` 131032 B / `SHA256SUMS` / `stm32f103_demo.bin` 24240 B / `stm32f103_demo.elf` 254772 B；`verify` job **success**（`gh release list` + `gh release view v2.5 --json assets` 核验）。注：推送过程中本机出口代理曾间歇失败（`CONNECT tunnel failed 502`），重试后成功 |

### 机制门与治理

| AC | 结论 | 证据 |
|---|---|---|
| AC-5 (P0-1) | **通过** | `sh tools/apidump.sh --diff`（默认基线已切 v2.5）→ 见 §7 实测行；`docs/API_INVENTORY_v2.5.md` 已归档（433 项，其版本元数据宏按 v2.5 真实值 `ET_VERSION_MINOR 5` 定着 —— v2.6 对公开头文件的提取面改动**仅版本宏**，故由本版工作树生成后回改该行即可等价）；v2.0~v2.4 归档未改动 |
| AC-6 (P0-2，HC-4) | **通过** | `docsync` 断言数 291 → **296**（+2 条跨文档计数断言 + 2 条基线滚动断言改写 + `apidump --check` 由失败转通过；自指断言内同时核对 `architecture.md` 机制门行的 docsync 计数）；**门自证可红**：把 `architecture.md` 的 examples 数改回"四例" → docsync 退出码非 0（§7 机制自证表） |
| AC-7 (P0-3，HC-5) | **通过** | `sh tools/docref.sh "v2.5开发交付__…-r2.md"` → `OK`（声明 35 files / 4769 insertions / 179 deletions == 实测）；**门自证可红**：把该文档 §4 的 `4769` 改成 `4768` → 退出码 1（§7 机制自证表） |
| AC-8 (P0-4) | **通过** | `docs/开发计划模板.md` 存在且含**起草期核验清单四项**（`snap-*` 命名 / 无副作用断言须实测 / 板侧落点库外全路径 / AC↔冻结形状互洽）；`README.md` checklist **第 10 条**指向它；`docs/评审记录模板.md` **§3.1** 增"AC ↔ 冻结 API 形状互洽（复核项）" |
| AC-9 (P0-5) | **通过** | `grep ET_VERSION et_config.h` → `0x020600` / `2.6.0`；**板上已验证**：重启横幅 `Embedded_Tools v2.6.0 (0x20600)`、`AT+VER` → `ver=2.6.0`（实机记录 §14.1） |

### 库内缺陷

| AC | 结论 | 证据 |
|---|---|---|
| AC-10 (P1-1/P1-2，HC-2) | **通过** | `[Suite] proto/modbus_master (32 cases) => pass=32 fail=0`（≥31）；`mbm.noise_self_echo` 逐字节复现 CO-6 形态：喂入主站自身请求回显（8B）→ 真应答 13B 被**正确解析**（`feed()==1`、`poll()==ET_MB_OK`、`result()==1`、`qty==4`）且 **`crc_err==0`**、`timeouts==0`（v2.5 行为：返回 0 / 保持 BUSY / 仅超时收场 + `crc_err` 被污染） |
| AC-11 (P1-1) | **通过** | 既有 25 例全绿（0x10 写路径、qty 边界 125/126、异常不重试、广播写不等应答、迟到不污染、CRC 坏/地址不符丢弃、`read_bc_mismatch` 等）；`test-g4` 双几何 487 例 ALL PASS |
| AC-12 (P1-3/P1-5) | **通过** | `%5c`/`%-3c`/`%3.1c`/`%*c` 与 host `snprintf` **逐字节对拍**（`log.char_field_width` / `log.char_field_matrix`）；`[Suite] debug/log (30 cases) => pass=30 fail=0`（≥30） |
| AC-13 (P1-4/P1-5，HC-3) | **通过** | `log.len_mod_jt` / `log.len_mod_big_float` / `log.len_mod_mix`：`%jd`/`%ju`/`%jx`/`%td`/`%tu`/`%Lf`/`%Lg` 消费对应实参后**哨兵取值正确**，输出为固定占位 `<?jd>`/`<?tu>`/`<?Lf>`；负向断言 `log.len_mod_no_literal`：输出中无字面 `%j`/`%t`/`%L` 残留 |
| AC-14 (P1-3/P1-4) | **通过** | `debug/et_log.h` 头注（支持面/已知不支持面/占位形态）、`docs/API_GUIDE.md` 8.1 规格表、实现三者逐项比对**差集为空**；API_GUIDE 8.1 末段显式声明该结论；`test/test_log.c` 逐条覆盖为证 |
| AC-15 (P1-6，HC-10) | **通过（CI 侧）** | `make test-asan` 与新增 `make test-asan-1k` 已常设于 `ci.yml`（Linux job 两步）；**本机不可执行**（MinGW 无 libasan / clang ASan 运行时本机初始化失败）已在 README「检出边界」与 §5 显式声明，**未声称本机已跑** |
| AC-16 (P2-1) | **通过** | `ci.yml` 的 Linux 步骤改 `make WERROR=1 test` / `make WERROR=1 ex`，Windows 纯 gcc 步骤加 `-Werror`；**本机同门实测**：`mingw32-make WERROR=1 test` → `ALL PASS (fail=0)` 且零告警输出（`grep -E "warning\|error"` 无命中） |

### 板侧与回归

| AC | 结论 | 证据 |
|---|---|---|
| AC-17 (P2-3/P2-4) | **部分通过（如实）** | **P2-3 通过**：`Core/et` 同步 v2.6（`diff -rq` 七目录 + `et_config.h` + `port.h` 全 OK）、`cmake --build --preset Debug` **0 warning**、FLASH **75144 B**（较 v2.5 +656 B）、下载校验通过。**P2-4 的"CO-6 板上注入对照"无法取得**：对照实验（从站完全不应答）显示 `disc=0 / crc=0` → **板未收到自身 TX 字节**（USART1 TX/RX 独立，无半双工回环），即 **CO-6 的受害形态在本板不复现**，故不存在"修复前/后"的板上对照；该修复在本硬件上属防御性，正确性由 host 侧 7 例承担（实机记录 §14.2） |
| AC-18 (P2-5，HC-12) | **通过（HC-12 的"不扩"分支）** | selftest **维持 20 套件**并已按 HC-12 要求**显式声明理由**：扩套件的原始目的要求**板上复跑**（host/板上结果可比对），本版虽已上板，但扩套件需新增 modbus 主从冒烟与 `et_log` 修饰冒烟并同步五处文档与体积表，属**新功能面**而非本版"解析边界清偿"范围 → 记 **v2.7 候选**（与板侧项同批）。`tools/smoke.sh` 断言行、README、API_GUIDE 8.4、实机记录、体积表**本版零改动**（与"维持 20"自洽）；板上实跑 **20/20 PASS**（v2.5 与 v2.6 固件各一次） |
| AC-19 (P2-6) | **通过** | 板上（v2.6 固件）：`AT+SELFTEST` → **`SELFTEST: 20/20 PASS`**；`AT+SELFSTOR` → `kv PASS` / `bootctl PASS` / `STORAGE SELFTEST PASS`；`AT+SIMUPGRADE 3` → `STAGED slot B` → 重启 → **`slot 1 CONFIRMED (self-check ok)`**；v2.4 从站分流复跑：`read 0..3 → 1,2,3,4` PASS、`read 1000..1001 → 1234, 0` PASS（实机记录 §14.1） |
| AC-20 (全版) | **通过** | `sh tools/docsync.sh` → `docsync: pass=296 fail=0`（CI 侧实测 295 ok + 自指断言 = 296；自指断言同时校验 `architecture.md` 机制门行）；本文首行 `本版 docsync：**296/296**` 与实测一致 |
| AC-21 (全版) | **通过（库内） / CI 待推送后复验** | `test`/`test-g4`/`test-tab`/`test-1k` 全绿（487×2 / 494 / 488）；`make ex` 五例 PASS；`gcovr` 行覆盖 **97.3%**（≥85%）；`sh tools/sizecheck.sh` `pass=3 fail=0`（v2.6 表行逐值）；**CI 七 job 中 6 job success**，唯一失败点 = docsync 自指计数（已在 `188e73f` 回刷为 296，**待推送后复验**） |
| AC-22 (P3-1/2/3) | **部分完成（逐项声明）** | **P3-1 做（轻量分支）**：`docs/bench.md` 增**口径声明**"仅供量级参考、不作回归判据"（附 v2.5 差幅 59% 的实测依据），并记 v2.7 候选（相对比值 + 预热）；**P3-2 做**：`CO-11` 在 `docs/v3-candidates.md` 的 **v2.6 复评记录**节与本文 §8 重述"维持暂缓 + 触发条件"；**P3-3 不做**：本机 ASan 执行路径（触发条件 = 安装 WSL/MSYS2，未满足，未静默略过） |

**apidump 结构体字段登记**：本版**无**既有冻结结构体的字段追加（无新增/修改 API 形状，`--diff` 期望 0/0）。**唯一的既有语义变更**登记如下：`et_modbus_master_stats_t.crc_err` 由"CRC 失败被丢弃的候选帧数（含从未成帧的噪声字节）"收紧为"**与在途事务严格同形**的候选帧 CRC 失败数" —— 字段类型与结构体布局**未变**，属**行为修正**（HC-1 的语义面），已在 `docs/API_GUIDE.md` 5.8 统计口径表与 §3 偏差表 D-2 说明。

---

## 3. 偏差记录

| # | 内容 | 原因 | 影响 |
|---|---|---|---|
| **D-1**（已更新：板接入后消项） | 板侧项**原计划全部依赖硬件**：G0-1（v2.5 走单）、P2-3（同步/构建）、P2-4（`CO-6` 板上对照）、P2-6（既有回归） | 2026-09-24 板接入（G474VET6 / COM12 / ST-Link）后：G0-1 **已执行**（§13）、P2-3 **已执行**（§14）、P2-6 **已执行**（§14）；**仅 P2-4 的"修复前后对照"无法取得** —— 受害形态（自身请求回显）在本板 USART1（TX/RX 独立）**不成立**（对照实验 `disc=0/crc=0`，§14.2） | 本交付由"板侧证据缺失"转为"**板侧证据齐备 + 一项对照不可得**"；未取得者**如实声明**，不伪造结论 |
| **D-2** | `crc_err` 统计口径收紧（行为修正，签名未动） | AC-10 要求"该路径下 `crc_err` 不增长"；而 HC-2 要求"`crc_err` 只统计构成帧的 CRC 失败" —— 二者共同要求把"从未成帧的噪声字节"从该计数中排除 | 既有用例全绿（`mbm.crc_bad_discarded` 断言 `crc_err==1` 仍成立，因该帧与在途事务严格同形）；已在 API_GUIDE 5.8 与 §2 登记区显式说明 |
| **D-3** | 读应答定长判据取"**严格等于 `2*exp_qty`**"而非计划 §5 裁量项中的"≤ `2*RD_QTY_MAX` 合法域" | 本机实测：合法域判据会把噪声里"恰为偶数的字节"（如 `04 46 …`）当成合法帧长 → 解析器停等一个**永不到齐**的长度，真应答随后到达也只能超时（`mbm.noise_self_echo` 首轮实现即因此失败）。换取"噪声不得伪造帧长"（HC-2 的前提）成立 | 更强的判据：噪声必须同时伪造 `addr`+`fc`+`bc` 三者才可能骗过定长，且有 CRC 兜底（此时计 `crc_err` 合理）；`mbm.noise_expected_shape` 用例固定该边界行为 |
| **D-4** | `test-1k` 与 `test-asan-1k` 两个 Makefile 目标为本版新增（计划仅要求 ASan 覆盖 1K） | 1K 变体此前只存在于评审人员的手工 gcc 命令里，CI 与文档无法一键复现；`test-1k` 使 AC-21 的"1K 变体全绿"成为可复现命令 | CI 步骤 +1（`make test-1k`）；`make` 目标面扩大，不影响既有目标 |
| **D-5** | 本版未扩 `selftest`（维持 20），故"维持 20"的声明与 `CO-10` 的"v2.6 扩至 21"不一致 | 板档期（HC-12 的"不扩须显式声明并给理由"分支） | 板侧冒烟对主站/日志仍零覆盖（`CO-10` 未关闭）；host 侧由 32+30 例承担。记 v2.7 候选 |
| **D-6** | 计划未列而本版实施的工具修复：`tools/apidump.sh` 的 TMPD 归一 + 取消逐文件 `rm` | 本机实测两处环境缺陷：① `TEMP=C:\...` 时 gawk `>>` 把反斜杠当转义 → 提取为空 → **误报漂移**，且无参数运行会**把清单覆盖成残缺版**；② 循环内 108 次 `rm` 在带安全删除 wrapper 的 shell 下每次数十秒 → 小时级 | `--check` 从"不可用/小时级"恢复到 21 秒；Linux/CI 行为不变（`cygpath` 分支不执行，`rm` 减少不影响语义）。属"门本身可用"的前提修复 |
| **D-7** | `docs/API_INVENTORY_v2.5.md` 按"本版工作树生成 + 版本元数据宏回改为 v2.5 值"的方式定着 | 生成基线需 v2.5 树；而 v2.6 对**公开头文件的提取面**改动仅 `et_config.h` 的版本宏（`et_log.h` 的改动是注释，apidump 剥离注释）→ 回改一行即与 v2.5 树生成结果等价 | 基线 433 项；`--diff` 比较时 `ET_VERSION*` 本就被忽略（API_STABILITY §5 表），故不影响机检判定；如实声明生成方式，便于复核 |

**无其它偏差**：库内改动全部限定在 `protocol/et_modbus_master.c`（内部逻辑）、`debug/et_log.c/.h`（修饰面 + 头注）、`Makefile`/`ci.yml`/治理脚本/文档；**无新增模块、无新增开关、无签名变更**（`apidump --diff` = 0/0）。

---

## 4. commit 范围

**记录范围定义**（`CO-5(v2.5)` 要求显式写明）：本节记录 `snap-2.6-start`（`02b1592`）.. **本版最后一次提交** `2376a11` 的全部提交；数字以 `git diff --stat` 的**实测**为准，不以写作时点 `HEAD` 为准；核验由 `tools/docref.sh`（本机发布前门）执行。**交付文档自身的回填提交（本节数字修订不改变行数）不计入该范围**。

```
82a0ae0 Docs: v2.5-r2 返工轮文本项 (CO-2/CO-3/CO-4) —— -r2 新增 §8 + 三处(+1)漏刷回刷
7077592 M2: v2.6 库内 —— 主站定长校验(CO-6) + et_log 修饰面(CO-7/CO-8) + 机制门 + 检出能力 + 工具修复 + 数字回刷
25f3316 Docs: 实机记录 §13 —— v2.5 板上逐字节走单补齐(G0-1)
c642418 Merge: v2.5 G0 文本返工与实机记录 §13 合入 v2.6 分支 (tag v2.5 已在其前定着)
2376a11 Docs: 实机记录 §14 —— v2.6 板侧同步与 CO-6 板上验证
```

`git diff --stat snap-2.6-start..2376a11` = **28 files changed, 2233 insertions(+), 57 deletions(-)**。

**分支与 tag 顺序**（HC-9）：`tag v2.5` 定着于 **`25f3316`**（= v2.5 库内成果 + G0 文本 + 板上走单证据，**不含 v2.6 库内改动**），其后 `dev/v2.6` 才合入 `main`（`c642418`）。

**CI**：待推送后核验（job 数仍为 7，步骤 +2）。

---

## 5. 未完成项与新发现问题

### 5.1 未完成项（挂账，附理由与触发条件）

| 项 | 计划编号 | 状态 | 理由 / 触发条件 |
|---|---|---|---|
| v2.5 板上逐字节走单 + v2.4 从站分流复跑 | G0-1 | ✅ **已完成**（2026-09-24，实机记录 §13） | 板接入后一次性补齐；走单脚本 `build/board_walk.py`（不入库） |
| `tag v2.5` 与 Release 四附件 | G0-5 | ⚠️ **本地已打 / 远端待推送** | `tag v2.5` = `25f3316`；远端推送被出口代理阻断（见 AC-4），网络恢复后一条命令即出 Release |
| `Core/et` 同步 v2.6 + 板上横幅/`AT+VER` | P2-3 | ✅ **已完成**（§14.1） | 0 warning，FLASH 75144 B；横幅 `v2.6.0 (0x20600)` |
| `CO-6` 板上"修复前/后"对照 | P2-4 | ❌ **不可得（如实）** | 受害形态（自身请求回显）在本板 USART1 上不成立（§14.2 对照实验）；真实 RS-485 单线半双工场景才适用 |
| `selftest` 20 → 21 | P2-5 | **不做**（HC-12 显式声明**维持 20**） | 扩套件须板上复跑才算满足原始目的；板上已实跑 20/20（v2.5 与 v2.6 各一次）。记 v2.7 候选 |
| `AT+SELFSTOR` / `AT+SIMUPGRADE 3` 回归 | P2-6 | ✅ **已完成**（§14.1） | v2.6 固件上 `STORAGE SELFTEST PASS`、`slot 1 CONFIRMED` |
| 本机 ASan 执行路径 | P3-3 | **不做** | 触发条件 = 安装 WSL/MSYS2（本机均无，v2.6 起草期实测）；CI 侧门已常设 |
| CI 七 job 核验 | AC-21 | ⏳ **待推送后核验** | 同 G0-5 的推送阻塞 |

### 5.2 新发现问题

| # | 问题 | 严重度 | 建议处置 |
|---|---|---|---|
| **N-1** | **`tools/apidump.sh` 在受限 shell 环境下的两处脆弱**（D-6）：`mktemp -d` 返回 Windows 形式路径 + 逐文件 `rm`。前者会让**无参数运行**（`sh tools/apidump.sh`，即"重新生成清单"）在失败环境中**把入库清单覆盖成残缺版**（77 行 / 0 项）—— 本版已修 TMPD 与 rm，但"无参数运行不做事前校验"本身仍可伤人 | 中 | 建议后续给无参数运行加"生成结果条目数 < 基线条目数的一半则拒绝写入"的守卫；或改为写临时文件 + 原子替换（`mv`） |
| **N-2** | 板侧证据链已连续两版缺失（v2.5 `CO-1` 未消、v2.6 全部板侧项挂账）→ `tag v2.5` 与 `tag v2.6` 均被阻塞，**发布链停摆** | 高 | 板接入是第一优先项；若长期不可得，建议在 v2.7 计划中重新评估"板上证据"的最低可接受形态（如仅 `AT+SELFTEST` + 单条走单），避免版本无限累积 |
| **N-3** | `%c` 的**精度**按 C 语义无作用，但库内其他规格的精度语义已在用例中逐条固定；`%c` 之外的 `'s'` 精度与域宽组合已覆盖 —— 规格矩阵现已闭合，唯一"文档化例外"是未知转换字符**不消费** | 低 | 维持现状（该例外已在头注 + API_GUIDE + 用例三处固定）；若未来要彻底消灭例外，须引入"未知转换字符消费 int"的语义，会改变既有行为，留 v3 评估 |
| **N-4** | bench 口径未改为相对比值（P3-1 轻量分支）：数字仍不可作跨版判据 | 低 | 已用文档声明封住误用面；v2.7 若需性能回归判别力，再实现"相对比值 + 预热 + 固定迭代" |

---

## 6. 遗留风险

| 风险 | 状态 | 说明 |
|---|---|---|
| **HC-9 真机证据与 tag 完整性** | ✅ **满足（本地）** | v2.5 的板上走单证据取自 `25f3316`（**不含 v2.6 库内改动**），`tag v2.5` 定着于该提交；v2.6 板上项在其后完成。唯一未完成面 = **远端推送**（网络），不影响本地证据自洽 |
| `CO-6` 修复的板上行为与 host 不一致 | **已澄清**（非"未验证"） | 对照实验证明本板**不存在**自身请求回显（`disc=0/crc=0`）→ `CO-6` 的修复属**防御性**，其有效性由 host 用例承担；真实单线半双工（RS-485）才是适用面 —— 记为**已知边界** |
| 读应答判据的"严格同形"分支 | 低 | 若噪声恰好伪造 `addr`+`fc`+`bc` 三者，仍会按帧长整帧丢弃（可能吃掉真应答前缀）→ 该极端形态由 `mbm.noise_expected_shape` 固定行为；工程上此类形态概率低，且不是"无限等待"而是"单次丢弃后继续" |
| CI 未核验（推送前） | 临时 | 推送后核验；本版 CI 面变化 = unit-tests job 增 `test-1k`/`test-asan-1k` 两步并改 `WERROR=1`，Windows 步骤加 `-Werror`；job 数仍为 7 |
| 本机治理脚本耗时（apidump/docsync） | 已缓解 | 修复后 `--check` 21 秒；`docsync` 因调用 apidump 仍在分钟级（本机环境），CI 侧不受影响；已在 README 检出边界小节建议后台执行 |
| `apidump` 清单残缺覆盖风险 | 已消除路径 | 见 N-1（修复后不再发生；原子替换守卫记 v2.7） |

---

## 7. 提交后回填

> 待全部提交与 CI 完成后回填：§4 的 commit 范围与 diffstat（须与 `tools/docref.sh` 输出逐值一致）、CI run 与七 job 结论、全量回归终值。

| 项 | 命令 | 结果 |
|---|---|---|
| 单元全量 | `mingw32-make test` / `test-g4` | **487** ALL PASS（fail=0），双几何同 |
| Tab 形态 | `mingw32-make test-tab` | **494** ALL PASS |
| 1K 变体 | `mingw32-make test-1k` | **488** ALL PASS |
| 零警告门 | `mingw32-make WERROR=1 test` / `WERROR=1 ex` | ALL PASS，`grep -E "warning\|error"` 无命中 |
| 配方载体 | `mingw32-make ex` | 五例 PASS |
| 覆盖率 | `mingw32-make coverage-build` + `PYTHONPATH= gcovr --print-summary --fail-under-line 85` | 行 **97.3%**（10261/10550）、函数 **99.2%**（1046/1054） |
| 冻结面 | `sh tools/apidump.sh --check` / `--diff` | check **一致**；diff 对 v2.5 基线 **新增 0 / 修改删除 0**（exit 0） |
| 文档同步 | `sh tools/docsync.sh` | 见下（AC-20） |
| §4 自指门 | `sh tools/docref.sh` | **OK**（28 files / 2233 insertions / 57 deletions 与实测逐值一致） |
| 体积 | `sh tools/sizecheck.sh` | **pass=3 fail=0**（23724/24/740、33272/24/2232、24100/28/740） |
| 板上构建 | `cmake --build --preset Debug`（G474 工程） | 0 warning；FLASH **75144 B** / RAM **5960 B**（Debug） |
| 板上回归 | COM12：`AT+SELFTEST` / `AT+SELFSTOR` / `AT+SIMUPGRADE 3` / 从站分流复跑 | 20/20 PASS、STORAGE PASS、slot 1 CONFIRMED、读 `1,2,3,4` PASS |
| CI | `gh run view 35974273462`（commit `f3303a0`，main push） | **6 job success / unit-tests(ubuntu) failure**，失败点**仅**在 `Doc-sync assertions`：自指断言要求"交付文档声明 = 实测"，而 **CI 实测 296**（本机修复前的日志为 293）→ 声明值回刷为 296（已在 `188e73f`：交付文档首行、`architecture.md` 机制门行、CHANGELOG）。**其余 6 job success，且新增的 `make WERROR=1 test`/`WERROR=1 ex`/`test-1k`/`test-asan`/`test-asan-1k` 在 Linux 全部通过**（AC-15/AC-16 的 CI 侧证据成立）。**`188e73f` 尚未推送**：本机出口代理持续 `CONNECT tunnel failed, response 502`（`gh` API 正常、直连 `github.com:443` 不通）；网络恢复后 `git -c http.version=HTTP/1.1 -c credential.helper='!gh auth git-credential' push origin main` 即触发复跑，预期七 job 全绿 |

**机制自证表**（门须可红）：

| 门 | 自证手法 | 结果 |
|---|---|---|
| docref §4 自指（AC-7） | 把 `v2.5…-r2.md` §4 的 `4769` 改成 `4768` → 重跑；改回后重跑 | 改后 **FAIL**（退出码 1，打印"§4 声明与实测不符"）；改回 **OK**（退出码 0） |
| docsync 跨文档计数（AC-6） | 把 `docs/architecture.md` 的 `五例` 改成 `四例` → 重跑；改回后复验 | 改后 **FAIL**（退出码 1）：`FAIL 跨文档 examples 数不一致: 实测=5 arch=四→4 README=五→5`；改回后本机 `pass=296 fail=0` |

---

## 8. CO 处置与候选池一致性

> 本节为 **HC-11 的第三落点**（v2.5 交付文档缺 §8 即评审不通过）。

### 8.1 三处 CO 措辞并置对账

三处落点 = ① `v2.6开发计划__解析边界清偿与检出能力补强.md` §1.1、② `docs/v3-candidates.md`「v2.6 复评记录」节、③ **本节**。

| CO | ① 计划 §1.1 处置 | ② `v3-candidates.md` 复评 | ③ 本节 | 一致 |
|---|---|---|---|---|
| CO-1(v2.5) | **纳入前置门 G0（v2.5-r2）**；板不可用则与 §3.4 合并为同一次板侧会话 | 由「v2.6 复评记录」节引用（未进候选池，属返工项） | ✅ **已完成**（板接入，实机记录 §13）；`tag v2.5` 本地定着 | ✅ |
| CO-2(v2.5) | **纳入 G0**（-r2 补 §8）+ 本版机制化（HC-11 写入 §2） | — | AC-2 通过；本节即第三落点 | ✅ |
| CO-3 / CO-4(v2.5) | **纳入 G0**（数字）+ **本版装门**（P0-2） | — | AC-3 通过；AC-6 门自证 | ✅ |
| CO-5(v2.5) | **纳入 G0**（回填）+ **本版机制化**（P0-3） | — | AC-2/AC-7 通过 | ✅ |
| CO-6(v2.5) | **纳入本版（P0 ★ 核心）** | — | AC-10/AC-11 通过（host 7 例）；**板上"修复前/后"对照不可得**（受害形态在本板 USART1 不复现，§14.2） | ✅（处置一致；对照不可得已如实声明） |
| CO-7(v2.5) | **纳入本版**（补实现，**不收窄文档**） | — | AC-12 通过；API_GUIDE 8.1 已更新 | ✅ |
| CO-8(v2.5) | **纳入本版** | — | AC-13 通过 | ✅ |
| CO-9(v2.5) | **拒绝入库 → v3 窗口** | **拒绝入库 v2.6 → 维持 v3 窗口** | 同措辞（本节 §8.2） | ✅ |
| CO-10(v2.5) | **纳入本版（P2）**，与 CO-1 同批 | — | **未做并显式声明维持 20**（D-5） | ⚠️ 与计划"纳入"不一致，**已如实声明理由**（板档期，HC-12 分支） |
| CO-11(v2.5) | **再次声明暂缓**（不得静默消失） | **再次暂缓（重新声明）** + 触发条件 | 同措辞（本节 §8.2） | ✅ |
| CO-12(v2.5) | **纳入本版**（P0-4 计划模板） | — | AC-8 通过 | ✅ |
| CO-13(v2.5) | **纳入本版**（采"或"分支：CI 侧零警告门）+ 环境安装需用户决策 | — | AC-15/AC-16 通过；本机 ASan 路径 P3-3 未触发 | ✅ |
| CO-14(v2.5) | **纳入本版**（P3 可选，二选一） | — | AC-22 的 P3-1（轻量分支：明示不作判据） | ✅ |

**差集**：仅 `CO-10` 一处为"计划纳入 vs 实际未做"，已按 HC-12 的显式声明分支处置并记 v2.7 —— **不属静默丢失**。

### 8.2 候选池措辞（与 `docs/v3-candidates.md` 一致）

- **`CO-9(v2.5)` `data()` → `read_into()` 形态变更**：**维持 v3 窗口**（触碰既有签名，属破坏性窗口；v2.6 为 MINOR 不触碰，有效窗口已在 API_GUIDE 5.8 明示）。
- **`CO-11(v2.5)` USART2 独立串口 / 共享口地址不符静默的板侧验证**：**再次暂缓**，触发 = 第二路 USB-TTL 或双板对连。
- **biquad（#8）**：**维持排队**，触发 = 用户提交具体频响指标。
- **32 位寄存器组合库级封装**：**维持拒绝（库级）**。

---

> 交付纪律：本交付中每条"通过"均有本文所列命令可复现；**未能核验者（板侧）一律不按通过计** —— 故本交付**不合格**（HC-9 未满足），与 v2.5 的处置逻辑一致。
