# v2.7 开发计划：从站解析对称化与板上自测收口

> 起草日期：2026-09-25 ｜ 状态：⏳ 待开发（交付后归档）｜ 版本号 **2.7.0**
> 输入：`v2.6评审记录__解析边界清偿与检出能力补强.md`（2026-09-25，结论 **返工**，`CO-1(v2.6)`…`CO-15(v2.6)` 共 15 条）
> 上一版被评终态：`HEAD` = **`c865f69`**（被评 `snap-2.6-start` = `02b1592`；**`tag v2.6` = `2e57b16`** 已发布，`gh release list` 显示 v2.6 为 **Latest**）
> 起草日实测基线：用例 **487**（×2 几何）/ 1K 变体 **488** / Tab 形态 **494** / 模块 **34** / 内建 selftest 套件 **20** / docsync 断言 **296**（**当前 `fail=2`**，见 CO-1）/ `apidump --diff` **0:0** / CI 七 job（其中 docsync 自指红）
> 结构说明：§1–§6 为规约结构；附 A（里程碑）、附 B（风险）为工程惯例补充

---

## 1. 目标与范围

### 1.1 输入说明与 CO 逐条处置

上一版评审记录**存在且可用**（载于仓库根，未入 git）。记录结论为**返工**，并把下一版输入显式分为三段：**§6.1 = `v2.6-r2` 返工轮前置门 G0′**（纯文本/门，**不含新开发**）、**§6.2 = v2.7 必含项**（代码/功能）、**§6.3 = v2.7 可选**。本计划**全部承接**，逐条处置如下（四选项均附落点或触发条件）。

**分流口径声明（本计划的关键裁定）**：`v2.6-r2` 是 **v2.7 的开发前置门**，不是 v2.7 的一部分 —— 理由有二：① v2.6 评审 §6 开篇明文「返工轮（`-r2`）不含新开发；代码类问题一律进下一版正式窗口」；② v2.5 的 `-r2` 轮**只有交付文档、无计划文档**（`ls *r2*.md` 实测：仅 `v2.5开发交付__...-r2.md`），返工轮的任务清单由**评审记录本身**充当，不需要第二个计划载体。故本计划把 r2 项收在 §3.0 前置门，**v2.7 的代码窗口起点 = r2 终态**。

| # | 问题摘要 | 来源 | 处置 | 理由 / 触发条件 |
|---|---|---|---|---|
| **CO-1**(v2.6) | `docsync` 在交付平台**恒红**：`apidump --check` 用 `diff -q` 字节级比对，工作树清单经 `core.autocrlf=true` 检出为 CRLF、生成件为 LF → 报"公开面漂移"（与 API 面无任何关系） | 评审 §2 AC-6/AC-20、E5/E6/E7/E8 | **纳入前置门 G0′-1** | 根因**起草期复测坐实**：`git config core.autocrlf` = `true`；工作树 `docs/API_INVENTORY.md` CR 计数 **757**，`git show HEAD:docs/API_INVENTORY.md \| grep -c $'\r'` = **0**；`.gitattributes` **不存在**。门不能用即 HC-4 不可用、AC-6/AC-20 不可复现 → **发布阻塞级** |
| **CO-2**(v2.6) | **已发布产物含不实内容**：`CHANGELOG.md` v2.6.0「挂账（如实声明）」段称板侧项**均未执行**、`tag v2.5` **亦未打**，与实机记录 §13/§14、`gh release view v2.5` 的事实**相反**；该段经 `release.yml` 注入，**Release v2.6 正文（Latest，公开）同样含此错误** | 评审 §1 表第 3 行、E8 | **纳入前置门 G0′-2** | 已发布物正确性；评审明示「**不得**靠重打 tag 解决」——改 CHANGELOG + `gh release edit v2.6 --notes` 重注 |
| **CO-3**(v2.6) | 交付文档**自相矛盾**：§1 状态声明 / §4「CI 待推送后核验」/ §5.1「远端待推送」/ 文末纪律句（「本交付**不合格**」）与 §2 AC-4/AC-17/AC-21、§7 终态结论互斥 | 评审 §1 表第 4 行 | **纳入前置门 G0′-3** | 纯文本；成因是终态回填只改了 §2/§7，未同步其余章节 |
| **CO-4**(v2.6) | **HC-5 门的设计性空隙**：`docref` 只核验 `<base>..<end>`，`<end>` 由作者自选为"最后一次**代码**提交"（`2376a11`），其后 **5 个文档提交不受检** —— 本版最严重两处文本错误（CO-2/CO-3）恰在此盲区 | 评审 §3 HC-5 行、§5 CO-4 | **纳入前置门 G0′-6** | 起草期读码确认：`tools/docref.sh:12-13` 注释即写「`<end>` 为**本版最后一次提交**」而实现信任文档自述范围 → 门对"提交后回填"零覆盖。**同时机制化**：`v2.7` 自身交付文档的 §4 范围定义须写 `<base>..HEAD`（见 HC-5） |
| **CO-5**(v2.6) | **从站侧同源缺陷未改**（改 A 漏 B）：`expected_len()` 对 `FC_WRITE_MULTIPLE` 直接 `return b[6] + 9u`（**字节数域先于 CRC 被信任**）；且 CRC 失败后仍按该未受信长度整段 `memmove` 前进，**不是逐字节重同步**。评审探针 **E10 实测复现**：喂 11 字节噪声 `11 10 00 00 00 01 04 DE AD BE EF` 后再喂真请求 `11 03 00 00 00 04 46 99` → `feed` 返回 **0**（无应答）、`crc_err=1`、`rxlen=6`，真请求被吞前 2 字节 | 评审 §5 CO-5、E10 | **纳入本版**（★ 核心，P0-1…P0-4） | 起草期读码逐处坐实：`protocol/et_modbus.c:40`（`return (n < 7u) ? 0u : ((uint32_t)b[6] + 9u)`）、`:271-273`（`process_frame(mb, need); memmove(mb->rx, mb->rx + need, ...)`）、`:199-202`（CRC 失败仅 `crc_err++` 后 `return`，无重同步信号）。与主站 v2.6 已修形态**不对称** |
| **CO-6**(v2.6) | `README.md:237` 冻结面 checklist 第 8 条仍写"当前 `docs/API_INVENTORY_v2.4.md`"，而默认基线已滚动至 v2.5；**不在新门覆盖范围内** | 评审 §3 HC-7 行、§5 CO-6 | **纳入前置门 G0′-4** | 起草期实测：`README.md:237` 确含 `docs/API_INVENTORY_v2.4.md`（同族漏刷第三例后的第四次） |
| **CO-7**(v2.6) | `port/stm32g474/README.md` v2.6 行写"板侧工程本版**未同步**"，而实机记录 §14 明载已同步（`diff -rq` 全 OK、0 warning、FLASH 75144 B） | 评审 §5 CO-7 | **纳入前置门 G0′-4** | 起草期实测：`port/stm32g474/README.md:84` 确含"未同步"，体积数字本身正确（24100/28/740），**错误仅在叙述** |
| **CO-8**(v2.6) | 板上"正常读恒有 `disc=1`"**未进交付文档**：实机记录 §14.3/§13.9 已记为遗留，但交付文档 §5.2/§6 **均未登记**；评审已机械定位（单字节前导噪声 → `disc=1 / crc=0 / st=2`，v2.5 与 v2.6 同值），剩余未知仅"该字节的物理来源" | 评审 §5 CO-8、E9/E11 | **纳入本版**（P1-5） | 遗留登记纪律：实机记录 ≠ 交付文档登记；物理来源定位需读 UART 错误标志或抓波形（**板侧项，与 selftest 同批会话**） |
| **CO-9**(v2.6) | 主站读路径**死代码/恒真子句**：① `handle_frame()` 的 `bc != 2*exp_qty` 分支**永不可达**（探针 E12 注入 `printf` 跑全量 487 例命中 **0** 次）；② `is_expected_shape()` 读分支的 `rx[2]==2*exp_qty` 子句在到达点**恒真** | 评审 §5 CO-9、E12 | **纳入本版**（P2-1） | 起草期读码确认：`protocol/et_modbus_master.c:172-179`（`feed()` 于 `:362` 已强制相等后才走到 `:396` 的 `handle_frame`）、`:48-49`（子句在 `:382` 到达点恒真）。属代码卫生，**清理须保持行为零变化** |
| **CO-10**(v2.6) | `L` + 整数转换（UB 域）**无覆盖**：`%Lu/%Ld/%Lx` 走 `j/t/L` 分支按 `long double` 消费。本机（Windows x64）实测不错位，但 x86-64 SysV 下 `long double` 属 memory-class，`va_arg` 可能不推进整数槽 → **本机无法验证**且 `test_log.c` 无用例 | 评审 §5 CO-10、E13 | **纳入本版**（P2-2，采"文档限定"分支） | 起草期读码确认：`debug/et_log.c:381-383`（`L`→`SP_LEN_BL`）、`:404-412`（`va_arg(*ap, long double)`）。**选文档限定而非补 Linux 用例**：本机无 Linux 执行路径（无 WSL/MSYS2，沿 v2.6 已声明的检出边界），补了也只能在 CI 跑；文档限定是**可在本机自证**的收敛方式（详见可裁量项 5） |
| **CO-11**(v2.6) | **计划 AC 落点过时**：AC-1 指向 `移植stm32实机记录.md` **§12**，实际证据在 **§13**（§12 仍是"未执行的走单（如实挂账）"占位节） | 评审 §2 AC-1 附注①、§5 CO-11 | **纳入前置门 G0′-5** | 属 `docs/开发计划模板.md` 起草期清单未覆盖的项 → 增第 5 项；实机记录 §12 占位节标注承接关系 |
| **CO-12**(v2.6) | AC-18 证据行**表述不实**：称"实机记录、体积表**本版零改动**"，而二者本版均有改动（§13/§14、v2.6 体积行） | 评审 §2 AC-18 附注、§5 CO-12 | **纳入前置门 G0′-4** | 纯文本；应为"**selftest 相关行**零改动" |
| **CO-13**(v2.6) | D-6 的"修复后本机 `--check` **21 秒**"**不可复现**：评审实测 `--check` **4m39s**（PortableGit sh）/ **4m53s**（系统 Git sh）、`--diff` **6m29s** | 评审 §4 D-6、E6/E7 | **纳入前置门 G0′-1**（同批重测） | 数字类声明漏环境注记即无效（README 发布纪律第 6 条）；须按实测改写 |
| **CO-14**(v2.6) | **`CO-10(v2.5)` 未关闭**：selftest 仍 20，**主站/日志在板上零冒烟覆盖**。v2.6 援引 HC-12「不扩」分支合法，但**v2.7 不得再次援引**；须新增 modbus（从站+主站）与 `et_log` 修饰冒烟套件 + 五处回刷 + 板上复跑 | 评审 §5 CO-14、§6.2 第 2 条 | **纳入本版**（P1-1…P1-4，**必办**） | 回刷面**起草期已探明**：除评审列举的五处外，另有 **4 处硬编码**必须同批改 —— `test/test_selftest.c:98`（`ET_CHECK_U32_EQ(20u, base)`）、`:151` 注释、`port/stm32f103/renode/smoke.sh:97`（`grep -q "SELFTEST: 20/20 PASS"`）、`README.md:193`。**套件数裁定见下方「CO-14 口径裁定」** |
| **CO-15**(v2.6) | bench **无回归判别力**：仅以文档声明封住误用面（P3-1 轻量分支），跨版数值仍不可比 | 评审 §5 CO-15、§6.3 | **纳入本版（P3 可选，时间盒 0.5 天）** | `docs/bench.md` 顶部已声明"不作判据"并把"相对比值化"记为 v2.7 候选（起草期实测确认，第 ③ 条）；本版**做与不做均可**，不做须在交付文档显式声明 + 再记 v2.8 |

**CO-14 口径裁定（套件数 21 vs 22）**：评审 CO-14 要求"新增 modbus（从站+主站）与 `et_log` 修饰冒烟套件"（**两类内容**），但 §6.2 与 `CO-10(v2.5)`/`v3-candidates.md` 均写目标数字是 **21**。本计划裁定为 **20 → 22**（新增 `modbus`、`log` **两个**内建套件），理由：① 套件是 `et_selftest_report_fn` 的**失败归因单元**（回调携带 `suite` 字段），把分属两个模块的断言合并进一个套件会让 `ET_SELFTEST_CHECK_FAIL` 无法区分归属，直接削弱该回调的设计价值；② 与 `test/test_selftest.c` 逐套件断言（`ev_find(..., "crc")` 等）的既有用法一致。**该差异（相对评审建议的 21）属"更严"，须在交付文档 §8 与 `docs/v3-candidates.md` 并置声明**，供下一轮评审裁决（沿 v2.6 D-3 的处置先例）。

**非 CO 项的处置声明**（评审 §4 偏差裁决中未被 CO 覆盖者，防静默丢失）：**D-1**（板侧挂账 → 板接入后消项）、**D-2**（`crc_err` 口径收紧）、**D-3**（定长判据取严格等于）、**D-4**（新增 `test-1k`/`test-asan-1k`）均已被评审判"**接受**"，无需处置；**D-5**（selftest 维持 20）已转化为 `CO-14(v2.6)`，落 P1-1…P1-4；**D-6 的耗时部分**归 G0′-1（附 CO-13）；**D-6 的行尾部分**归 G0′-1（附 CO-1）；**D-7**（`API_INVENTORY_v2.5.md` 的"人工等价性论证"降级）归 **P2-3**（`git worktree` 真生成复核）。

### 1.2 本版做什么

1. **从站侧解析边界对称化（CO-5，★ 核心）**——把 v2.6 在主站侧确立的判据（切片前校验字节数域、CRC 失败逐字节重同步、`crc_err` 只计成帧者）**对称移植到 `et_modbus.c`**；补用例 + **板上注入验证**（同批板侧会话）。
2. **板上自测覆盖收口（CO-14，关闭 `CO-10(v2.5)`）**——selftest 内建套件 **20 → 22**，新增 `modbus`（从站+主站）与 `log`（修饰面）套件；九处回刷；板上 `AT+SELFTEST 22/22` 复跑。
3. **板上 `disc=1` 物理来源定位（CO-8）**——读 UART 错误标志或抓取波形，定位单字节前导噪声的来源；**交付文档 §5.2/§6 正式登记**（不再只留在实机记录）。
4. **代码卫生与文档收敛（CO-9/CO-10）**——清理主站读路径死代码与恒真子句（行为零变化）；`L` + 整数转换的语义在 API_GUIDE 显式限定。
5. **证据链补强（D-7）**——用 `git worktree` 在 `v2.5` 树上**真生成**一次基线清单并逐字节比对归档件，消除 v2.6 的"人工等价性论证"。
6. **前置门 G0′（`v2.6-r2` 返工轮）**——7 项纯文本/门修复，**v2.7 代码窗口的前置条件**。

### 1.3 明确不做什么（Non-goals）

- **不做**任何触碰既有签名的变更（`apidump --diff` 必须纯增；本版预期**新增 0 项 / 修改删除 0 项**——固定内联尺寸的静态缓冲在 selftest 内部，不进公开面）；
- **不做** `data()` → `read_into()` 形态变更（`CO-9(v2.5)` 维持 v3 破坏性窗口）；
- **不做** `et_log` 浮点支持（`%f` 族仍为"消费 + 可见占位"；本版只做 `L` 的**文档限定**，不新增浮点能力）；
- **不做** USART2 独立串口 / 共享口地址不符静默的板侧验证（`CO-11(v2.5)` **再次暂缓**，硬件阻塞：bench 仅一路 USB-TTL；触发 = 第二路 USB-TTL 或双板对连）；
- **不做**selftest 的"发布默认启用"（`ET_MODULE_SELFTEST` 维持默认 0，避免发布固件体积增项）；
- **不做** Modbus TCP/ASCII、主站轮询调度器与多从站表（v2.5 既定边界维持）；
- **不做** biquad / 32 位寄存器库级封装 / 四项破坏性候选（v2.5/v2.6 结论维持）；
- **不做**本机 ASan 环境安装（需用户决策；触发 = 安装 WSL 或 MSYS2）；
- **不做**动态内存 / RTOS / 内部取时基 / i2c 抽象 / 签名启动 / 通配 / 多行编辑（**连续十七版**永久边界）；
- **不做**新模块（本版无新增模块，模块数维持 **34**）。

---

## 2. 硬性约束（违反任一条即交付不合格）

- **HC-1 契约零破坏**：`sh tools/apidump.sh --diff` 对 v2.6 基线必须"修改删除 0 项"；本版预期**新增 0 项**（若确有新增，须逐项说明理由且仍为纯增）；既有 API 语义与既有宏值不变。
- **HC-2 从站不得信任噪声字节（与主站对称）**：`et_modbus.c` 在任何**切片/前进**之前，必须完成字节数域合理性校验——`FC_WRITE_MULTIPLE` 的 `b[6]` 须等于 `2*((b[4]<<8)|b[5])`，不符即**逐字节重同步**（不得按该未受信长度整段前进）；CRC 失败的"**从未成帧**"序列不计入 `crc_err`。判据与主站 `et_modbus_master.c` 在 v2.6 确立的口径**同一语义**。
- **HC-3 统计口径与文档一致**：`crc_err` / `discarded` / `addr_mismatch` 三个计数的**何时加一**须在 `docs/API_GUIDE.md` 的从站节（与主站 5.8 同构）逐条写明，且与实现**差集为空**；口径收紧属**行为修正**（字段与签名未动），须在交付文档 §2 登记区显式说明。
- **HC-4 前置门 G0′ 全绿**：§3.0 的 7 项全部完成且四门读数（`docsync` / `apidump` / `sizecheck` / `docref`）在本机可复现为绿，**否则不得进入 v2.7 代码窗口**（v2.6 的返工正是"门不可用仍交付"所致）。
- **HC-5 §4 自指的记录范围必须覆盖到 `HEAD`**：v2.7 交付文档 §4 的范围须写为 `<snap-2.7-start>..HEAD`（或写明"含全部文档提交"的等价表述），且 commit 清单与 diffstat 与实测逐值一致，由**本机**门（CI 不跑，沿 sizecheck 先例）核验；`docref.sh` 的锚点判据须在 G0′-6 中一并收窄。
- **HC-6 库不变式**：零动态内存、多实例句柄化、`core/`+`algorithm/` 零硬件依赖、不内部取时基、不新增模块开关、不追加/改动既有冻结结构体字段；selftest 新增套件的一切缓冲须为**文件级静态**（不得 `malloc`）。
- **HC-7 数字回刷同批**：selftest 套件数（20→22）的**九处引用**必须在同一提交内回刷（清单见 AC-13）；用例数、examples 数、docsync 计数、模块数、`README`（特性表/结构树/质量门）、`architecture.md` 机制门行、`API_GUIDE`、`CHANGELOG`、`port/*/README` 体积表（sizecheck 逐值）同批。
- **HC-8 板侧落点必须写明库外路径**：板侧任何改动一律标注为**板侧工程私有文件（库外，不在本仓 git）**，路径写全（`D:\code\STM32CubeMX\G474VET6_ET_TEST\Core\Src\et_demo.c`、同步目录 `...\Core\et\`、走单脚本 `build/board_walk.py`），禁止写成库内 `examples/`。
- **HC-9 真机证据与 tag 完整性**：CO-5 的板上注入验证与 selftest 22 的板上复跑须在**同一板侧会话**取得；`tag v2.7` 指向的 commit **不得含 v2.7 之后的库内改动**；编译通过不算验收。
- **HC-10 内存门**：`make test-asan` 与 `test-asan-1k` 须覆盖**新增的 modbus/log 套件代码路径**且 CI 绿；本机不可执行须在文档显式声明为已知边界，**不得声称本机已跑**。
- **HC-11 CO 一致性三落点**：本计划 §1.1 / `docs/v3-candidates.md`（新增「v2.7 复评记录」节）/ **交付文档 §8** 三处 CO 措辞必须一致（含 CO-14 的 21→22 差异声明）；交付文档缺 §8 即不合格。
- **HC-12 selftest 扩套件全链（本版必办，不得再援引"不扩"分支）**：内建套件数由 20 变更的**同一提交**内，必须同步：`et_selftest.c` 套件表与注释、`test/test_selftest.c` 的 `base` 断言、`port/stm32f103/renode/smoke.sh` 的 `SELFTEST: N/N PASS` 断言、`README.md`、`docs/API_GUIDE.md` 8.4、`移植stm32实机记录.md`（新增节）、`port/*/README.md` 体积表、`CHANGELOG.md`；且**板上 `AT+SELFTEST` 复跑结果可比对**。

---

## 3. 任务分解

### 3.0 前置门 G0′：`v2.6-r2` 返工轮（纯文本/门，**无新开发**，约 1 天）

> 交付物命名带 `-r2` 后缀：`v2.6开发交付__解析边界清偿与检出能力补强-r2.md`（沿 v2.5 先例）。**G0′ 全绿后**，`snap-2.7-start` 方可定着，本版代码窗口方可开启（HC-4）。

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| G0′-1 | **`apidump --check` 行尾敏感修复（CO-1）+ 耗时重测（CO-13）**：三选一——① `gen_body` 输出统一 LF；② 比对改**条目集合比对**（`item_lines` 手法，`--diff` 已天然抗行尾）；③ 新增 `.gitattributes` 对 `docs/*.md` 显式 `text eol=lf`。修毕在本机复跑至 `apidump --check` exit 0、`docsync` `fail=0`，并把 `--check`/`--diff` 实测耗时（附 shell 与命令）重写进文档 | `tools/apidump.sh`（`:183-191` `check` 分支、`:179` `item_lines`）、`.gitattributes`（可能新增）、`README.md`（检出边界小节的耗时句）、r2 交付文档 | 修法②**不依赖工作树行尾**，最稳；修法③ 会影响全仓 `docs/*.md` 的检出行为，须评估 `sizecheck`/`docref`/`docbuild` 是否受影响。**门须自证可红**（改一处 API 签名 → 仍须红） | P0 |
| G0′-2 | **CHANGELOG 挂账段重写 + Release 正文更正（CO-2）**：v2.6.0「挂账」段改为"板侧已执行（§13/§14）、`tag v2.5` 已打并发布、仅 P2-4 板上对照不可得"；用 `gh release edit v2.6 --notes` **重注正文**（**禁止**重打 tag）；声明更正方式与时间 | `CHANGELOG.md`（v2.6.0「挂账」段）、GitHub Release v2.6 正文、r2 交付文档 | 已发布物正确性；`release.yml` 会从 CHANGELOG 取正文，故两处须**同时**改，否则下次发布再次注入旧文 | P0 |
| G0′-3 | **交付文档终态统一（CO-3）**：§1 状态声明、§4「CI」行、§5.1 的 G0-5/CI 行、文末纪律句全部改为与 §2/§7 一致的终态表述；保留"未能核验者不按通过计"原则，但不再自判"不合格" | `v2.6开发交付__解析边界清偿与检出能力补强.md`（多节）、同文件 `-r2` 版的修订声明 | 纯文本；**与 G0′-6 的门改动联动**（门收窄后这类"回填后不自洽"应能被抓到） | P0 |
| G0′-4 | **三处同族漏刷与表述（CO-6/CO-7/CO-12）**：① `README.md:237` → `docs/API_INVENTORY_v2.5.md`；② `port/stm32g474/README.md:84` v2.6 行 → "板侧已同步（见实机记录 §14）"；③ v2.6 交付文档 AC-18 证据行 → "**selftest 相关行**零改动" | `README.md:237`、`port/stm32g474/README.md:84`、`v2.6开发交付__...md`（AC-18 行） | 纯文本；**并须评估是否补门**（三处均落在此前"点名式装门"的盲区，根因见 CO-4） | P0 |
| G0′-5 | **计划模板第 5 项 + 实机记录占位节（CO-11）**：`docs/开发计划模板.md` §7 起草期核验清单增第 5 项"**证据落点章节号 + 占位节清理**"（附 v2.6 反例：AC-1 指 §12、证据在 §13）；`移植stm32实机记录.md` §12 标题处标注"已由 §13 承接" | `docs/开发计划模板.md`（§7）、`移植stm32实机记录.md`（§12 占位节） | 纯治理；**本计划已按第 5 项起草**（见文末合规声明） | P0 |
| G0′-6 | **`docref` 锚点收窄（CO-4）**：判据改为覆盖 `<base>..HEAD`，或增补"交付文档 §2/§7/§8 与 `CHANGELOG` 的终态一致性"断言 | `tools/docref.sh`（`:12-13` 注释与 `:44-73` 解析/比对段）、`docs/评审记录模板.md`（核验项引用）、r2 交付文档 | 门若不收窄，**r2 轮自身仍会被同一盲区放过**（CO-4 原文）；**须自证可红**：把一个**纯文档提交**放在 `<end>` 之后 → 门仍能红 | P0 |
| G0′-7 | **r2 四门读数 + CI + 自指声明**：按既有纪律重出 `docsync`/`apidump`/`sizecheck`/`docref` 读数与 CI run 链接，补齐 `本版 docsync：N/N` 自指声明 | r2 交付文档、CI | 发布纪律第 1/7 条；**G0′ 的完成证据** | P0 |

### 3.1 P0 从站侧解析边界对称化（CO-5，★ 核心，约 1.5 天）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P0-1 | **`expected_len()` 切片前校验（CO-5 前半）**：`FC_WRITE_MULTIPLE` 分支在 `return b[6]+9` 之前校验 `(uint32_t)b[6] == 2u*((uint32_t)b[4]<<8 \| b[5])`，不符返回 0（= "尚不可判定"，交静默路径界定）；`b[6]` 不再是"先于校验被信任的帧长" | `protocol/et_modbus.c`（`:28-44` `expected_len`） | 内部逻辑修复，**API 零变化**（HC-1）；行为变化：形如 `E10` 的伪写入帧不再伪造长度 | P0 |
| P0-2 | **CRC 失败的前进策略对称化（CO-5 后半）**：`process_frame()` 的 CRC 失败分支区分"**与在途期望同形的候选帧**"（计 `crc_err` 并按帧长丢弃）与"**从未成帧**"（计 `discarded` 并**逐字节重同步**）；`feed()` 的处理循环改为按 `process_frame()` 的返回**决定前进量**（不再无条件 `memmove(rx+need)`） | `protocol/et_modbus.c`（`:185-209` `process_frame`、`:259-274` `feed` 循环）、`protocol/et_modbus.h`（若需注明语义） | 行为修正：`crc_err` / `discarded` 口径收紧（HC-3 语义面须登记）；静默路径（`et_modbus_tick` `:314-319`）的"残帧按整帧兜底"语义**保持不变** | P0 |
| P0-3 | **从站用例补齐（与主站同构）**：新增"伪写入帧长度域不可信"矩阵——① 噪声 11 字节伪写入 + 真读请求（E10 形态）→ 真请求被正确应答；② 噪声后接真写入请求；③ `crc_err` 不因未成帧字节增长；④ 逐字节重同步后 qty 边界（`ET_MODBUS_WR_QTY_MAX`）与 `bc != 2*qty` 拒绝路径不回退 | `test/test_modbus.c` | 用例数 +≥8；docsync/README 计数同批回刷（HC-7） | P0 |
| P0-4 | **口径文档登记（HC-3）**：`docs/API_GUIDE.md` 从站节新增统计口径表（与主站 5.8 同构），逐条写明 `crc_err`/`discarded`/`addr_mismatch` 的加一条件；交付文档 §2 登记"行为修正（语义面）" | `docs/API_GUIDE.md`、`protocol/et_modbus.h`（头注）、交付文档 §2 | 文档—实现差集须为空（HC-3）；沿用 v2.6 D-2 的处置形态 | P0 |

### 3.2 P1 板上自测覆盖收口与 `disc` 定位（CO-14 + CO-8，约 2 天）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P1-1 | **`modbus` 套件（从站+主站纯逻辑冒烟）**：从站段覆盖 feed 快路径/静默路径/伪长度噪声重同步；主站段覆盖定长严判/噪声逐字节重同步/`crc_err` 口径；缓冲用**文件级静态**（从站 rx/tx 取最小合法 8B 起；主站须 ≥ `ET_MODBUS_ADU_MAX`，记 RAM 增项） | `debug/et_selftest.c`（新套件段 + `g_suites` 表）、`et_config.h`（无需改动，`ET_MODULE_MODBUS` 默认 1） | RAM 增项约 512B+（主站双缓冲）→ 板侧自测固件体积/RAM 增项须记档（发布配置 `ET_MODULE_SELFTEST=0` 不受影响） | P0 |
| P1-2 | **`log` 套件（修饰面冒烟）**：覆盖域宽/左对齐/精度生效（`%5c` 等）、`j/t/L` 消费与占位、未知转换字符不消费；输出经 `port_putc` 捕获断言（不得依赖 host `snprintf`） | `debug/et_selftest.c`（新套件段 + `g_suites` 表） | 需自备 putc 捕获与最小缓冲；**不得引入 `snprintf` 依赖**（板上无 libc 格式化） | P0 |
| P1-3 | **九处回刷（HC-7/HC-12）**：`debug/et_selftest.c`（计数注释）、`test/test_selftest.c:98`（`20u` → `22u`）、`:151` 注释、`port/stm32f103/renode/smoke.sh:97`（`SELFTEST: 20/20 PASS` → `22/22`）、`README.md:46`、`:188`、`:193`、`docs/API_GUIDE.md:1423`、`CHANGELOG.md`（+ 实机记录/体积表/交付文档） | 上述九处 + `移植stm32实机记录.md`、`port/*/README.md`、交付文档 | 回刷面最大项；`test/test_selftest.c` 与 `smoke.sh` 是**硬断言**（漏改即 CI 红） | P0 |
| P1-4 | **板侧同批会话**：① 板侧同步 v2.7（`Core/et/` 全量重拷 + `diff -rq` + 0 warning + FLASH/RAM 读数）；② `AT+SELFTEST` → `22/22 PASS`、`AT+SELFSTOR` → PASS、`AT+SIMUPGRADE 3` → CONFIRMED；③ **CO-5 注入验证**：板侧 demo 增注入命令，复跑 E10 形态，记录"修复前（无应答）/修复后（正确应答）"对照与统计终值；④ v2.4 从站分流走单复跑 | 板侧工程私有（库外）：`...\G474VET6_ET_TEST\Core\Src\et_demo.c`、`...\Core\et\**`、走单脚本 `build/board_walk.py`；记录进 `移植stm32实机记录.md`（新增 §15） | **本版唯一"只有板上能证伪"的证据**（HC-9）；改板侧共享 USART1 分流路径 → 必须同会话复跑既有走单项 | P0 |
| P1-5 | **`disc=1` 物理来源定位（CO-8）**：读 USART 错误标志（ORE/NE/FE 等）或抓取一次波形，定位正常读时的单字节前导噪声来源；结论写入实机记录 §15 **并在交付文档 §5.2/§6 正式登记** | 板侧工程私有（库外）：`...\Core\Src\et_demo.c`（错误标志读点）；`移植stm32实机记录.md`、交付文档 §5.2/§6 | 若定位到来源且属**库缺陷** → 须在交付文档登记为**新发现**并给处置（可能触发计划外改动，须走 §5 不可偏离规则报备）；若属**环境/物理层**（线路反射、上电瞬态）→ 登记为已知现象 + 判据（`disc` 不应答路径基线值） | P1 |

### 3.3 P2 代码卫生与证据链（约 1 天）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P2-1 | **死代码与恒真子句清理（CO-9）**：① `handle_frame()` 的 `bc != 2*exp_qty` 分支（`et_modbus_master.c:175-179`）——改为断言/注释说明或删繁；② `is_expected_shape()` 读分支的 `rx[2]==2*exp_qty` 子句（`:48-49`）——同上。**前提：行为零变化** | `protocol/et_modbus_master.c` | 若不删而是"改断言"，须确认 `ET_ASSERT` 在发布配置下的编译行为（不得引入新开销）；**全量 487 例须全绿** | P1 |
| P2-2 | **`L` + 整数转换语义限定（CO-10）**：`docs/API_GUIDE.md` 8.1 规格表与 `et_log.h` 头注显式限定"`L` 仅对浮点转换有定义；`%Lu/%Ld/%Lx` 属未定义行为，库按 `long double` 消费并输出占位"；**不新增实现** | `docs/API_GUIDE.md`（8.1）、`debug/et_log.h`（头注） | 纯文档；消除 x86-64 SysV 的未验证面（本机无 Linux 执行路径，见可裁量项 5） | P1 |
| P2-3 | **`git worktree` 真生成基线清单（D-7）**：`git worktree add` 检出 `v2.5` 树 → 在其上运行 `apidump` 生成清单 → 与归档 `docs/API_INVENTORY_v2.5.md` **逐字节比对**；结果（含差异行数与内容）写入交付文档 | `tools/apidump.sh`（只读使用）、`docs/API_INVENTORY_v2.5.md`（只读）、交付文档 | 消除 v2.6 的"人工等价性论证"降级（D-7 的**有条件接受**条件项）；若比对出差异 → 须评估是否影响 HC-1 的基线语义并报备 | P2 |

### 3.4 P3 可选（时间盒 0.5 天，不阻塞发布）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P3-1 | **bench 相对比值化（CO-15）**：以稳定基线操作为 1.0 的**相对比值** + 预热 + 固定迭代；或维持"不作判据"声明。**不做须显式声明 + 再记 v2.8** | `tools/bench.c`、`docs/bench.md` | 口径变更 → 文档须给"新旧数值对照"；否则历史数值不可比 | P2 |

---

## 4. 验收标准（每条可客观核验，标注对应任务）

**前置门 G0′**

- **AC-1**（G0′-1，HC-4）`sh tools/apidump.sh --check` 在**本机** exit 0（不再输出"公开面漂移"）；`sh tools/docsync.sh` → `pass=N fail=0`，且交付文档首行 `本版 docsync：N/N` 与实测相等。**门自证可红**：改动任一被登记的 API 签名 → `--check` 非零；改回即绿。
- **AC-2**（G0′-1，CO-13）交付文档载本机 `apidump --check` 与 `--diff` 的**实测耗时**（附 shell 路径与命令），且不再出现"21 秒"这一不可复现读数。
- **AC-3**（G0′-2）`grep -n "板侧项未执行\|亦未打" CHANGELOG.md` 在 v2.6.0 节命中数为 **0**；`gh release view v2.6 --json body` 的正文与 `CHANGELOG.md` v2.6.0 节关于板侧/tag 的表述**一致**；交付文档声明更正方式（`gh release edit v2.6 --notes`）与更正时间。
- **AC-4**（G0′-3）v2.6 交付文档 §1 状态声明、§4 CI 行、§5.1 的 G0-5/CI 行、文末纪律句与 §2/§7 的终态表述**无互斥**：交付文档以逐句列举方式给出比对结果，且 `grep -c "不合格\|待推送后核验"` 在该文档的终态版中为 0（除历史引用）。
- **AC-5**（G0′-4）① `README.md` 第 8 条所处行不再含 `API_INVENTORY_v2.4.md`（改为 v2.5）；② `port/stm32g474/README.md` 的 v2.6 行不再含"未同步"且指向实机记录 §14；③ v2.6 交付文档 AC-18 证据行含"selftest 相关行零改动"。
- **AC-6**（G0′-5）`docs/开发计划模板.md` §7 含**五项**（新增"证据落点章节号 + 占位节清理"并附 v2.6 反例）；`移植stm32实机记录.md` §12 标题处含"已由 §13 承接"。
- **AC-7**（G0′-6）`tools/docref.sh` 的判据覆盖到 `HEAD`（或含终态一致性断言）；**门自证可红**：构造"`<end>` 之后新增一个纯文档提交"的场景 → 门红（改法：临时在交付文档 §2 写入与 CHANGELOG 不一致的终态表述或制造 diffstat 失配）。

**从站侧解析**

- **AC-8**（P0-1/P0-2，HC-2）**E10 形态在修复后**：先喂 `11 10 00 00 00 01 04 DE AD BE EF` 再喂 `11 03 00 00 00 04 46 99` → `et_modbus_feed` 返回 **1**，应答 = `11 03 08 …`（qty=4 的正确数据），且 `stats.crc_err` **不增长**；**对照**：同一探针在 `snap-2.7-start` 的从站源上返回 **0**、`crc_err` 增长（该对照须由评审方独立复跑，提交方须在交付文档 §7 记录申请与该分离方法）。
- **AC-9**（P0-1/P0-2）`test/test_modbus.c` 新增用例 ≥8 例且全绿；既有从站用例（含 0x10 写、qty 边界、异常码、广播、静默路径、地址不符）**全部仍绿**；核验命令：`mingw32-make test` 中 `proto/modbus` 套件 `fail=0`。
- **AC-10**（P0-3）**主从同构对拍**：至少一组噪声形态在从站（`test_modbus.c`）与主站（`test/test_modbus_master.c` 的 `mbm.noise_*`）两侧有**同形态用例**，且两侧语义一致（噪声不伪造长度、`crc_err` 不污染、真帧被正确解析）；交付文档列出该对应表。
- **AC-11**（P0-4，HC-3）`docs/API_GUIDE.md` 从站节含 `crc_err`/`discarded`/`addr_mismatch` 的加一条件表；实现与文档**差集为空**（逐条比对，以 AC-9 的用例逐条覆盖为证）；交付文档 §2 登记"行为修正（语义面）"。

**板上自测与板侧**

- **AC-12**（P1-1/P1-2）`debug/et_selftest.c` 的 `g_suites[]` 含 `modbus` 与 `log` 两个新条目；`et_selftest_suite_count()` 在默认模块配置下返回 **22**；`test/test_selftest.c:98` 的断言为 `ET_CHECK_U32_EQ(22u, base)` 且该用例绿。
- **AC-13**（P1-3，HC-7/HC-12）九处计数回刷**全部命中且无遗漏**：交付文档以逐行 diff 清单给出（`et_selftest.c` 注释、`test_selftest.c:98`、`test_selftest.c:151`、`renode/smoke.sh:97`、`README.md:46`、`:188`、`:193`、`API_GUIDE.md:1423`、`CHANGELOG.md`）；`sh tools/docsync.sh` 全绿为证。
- **AC-14**（P1-4，HC-9）`移植stm32实机记录.md` §15 载：`Core/et` 同步 `diff -rq` 全 OK + 0 warning + FLASH/RAM 逐值；`AT+SELFTEST` → **`SELFTEST: 22/22 PASS`**（SKIP 计入，格式与 smoke 断言一致）；`AT+SELFSTOR` → PASS；`AT+SIMUPGRADE 3` → CONFIRMED；v2.4 从站分流走单复跑逐字节记录在位。
- **AC-15**（P1-4，HC-9）§15 含 **CO-5 板上注入对照**：同一注入形态在 v2.6 固件（修复前）与 v2.7 固件（修复后）的**应答有无 + `crc_err`/`discarded` 终值**对照；修复前的数据若因板侧实现无法回退而不可得，须**显式声明不可得**并给出替代证据（不得伪造）。
- **AC-16**（P1-5，CO-8）`disc=1` 的物理来源有**定位结论**（UART 错误标志读数或波形记录，二者至少一项，附原始读数/截图路径）；**交付文档 §5.2 与 §6 均含该条目**（`grep -c "disc" 交付文档` ≥2 处且不在同一节）；若未定位到来源，则须声明"已排除的假设清单 + 下一步手段"，**不得只留在实机记录**。

**代码卫生与证据链**

- **AC-17**（P2-1）死代码清理后 `mingw32-make test` / `test-g4` **全绿**（487 例量级不回退）；交付文档说明改法（删繁 vs 改断言）与"行为零变化"的论证；`et_modbus_master.c` 读路径不再含"到达点恒真"的子句（以改后源码为证）。
- **AC-18**（P2-2）`docs/API_GUIDE.md` 8.1 与 `debug/et_log.h` 头注均含"`L` 仅对浮点转换有定义、整数转换属未定义行为"的限定；两处表述一致。
- **AC-19**（P2-3，D-7）交付文档载 `git worktree add` 生成 `v2.5` 树清单与 `docs/API_INVENTORY_v2.5.md` 的逐字节比对结果（命令 + 差异行数；差异为 0 或逐行列出并解释）。
- **AC-20**（全版）`sh tools/docsync.sh` `pass=N fail=0`；`sh tools/apidump.sh --diff` 对 v2.6 基线"修改删除 0 项"且 exit 0；`sh tools/sizecheck.sh` `pass=3 fail=0` 且 v2.7 表行逐值相等；`sh tools/docref.sh` OK；CI 全 job 绿（含 renode-smoke，其 `SELFTEST: 22/22 PASS` 断言已更新）。
- **AC-21**（全版）`mingw32-make test`、`test-g4`、`test-tab`、`test-1k` 全绿；`make ex` 五例 PASS；`gcovr` 行覆盖 ≥85%（记实测值）；`make test-asan` 与 `test-asan-1k` CI 绿；模块数维持 34。
- **AC-22**（P3-1）P3 项：做了 → 给出 bench 口径变更说明 + 新旧数值对照；未做 → 交付文档 §8 显式声明"不做 + 理由"并记 v2.8（**禁静默略过**）。

**AC ↔ 冻结形状互洽检查（计划模板 §7 第④项，起草期已完成）**：本版 P0-1/P0-2/P2-1 均为**内部实现修复**，P1-1/P1-2 为 selftest 内部新增（静态缓冲在 `.c` 内，**不进公开头文件**），故 AC-8…AC-13 的断言对象（既有函数返回值、既有统计字段、selftest 套件计数）与冻结形状**无冲突面**。唯一需登记者为 `et_modbus_stats_t.crc_err` / `.discarded` 的**语义收紧**（字段类型与结构体布局未动）—— 属**行为修正**，须在交付文档 §2 显式说明（HC-1 的语义面），与 v2.6 D-2 同形态。**本版预期 `apidump --diff` = 新增 0 项 / 修改删除 0 项。**

---

## 5. 可裁量项（开发者可自主决策，其余不得偏离）

1. **G0′-1 的行尾修法三选一**：`gen_body` 输出归一化 / `--check` 改条目集合比对 / `.gitattributes`。前提：`apidump --check` 必须对本机、对 CI 都稳定为绿；若选 `.gitattributes`，须实测 `docsync`/`sizecheck`/`docref`/`docbuild` 四门不受影响并记录。
2. **P0-2 的"同形判据"实现写法**：可在 `et_modbus.c` 内新增静态辅助（如主站 `is_expected_shape` 的从站对应物）或内联；前提是语义与主站同一口径。**从站侧无"在途事务"概念**（从站是被动方）——"同形"的判定基准须自行定义并写入 HC-3 的文档表（建议：帧首 = 本站地址或广播地址 且 功能码属已支持集合 且 长度域自洽）。
3. **P1-1/P1-2 的套件内部组织**：modbus 套件内从站段与主站段的顺序、断言密度；log 套件是否复用 `test_log.c` 的用例向量。
4. **P1-1 的缓冲尺寸**：主站须 ≥ `ET_MODBUS_ADU_MAX`（256B）；是否复用一个共享缓冲（须证明两段不并发）或各自独立（RAM +512B）。
5. **CO-10 的两分支**：本计划选"文档限定"（理由见 §1.1）；若开发者在本机/CI 找到 Linux 执行路径（如新增 CI 步骤跑 `%Ld` 用例），**允许改采"补用例"分支**，但须在交付文档说明并同步更新 AC-18。
6. **P2-1 的改法**：删繁 vs 改为断言 vs 改为注释说明 —— 三者均可，前提是行为零变化且不新增发布配置开销。
7. **P2-3 的比对口径**：是否同时比对 `v2.4` 归档（非必需）。
8. **P3-1 的形态**：相对比值 vs 维持"不作判据"。
9. **板侧命令名与注入实现细节**（P1-4 的 `CO-5` 注入命令、`disc` 来源读点）。
10. **G0′ 与 §3.1/§3.2 的执行顺序**：G0′ 可先行（推荐）；`dev/v2.7` 分支上的库内改动（P0/P1-1/P1-2/P2）可在板侧会话之前完成，但**板侧会话必须在 G0′ 之后**（否则板侧走单证据落在"门仍红"的树上）。
11. **文档章节位置与措辞**（新增口径表放 API_GUIDE 的哪一节、实机记录 §15 的小节划分）。

**不可偏离**：§1.1 的 CO 处置结论（尤其 **CO-5 纳入本版**、**CO-14 必办**、`CO-11(v2.5)` 再次暂缓、**CO-14 的 21→22 裁定与差异声明义务**、CO-10 采"文档限定"分支）、HC-1…HC-12 全部、G0′ 作为 v2.7 代码窗口前置条件的顺序规则（HC-4）、板侧落点的库外标注（HC-8）。

---

## 6. 基线

| 字段 | 内容 |
|---|---|
| **首选基线** | **`tag v2.6`**（= **`2e57b16`**，起草期实测存在，`gh release list` 显示 v2.6 为 **Latest**）**＋ G0′ 的 `-r2` 文本/门修订终态** |
| **当前可用起点** | `c865f69`（`origin/main` 的 HEAD，起草期实测）＝ v2.6 被评终态；G0′ 在其上追加修订提交 |
| **快照（动手前必打）** | `git tag snap-2.7-start <G0′ 终态 commit>` ＋ `git push origin snap-2.7-start`（**不带 `v` 前缀**） |
| **分支规则** | 库内改动在 `dev/v2.7`；**G0′ 的 `-r2` 修订在 `main` 上完成再开分支**（r2 属发布后补正，不是 v2.7 的产物）；`tag v2.7` 必须在下一版代码合入 `main` **之前**定着 |
| **顺序规则（板不可用时的合法路径）** | ① G0′ 全部完成并四门绿 → ② 打 `snap-2.7-start`（打完后**复核** `gh run list --limit 3` 无新 run、`gh release list` 无新增 —— **`snap-*` 不匹配 `release.yml` 的 `tags: [v*]`**，该结论已在 v2.6 起草期以 run `35378057777` 实测坐实）→ ③ 库内项（P0/P1-1/P1-2/P2）在 `dev/v2.7` 推进 → ④ 板到达后做 P1-4/P1-5（一次会话完成 selftest 22 + CO-5 注入 + CO-8 定位 + 既有回归）→ ⑤ 全量回归 → 交付文档 → `tag v2.7` |
| **偏离申报** | 实际起点与上述不符（例如 G0′ 未完成即开工）时，须在交付文档首行声明；**G0′ 未完成即进入代码窗口属 HC-4 违反**，无论是否声明，交付均判不合格 |

**完成 G0′ 后的实测复核清单**（起草期未做，因 G0′ 未执行；开发方完成后须逐条实测并记录）：

```sh
git tag -l 'snap-2.7-start'                      # 快照存在
gh run list --limit 3                            # 无新 run（快照 tag 无副作用复核）
sh tools/apidump.sh --check   ; echo "exit=$?"   # 期望 exit=0（CO-1 已修）
sh tools/docsync.sh           ; echo "exit=$?"   # 期望 fail=0
```

---

## 附 A：里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 |
|---|---|---|
| G0′ | v2.6-r2 返工轮：行尾修复 + CHANGELOG/Release 更正 + 交付文档终态 + 三处漏刷 + 模板第五项 + docref 收窄 → 四门绿 | D+1.0 |
| M1 | CO-5 从站解析对称化（P0-1…P0-4）+ 用例 + 文档登记 | D+2.5 |
| M2 | selftest `modbus`/`log` 两套件 + 九处回刷（P1-1…P1-3） | D+3.5 |
| M3 | 代码卫生与证据链（P2-1…P2-3） | D+4.5 |
| M4 | 板侧同批会话：同步 + `AT+SELFTEST 22/22` + CO-5 注入对照 + `disc` 定位 + 既有回归 | D+6.0 |
| M5 | 全量回归（四门 + CI 七 job）→ 交付文档（含 §8）→ **tag v2.7** | D+6.5 |

## 附 B：风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| **G0′ 未完成即开工** | 门恒红 → v2.7 的 HC-4/AC-20 不可复现，重演 v2.6 的返工 | §6 顺序规则 + HC-4（交付判不合格）；G0′ 完成清单逐条实测 |
| `.gitattributes` 修法波及全仓检出行为 | `sizecheck`/`docref`/`docbuild` 可能连带变化 | 可裁量项 1 要求选该法时四门实测；否则优选"条目集合比对"（不依赖行尾） |
| CO-5 的修复改变既有从站用例语义（`crc_err` 口径） | 既有用例可能红 | AC-9 明确"既有语义不回退"；若必须改断言，须登记为**行为修正**并说明理由 |
| 从站"同形判据"定义不当（从站无在途事务） | 判据失效或误伤合法帧 | 可裁量项 2 要求口径写入 HC-3 文档表；AC-10 用主从同构用例固定边界 |
| selftest 新增套件吃掉 RAM（主站双 256B 缓冲） | 板侧自测固件 RAM 紧张（F103 更紧） | 可裁量项 4（复用缓冲）＋记录 RAM 增项；发布配置 `ET_MODULE_SELFTEST=0` 不受影响 |
| `smoke.sh` / `test_selftest.c` 的硬编码 20 漏改 | CI 双 job 红（renode + host） | AC-13 逐行 diff 清单 + HC-12 同批提交 |
| CO-8 的 `disc=1` 未定位到来源 | 交付文档登记不足 → 复现评审"未登记"问题 | AC-16 给出"已排除假设清单 + 下一步手段"的合格替代证据 |
| 板档期 | P1-4/P1-5 受阻（本版**无**"不扩"退路） | CO-14 已明示 v2.7 不得再援引"不扩"分支；若板不可用，须走 §5 报备并**显式改期**（不得静默降级），同时把 selftest 的 **host 侧**验证做满（`test_selftest.c` 覆盖两新套件） |
| CO-14 的 21→22 差异被下轮评审判为偏离 | 返工风险 | §1.1 已给裁定与理由，并要求在**交付文档 §8 与 `v3-candidates.md` 并置声明**（HC-11），供裁决而非隐藏 |
| `docref` 收窄后 r2 轮自身可能红 | G0′ 卡住 | 先把 r2 文档终态改好，再跑收窄后的门；门自证可红用**临时构造**而非真实违规（AC-7） |

> 环境注记（沿既有记录）：Windows 侧 `make` → `mingw32-make`；`apidump.sh` 锁 `LC_ALL=C`；本机 `wsl` 未安装、`/c/msys64` 不存在、MinGW 无 `libasan`（**起草期实测，与 v2.6 声明一致**）；体积读数以本机 CubeCLT 工具链为准并附环境注记；`apidump --check` / `--diff` 在本机为**分钟级**（v2.6 评审实测 `--check` 4m39s / `--diff` 6m29s），计划中的等待窗口按此预留。

---

> **起草期合规声明（`docs/开发计划模板.md` §7 五项自检）**：
> ① **内部 tag 命名**：本计划所有内部快照一律 `snap-2.7-start`（不带 `v` 前缀）；`v*` 会命中 `release.yml` 的 `tags: [v*]`，该结论**由 v2.6 起草期实测坐实**（run `35378057777`），本计划**未**声称"快照 tag 无副作用"这类未实测断言，且已在 §6 写明打完快照后的复核命令。
> ② **"无副作用/不会触发"类断言**：本计划不含未经实测的此类断言。文中出现的"`apidump --check` exit 0""docsync fail=0"等均为**待开发方实测**的目标值，非既成事实；起草期亲自实测的事实限于：分支 `main`、`HEAD c865f69`、`tag v2.6 = 2e57b16`、`gh release list` 显示 v2.6 为 Latest、`core.autocrlf=true`、`.gitattributes` 不存在、工作树 `docs/API_INVENTORY.md` CR=757 而 `HEAD` blob CR=0、selftest 内建套件 20（源码逐条计数 + `test_selftest.c:98` 硬断言 + `smoke.sh:97` 硬断言）、`README.md:237` 仍写 v2.4 归档、`port/stm32g474/README.md:84` 写"未同步"、`docs/bench.md` 已含"不作判据"口径声明。
> ③ **板侧落点标注**：P1-4/P1-5 的板侧落点一律标注为**库外私有文件**并写全路径（`D:\code\STM32CubeMX\G474VET6_ET_TEST\Core\Src\et_demo.c`、同步目录 `...\Core\et\`、走单脚本 `build/board_walk.py`），未写成库内 `examples/`（HC-8）。
> ④ **AC ↔ 冻结 API 形状互洽**：已执行，结论见 §4 末段 —— 本版全部改动为内部实现与 selftest 内部新增，**预期新增 0 项 / 修改删除 0 项**；唯一登记项是 `et_modbus_stats_t` 两个计数的**语义收紧**（字段与布局未动）。
> ⑤ **证据落点章节号 + 占位节清理**（本计划新增项，来源 `CO-11(v2.6)`）：本计划所有 AC 的证据落点均写明**章节号**（实机记录指向**新增 §15**，因 §14 已被 v2.6 占用；§12 为历史占位节，其承接关系由 G0′-5 标注）；未使用"待补"类占位指向。
>
> **维护约定**：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v2.7 后本文档归档不再改动。计划一经交付开发即**冻结**，§1.1 的 CO 处置结论、HC-1…HC-12、§5 的"不可偏离"清单不得单方修改。
