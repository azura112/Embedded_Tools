# v2.0 开发计划：契约冻结与工程单一来源

> 起草日期：2026-09-09 ｜ 基线：v1.9（HEAD `b4c8221`，板面零欠账、四 tag 收口完毕）｜ 状态：✅ 已交付（2026-09-09，见 v2.0开发交付__契约冻结与工程单一来源.md），本文档归档不再改动
> 上一版本：[v1.9] 27 模块 / 340 用例 × 2 几何 / 覆盖 97.0% / docbuild 门 / docsync 122
> 输入：v1.9 交付 §5.3"显式构建清单各自为政是三处 tag 翻车共因"自训；v1.9 验收遗留（3 处数字待回刷 + "size 读数 vs 体积表"门建议）；v1.9 §5.4 记录的 v2.0 候选（smap 折叠/通配、u32 全键域、shell Tab）

---

## 1. 版本定位

- **版本号 v2.0 的含义**：**契约稳定里程碑，而非破坏性版本**。v1.0→v1.9 全程纯增量兑现了承诺，v2.0 将这一事实固化为公开契约：API 冻结声明 + 弃用流程 + 构建单一来源。semver 策略不变——破坏性变更（若有）留给显式的 v3.0 窗口。
- **本版主题**：**定契约、断根因**——把"库该怎么发布、API 承诺什么"从散落的纪律条款变成机制（单一来源、size 门、冻结清单），同时清偿 v1.9 遗留与三个"仅记不排"候选。
- **不变式**（继续遵守）：零动态内存、多实例句柄化、core/algorithm 层零硬件依赖、ISR-safe 范围显式标注。

## 2. v1.9 现状盘点

| 项 | 状态 |
|---|---|
| 模块 | 27 个，340 用例 × 2 几何，覆盖 97.0%，CI 七 job（双交叉 + Renode smoke + docbuild + 覆盖率门）+ docsync 122 断言 |
| 平台 | host / F103（编译 + Renode）/ G474（真机全链路实证）——板面零欠账 |
| 治理遗留 | ① F103 体积表两行 stale（16852→17716、25352→26180）+ 交付 §6 docsync 121→122 ② "size 读数 vs README 体积表"断言未落地（v1.9 验收建议） |
| 结构性根因 | release.yml / ci.yml / Makefile **三份显式构建清单各自维护**——v1.7/v1.8 连踩、v1.9 三处 tag 翻车共因；README 已由 docbuild 单源化，CI 尚未跟进 |
| 候选堆积 | v1.9 明示"仅记不排"三项：smap 大小写折叠/通配、u32 全键域容器、shell Tab 补全（五版排除）——v2.0 必须**逐项决议关闭**，不许再滚 |

## 3. 开发范围

### P0 工程单一来源与遗留清偿（约 1 天，M1 先行）

| # | 任务 | 验收标准 |
|---|---|---|
| 1 | **3 处数字回刷**（17716 / 26180 / 122）+ "v1.8 人工 Release"追补自动产物（08ef770 树 + `-DET_MODULE_SELFTEST=1`，按交付文档 §5 方法） | `[数字回刷]` 小提交；port README/CHANGELOG/交付文档一致 |
| 2 | **CI 构建清单单源化**：ci.yml/release.yml 的 ARM 交叉构建不再内联命令，改调 `tools/docbuild.sh`（README 即唯一事实来源，docsync 版本链已有先例）；Makefile 保留为本地入口 | 三处清单收敛为两处（Makefile 薄壳 + README）；过渡期新旧命令并行跑一次比对产物一致 |
| 3 | **size 门**：交叉 job 的 `arm-none-eabi-size` 读数上传为 artifact 并在 job summary 输出；新增 `tools/sizecheck.sh`（本地发布前执行：全 port 构建 → 与体积表当前版本行逐值比对） | sizecheck 对故意改坏的行变红（自证）；发布 checklist 引用 |
| 4 | 版本宏升至 2.0.0 | 发布时与 tag 核对一致 |

**size 门设计决议（预填）**：CI ubuntu 的 apt 工具链版本与本机 CubeCLT 存在布局级差异，**不要求 CI 数字等于表值**——表行维护"本机 CI 等价环境"读数，门只做"读数可追溯 + 表行存在且格式合法"；精确相等由本地 sizecheck.sh 保证。

### P1 API 冻结评审（★ v2.0 核心，1 天，产出 `docs/API_STABILITY.md`）

| # | 任务 | 说明 |
|---|---|---|
| 1 | **API 清单机制化**：`tools/apidump.sh` 从全部公开头文件提取符号/签名/上下文标注，生成清单入 docs（`API_INVENTORY.md` 或 API_GUIDE 附章） | docsync 断言清单与头文件一致（新增公开 API 未登记即红） |
| 2 | **冻结声明**：v2.0 起公开 API 冻结；MINOR 只允许追加；**破坏性变更唯一入口 = 弃用流程**（标记 `ET_DEPRECATED` 宏 → 保留 ≥2 个 MINOR → 仅在 MAJOR 移除） | `ET_DEPRECATED` 用 `__attribute__((deprecated))` + 非 GCC 回退 |
| 3 | **一致性审计（兼容性修复窗口）**：逐模块盘点返回风格/参数序/命名/标注四类不一致——只允许**纯文档修正与新增**；触碰既有签名的项一律记入 `docs/v3-candidates.md`，本版不改 | 审计结论表入 API_STABILITY.md，每项有处置（fixed / doc-only / v3 候选） |
| 4 | README/API_GUIDE 链接冻结声明；CHANGELOG 标注 "v2.0 — API freeze" | docsync 断言引用存在 |

### P2 候选决议关闭（1~1.5 天，时间盒）

v1.9 §5.4 三项逐项给终局决议，**不许顺延**：

| 候选 | v2.0 决议（预填） |
|---|---|
| smap 大小写折叠/通配 | **做折叠，不做通配**：`et_smap_get_ci`（FNV 前 ASCII 折叠，与区分大小写共表）≤0.5 天 + 单测 ≥4；通配评估结论"需求未证实，关闭"入 v3 候选清单 |
| u32 全键域容器 | **关闭不做**：v1.8 键 0 FAQ + 应用侧 `key+1` 偏移方案已覆盖，终局决议写入 FAQ（引用即止，不再立项） |
| shell Tab 补全 | **做**：命令表 const 前缀匹配，单候选补全 + 多候选列选项；仅命令名（无路径/历史）；静态开销，`ET_SHELL_TAB` 默认 0。五版排除的理由是防范围蔓延，v2.0 以冻结收尾正当其时。≤1 天 + 单测 ≥6 |

### P3 发布工程补全（约 0.5 天）

| # | 任务 | 说明 |
|---|---|---|
| 1 | Release 正文自动生成：release.yml 注入 CHANGELOG 对应版本节（v1.7 P2-2 遗留） | 手工正文 → 模板生成，双源漂移终结 |
| 2 | 新 port 脚手架 `port/_template/`：port.h 契约四件套骨架 + 裁剪/几何宏清单 + 接入 checklist（G474/F103 经验浓缩） | 社区移植入口；docsync 断言模板含契约必选项 |

### P4 可选（时间盒 0.5 天，不阻塞）

- bench v2.0 行（smap_ci/tab 开销入表）；apidump 接入 CI 产 artifact；GitHub Issue 模板（移植求助/Bug 复现模板——库公开后引流）。

## 4. 交付标准（DoD）

1. 沿用 v1.9 全部 DoD（docsync 全绿、双几何、docbuild、数字回刷纪律、交付表覆盖率行）；
2. **单一来源生效**：ci.yml/release.yml 中不得再现完整构建命令（docsync/CI 双重断言 grep 防回归）；
3. **冻结契约三件套**：API_STABILITY.md + apidump 清单 + `ET_DEPRECATED` 宏——缺一不发 v2.0 tag；
4. **候选零堆积**：§3-P2 表三项每项有终局决议落文档；本版后不存在"仅记不排"悬置项；
5. size 门生效：sizecheck.sh 自证有效 + 交付文档体积数字与新鲜构建一致。

## 5. 里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 |
|---|---|---|
| M1 | P0 全部（回刷/单源化/size 门/2.0.0） | D+1 |
| M2 | P1 冻结评审完成（清单 + 声明 + ET_DEPRECATED） | D+2 |
| M3 | P2 三项决议 + smap_ci / shell Tab 合入 | D+3.5 |
| M4 | P3 发布补全 + P4 时间盒 | D+4 |
| M5 | 全量回归（CI 3 连绿）→ **tag v2.0** | D+4.5 |

## 6. 明确不做（Non-goals）

- **不做任何破坏性 API 变更**（冻结即本版目的；审计发现的破坏项只入 v3 候选清单）；
- 不做 smap 通配、不做 u32 全键域容器（本版终局关闭）；
- 不做 i2c/spi 抽象、RTOS、安全启动/签名、浮点格式化、动态内存（永久边界）；
- shell 不做路径补全/多行编辑/历史搜索（Tab 只补命令名）；
- 不做多语言文档与网站托管（README + docs/ 足够）。

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 冻结审计发现较多"想改"的不一致 | v2.0 失焦为破坏性版本 | 纪律预填：触碰签名一律 v3 候选；本版只做文档/新增，评审时间盒 1 天到期即决 |
| CI 单源化改造引入新断裂 | 门禁变红拖全线 | 过渡期新旧并行比对产物一次；docsync 断言"CI 无内联完整命令"防回流 |
| size 门在 CI 工具链版本漂移下误报 | 门禁疲劳 | 决议已隔离（CI 只存读数，精确比对在本地 sizecheck.sh）；表行注明测量环境 |
| apidump 提取启发式漏符号（宏/typedef） | 清单不完整致冻结承诺虚高 | 头文件公开面以 `et_` 前缀 + 命名规约提取，人工抽查一类一次；清单标注"签名面"而非"全部标识符" |
| Tab 补全 ANSI 解析与历史 ESC 序列纠缠 | shell 用例回归 | 与 v1.6 历史共用 ESC 状态机（单一解析路径），用例覆盖 ESC 混发场景 |

---

> 维护约定：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v2.0 后本文档归档不再改动。
