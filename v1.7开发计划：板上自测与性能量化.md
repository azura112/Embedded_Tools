# v1.7 开发计划：板上自测与性能量化

> 起草日期：2026-09-06 ｜ 基线：v1.6（HEAD `b669823`，tag v1.6 挂账中）｜ 状态：✅ 已交付（2026-09-06, 见 [v1.7开发交付：et_selftest库化与bench基线](./v1.7开发交付：et_selftest库化与bench基线.md)；板上记录与顺序收口挂账见交付 §5）
> 上一版本：[v1.6] 23 模块 / 291 用例 × 2 几何全过 / 覆盖 96.9% / 双几何制度 + tickless API + G4 入库
> 输入：v1.6 挂账（走单回填 + tag 补打、bench 顺延、Renode G4 顺延）；G474 工程 AT+SELFTEST 为工程私有实现（未库化）；验证金字塔缺口：PC 单测 → CI 仿真 → **板上自测无库级组件**

---

## 1. 版本定位

- **版本号 v1.7**：新增 et_selftest 组件 + bench 基准 + CHANGELOG，均为纯增量，无破坏性变更（MINOR）。
- **本版主题**：给验证金字塔**封顶**——库级板上自测组件让任何 port 一条命令获得全模块冒烟能力（v1.0"PC 可全量单测"的最后一块），host 基准把性能从"感觉"变成"数字"，CHANGELOG 把六版历史收敛为单一入口。
- **不变式**（继续遵守）：零动态内存、多实例句柄化、core/algorithm 层零硬件依赖、ISR-safe 范围显式标注。

## 2. v1.6 现状盘点

| 项 | 状态 |
|---|---|
| 模块 | 23 个，291 用例 × 2 几何全过，覆盖 96.9%，docsync 59 断言，CI 双交叉编译 + Renode smoke + 覆盖率门 |
| 平台验证 | host（291×2）→ F103（编译 + Renode 仿真门）→ **G474（真机 13/13 自测）** |
| 挂账 | ① v1.6 升级链走单 5 条待上板回填 + CI 3 连绿后补打 tag v1.6 ② bench 基准两次顺延（v1.6 P3-1"下版按需"）③ Renode G4 两次顺延——G474 真机已承担 G4 验证职责，仿真价值存疑，需政策了断 |
| 结构性缺口 | 板上自测（AT+SELFTEST 13 套件）是 G474 工程私有代码——新 port 无法复用，与"PC 可全量单测"的库级承诺不对称 |

## 3. 开发范围

### P0 v1.6 收口与政策了断（约 0.5 天，与 M2 并行不互堵）

| # | 任务 | 验收标准 |
|---|---|---|
| 1 | **v1.6 收口**：G474 板执行走单 5 条叙事（runbook 就绪），回填实机记录 §5；CI `cross-stm32g474` 满 3 连绿 | 走单结果表落数 + docsync 断言绿 → 按 checklist 走 release.yml 补打 **tag v1.6** |
| 2 | **Renode G4 正式关闭**（政策决定，非顺延）：G474 真机已承担 G4 平台验证职责，仿真不再排期；port README 挂账表改"不排期（真机覆盖）" | docsync 断言同步 |
| 3 | `et_config.h` 版本宏升至 1.7.0 | 发布时与 tag 核对一致 |

### P1 `et_selftest` —— debug/ 板上自测组件（★ 核心，2 人日）

把 G474 工程的 AT+SELFTEST 模式**库化**：任何 port 接入后，一条命令跑全模块冒烟。

- **形态**：`debug/et_selftest.c/.h` + `ET_MODULE_SELFTEST` 开关（**默认 0，发布可裁剪**——记录全启用时的 text 增量）；
- **框架**（自带轻量断言宏 ~30 行，不与 host 侧 `test/et_test.c` 共享代码，文档声明二者关系）：
  - 套件注册表 + 用例计数/失败计数 + `et_selftest_report_fn` 回调（接 et_log 或 shell 应答）；
  - `et_selftest_run_all(report, user)` 一次跑全部；`et_selftest_run_suite(name)` 单套件；
- **用例集**：移植 G474 已验证的 13 套件冒烟子集（ringbuf/queue/mempool/list/filter/fsm/sched/event/stimer/crc/frame/softclock/wdt），**补齐 v1.4~v1.6 新模块**（kv 只读冒烟/xmodem 短传输/atcmd+shell 行解析/bootctl 状态头校验），掉电注入类 host-only 用例不移植（文档注明覆盖边界）；
- **host 侧复用**：同一份 et_selftest 在 host 构建下也注册进 `test/test_main.c` 跑一遍（板上/PC 结果可比对）；
- **接入配方**：API_GUIDE 新章节 + G474 工程的私有 SELFTEST 代码退役改为调用库组件（双向同步纪律）；
- 单测：自测框架自身 ≥ 8 例（套件注册/失败计数/报告回调/空套件/裁剪开关）；
- **真机验收**：G474 板上 `AT+SELFTEST`（库化版）全绿记录，实机记录补章节。

### P2 `tools/bench.c` —— host 基准套件（约 1 天，两次顺延收编）

| 基准 | 说明 |
|---|---|
| ringbuf 吞吐 | 单字节流 + 块粒度 MB/s（POW2 与非 POW2 各一组） |
| crc 吞吐 | crc16/crc32 位算法 vs `ET_CRC_TABLE=1` 查表，MB/s 对比 |
| xmodem 有效吞吐 | 128B / 1K 块型，含协议开销的净速率 |
| kv 操作速率 | host 虚拟 flash 上 set（追加）/get/压实 单位时间次数 |
| filter/fsm 单次耗时 | 每操作 ns 级 |

- 固定迭代数 + 多轮取中位数，结果写 `docs/bench.md`（**环境注记强制**：CPU/编译器/flags，沿 checklist）；数字随版本更新，docsync 断言文件存在 + 版本行；
- 不承诺跨机器可比性（文档声明：数字仅用于版本间回归对比与量级判断）。

### P2-2 `CHANGELOG.md`（约 0.5 天）

- v1.0→v1.6 六版从计划/交付文档提炼，Keep-a-Changelog 风格（Added/Changed/Fixed/挂账）；模板注明后续版本随交付更新；
- docsync 断言：文件存在 + 最新版本条目与 `ET_VERSION_STRING` 一致；
- release.yml 附 CHANGELOG 到 Release 正文（可选增强）。

### P3 可选（时间盒各 0.5 天，不阻塞发布）

| # | 任务 | 说明 |
|---|---|---|
| 1 | demo tickless 实装 | `stm32f103_demo` / `stm32g474_demo` 主循环改用 `next_due()` + WFI + RX 中断唤醒（配方 11.8 落地示范），Renode smoke 与真机日志验证不回归 |
| 2 | F103 Renode smoke 增 selftest 断言 | 仿真里跑 `AT+SELFTEST`（若 F103 几何下 text 预算允许） |
| 3 | `et_map`（RAM 定容哈希表） | 仅记录为 v1.8 候选，本版不展开 |

## 4. 交付标准（DoD）

1. 沿用 v1.6 全部 DoD（docsync 全绿门、双几何硬性项、量化声明附复现命令与环境注记）；
2. **et_selftest 附加**：`ET_MODULE_SELFTEST=0` 编译烟测（空 TU）；全启用 text 增量记录并写入 port README 体积表；板上跑通记录入实机文档 + docsync 断言；
3. bench 数字必须多轮中位数 + 环境注记，无注记数字不入文档；
4. CHANGELOG 版本链与 tag/docsync 三方一致；
5. tag v1.7 前置：v1.6 走单已回填、tag v1.6 已补打（顺序收口，不留双版本挂账）。

## 5. 里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 |
|---|---|---|
| M1 | P0-2/P0-3（G4 政策关闭 + 1.7.0）；P0-1 走单回填与板执行并行推进 | D+0.5 |
| M2 | `et_selftest` host 全绿（框架 + 全套件） | D+2 |
| M3 | G474 板上库化 SELFTEST 记录；v1.6 走单回填完成 + **tag v1.6** | D+3 |
| M4 | bench + CHANGELOG | D+4 |
| M5 | P3 时间盒 → 全量回归（CI 实跑）→ **tag v1.7** | D+5 |

## 6. 明确不做（Non-goals）

- 不做 shell Tab 补全 / 多行编辑（维持两版边界）；
- 不做 `et_map`/新容器（v1.8 候选）；不做 i2c/spi 总线抽象（超出库定位）；
- Renode G4 本版**正式关闭**（政策，见 P0-2）；F103 真机维持常设挂账；
- 不做安全启动/加密/bootloader 工程（沿 v1.5 边界）；RTOS / 浮点格式化 / 动态内存依旧不做；
- et_selftest 不追求与 host 测试用例数对等（掉电注入等 host-only 类不移植，覆盖边界文档化）。

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| et_selftest 用例集与 host 测试漂移（两套断言） | 板上结果失真 | 用例集从 G474 已验证实现移植 + host 侧同跑比对（§3-P1）；漂移由 docsync 版本断言间接约束 |
| 全模块 selftest 的 Flash 占用 | 小平台装不下 | 默认裁剪 + 体积增量入表；分套件运行 API 支持按需启用 |
| bench 数字噪声 | 版本间误判 | 固定迭代 + 多轮中位数 + 明确"仅版本内对比"声明 |
| v1.6 走单仍依赖板子档期 | v1.7 tag 双版本挂账 | DoD #5 硬性顺序：v1.6 未收口不接 v1.7 tag；板执行为 M3 关键路径，提前到与 M2 并行 |
| CHANGELOG 提炼遗漏 | 版本记录失真 | 逐版对照交付文档"重点概况"章节；docsync 断言版本行齐全 |

---

> 维护约定：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v1.7 后本文档归档不再改动。
