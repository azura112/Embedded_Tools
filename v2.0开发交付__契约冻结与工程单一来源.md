# v2.0 开发交付：契约冻结与工程单一来源

> 交付日期：2026-09-09 ｜ 版本：v2.0.0（`ET_VERSION = 0x020000`，**API freeze**）｜ 基线：v1.9（`b4c8221`）
> 计划文档：[v2.0 开发计划__契约冻结与工程单一来源](./v2.0开发计划__契约冻结与工程单一来源.md)（状态：✅ 已交付归档）
> 远程：https://github.com/azura112/Embedded_Tools（main @ 本交付提交）

## 1. 重点概况（TL;DR）

v2.0 是**契约稳定里程碑**：v1.0→v1.9 全程纯增量兑现的承诺，从"散落的纪律条款"固化为**机制**——公开 API 冻结（清单机读 + 漂移即红）、构建单一来源（README=文档即测试的完成体，CI/Release 不再内联任何完整构建命令）、体积数字由门守护而非由人守护。同时候选三项逐项终局关闭，**本版后不存在"仅记不排"悬置项**。

- **P0 工程单一来源与遗留清偿**：ci.yml/release.yml 的 F103/G474 交叉构建与 coverage **全部转调** `tools/docbuild.sh`/`Makefile`（f103 docbuild 块升级为"默认 + `_selftest`"双构建，Renode smoke 改消费 `_selftest.elf`；过渡期新旧产物 sha256 逐字节一致）；三处内联清单收敛为两处（Makefile 薄壳 + README 定界块）+ **docsync/CI 双断言防回流**。`tools/sizecheck.sh` 体积表逐值门（本地发布前、与当前版本行 text/data/bss 比对、CI 只存读数不猜等值——v1.9 决议落地）：**首跑即抓到版本宏 -4B、M3 代码 +124B 两次真实漂移**，自证有效。v1.9 遗留三处数字回刷（16852→17836、25352→26304、docsync 121→142），v1.8 Release 产物核对在位。
- **P1 API 冻结（★ 核心）**：`tools/apidump.sh` 从公开头提取签名面（**179 函数 / 66 类型 / 96 宏**）生成 `docs/API_INVENTORY.md`；docsync 内嵌 `--check`——**新增公开 API 未登记清单即红**（首跑就抓到我新增 `ET_DEPRECATED` 未登记；CI 首红定位 gawk UTF-8 locale 对含 🏠 emoji 注释行的正则漂移，锁 `LC_ALL=C` 根治——机制上线第一天就抓住两类问题）。`docs/API_STABILITY.md`：v2.0 起冻结、MINOR 只追加、**破坏性变更唯一入口 = 弃用流程**（`ET_DEPRECATED` → ≥2 个 MINOR → MAJOR 移除）+ port 契约同冻结（新增钩子走默认实现/弱符号 + `ET_VERSION` 门）。一致性审计 7 项（回调 user 位两派/foreach 形状/动词命名等）：触碰签名者一律入 `docs/v3-candidates.md`（本版零破坏），5 头上下文标注缺失 **doc-only 补齐**（清单 `--check` 实证签名面未变）。README/API_GUIDE 链接与 CHANGELOG "v2.0 — API freeze" 标注齐。
- **P2 候选决议关闭（零堆积）**：**做折叠，不做通配**——`et_smap_put_ci/get_ci/del_ci`（ASCII 折叠入共用表/池，与大小写敏感条目共表共存，4 例，折叠查找 11.0 ns/op）；**shell Tab 命令名补全**——`ET_SHELL_TAB` 默认 0（关闭态直透零变化），大小写敏感前缀匹配、唯一补全/多候选列名重绘/无匹配响铃/参数段静默，7+1 例双形态矩阵（`make test-tab` 常设 CI，与历史共用单一 feed 状态机、ESC 混发用例覆盖）；u32 全键域容器**终局关闭**（键 0 FAQ 更新：`key+1` 偏移为唯一路径，不再立项）。
- **P3/P4 发布工程**：release.yml **注入 CHANGELOG 对应版本节**为 Release 正文（无节即红——手工正文双源漂移终结，v1.7 遗留收编）；`port/_template/` 移植脚手架（契约四件套骨架 + 几何三宏 + `#error` 守卫样例 + 8 步接入 checklist + 常见坑速查；模板双态验证：填 -D 编译过、漏 -D 即 #error）；bench v2.0 行、CI 上传 API 清单 artifact、GitHub Issue 模板（移植求助/Bug 复现）。
- **过程自省（纪律外溢两例，本版内修复）**：CI YAML 步骤名裸冒号 + 块标量内顶格多行 printf 造成解析红（run 34367730635）；apidump 本地/CI 清单环境差。教训与"三份清单各自为政"同源——**复制即漂移**，凡复制文本/命令处必须有单一来源 + 机读比对。

## 2. 逐项验收对照（v2.0 计划 §3）

| # | 任务 | 验收结果 |
|---|---|---|
| P0-1 | 三处数字回刷 + v1.8 产物 | ✅ `[数字回刷]` 提交链；sizecheck 3/3；v1.8 Release 工件四件在位 |
| P0-2 | CI/Release 单源化 | ✅ 两 yml 无内联完整构建命令（docsync 反向断言防回流）；过渡期新旧产物 **sha256 一致** |
| P0-3 | size 门 | ✅ `sizecheck.sh` 自证有效（抓到两次漂移即红→修→绿）；CI 交叉 job 读数上 artifact+step summary |
| P0-4 | 版本 2.0.0 | ✅ `0x020000`，与 tag 核对一致 |
| P1-1 | apidump 清单机制 | ✅ 341 项机读清单，docsync `--check` 断言（新增未登记即红） |
| P1-2 | 冻结声明 + `ET_DEPRECATED` | ✅ API_STABILITY §1/§3；宏 GCC attribute + 非 GCC 空展开，编译双配置验证 |
| P1-3 | 一致性审计 | ✅ 7 项处置表（fixed doc-only×1 / 无需×2 / v3 候选×4）；本版零签名变更（清单 --check 证明） |
| P1-4 | 文档链接标注 | ✅ README/API_GUIDE/CHANGELOG 三处 |
| P2 | 三项决议 | ✅ 折叠做（11ns）/通配关/u32 关/Tab 做——**候选零堆积**达成 |
| P3-1 | Release 正文自动生成 | ✅ CHANGELOG 节注入 + 无节即红 |
| P3-2 | port/_template | ✅ 骨架编译验证 + 守卫负验证 + docsync 6 断言 |
| P4 | 时间盒三项 | ✅ bench v2.0 行 / apidump artifact / Issue 模板——全部完成，无遗留 |

## 3. 里程碑对照

| 里程碑 | 内容 | 提交 |
|---|---|---|
| M1 | P0 全部（回刷/单源化/sizecheck 门/2.0.0） | `0c92ea8` |
| M2 | P1 冻结三件套 + 审计（apidump/STABILITY/DEPRECATED/v3-candidates） | `2d77fc6` |
| M3 | P2 候选关闭（smap_ci/shell Tab）+ CHANGELOG 填充 | `8525a33` |
| M4 | P3 发布补全 + P4 时间盒 + YAML/locale 两修复 | `19f2fff` / `2a02867` / `5138b84` |
| M5 | 回归终值 + 交付文档 + Release 正文提取修复（tag v2.0 定着点） | `99fbc19` / `6bb4f48` |

## 4. DoD 对照（v2.0 §4）

| DoD | 状态 | 证据 |
|---|---|---|
| 1 沿用 v1.9 全部 DoD | ✅ | docsync **142/142**；345×2 双几何全绿（1K=346，test-tab 开启形态 8 例）；交付表含覆盖率行 |
| 2 单一来源生效（不得再现完整构建命令） | ✅ | docsync 反向断言（两 yml 无 `-o build/stm32f103_demo.elf`）+ CI docbuild/交叉 job 转调 |
| 3 冻结契约三件套缺一不发 tag | ✅ | API_STABILITY + apidump 清单 + ET_DEPRECATED 齐 |
| 4 候选零堆积 | ✅ | v3-candidates.md 每项有终局；本版后无"仅记不排" |
| 5 size 门生效 + 交付数字与新鲜构建一致 | ✅ | sizecheck pass=3（17836/26304/18208 终值） |

## 5. 挂账与后续

1. ✅ 已闭环：tag v2.0 定着 `6bb4f48`（首跑 Release 因正文提取不认简 tag 红一次→当场修→二跑全绿；Release 正文自动注入 CHANGELOG 节机制验证通过，工件 elf/bin/demo.exe/SHA256SUMS 在位，v2.0 置 Latest）。
2. v3.0 评审窗口素材池 = docs/v3-candidates.md（回调 user 位统一/foreach 形状/动词命名三项破坏性候选 + 关闭决议复审），**无到期义务，只有需求触发**。
3. 永久边界不变：RTOS/浮点格式化/动态内存/i2c/签名启动/多行编辑等（Non-goals 连续六版）。

## 6. 量化数据复现（环境注记）

| 项 | 复现命令 | 环境 |
|---|---|---|
| 单元全量 | `make test` / `make test-g4` RESULT 行 **345×2**；1K 变体 346；`make test-tab` 8 例形态 | MinGW gcc 16.1.0 Git Bash；CI ubuntu make 同绿 |
| 覆盖率 | CI `coverage` job → `lines: 96.9% (7160 out of 7387)`，门 ≥85% | ubuntu-latest 系统 gcc + gcovr（run `34369232029`） |
| docsync | `sh tools/docsync.sh` → **142/142**（内嵌 apidump --check，`LC_ALL=C`） | Git Bash |
| API 清单 | `sh tools/apidump.sh --check`（179 函数/66 类型/96 宏，共 341 项） | 同上；跨环境须先锁 C locale |
| ARM 体积终值 | `sh tools/sizecheck.sh` → 3/3；F103 **17836**(默认)/**26304**(selftest)、G474 **18208**；CubeMX v2.0 同步后另记 | GNU Tools for STM32 13.3.rel1 + arm-none-eabi-size |
| bench | `make bench`/`make bench-table`（docs/bench.md v2.0 行：smap_ci 11.0 ns/op、map 2.0、smap 8.0） | 同 v1.7 基线注记 |
