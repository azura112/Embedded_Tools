# v1.4 开发计划：命令交互与传输生态

> 起草日期：2026-09-03 ｜ 基线：v1.3（tag `5d4d82b`）｜ 状态：✅ 功能交付完成 v1.4（2026-09-03）——**tag 暂缓**（P0 外部依赖：远程仓库未建立，DoD #5 硬性项不满足）
> 变更记录：M1~M5 功能项全部完成；P0-1/P0-2（远程+CI 实跑）因账号侧阻断（无 gh CLI、GitHub 443/22 均不可达、SSH key 未注册）挂起，按计划 §7 风险对策"tag 推迟不推迟功能"处理；P3-3（demo 串联）经评估列为 v1.5 候选。
> 上一版本：[v1.3] 19 模块 / 206 用例全过 / 行覆盖 97.0% / Renode CI 门 + 发布 verify 两段式
> 输入：v1.3 交付常设挂账（真机不阻塞、CI 3 连绿待远程）；v1.3 验收保留项（ARM 体积数字未注明测量环境，不可复现）

---

## 1. 版本定位

- **版本号 v1.4**：新增两个模块 + 一组增量 API + 工程收尾，无破坏性变更（MINOR）。既有 19 模块公开 API 一律不动。
- **本版主题**：把库从"组件齐"推向"**设备可交互、固件可更新**"——交付行式交互壳 `et_shell`（atcmd 之上）与 XMODEM-CRC 传输 `et_xmodem`（protocol 层），同时把拖了三版的**远程仓库 + CI 实跑**升为 P0 正式验收项。
- **不变式**（继续遵守）：零动态内存、多实例句柄化、core/algorithm 层零硬件依赖、ISR-safe 范围显式标注。

## 2. v1.3 现状盘点

| 项 | 状态 |
|---|---|
| 模块 | 19 个（core 4 / algorithm 2 / sys 4 / protocol 3 / storage 1 / drivers 3 / debug 2），206 用例全过，覆盖 97.0% |
| CI/发布 | renode-smoke 门 + 覆盖率门 + 交叉编译 + verify→release 两段式——**均未实跑**（仓库无 remote，v1.3 起挂账） |
| 仿真/真机 | Renode smoke 5 断言交付；真机为常设挂账（不阻塞发布，政策已固化） |
| 验收遗留 | v1.3 交付的 ARM `text=11208` 在验收环境不可复现（±16B 布局级差）——量化声明的测量环境未注明，需按 checklist 补正 |
| 生态缺口 | 无交互壳（用户须自行在 atcmd 上包 prompt/echo）；无固件传输协议（bootloader 场景缺最后一环） |

## 3. 开发范围

### P0 工程收尾（约 0.5 天，**外部依赖：需账号侧创建远程仓库**）

| # | 任务 | 验收标准 |
|---|---|---|
| 1 | **建立 git remote 并推送 main + 全部 tag**，观察 CI 首跑 | ubuntu/windows 单测矩阵 + 覆盖率门 + 交叉编译 + **renode-smoke** 全绿；连续 3 次 run 绿 → v1.3 挂账"CI 3 连绿"正式销账 |
| 2 | 验证 release.yml：推测试 tag 走通 verify→release 两段 | GitHub Release 产出 host 报告 + ARM ELF/BIN 附件 |
| 3 | v1.3 验收保留项补正：port README 的 ARM 体积条目注明测量环境（工具链精确版本 + shell）或改为可复现数值 | 按新环境实测值更新，标注复现命令 |
| 4 | `et_config.h` 版本宏升至 1.4.0 | 发布时与 tag 核对一致 |

### P1 `et_xmodem` —— protocol/ 固件传输（约 1.5 天）

XMODEM-CRC 接收器为核（bootloader 拉取固件镜像），发送器为可选编译项。与既有资产强协同：

- CRC 复用 `et_crc16_ccitt_update(seed, …)` 流式 API——XMODEM-CRC 种子为 0x0000（与 CCITT-FALSE 的 0xFFFF 仅初始值之差，**流式种子天然支持，无需新增 CRC 变体**）；
- 超时/重试由调用方注入 `now`（host 虚拟时间可全速测试），不内部取时基——与 stimer/sched 同风格；
- `ET_XMODEM_1K=1` 可选 1024B 块（STX），默认关。

```c
typedef enum { ET_XM_ACK, ET_XM_NAK, ET_XM_CAN, ET_XM_IDLE, ET_XM_DONE, ET_XM_ERR } et_xm_act_t;
void et_xmodem_rx_init(et_xmodem_t *x, uint8_t *buf, uint32_t cap, et_xm_sink_fn sink, void *user);
et_xm_act_t et_xmodem_rx(x, ch, now);          /* 逐字节喂, 返回应答动作 */
et_xm_act_t et_xmodem_rx_tick(x, now);         /* 超时驱动(1s 无 SOH 重发 NAK, 10s 静默退出) */
```

- 状态机覆盖：SOH/STX 分块、块号 + 补码校验、序号回绕（0→1）、CRC 失败 NAK、重复块重发 ACK、EOT 两段确认、CAN×2 立即中止；
- `sink` 回调把连续收到的数据交给调用方（写 flash 走 `port_flash_write`，或 RAM）；
- 单测 ≥ 15 例：正常流、序号回绕、重复块、CRC 坏块重传、超时重 NAK、10s 静默、CAN 中止、1K 模式、缓冲满拒绝、脏字节抗噪

### P2-1 `et_shell` —— debug/ 交互壳（约 1 天，时间盒严格）

et_atcmd 之上的薄交互层，不做解析重复：

- 输入回显 + 退格擦写（终端 `\b \x1b[K` 序列，可关）；行首提示符（可定制/可关）；
- `help` 命令自动生成：扫描命令表输出名称 + 帮助串（命令表新增可选 `help` 字段，atcmd 结构体**追加字段不改旧语义**，C99 指定初始化器兼容）；
- 不做：历史记录、Tab 补全、多行编辑（Non-goals，v1.5 再议）；
- 单测 ≥ 10 例：回显开关、退格行首边界、超长行、help 输出逐行比对、atcmd 事件透传

### P2-2 `et_kv` 增量 API：`et_kv_iter`（约 0.5 天）

枚举有效 key（诊断导出/上位机批量读取场景），纯读侧实现：

- `et_kv_iter_init(kv)` / `et_kv_iter_next(kv, &it)`（返回 key 名 + 长度 + 值所在信息）；只读、不触碰页状态；
- 单测 ≥ 6 例：多 key 枚举完整性、tombstone 不出现、多版本只出最新、空库/满库边界、迭代中 set 的安全性（快照语义：迭代基于当前活跃页）

### P3 可选（不阻塞发布）

| # | 任务 | 说明 |
|---|---|---|
| 1 | `ET_CRC_TABLE=1` 查表优化 | 256 项静态表换吞吐（默认关保持零 RAM）；表可由宏选择放 flash 段 |
| 2 | API_GUIDE §9 新配方：断言故障现场落 kv | et_assert 钩子 + et_kv 组合，重启后 `et_kv_get("fault")` 取现场——纯文档配方，展示 debug×storage 协同 |
| 3 | demo 串联 | `stm32f103_demo` 增 shell 入口（`AT+` 命令桥接）+ xmodem 收数写参数区演示（可选） |

## 4. 交付标准（DoD）

1. 沿用 v1.3 全部 DoD（.c/.h 成对 + 上下文标注 + 开关 `#ifndef` 烟测 + 套件注册 + 零警告零 malloc）；
2. **量化声明一律附复现命令 + 环境注记**（v1.3 验收教训写入 checklist 的强化条目）；
3. et_xmodem 附加：对端行为矩阵测试（host 虚拟时间驱动，无真实延时）；文档给出与 `port_flash_write` 组接的 bootloader 数据通路图；
4. et_shell 附加：文档明确"atcmd 仍可独立使用"，shell 为可选增强；
5. CI 首跑 3 连绿作为 P0 验收硬性项，未完成不得打 tag v1.4。

## 5. 里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 | 结果 |
|---|---|---|---|
| M1 | P0 远程建立 + CI 3 连绿 + 收口补正 | D+0.5（依赖远程创建） | 🟡 收口补正完成（README/checklist/环境注记/1.4.0）；远程建立与 CI 实跑因账号侧阻断挂起（详见交付文档 §6） |
| M2 | `et_xmodem` 合入 | D+2 | ✅ 17 例对端行为矩阵全绿（`7e5384f`），双变体均过 |
| M3 | `et_shell` + `et_kv_iter` 合入 | D+3 | ✅ shell 15 例（`1e0e27d`）+ kv_iter 7 例（`c59b3bc`）全绿 |
| M4 | P3 可选项（时间盒内）+ 全量文档同步 | D+4 | ✅ P3-1 CRC 查表（`316c85f`）+ P3-2 配方 11.6；P3-3 demo 串联列 v1.5 候选 |
| M5 | 全量回归（CI 实跑 + 本地）→ **tag v1.4** | D+5 | ✅ 本地回归 245 全绿 + ARM 零警告 + 覆盖 97.1%；🟡 **tag 按 DoD #5 暂缓**（P0 硬性项未满足） |

## 6. 明确不做（Non-goals）

- 不做 YMODEM/ZMODEM/批传输协议——XMODEM-CRC（含 1K 变体）已覆盖 bootloader 拉镜像场景；
- et_shell 不做历史/Tab 补全/多行编辑/脚本解释器——定位是"调试够用"的行式壳；
- 不做通用文件系统、加密、压缩、RTOS 适配（沿 v1.2/v1.3）；
- 不改既有 19 模块公开 API 语义（atcmd 命令表追加 help 字段为唯一例外，追加不改旧）；
- 不引入浮点、不引入动态内存。

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 远程仓库创建依赖账号操作 | M1 阻塞 | P0 标注外部依赖，账号侧就绪前开发不中断（M2/M3 与 P0 并行）；tag 推迟不推迟功能 |
| CI 首跑暴露环境差异（如 Renode 版本漂移） | 门禁红 | Renode 已固定 1.16.1；失败保留日志 artifact，按 v1.3 smoke.sh 头注的三坑排查 |
| xmodem 对端行为组合爆炸 | 测试遗漏 | 对端脚本化 test double + 虚拟时间，按状态机边覆盖；矩阵清单入测试文件头注 |
| shell 范围膨胀（历史/补全诱惑） | 时间盒失守 | Non-goals 显式排除；help 之外的新需求一律记 v1.5 候选 |
| atcmd 命令表加字段引发兼容疑虑 | 用户升级顾虑 | 追加字段在结构体尾部 + 指定初始化器兼容示例 + 文档声明 ABI 策略（源级兼容，重编译即可） |

---

> 维护约定：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v1.4 后本文档归档不再改动。
