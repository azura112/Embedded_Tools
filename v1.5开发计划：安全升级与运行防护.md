# v1.5 开发计划：安全升级与运行防护

> 起草日期：2026-09-05 ｜ 基线：v1.4（HEAD `6f346b0`，tag v1.4 待 CI 销账后补打）｜ 状态：草案
> 上一版本：[v1.4] 21 模块 / 245 用例全过 / 行覆盖 97.1% / xmodem + shell + kv_iter
> 输入：v1.4 交付挂账（远程+CI 销账、demo 串联 v1.5 候选、shell 历史"再议"）；v1.4 验收发现（port README 同步声明脱节——"声称未做"第二次出现，需根治）

---

## 1. 版本定位

- **版本号 v1.5**：新增 et_bootctl / et_wdt 两模块 + shell 可选历史，port 契约再演进（wdt 三件套，纯增量），无破坏性变更（MINOR）。
- **本版主题**：把 v1.4 的"固件可更新"补上**安全**二字——镜像容器校验 + A/B 试运行/确认/回滚状态机；同时以看门狗模块闭环 v1.2 以来 flash 契约中的喂狗指引，并以**脚本化文档同步断言**根治两次复发的"声称与实际脱节"。
- **不变式**（继续遵守）：零动态内存、多实例句柄化、core/algorithm 层零硬件依赖、ISR-safe 范围显式标注。

## 2. v1.4 现状盘点

| 项 | 状态 |
|---|---|
| 模块 | 21 个（core 4 / algorithm 2 / sys 4 / protocol 4 / storage 1 / drivers 3 / debug 3），245 用例全过，覆盖 97.1%，双变体（XM 1K / CRC 表）全绿 |
| 挂账 | ① 远程+CI 首跑（账号侧阻断，tag v1.4 按 DoD #5 暂缓）② release 实跑 ③ demo 串联（v1.4 列为 v1.5 候选）④ 真机（常设，不阻塞） |
| shell 预留 | v1.4 Non-goals 明示"历史记录、Tab 补全、多行编辑 v1.5 再议"——本版收编历史，补全仍排除 |
| 验收遗留 | v1.4 交付声称"port README 体积表 v1.4 行 + 测量环境注记"实际未写入（f2342fe 仅改平台表一行）——同类脱节 v1.2 已发生一次，靠人肉 checklist 防不住 |

## 3. 开发范围

### P0 销账与根治（约 0.5 天；**前置依赖：账号侧建远程**）

| # | 任务 | 验收标准 |
|---|---|---|
| 1 | 推送 main + 全部 tag，CI 首跑；连续 3 run 绿后**补打 tag v1.4** | 单测矩阵 / 覆盖率门 / 交叉编译 / renode-smoke 全绿；v1.4 的 verify→release 流实跑一次，产出 Release 附件 |
| 2 | 补上 v1.4 缺失的 port README 内容：体积表加 v1.4 行（`text=12276 data=4 bss=276`）+ 测量环境注记（工具链精确版本 + shell + `arm-none-eabi-size`） | 与 v1.4 验收实测一致；表内 v1.1~v1.5 逐版本可追溯 |
| 3 | **根治"声称未做"：新建 `tools/docsync.sh`**——集中存放文档同步断言（每条 = grep 断言 + 一句话说明），覆盖本版及既往所有"已同步"声明点；发布 checklist 与 CI 均调用 | 首批断言含 v1.4 缺口两条；脚本在 P0-2 完成后全绿，且能对故意破坏的文档变红（自证有效） |
| 4 | `et_config.h` 版本宏升至 1.5.0 | 发布时与 tag 核对一致 |

### P1 `et_bootctl` —— storage/ 安全升级状态机（★ 核心，2 人日）

v1.4 通路（xmodem 收镜像 → `port_flash_write` 写槽位）之上补齐"安全"闭环。**交付的是库组件，不是 bootloader 工程**——跳转/搬运逻辑属应用，本模块负责可复用的镜像头校验与升级状态记录：

- **镜像头 `et_boot_img_hdr_t`（32B，槽位首部）**：magic / 头版本 / 镜像大小 / `img_crc32`（复用 et_crc32）/ 镜像版本号 / 头自身 CRC——`et_bootctl_verify_hdr()` + 全镜像 CRC 校验 API；
- **状态记录（独立状态扇区，不依赖 et_kv——bootloader 场景可不启用 kv）**：append-only 递减位编码（0→1 写，任意断电只丢"最后一步"，状态机天然可恢复），记录：试运行标志（slot）、启动尝试计数、确认标记；
- API 预览：

```c
bool et_bootctl_init(bc, const et_bootctl_cfg_t *cfg);   /* cfg: 状态扇区 + A/B 槽位几何 */
bool et_bootctl_verify_image(bc, slot);                   /* 头 + 全镜像 CRC */
bool et_bootctl_stage(bc, slot);                          /* 置试运行 + 清计数 */
bool et_bootctl_confirm(bc, slot);                        /* 应用自检通过后确认 */
uint32_t et_bootctl_boot_attempt(bc, slot);               /* bootloader 开机调用: 计数+1 并返回 */
bool et_bootctl_should_rollback(bc, slot);                /* 试运行且计数超阈值 → true */
```

- 单测 ≥ 18 例：**掉电矩阵**（stage/confirm/计数写入三类断点各 ≥2）、递减位编码边界（计数耗尽）、坏头/坏 CRC 拒绝、双槽互斥、状态扇区坏块自愈（重初始化）、cfg 非法拒绝

### P2-1 `et_wdt` —— sys/ 看门狗（约 1 人日，含契约演进）

port 契约第二次演进，沿用 v1.2 flash 契约的流程纪律（默认决议预填 + 冻结点先行）：

| 评审要点 | 默认决议（待确认） |
|---|---|
| 契约范围 | `port_wdt_enable(timeout_ms) / port_wdt_feed() / port_wdt_disable()`；**仅 `ET_MODULE_WDT=1` 时必选** |
| 不可停问题 | IWDG 类一旦启动不可停 → 契约声明 disable "尽力而为，返回 bool"；文档明示 F103 IWDG 语义 |
| 超时下限 | 契约要求 timeout ≥ `PORT_FLASH_ERASE_MS_MAX` × 2（与既有擦除指引闭环，防擦扇区期间喂狗失败） |
| 上下文 | feed 可 🔒ISR；enable/disable 仅 🏠MAIN |

- host port：软件模拟（喂狗窗口检查 + 超时回调注入，供单测）；F103 port：IWDG 寄存器直驱（PR/RLR 计算 + kaR 序列，PM0056/参考手册标注）；
- et_wdt 本体为薄封装 + **阻塞段保护 helper**：`et_wdt_guard(fn)` 在长操作前后自动喂狗（文档给出与 flash 擦除组合的配方）；
- 单测 ≥ 8 例：host 模拟超时触发、窗口内 feed、guard 正常/异常路径、契约边界（timeout 下限拒绝）

### P2-2 shell 可选历史（时间盒 0.5 天，v1.4"再议"收编）

- `ET_SHELL_HISTORY_N`（默认 0=关，开则静态环形缓冲 N 条）；上/下键 ANSI `ESC[A/B` 解析；仅命令行回放，不做模糊匹配；
- 单测 ≥ 5 例：环形覆盖最旧、跨断行恢复、历史中编辑后回车、关闭时字节直透
- Tab 补全 / 多行编辑**维持 Non-goal**

### P3 demo 串联（1 人日，v1.4 挂账收编）

`stm32f103_demo` 全链路叙事：shell 敲 `AT+UPGRADE` → xmodem 接收镜像写 B 槽 → `et_bootctl_verify_image` + `stage` → 重启 → bootloader 演示段（demo 内模拟）按 `should_rollback`/`confirm` 决策。串口日志可完整观测一次"升级→确认"与一次"超次回滚"。

### P4 工程化（可选）

| # | 任务 | 说明 |
|---|---|---|
| 1 | Release 附件补 sha256 校验和 | release.yml 生成 SHA256SUMS 文件 |
| 2 | 覆盖率门槛维持 | 新模块并入后整体不低于 97.1% − 1pt |

## 4. 交付标准（DoD）

1. 沿用 v1.4 全部 DoD（.c/.h 成对 + 上下文标注 + 开关 `#ifndef` 烟测 + 套件注册 + 零警告零 malloc + 量化声明附复现命令与环境注记）；
2. **docsync.sh 全绿作为发布硬性门**：任何"文档已同步"声明必须对应脚本内断言，无断言的声明视为未同步（根治条款）；
3. et_bootctl 附加：掉电矩阵用例全绿；状态扇区布局图入头文件注释；镜像头格式版本化（`hdr_ver`，向后兼容声明）；
4. et_wdt 附加：契约章节（API_GUIDE port 章）先于实现合入；F103 实现注明寄存器依据；
5. demo 串联交付后，port README 的 Renode smoke 断言酌情新增 1 条升级链路日志断言（时间盒内可选）。

## 5. 里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 |
|---|---|---|
| M1 | P0 销账（CI 3 绿 + 补 tag v1.4 + 体积行 + docsync 全绿） | D+0.5~1（依赖远程创建，与 M2 并行不互堵） |
| M2 | `et_bootctl` 合入（含掉电矩阵） | D+3 |
| M3 | `et_wdt` 合入（契约冻结先行） | D+4 |
| M4 | shell 历史 + demo 串联 | D+5 |
| M5 | 全量回归（CI 实跑）→ **tag v1.5** | D+5.5 |

## 6. 明确不做（Non-goals）

- 不做 bootloader 本体工程（链接脚本双区/搬运例程/固件加密签名）——交付库组件 + 配方文档，完整 boot 工程属应用侧；
- 镜像完整性只做 CRC32，**不做加密/签名/防回滚版本号强制**（安全启动是另一档问题，显式排除）；
- shell 不做 Tab 补全 / 多行编辑 / 模糊历史匹配；
- 不做窗口看门狗（WDT）、多看门狗实例；et_kv 不迁移到 bootctl 状态扇区；
- 不改既有 21 模块公开 API 语义；RTOS / 浮点 / 动态内存依旧不做。

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 远程创建仍被账号侧阻断 | M1 无法销账，v1.5 无法 tag | 开发不中断（M2~M4 并行）；M1 拆为"远程就绪前全部本地可完成项"（体积行/docsync/契约文档），CI 项单独挂账 |
| bootctl 状态断电时序难穷举 | 罕见断点状态损坏 | 递减位编码使任意断点只丢最后一步（设计即兜底）+ 三类断点矩阵；沿用 kv 的 host 注入基建 |
| wdt 契约演进争议 | 返工 | 默认决议预填 + 时间盒 0.5 天；IWDG 不可停等语义以 ST 手册为准并写入契约 |
| docsync 断言维护成本增长 | 脚本腐化 | 断言条目与交付文档章节一一对应（单一事实来源）；CI 红即阻断，防止静默失效 |
| demo 串联引入的 flash 写入在 Renode 中瞬时完成 | 演示失真 | 升级链路断言只看功能日志（镜像 CRC 通过/状态迁移），时序项标注以真机为准（沿 v1.3 政策） |

---

> 维护约定：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v1.5 后本文档归档不再改动。
