# v1.2 开发交付：et_kv 掉电存储与实测补录

> 交付日期：2026-09-02 ｜ 版本：v1.2.0（`ET_VERSION = 0x010200`）｜ 基线：v1.1（`472a2a9`）
> 计划文档：[v1.2 开发计划：et_kv 存储与实测补录](./v1.2开发计划：et_kv存储与实测补录.md)（状态：✅ 已交付）

## 1. 重点概况（TL;DR）

v1.2 把库的可靠性从"PC 可测"推进到"**掉电可靠**"：

- **新模块 et_kv（`storage/`）**：双扇区乒乓键值掉电存储。页头 `magic|seq|state|crc32`，state 逐位编程 MOVING→COMMITTED 作为**页生效前的最后一次写**——任何断点掉电自动弃半成品页、源页无损；记录追加写、单记录 CRC 独立损坏不影响其他 key；页满自动压实去重，`et_kv_commit()` 可手动压实；断电恢复矩阵单测全绿。
- **port 契约演进（纯增量）**：`port.h` 新增 flash 三件套（read/write/erase_sector）+ 几何宏 + 语义约定（4B 对齐、只允许 1→0 写、短写如实上报），仅在 `ET_MODULE_KV=1` 时为必选 —— 老平台零影响。
- **F103 寄存器级 flash 驱动**：不依赖 HAL，按 PM0056 解锁/页擦/半字编程；参数区 = 片内 flash 末端 16K（16×1KB），链接脚本 `ASSERT` 防止镜像侵入。
- **demo 真机价值升级**：`stm32f103_demo` 新增重启计数（每次上电 +1 读回写闪）与软时钟 UTC 时间 30s 持久化 —— 掉电保存的最直观演示。
- **工程化（P4）**：CI 覆盖率门槛 **行覆盖 ≥85%**（本地基线：**行 96.9% / 函数 99.5%**，et_kv 91.8%），新增 tag 发布工作流（v\* → 全量测试 + ARM ELF/BIN → GitHub Release）。
- **顺延（P3 变更为已完成）**：et_softclock 软时钟本来是可选项，实际 1 日内完成并交付（13 例全绿）。**P2 仿真/真机挂账延续**（Renode 未安装、真机硬件未到位 → 转 v1.3 挂账清单）。

## 2. 逐项验收对照

### P0 契约评审（M1，`5f68f28`）

`exp/kv` 提案 5 个评审要点 + 新增"删除语义"逐条形成决议，追加至提案"决策记录"并合入 main：

| # | 要点 | 决议 |
|---|---|---|
| 1 | 写入粒度 | **4B 对齐**（F103 半字编程为超集，port 内部补帽） |
| 2 | 擦除耗时 | 契约声明调用方负责喂狗；`PORT_FLASH_ERASE_MS_MAX` 提供时间参考 |
| 3 | 擦写期间中断 | 仅 🏠MAIN；port 临界区包完整序列（双兜底） |
| 4 | 乒乓与磨损 | 固定双扇区乒乓，不做均衡 v2；寿命算并入文档 |
| 5 | 冻结点 | 契约文档（API_GUIDE §9）先于实现合入 |
| +1 | 删除语义 | `et_kv_del()` 写 tombstone，读/搬运均跳过 |

`port.h` flash 契约、`et_config.h`（v1.2.0 + `ET_MODULE_KV/SOFTCLOCK` + 几何三宏）随之落地。

### P1 et_kv 主体（M2/M3，`7982d12` + `996ef43`）

| 项 | 交付物 | 验收 |
|---|---|---|
| host flash 模拟器 | `port/host/`：RAM 扇区 + 位写约束（0→1 拒绝）+ 一次性掉电截断注入 + 半扇区初始化注入 | 异常注入是掉电矩阵的根基 |
| et_kv 模块 | `storage/et_kv.c/.h`（8 API + stats） | 追加写/多版本/seq 仲裁/坏页自动搬迁全部自洽 |
| F103 驱动 | `port_stm32f103.c` FLASH 段（KEYR 解锁/PER 页擦/16bit 编程/BSY 超时） | PM0056 3.3.1/3.3.2/3.3.4 逐项核对，注释标注 |
| demo | 重启计数（扇区 14/15）+ 软时钟恢复/持久化 | ARM 编译零警告（text=10892 data=4 bss=276） |

### 掉电恢复矩阵（test/test_kv.c 28 例，类 ≥2 断点）

| 断点类 | 例数 | 机制 |
|---|---|---|
| 页头半写（对齐/后续写） | 4 | CRC 只盖 8B magic\|seq；state 位翻转页弃用 |
| 记录半写 / 脏尾 | 5 | 不可解析 → 自动搬迁修复 |
| 压缩前/中/后 | 8+ | MOVING 页弃用，源页仍有效 |
| 坏记录 CRC / 删除 | 3 | 单记损坏不影响其他 key（回退旧版） |
| 硬语义（混入仿坏页 / 写违约注入） | 4 | init 斜自愈 |

另 5 例 = 基本读写/边界/容量；断电点 cosmetically 重复重启验证。全部 `ALL PASS`。

### P3 et_softclock（M2 交付，`sys/`）

毫秒 tick → UTC 日历（Hinnant `civil_from_days` 纯整数闰历），32 位无符号秒（1970~2106）；与 et_kv 协同提供时间持久化；单测 13 例（闰年/月末/回绕/大步进/多实例）全绿。

### P4 工程化

- CI 覆盖率门槛：`ci.yml` gcovr `--fail-under-line 85`；本地基线 **96.9% line / 99.5% func**（含新增代码，`et_kv` 91.8% / `et_softclock` 96%）。
- 发布工作流：`release.yml`（tag v* → make test + demo + ARM ELF/BIN → GitHub Release 附件）。

## 3. 质量数据

| 项 | 数值 |
|---|---|
| 模块 | 18（+storage/et_kv 1、sys/et_softclock 1；storage 层新建） |
| 主机单测 | **191 例 ALL PASS**（v1.1 150 + kv 28 + 软时钟 13） |
| ARM 编译 | 零警告（`-Wall -Wextra -pedantic -Os`），text=10892 data=4 bss=276（Flash~17%） |
| 行覆盖率 | 96.9%（≥85% 门） |
| 掉电注入点 | 28 例矩阵全绿，重开全部可恢复 |
| 内存 | 依旧零动态分配（库内无 malloc） |

## 4. 文档同步

- `docs/API_GUIDE.md`：新增 4.4 软时钟、6 章 storage/et_kv、9 章 flash 契约小节 + 接口面板/恢复矩阵；章节重编号与目录同步；配置表更新（新开关、$PORT_FLASH_*、版本 1.2.0）。
- `port/stm32f103/README.md`：flash 布局/PM 对照表/构建（含 `-Istorage`）/某板烧录/验证状态。
- `README.md`：18 模块、目录树、快速开始（`-Istorage`）、KV 使用、质量门控与发布自动化。
- 计划文档状态行与里程碑已更新。

## 5. 开发中发现并修复的问题（超出原计划范围）

1. `et_kv_format/init-EMPTY` 只写页头不提交 state → 重开全 INVALID。修复：两路径补 `page_commit_state`（COMMITTED）。
2. 页头 CRC 覆盖 12B 含 state → state 翻转后 CRC 失效 → **改为只盖 magic|seq 8 字节**（state 独立于校验，决议落地）。
3. host 掉电注入"永远生效"阻塞 init 重开修复写 → 改为**一次性截断**（截断后自动复位，模拟重启）。
4. 压缩去重把 tombstone 的旧版本也"迁活" → newer_visit 判定改为"新者胜（含删除）"。
5. F103 页地址无选择寄存器（页擦 = 取指页）→ 参数区布局 + 链接 ASSERT 双兜底（重要真机风险，见 #7 风险）。——PM0056 条款确认。

## 6. 范围外事项 / 顺延清单（挂账债务）

| 项 | 状态 | 去向 |
|---|---|---|
| Renode/QEMU 仿真 | ⏳ 本机未安装 Renode → 未跑 | v1.3 挂账（命令已入 port README） |
| F103 真机实测记录 | ⏳ 无硬件 → 未烧录 | v1.3 挂账（demo 已含可直接跑的重启计数验证） |
| 磨损均衡 v2 / 多扇区映射 | 明确 Non-goal | 双扇区乒乓已覆盖参数场景 |
| 加密/压缩/通用事务 | Non-goal | — |

## 7. 提交与版本

| 里程碑 | 提交 | 说明 |
|---|---|---|
| M1 | `5f68f28` | 评审决议 + exp/kv 合入 + 契约演进 |
| M2 | `7982d12` | host flash 模拟 + et_kv 28 例 + et_softclock 13 例 |
| M3 | `996ef43` | F103 flash 驱动 + demo 重启计数/时钟 + CI 门槛/发布流 + 文档 |
| M5 | 本文本 | 回归 191 + 交付文档 + **tag v1.2** |

版本钉：`et_config.h` → 1.2.0 / `0x010200`；tag **v1.2**（与 ET_VERSION 一致，发布即验证）。