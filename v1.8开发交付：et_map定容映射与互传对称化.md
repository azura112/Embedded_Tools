# v1.8 开发交付：et_map 定容映射与互传对称化

> 交付日期：2026-09-06 ｜ 版本：v1.8.0（`ET_VERSION = 0x010800`）｜ 基线：v1.7（`64a1387`）
> 计划文档：[v1.8 开发计划：定容映射与互传对称化](./v1.8开发计划：定容映射与互传对称化.md)（状态：✅ 代码已交付，板上顺序收口挂账见 §5）
> 远程：https://github.com/azura112/Embedded_Tools（main @ 本地 `50dd034`，推送因网络中断重试中）

## 1. 重点概况（TL;DR）

v1.8 完成**容器补齐 + 传输对称化**两块最后拼图，并固化覆盖率治理：

- **新模块 et_map**（core/，第 25 模块）：定容开放寻址哈希表——u32 键值（键 0=空槽、0xFFFFFFFF=墓碑，用户键域 [1, 0xFFFFFFFE]）、线性探测 + **probe_limit 退化拒绝**（嵌入式无界查找不可接受）、墓碑删除保探测链、foreach 遍历中删安全；头文件自带容量指引（负载 ≤0.7、质数容量）与**满表无空槽即使有墓碑也拒绝**的经典语义声明。**13 例全绿**（含探测上限退化/边界、遍历中删、多实例、极值键）。
- **新模块 et_xmodem_tx**（protocol/，第 26 模块）：与接收器对称的 MCU 发送端——共享协议常量与 `et_xmodem_crc16` 助手（单一事实来源防漂移，DoD #4）、src 流式读 + 0x1A 填充、NAK/超时重发 ≤ retry_max、EOT 两段确认、CAN 中止。**14 例全绿，含 tx→rx 内存回环双块型（DoD #4：回环用例入常规回归）**；selftest 的 xmodem 套件同步升级为库级 tx↔rx 回环（替代手写编码器）。
- **P0 治理固化**：docsync 新增**交付文档覆盖率行**循环断言（每份 `v1.x开发交付*.md` 必含覆盖率行，删行即红）——v1.6/v1.7 连续两版漏覆盖率行的治理缺口关闭，历史交付补行；版本宏 1.8.0。
- **P2-2 顺延收编**：F103 demo 补 **RX 中断 + 环形缓冲**（修复轮询收包在 WFI 下的丢包隐患，G474 教训闭环到 F103）+ tickless（next_due 门控）+ `AT+SELFTEST`（库组件）；G474 示例/CubeMX demo 同步；**Renode smoke 注入 AT+SELFTEST 并断言 `15/15 PASS`**；bench v1.8 复测行（无回归）。

## 2. 逐项验收对照

### P0 顺序收口与治理固化

| # | 任务 | 结果 |
|---|---|---|
| 1 | v1.6 走单回填 + tag v1.6 | ⏳ 挂账（工具/runbook 就绪，板执行依赖档期） |
| 2 | 库化 selftest 板上记录 + tag v1.7 | ⏳ 挂账（同上，顺序收口） |
| 3 | **覆盖率行固化** | ✅ docsync 循环断言（README"行覆盖"行 + 逐交付文档），v1.6/v1.7 交付补行，**86 断言全绿**；自证：删任一交付文档覆盖率行该文件即红 |
| 4 | 版本宏 1.8.0 | ✅ `0x010800`，README/API_GUIDE/CHANGELOG 四方一致 |

### P1 et_map（M2）

| 项 | 结果 |
|---|---|
| API | 计划签名逐条落地（init/put/get/del/count/clear/foreach）；键空间语义按头文件决议（键 0 空 + 0xFFFFFFFF 墓碑，init/put 显式拒绝保留键） |
| 退化语义 | probe_limit 超限即拒绝（get/put/del 一致）；用例覆盖"恰达上限可查/超 1 次拒绝"边界 |
| 发现并修正确认 | **满表无空槽时 put 拒绝即使有墓碑**（新键不存在无法证明——经典开放寻址语义），测试预期按实现语义修正并写入头文件（非放宽产品代码） |
| 单测 | **13 例**：init 校验/基础插查/保留键/重复覆盖/count+clear/同桶碰撞链/删洞复用/满表拒绝/探测上限退化×2/foreach 删安全/多实例/极值键 |

### P2 et_xmodem_tx（M3）

| 项 | 结果 |
|---|---|
| 对称性 | 共享 `ET_XM_SOH/STX/EOT/ACK/NAK/CAN` 常量与 `et_xmodem_crc16`（et_xmodem.h 新增，单一事实来源）；帧格式/填充/两段确认与接收器逐项对齐 |
| 数据源 | `src(user, off, dst, want)` 流式读 + 返回实际字节数，短读/数据尽以 0x1A 补齐块型（与 tools/xmodem_send.py 语义一致） |
| 对端矩阵 | **13 例**：init 校验（含 1K 未使能拒绝）/NAK/'C' 起步/ACK 推进/NAK 重发同块/超时重发/重传超限 ERR/对端 CAN/脏字节忽略/DONE 粘滞/短读填充 |
| **端到端回环** | **tx(库)→rx(库) 内存回环**：128B（3 块）与 1K（2 块，`ET_XM_1K=1` 构建）双块型逐字节一致 + 双侧 DONE——DoD #4 落地，防协议漂移 |
| 开发中发现 | 重传计数被发送函数误清零（重发路径与首发共用）→ 计数清零移至 ACK 成功路径，单测钉死 |

### P2-2 v1.7 P3 顺延收编（M4）

| # | 任务 | 结果 |
|---|---|---|
| 1 | demo tickless 实装 | ✅ F103/G474 示例 + CubeMX demo：主循环 next_due() 门控定时器处理 + WFI；**唤醒源 = SysTick(1ms) + USART1 RX 中断**（配方 11.8 必查项）；**连带修复三处 cmd_upgrade 直轮询 RDR 被 RX 中断抢字节的隐患**（统一改排空环形缓冲） |
| 2 | F103 Renode smoke 增 selftest 断言 | ✅ F103 demo 接入库 selftest（`AT+SELFTEST` 可重跑；**开机自跑一次**作为 smoke 断言载体，ET_MODULE_SELFTEST=1 构建 text 25200 < 64K）；Renode smoke 断言 `SELFTEST: 15/15 PASS`（15 PASS + 2 SKIP）——CI 实跑验证 |
| 3 | bench v1.8 基线行 | ✅ `make bench` 复测：各项 ±5% 噪声内无回归，版本行入 docs/bench.md |

## 3. 里程碑对照

| 里程碑 | 提交 | 内容 |
|---|---|---|
| M1-M4 | `50dd034` | 治理/版本 + et_map + et_xmodem_tx + P2-2 收编 + 文档同步 |
| M5 | 本次交付提交 | 交付文档（含覆盖率行）+ 计划状态归档；**tag v1.8 挂账**（顺序收口） |

## 4. DoD 对照（v1.8 §4）

| # | 条款 | 状态 |
|---|---|---|
| 1 | v1.7 全部 DoD（docsync 全绿 / 双几何 / 量化+环境注记） | ✅ docsync **86/86**；**326×2** 几何全绿（1K 变体 327） |
| 2 | 交付复现表覆盖率行 docsync 断言 + 自证 | ✅ 循环断言（README + 逐交付文档），v1.6/v1.7 补行，本表 §6 含覆盖率行 |
| 3 | et_map 容量指引与退化行为入头文件 | ✅ 头注（负载 ≤0.7/质数/probe_limit 语义/满表墓碑语义）；表驻任意存储（init 清零） |
| 4 | et_xmodem_tx 与 rx 共享常量 + 回环入套件 | ✅ et_xmodem_crc16/协议常量共享；`xmtx.loopback_128/1k` 常规回归 |
| 5 | tag v1.8 前置 v1.6/v1.7 双 tag 已补打 | ⏳ 板上收口后按序打 tag（沿既有规则） |

## 5. 挂账与后续（顺序收口）

1. **v1.6 走单 5 条回填** → 补打 **tag v1.6**；
2. **库化 selftest 板上记录**（AT+SELFTEST/SELFSTOR）→ 补打 **tag v1.7**；
3. v1.8 新增板面验证（F103 smoke 已覆盖 selftest；G474 板 `AT+SELFTEST`/`AT+SELFSTOR`/xmodem_tx 场景可选）→ **tag v1.8**；
4. v1.9 候选：字符串键映射、键 0 支持需求评估。

## 6. 量化数据复现（环境注记）

| 声明 | 复现命令 | 环境 |
|---|---|---|
| **行覆盖 96.8%**（6744/6964） | `gcc --coverage -DET_MODULE_SELFTEST=1 …`（Makefile test 源清单）→ `gcovr --root . --print-summary` | gcovr **8.6** / MinGW gcc **16.1.0** / Git Bash |
| host 326 例 × 2 几何 ALL PASS（1K 变体 327） | `make test` / `make test-g4`；1K 变体：gcc 追加 `-DET_XM_1K=1` | MinGW gcc 16.1.0 / Git Bash |
| docsync | `sh tools/docsync.sh` → **86/86** | Git Bash |
| ARM 零警告双平台 | 两 port README 构建命令（F103 text **25180**（selftest 启用）/ G474 默认 **17108**），`arm-none-eabi-size` 读数 | GNU Tools for STM32 **13.3.rel1**（STM32CubeCLT 1.18.0） |
| 板端固件（CubeMX 集成，v1.8 同步） | `cmake --preset Release && cmake --build build/Release --clean-first`（text **31812**） | 同上 + CMake/Ninja |
| xmodem 端到端 | `xmtx.loopback_128` / `loopback_1k` 常规回归 + `tools/xmodem_send.py`（host 自测记录沿 v1.6） | Python 3.12.4（串口模式需 pyserial） |
