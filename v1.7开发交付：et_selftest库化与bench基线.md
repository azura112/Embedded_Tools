# v1.7 开发交付：et_selftest 库化与 bench 基线

> 交付日期：2026-09-06 ｜ 版本：v1.7.0（`ET_VERSION = 0x010700`）｜ 基线：v1.6（`b669823`）
> 计划文档：[v1.7 开发计划：板上自测与性能量化](./v1.7开发计划：板上自测与性能量化.md)（状态：✅ 已交付，板上记录与顺序收口挂账见 §5）
> 远程：https://github.com/azura112/Embedded_Tools（main @ `fe65714`）

## 1. 重点概况（TL;DR）

v1.7 给验证金字塔**封顶**，把"G474 工程私有的好东西"升格为库级承诺：

- **新模块 et_selftest**（debug/，第 24 模块，`ET_MODULE_SELFTEST` **默认 0 可裁剪**）：板上自测组件——17 内建套件 + 结构化报告回调（BEGIN/PASS/FAIL/SKIP/CHECK_FAIL/DONE）+ 动态注册槽（4）+ **存储门控**（kv/bootctl 破坏性套件默认 SKIP，显式配置才实跑）。**框架自测 9 例 + host 全套件复跑 → 全库 300 例 × 2 几何全绿**。
- **接入配方落地**：G474 工程私有 AT+SELFTEST **退役改调库组件**（`AT+SELFTEST` 非存储 / `AT+SELFSTOR` 存储套件破坏性实跑 + demo kv 状态重建），Core/et 库源全量同步至 v1.7。
- **host 基准**：`tools/bench.c`（`make bench` / `bench-table`）+ `docs/bench.md` 基线表——固定迭代 + 5 轮中位数 + **防 DCE volatile 汇聚**（开发中实测：无防护时 -O2 把纯计算循环当死代码消除，crc"吞吐"虚高 4 倍）+ 环境注记强制。
- **CHANGELOG.md**：Keep-a-Changelog 风格，v1.0→v1.7 七版从交付文档提炼，docsync 断言版本链一致。
- **P0 政策了断**：Renode G4 由挂账转**正式关闭**（不排期：G474 真机已承担 G4 平台验证职责）；版本宏 1.7.0。

## 2. 逐项验收对照

### P0 v1.6 收口与政策了断

| # | 任务 | 结果 |
|---|---|---|
| 1 | v1.6 走单回填 + tag v1.6 补打 | ⏳ **挂账**——工具/固件/runbook 已就绪（v1.6 交付 §5），板执行依赖档期；按 DoD #5 顺序收口：**v1.6 未收口不接 v1.7 tag** |
| 2 | Renode G4 正式关闭 | ✅ port README 挂账表改"**不排期（真机覆盖）**"+ 实机记录同步 + docsync 断言 |
| 3 | 版本宏 1.7.0 | ✅ `0x010700`，README/API_GUIDE/CHANGELOG 四方一致 |

### P1 et_selftest（M2，★ 核心）

| 项 | 结果 |
|---|---|
| 形态 | `debug/et_selftest.c/.h` + `ET_MODULE_SELFTEST`（默认 0）；**全启用体积增量实测**（G474 demo `-Os`）：text 15948→**23868**（+7920）、bss 596→**2268**（+1672），已入 port README 体积表——DoD #2 |
| 框架 | 套件注册表（编译期内建表随模块开关自动增减）+ 动态注册槽（4，重名拒绝）+ 结构化报告回调（组件内零格式化，接 et_log/shell 由应用决定）+ 失败计数（CHECK_FAIL 带行号） |
| 用例集 | **17 内建**：13 个 G474 已验证套件移植 + atcmd+shell 行解析(RAM) + xmodem 短传输(RAM 环回,含坏 CRC→NAK→重发路径) + kv 冒烟(门控) + bootctl 状态机(门控)；**掉电注入类 host-only 不移植**（覆盖边界已文档化） |
| host 复用 | 同一份组件注册进 `test/test_main.c`（`debug/selftest` 套件），存储门控在 host 虚拟 flash 上全开实跑——**板上/PC 结果可比对** |
| 单测 | 框架自测 **9 例**（≥8 达标）：套件计数/未知套件/门控跳过/单套件/动态注册重名拒绝/静默跑/run_all 全过/失败注入/CHECK_FAIL 行号 |
| 真机验收 | ⏳ 库化版 `AT+SELFTEST` 上板记录挂账（私有版 13/13 已记录于 v1.6） |

开发中修正的 3 处断言/实现错（host 预验证抓出，未上板浪费）：

1. `et_shell_feed` 返回**行完成**而非逐字节真——逐字节断言改为仅行尾检查；
2. `et_xmodem` 的 `total` 在 EOT 二段确认（DONE）时被 `session_reset` 清零——断言移到握手前；
3. 组件框架对动态套件漏发 SUITE_PASS/FAIL 事件——统一由运行器上报（与内建一致）。

### P2 bench（M4，两次顺延收编）

| 项 | 结果 |
|---|---|
| 覆盖 | ringbuf 单字节/块(POW2+非 POW2) / crc16-ccitt(表加速唯一变体)/crc32 / xmodem 有效吞吐 / kv set+get / filter/fsm 单次 ns |
| 方法 | 固定迭代 + **5 轮中位数** + `clock()` CPU 时间 + **防 DCE volatile 汇聚**；无注记数字不入文档 |
| 产出 | `make bench` / `make bench-table` + `docs/bench.md`（基线表 + 观察备注 + 版本记录行） |
| docsync | bench 文档存在性 + 环境注记条款 + 基线版本行断言 |

### P2-2 CHANGELOG（M4）

- Keep-a-Changelog 风格，v1.0→v1.7 逐版从交付文档"重点概况"提炼（Added/Changed/Fixed/挂账）；
- docsync 断言最新版本条目与 `ET_VERSION_STRING` 一致（三方：tag/et_config/CHANGELOG）；
- release.yml 附 CHANGELOG 为可选增强，本版未做（下版按需）。

## 3. 里程碑对照

| 里程碑 | 提交/Run | 内容 |
|---|---|---|
| M1-M4 | `fe65714` | P0 政策/版本 + et_selftest 库化 + G474 退役 + bench + CHANGELOG |
| M5 | 本次交付提交 | 交付文档 + 计划状态归档；**tag v1.7 挂账**（§5 顺序收口） |

## 4. DoD 对照（v1.7 §4）

| # | 条款 | 状态 |
|---|---|---|
| 1 | v1.6 全部 DoD（docsync 全绿 / 双几何硬性项 / 量化+环境注记） | ✅ docsync **69/69**；**300×2** 几何全绿；数字均可复现（§6） |
| 2 | et_selftest：`=0` 烟测 / 全启用增量入体积表 / 板上记录+断言 | ✅ 默认构建即空 TU 烟测（Makefile 全量编译覆盖）；增量 **+7920/+1672** 入 G474 体积表；⏳ 板上记录待回填 |
| 3 | bench 中位数 + 环境注记 | ✅（并实测修复 DCE 虚高） |
| 4 | CHANGELOG 版本链三方一致 | ✅ docsync 断言 |
| 5 | tag v1.7 前置 v1.6 收口 | ⏳ 走单回填 + tag v1.6 后按序打 tag |

## 5. 挂账与后续（顺序收口）

1. **v1.6 走单 5 条回填**（板 + `tools/xmodem_send.py`，runbook 在实机记录 §5）→ 补打 **tag v1.6**；
2. **库化 selftest 板上记录**：烧 v1.7 固件 → `AT+SELFTEST`（15 套件 PASS + 2 SKIP）→ `AT+SELFSTOR`（kv/bootctl 破坏性实跑）→ 实机记录补库化版结果；
3. 两项完成、CI 保持绿后按 checklist 走 release → **tag v1.6、v1.7 相继发布**；
4. P3 未做：demo tickless 实装 / F103 Renode smoke 增 selftest / et_map（v1.8 候选）。

## 6. 量化数据复现（环境注记）

| 声明 | 复现命令 | 环境 |
|---|---|---|
| host 300 例 × 2 几何 ALL PASS | `make test` / `make test-g4` | MinGW gcc **16.1.0** / Git Bash |
| docsync | `sh tools/docsync.sh` → **69/69** | Git Bash |
| ARM 零警告双平台 | 两 port README 构建命令（F103 text **15580** / G474 默认 **15948**） | GNU Tools for STM32 **13.3.rel1**（STM32CubeCLT 1.18.0） |
| selftest 全启用增量 | G474 demo 构建加 `-DET_MODULE_SELFTEST=1` → text **23868** / bss **2268** | 同上 |
| 板端固件（CubeMX 集成,库化 selftest） | `cmake --preset Release && cmake --build build/Release --clean-first`（text **31096**） | 同上 + CMake/Ninja |
| bench 基线 | `make bench` / `make bench-table` → docs/bench.md | -O2,MinGW gcc 16.1.0,AMD Ryzen 9 7945HX |
