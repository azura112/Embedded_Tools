# v1.9 开发计划：板上收口与字符串映射

> 起草日期：2026-09-06 ｜ 基线：v1.8（HEAD `08ef770`，tag v1.6/v1.7/v1.8 顺序收口挂账）｜ 状态：草案
> 上一版本：[v1.8] 26 模块 / 326 用例 × 2 几何 / 覆盖 96.8% / docsync 87 / et_map + et_xmodem_tx
> 输入：v1.8 验收两处遗留（README F103 构建命令断裂；25200/17、87 三处数字滞后）；v1.6~v1.8 交付挂账（走单 5 条 + selftest 板上记录 + v1.8 板面验证 → 三 tag 按序补打）；v1.8 计划预告的 et_map 字符串键候选

---

## 1. 版本定位

- **版本号 v1.9**：板上收口（本版主线）+ 缺陷与治理清偿 + 新增字符串映射模块，纯增量（MINOR）。
- **本版主题**：**先还债，再扩容**——连续三版挂账的板面验证在本版一次性清偿（三 tag 按序补打），文档断裂类缺陷制度化根治（文档命令可执行化）；然后交付 v1.8 预告的字符串键映射。
- **不变式**（继续遵守）：零动态内存、多实例句柄化、core/algorithm 层零硬件依赖、ISR-safe 范围显式标注。

## 2. v1.8 现状盘点

| 项 | 状态 |
|---|---|
| 模块 | 26 个，326 用例 × 2 几何全绿，覆盖 96.8%，docsync 87，CI 六 job（含双交叉编译 + Renode smoke + 覆盖率门） |
| 板面挂账 | **三版未清**：① v1.6 走单 5 条（升级链/WDTEST/LED按键，runbook 就绪）② v1.7 库化 selftest 板上记录 ③ v1.8 板面验证（tickless WFI 唤醒 + RX 中断收包）→ tag v1.6/v1.7/v1.8 全部悬置 |
| 验收遗留 | ① `port/stm32f103/README.md` 构建命令断裂（demo 的 selftest 调用无 `#if` 守卫，命令缺 `-DET_MODULE_SELFTEST=1` 与 `storage/*.c`——CI 带宏故 CI 绿，文档路径断）② 数字滞后三处：F103 selftest 构建实测 25200（文档 25180）、smoke 断言 17/17（文档 15/15）、docsync 87（文档 86） |
| 工具前置 | 本机 pyserial / Renode 均未安装（走单 ③ xmodem 真传依赖 pyserial；仿真以 CI 为准） |
| 候选 | v1.8 计划记录的 v1.9 候选：字符串键映射、键 0 支持评估 |

## 3. 开发范围

### P0 板上收口（★ 本版主线，约 1 天，板 + 上位机）

一次上板按序清偿三版挂账，**顺序补打 tag v1.6 → v1.7 → v1.8**：

| # | 任务 | 验收 |
|---|---|---|
| 0 | 前置：`pip install pyserial`（xmodem_send.py 串口模式所需，本机未装） | `python -c "import serial"` 通过 |
| 1 | **v1.6 走单 5 条**（runbook：实机记录 §5）：SIMUPGRADE even→超次回滚 / odd→confirm / **AT+UPGRADE xmodem 真传** / AT+WDTEST IWDG 真超时复位 / LED 极性 + 按键 | 实机记录 §5 结果表落数 + docsync 断言绿 → release.yml 走一遍 → **tag v1.6** |
| 2 | **v1.7 库化 selftest 板上记录**：烧当前固件 → `AT+SELFTEST`（期望 17 套件全过，含 kv/bootctl 2 SKIP）→ `AT+SELFSTOR`（破坏性实跑 + demo 状态重建） | 实机记录补库化版结果 → 核对 CI 绿 → **tag v1.7** |
| 3 | **v1.8 板面验证**：tickless 主循环实测（WFI 下心跳/按键/shell 输入均正常——**RX 中断唤醒的直接证据**：睡眠中敲 `AT+VER` 须应答）；G474 板顺带 xmodem_tx 上报场景（可选） | port/实机文档落记录 → **tag v1.8** |

**关键路径**：P0 是 v1.9 tag 的硬性前置（顺序收口规则）；P1~P3 与 P0 并行开发不互堵。F103 真机维持常设挂账不排期。

### P1 缺陷清偿与文档可执行化（约 0.5 天）

| # | 任务 | 验收标准 |
|---|---|---|
| 1 | `stm32f103_demo.c` 的 selftest 调用补 `#if ET_MODULE_SELFTEST` 守卫 | 文档命令**无宏原样执行**编译链接通过；带宏构建行为不变（CI/smoke 回归） |
| 2 | 三处数字回刷（25200 / 17、87 全表核对） | 交付/README/实机记录与实际构建一致 |
| 3 | **文档命令可执行化**：CI 新增 `docbuild` job——提取 `port/stm32f103|stm32g474/README.md` 的构建代码块**原样执行**（含 objcopy/size） | 文档命令成为 CI 门；今后同类断裂 CI 直接红 |
| 4 | checklist 增条款：交付定稿后凡改变可观测数字的提交，**同提交必须回刷**所有引用处（体积表/断言计数/用例数） | 写入 README checklist；docsync 尽力断言（新鲜 size 读数 vs 体积表 vN 行） |

### P2 `et_smap` —— core/ 字符串键映射（约 1 天，v1.8 预告候选）

**独立轻量模块**，不动 et_map 的 u32 热路径（两种键型分离，避免 union/双分支拖慢 u32 侧）：

```c
/* 键为字符串, 值 u32; 定容 + 内嵌 key pool(init 给字符缓冲), 零动态内存 */
bool et_smap_init(sm, slots, cap, keybuf, keybuf_size, probe_limit);
bool et_smap_put(et_smap_t *sm, const char *key, uint32_t val);  /* 键超长拒绝 */
bool et_smap_get(const et_smap_t *sm, const char *key, uint32_t *val);
bool et_smap_del(et_smap_t *sm, const char *key);
uint32_t et_smap_count(const et_smap_t *sm);
```

**默认决议**（评审时间盒 0.5 天）：

- 哈希：FNV-1a 32 位；探测/墓碑/`foreach` 语义**与 et_map 完全一致**（文档交叉引用，不重复定义）；
- 键约束：非空、NUL 结尾、≤ `ET_SMAP_KEY_MAX`（默认 16，可 `-D`），大小写敏感；键内容拷入内嵌 pool（调用方可释放原串）；
- pool 满：put 拒绝（不静默截断）；`init` 校验 pool ≥ 最坏键占用；
- **键 0 评估决议**：et_map 维持键 0 保留（空槽标记是实现事实，改键域要重设计墓碑）——文档 FAQ 写明理由；确有需要走 `key+1` 应用侧偏移，不做库级变体。

- 单测 ≥ 12 例：基础插查删改、超长键拒绝、pool 满拒绝、同前缀键碰撞（FNV-1a 桶冲突链）、删洞复用、拷入语义（原串释放后仍可读）、foreach 删安全、大小写敏感、init 校验（pool/cap）

### P3 可选（时间盒 0.5 天，不阻塞发布）

| # | 任务 | 说明 |
|---|---|---|
| 1 | `ET_CRC_TABLE` 扩展至 CRC32 | bench 显示 crc32 位算法 125 MB/s vs crc16 查表 400 MB/s——表路径补齐（256×4B 驻只读段，同 `ET_CRC_TABLE_SECTION` 机制） |
| 2 | bench v1.9 基线行 | `make bench` 复测（含 smap 查找速率），环境注记纪律 |
| 3 | 字符串键 demo 用例 | shell 命令表/配置项查表示范（配方式，非必须） |

## 4. 交付标准（DoD）

1. 沿用 v1.8 全部 DoD（docsync 全绿、双几何硬性项、量化声明附复现命令 + 环境注记、交付表含覆盖率行）；
2. **P0 三 tag 收口为 v1.9 tag 前置**（顺序收口链清零，本版后不再存在多版本悬置 tag）；
3. **文档即测试**：port README 构建命令由 CI `docbuild` job 守护，新增 port/README 必须同批入 job；
4. et_smap 附加：语义与 et_map 的重叠部分以交叉引用文档化（防两套措辞漂移）；
5. 数字回刷纪律生效：本版交付文档定稿后若再有修复提交改变可观测数字，同一提交内回刷并在 commit message 注明。

## 5. 里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 |
|---|---|---|
| M1 | **P0 板上收口 + tag v1.6/v1.7/v1.8 补打**（关键路径） | D+1 |
| M2 | P1 缺陷清偿 + docbuild job | D+1.5 |
| M3 | `et_smap` 合入 | D+2.5 |
| M4 | P3 时间盒 + 全量文档同步 | D+3 |
| M5 | 全量回归（CI 实跑）→ **tag v1.9** | D+3.5 |

## 6. 明确不做（Non-goals）

- shell Tab 补全/多行编辑维持排除（连续四版，无需求信号）；
- et_map 不做键 0 支持、不做动态扩容；et_smap 不做大小写折叠/通配查找（有需求 v2.0 评）；
- 不做 i2c/spi 总线抽象、安全启动/签名、RTOS、浮点格式化、动态内存（全部沿既有边界）；
- F103 真机维持常设挂账（不排期）；Renode G4 维持政策关闭；
- 不再新增"顺延候选"堆积：本版 P3 三项做完或关闭，不留尾巴到 v2.0。

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 板档期再落空 | 三 tag 继续悬置，v1.9 tag 也被 DoD #2 锁住 | P0 提前到 M1 第一优先；若仍不可行，v1.9 交付状态如实标注"四 tag 悬置"并停止叠加新功能规划，直至清账（版本冻结规则） |
| pyserial/Renode 本机安装受阻 | 走单/仿真复核受限 | pyserial 仅 M1-0 一条 pip；仿真以 CI 门为准（本机不强制） |
| xmodem 真传受上位机时序影响 | 走单③失败误判库 bug | 先 `--emit` 文件流自证（v1.6 已验证链路），再串口实传；失败留 `--verbose` 日志区分库/工具/链路 |
| docbuild job 提取 README 命令脆弱（多行/注释） | CI 红误报 | 构建块定界符规范（README 中用统一标记），job 提取失败报"文档定界错误"而非静默跳过 |
| smap 内嵌 pool 参数复杂度劝退用户 | 采用率低 | 头文件给"最小可用示例"（3 行 init）；demo 用例（P3-3）示范 |

---

> 维护约定：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v1.9 后本文档归档不再改动。
