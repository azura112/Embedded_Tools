# v2.5 开发计划：Modbus 主站补齐与日志规格解析加固

> 起草日期：2026-09-19 ｜ 基线：**main @ `b0fc5d8`（= tag `v2.4`，origin/main 同步，工作区 clean）** ｜ 状态：⏳ 待开发（交付后归档）
> 上一版本现状：33 模块 / **432 用例 × 双几何**（1K 变体 433）/ 四例自检配方进 CI / `apidump --diff` 对 v2.3 纯增 22/0 / **docsync 259/259 实测复验** / sizecheck 3/3
> 输入：**上一版评审记录不存在**（见 §1.1）→ CO 清单改由 v2.4 交付文档 §8 挂账、§5 已知限制、§1 过程自省与实机记录 §11 逐条提取
> 结构说明：§1–§6 为本计划规约结构；附 A（里程碑）、附 B（风险）为工程惯例补充

---

## 1. 目标与范围

### 1.1 输入说明：上一版评审记录缺失的处置

按规约，本计划应逐条处置 `v2.4评审记录__<slug>.md` 中的 CO-n。**该文件不存在**——全仓文件检索（`find`）与 `git log --all --name-only` 均无任何 `*评审*` 命名的文件（历史各版亦从未产出过评审记录，仅有序号 v1.x–v2.4 的计划/交付两类文档）。

为不让遗留项静默丢失，本计划把 v2.4 交付材料中**全部显式遗留项**提取为 CO-1…CO-11 并逐条给出处置；同时把"评审记录载体缺失"本身立为本版治理项（§3.1 任务 P0-4），使 v2.5 之后该环节有落点。

**CO 逐条处置表**（"暂缓/拒绝"均附触发条件，条件未触发即不消耗版本容量）

| # | 来源 | 遗留项 | 处置 | 理由 / 触发条件 |
|---|---|---|---|---|
| CO-1 | v2.4 交付 §8.1 | tag `v2.4` 待执行 | **已关闭** | 本计划基线即 tag `v2.4`（`b0fc5d8`，Release 四附件在位）；无残留动作 |
| CO-2 | v2.4 交付 §8.2 | USART2 独立串口（生产建议，免分流歧义） | **暂缓** | 硬件阻塞：bench 仅一路 USB-TTL（CH343→COM12），无法验证独立口。触发条件 = 获得第二路 USB-TTL 或双板对连场景；届时单独立项 |
| CO-3 | v2.4 交付 §8.3 | Modbus 主站模式（轮询调度协同） | **纳入本版**（P1 ★★ 核心） | 从站已落地，主站是唯一有真实工业场景的缺口；**但按 HC-4 收窄为"单事务状态机"，不含轮询调度器**（调度归 et_sched + 应用） |
| CO-4 | v2.4 交付 §8.3 | 定点 biquad（复评触发=具体频响指标） | **维持排队（本版复评不通过）** | 复评时点（2026-09-19）仍无触发信号：库内无陷波/高通截止/峰值 EQ 用例，两个 IIR 形态需求仍由 et_lpf1 覆盖。**不纳入本版**，落档更新为"v2.5 复评完成，维持排队" |
| CO-5 | v2.4 交付 §8.3 | 单向/双向 32 位寄存器组合的**库级封装** | **拒绝（库级）** | 与 v2.4 既定原则冲突：库只管协议与访问控制，值域语义属应用钩子。文档已覆盖约定（API_GUIDE 11.13 注③"32 位参数用两寄存器时约定高低字顺序"），无需新增实现 |
| CO-6 | v2.4 交付 §8.4 | 四项破坏性候选（回调 user 位置/foreach 形状/动词统一/命名收敛） | **维持 v3 窗口** | 触碰既有签名，须在破坏性窗口一次性处置（`docs/v3-candidates.md` 首行纪律）；本版为 MINOR，不触碰 |
| CO-7 | v2.4 交付 §8.4 / P3-2 | selftest 套件是否由 20 扩至 21 | **纳入本版 P3（时间盒 0.5 天）** | v2.4 已把该决议挂到 v2.5；本版新增主站，modbus 冒烟套件的价值上升。**回刷清单为硬条件**（HC-9）；若时间盒冲突则不做，但必须在交付文档显式声明"维持 20"并说明理由，再记 v2.6 |
| CO-8 | 实机记录 §11 自省① | `et_modbus_tick()` 静默路径产生的应答，调用侧漏 flush（demo 已就地修，**文档范式未修**） | **纳入本版**（§3.3 任务 P1-4） | 现状 API_GUIDE 5.7 的示例循环把 `tick()` 排在应答发送之后，与致错形态同形 → 同样的坑会再咬下一个使用者。修法：范式修正 + 示例断言 + demo 统一 flush 点 |
| CO-9 | 实机记录 §11 自省② | `et_log` 不支持域宽/补零：`%02x` 输出字面量**并错位消费后续实参** | **纳入本版**（P0 ★ 核心，缺陷清偿） | 已在 v2.5 起草期用 host 探针**实证复现**（见 §1.2 证据），且仓内三处调用点受影响（g474 demo `%02x` 板面可见错、g474 demo 22 行手工补零绕行、posix_demo `%.*s` 静默错）。属"静默错误"缺陷类，非风格问题 |
| CO-10 | 实机记录 §11 自省③ | `tools/modbus_master.py` 自身三处 bug | **已关闭** | v2.4 当轮修复，并与 C 库组帧跨实现逐字节一致（`11 03 00 00 00 04 46 99`） |
| CO-11 | 实机记录 §11 已知限制 | 共享串口分流下"发往其它从站地址的帧"被分流到 shell，地址不符静默无法在板上验证 | **暂缓**（与 CO-2 同源） | 依赖 CO-2 的独立口方案；本版主站走单改用 **PC 从站仿真器**，不依赖该路径，故不阻塞 |

### 1.2 本版做什么

1. **`et_log` 规格解析加固（缺陷清偿，★ 核心之一）**——把"不支持规格 → 字面回显 + 实参错位"的静默失效，改为"完整吃掉规格序列 + 实参消费与 C 语义一致"；补齐嵌入式日志真实需要的域宽/补零/左对齐/精度子集；清除板侧 demo 22 行手工补零绕行。
2. **`et_modbus_master`（第 34 模块，★★ 核心）**——Modbus RTU **主站**：0x03/0x04 读、0x06/0x10 写、广播写不等应答、应答超时重发、异常码上报、迟到/陈旧字节不污染下一事务；与从站**共享协议常量**（沿 `et_xmodem_tx` 先例）。
3. **板侧同步与真机走单**——`Core/et` 同步 v2.4→v2.5；G474 挂主站命令组，与 **PC 侧 `modbus_master.py --slave` 从站仿真器**做字节级真机走单（含超时重发与异常路径）；`et_log` 域宽输出行真机复验。

**起草期证据（CO-9 实证，host 探针，已复现后删除探针）**

```
ET_LOGI("p", "addr=0x%02x (kv regs %u..%u)", 17u, 1000u, 1003u);
  → addr=0x%02x (kv regs 17..1000)      # 字面回显 + 实参整体前移一格（板侧现象一致）
ET_LOGI("p", "n=%zu tail=%u", (size_t)7u, 99u);
  → n=%zu tail=7                        # 后续实参取到前一个实参的值
ET_LOGW("p", "unknown(%u): %.*s", 3u, 3, "ABC");
  → unknown(3): %.*s                    # posix_demo.c:61 现网同形
ET_LOGI("p", "t=%04u ms", 42u);
  → t=%04u ms                           # 域宽：字面回显
```

### 1.3 明确不做什么（Non-goals）

- **不做** Modbus TCP / ASCII / 网关 / 多主站（超库定位）；
- **不做**主站轮询调度器与多从站表（HC-4：调度策略属应用，与 et_sched 职责重叠）；
- **不做** USART2 独立串口（CO-2 暂缓，硬件阻塞）；**不做**共享口分流局限的板侧实测（CO-11）；
- **不做**定点 biquad（CO-4 复评维持排队）；**不做** 32 位寄存器组合的库级封装（CO-5 拒绝）；
- **不做**任何破坏性变更（CO-6 四项维持 v3 窗口）；`apidump --diff` 必须纯增；
- **不做**浮点格式化（`%f/%e/%g` 仍不支持；本版只保证"可见占位 + 实参不错位"）；
- **不做**动态内存 / RTOS / 内部取时基 / i2c 抽象 / 签名启动 / 通配 / 多行编辑（连续十五版永久边界）；
- **不做** selftest 扩套件的强制项（P3 时间盒，见 CO-7）。

---

## 2. 硬性约束（违反任一条即交付不合格）

- **HC-1 契约零破坏**：`sh tools/apidump.sh --diff` 对 `docs/API_INVENTORY_v2.4.md` 基线必须"新增 N 项 / **修改删除 0 项**"；既有 API 语义与既有宏值不变（`ET_VERSION*` 元数据宏除外）。
- **HC-2 et_log 实参消费正确性（缺陷清偿门）**：对**形状可识别**的规格序列（合法标志 + 十进制域宽（含 `*`）+ 精度 `.N`/`.*` + 长度修饰 + 转换字符），实参消费次数与 C 语义一致，**不得错位**；不得以"字面回显"方式静默降级。
- **HC-3 不支持规格必须可见**：`%f/%e/%g/%n/%lc/%ls` 等仍不支持的规格，须按 C 语义**消费对应实参**并输出**文档固定的可见占位**（禁写内存、禁字面回显）；未知转换字符输出可见占位且不消费（该例外须在 `et_log.h` 头注与 API_GUIDE 明示）。
- **HC-4 主站职责边界**：`et_modbus_master` 只做**单事务**状态机（一次一个在途请求）；不内置多从站轮询表、不内置调度器、不内部取时基（`now_ms` 由调用方注入）、超时阈值由调用方按波特率/帧长换算后写入 cfg。
- **HC-5 协议常量单一来源**：主站必须 `#include "et_modbus.h"` 复用 `ET_MODBUS_FC_*`/`ET_MODBUS_EXC_*`/`ET_MODBUS_*_QTY_MAX`/`ET_MODBUS_ADU_MAX`，**禁止复制数值**；模块开关沿用 `ET_MODULE_MODBUS`（沿 `et_xmodem_tx` 共享 `ET_MODULE_XMODEM` 先例，不新增开关）。
- **HC-6 库不变式**：零动态内存、多实例句柄化、`core/`+`algorithm/` 零硬件依赖、ISR-safe 范围显式标注、🏠MAIN 上下文标注；新增结构体不得追加/改动既冻结结构体字段。
- **HC-7 数字回刷同批**：模块数、用例数、examples 数、docsync 计数、README 特性表/结构树、architecture 选型表、API_GUIDE、CHANGELOG、**port/*/README 体积表（sizecheck 逐值门）**必须与代码同批更新——et_log 加固必然改变体积，表行不刷即红。
- **HC-8 示例即证据**：`examples/ex_modbus_master.c` 必须进 `make ex`，断言仅确定性值（**字节级/逐字符比对**），禁止耗时断言；示例只用公开 API。
- **HC-9 selftest 若扩必须全链回刷**：套件数若由 20 改 21，`smoke.sh` 断言行、README、API_GUIDE 8.4、实机记录、体积表须同批；若不扩，交付文档显式声明"维持 20"并给理由（禁含糊）。
- **HC-10 真机证据**：主站与 et_log 域宽两项必须有**板上逐字节记录**（请求帧、应答帧、重发序列、解析值、日志行原样），编译通过不算验收。
- **HC-11 候选池一致性**：CO 处置结论必须与 `docs/v3-candidates.md`、交付文档 §8 三处措辞一致（不得一处"维持排队"另一处"已关闭"）。

---

## 3. 任务分解

### 3.1 P0 治理常规与候选池处置（约 0.5 天）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P0-1 | apidump 基线滚动：归档 `docs/API_INVENTORY_v2.4.md`，`--diff` 默认基线切至 v2.4（417 项比较单元） | `docs/API_INVENTORY_v2.4.md`（新增）、`tools/apidump.sh`（默认基线变量）、`docs/API_STABILITY.md`（§5 基线规则行） | 只动治理脚本与归档；不改头文件 → 冻结面零影响；v2.3/v2.2 归档只读保留 | P0 |
| P0-2 | **候选池逐条落档**：biquad 复评结论（维持排队，下次触发=具体频响指标）、32 位组合封装拒绝理由、主站"单事务不含调度器"边界、selftest 21 纳入本版 P3 | `docs/v3-candidates.md`（#8 状态行 + 新增"v2.5 复评记录"小节） | 纯文档；与 CO-4/CO-5/CO-7 措辞对齐（HC-11）；无代码影响 | P0 |
| P0-3 | 版本宏升 2.5.0 | `et_config.h`（`ET_VERSION`/`ET_VERSION_STRING`/`ET_VERSION_MAJOR/MINOR`） | 体积宏变化被 apidump 白名单忽略；触发 sizecheck 表行需要新增 v2.5 行 | P0 |
| P0-4 | **评审记录载体建立**（关闭 §1.1 缺口）：新增评审记录模板（CO 编号规则/处置字段/与计划互引/必须逐条不留白）+ 开发流程 checklist 增一行 | `docs/评审记录模板.md`（新增）、`README.md`（开发流程 checklist）、`tools/docsync.sh`（可选：断言"当前版本交付文档须引用评审记录或声明缺失"） | 纯治理；若加 docsync 断言则同步回刷计数 | P1 |

### 3.2 P0 `et_log` 规格解析加固（★ 核心之一，缺陷清偿，约 1.5 天）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P1-1 | **规格解析器重写**：`%` 后按 C 语义吃掉 标志{`-` `0` `+` 空格 `#`} + 十进制域宽（含 `*`，从实参取）+ 精度（`.N` / `.*`）+ 长度修饰{`h` `hh` `l` `ll` `z`}；整数支持最小宽度与补零/左对齐/符号/`#` 前缀，字符串支持 `%.Ns`/`%.*s` 截断；域宽设上限常量（超限按上限输出，不无限刷字符） | `debug/et_log.c`（`vformat` 重写）、`debug/et_log.h`（头注更新支持面与占位规约） | **行为变化**：既有已支持规格语义不变（HC-1），新增子集为纯扩展；`emit_uint` 需支持最小位数参数 → 局部重构，不新增公开 API（apidump 应保持零新增或仅新增上限宏） | P0 |
| P1-2 | **不支持规格可见化 + 实参消费**：浮点族按 `double` 消费、`%n` 消费指针但不写内存、`%lc/%ls` 消费后输出占位；未知转换字符输出占位不消费（头注明示例外） | `debug/et_log.c`、`debug/et_log.h` | 关闭 HC-2/HC-3 的静默失效路径；输出字形变化 → 需在 API_GUIDE 固定并测 | P0 |
| P1-3 | **单测矩阵（≥16 例，含错位检测）**：每条规格序列后追加哨兵实参并断言其取值正确（这是错位检测的关键手法）；域宽/补零/左对齐/精度/`*`/长度修饰/**与 host `snprintf` 对拍**；不支持规格的占位与不错位；域宽上限；`%%`/尾部 `%` 边界 | `test/test_log.c`（新增用例）、`Makefile`（若新增测试源则登记 `TEST_SRC`——ubuntu 显式清单门） | 用例数 432 → 约 450；docsync/README 计数回刷 | P0 |
| P1-4 | **调用点清理**：删除 g474 demo 22 行手工补零（改 `%04u-%02u-%02u %02u:%02u:%02u`）；确认 posix_demo `%.*s` 输出正确（由 P1-3 用例守护）；全库 grep 复查无遗留不支持规格 | `examples/stm32g474_demo.c`、`examples/posix_demo.c`（如保留 `%.*s` 则仅验证） | 板面日志行变化 → 需真机复验（P2-3）；删除绕行代码减小 demo 体积 | P1 |
| P1-5 | 文档与体积表：API_GUIDE 的 et_log 章节更新支持面/占位/上限；`port/stm32f103/README.md`、`port/stm32g474/README.md` 增 v2.5 体积行 | `docs/API_GUIDE.md`、`port/*/README.md` | sizecheck 3/3 逐值门要求表行与实测一致（HC-7） | P0 |

### 3.3 P1 `et_modbus_master` 主站（第 34 模块，★★ 核心，约 2.5 天）

**形状约定（冻结；字段命名与内部布局见 §5 可裁量项）**

```c
/* protocol/et_modbus_master.h —— #include "et_modbus.h" 复用 ET_MODBUS_* 常量 */
typedef enum {
    ET_MB_IDLE = 0, ET_MB_BUSY, ET_MB_OK, ET_MB_EXC,
    ET_MB_TIMEOUT, ET_MB_REJECT          /* REJECT = 组帧参数非法/未初始化 */
} et_mb_status_t;

typedef struct {
    uint16_t addr;                 /* 目标从站 1~247; 0 = 广播(仅写, 不等应答) */
    uint32_t resp_timeout_ms;      /* 应答超时(调用方按帧长+3.5字符换算) */
    uint8_t  retry_max;            /* 超时重发次数上限 */
} et_modbus_master_cfg_t;

bool            et_modbus_master_init(et_modbus_master_t *m, const et_modbus_master_cfg_t *cfg,
                                      uint8_t *rxbuf, uint32_t rxcap, uint8_t *txbuf, uint32_t txcap);
bool            et_modbus_master_read(et_modbus_master_t *m, uint8_t fc, uint16_t reg, uint16_t qty);
bool            et_modbus_master_write(et_modbus_master_t *m, uint8_t fc, uint16_t reg,
                                       const uint16_t *vals, uint16_t qty);
const uint8_t  *et_modbus_master_tx(const et_modbus_master_t *m, uint32_t *len);   /* 取待发请求(含 CRC) */
void            et_modbus_master_sent(et_modbus_master_t *m, uint32_t now_ms);     /* 已上线 → 起等应答 */
uint32_t        et_modbus_master_feed(et_modbus_master_t *m, const uint8_t *d, uint32_t n);
et_mb_status_t  et_modbus_master_poll(et_modbus_master_t *m, uint32_t now_ms);     /* 推进/超时重发/终态 */
uint16_t        et_modbus_master_result(const et_modbus_master_t *m, uint16_t *qty);
uint8_t         et_modbus_master_exc(const et_modbus_master_t *m);
void            et_modbus_master_stats(const et_modbus_master_t *m, et_modbus_master_stats_t *st);
```

**语义要点**：超时 → 重发同一请求 ≤ `retry_max` 次 → `ET_MB_TIMEOUT`；应答地址不符 / CRC 坏 → 丢弃并计数，继续等至超时；异常应答（`fc|0x80`）→ `ET_MB_EXC` **不重试**；广播写 → `sent()` 后立即 `ET_MB_OK`（不等应答）；终态后的迟到/陈旧字节 → 丢弃并计数，**不得污染下一事务**；功能码仅 0x03/0x04/0x06/0x10，其余 `read/write` 返回 false。

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P1-6 | 主站模块实现：字节流应答解析（长度域定帧 + CRC16-MODBUS 校验 + 地址过滤）、请求组帧、单事务状态机（IDLE/BUSY/终态）、超时重发、统计计数、多实例 | `protocol/et_modbus_master.c`（新增）、`protocol/et_modbus_master.h`（新增）、`Makefile`（`PROTO_SRC` + `TEST_SRC` 登记）、`et_config.h`（仅注释行更新模块清单，不新增开关） | 新模块 = README 模块数 33→34；F103/G474 体积增长（无 gc-sections 的 docbuild 会把新 .o 计入）→ 体积表回刷；apidump 纯增 | P0 |
| P1-7 | 主站单测 ≥20 例：0x03/0x04/0x06/0x10 正常流；分片应答；超时→重发→耗尽；重发后成功；迟到应答不污染下一事务；CRC 坏/地址不符丢弃；异常应答不重试；广播写不等应答；qty 边界（0/125/126、123/124）；rxcap/txcap 不足；多实例；统计累积；未初始化/非法参数 REJECT | `test/test_modbus_master.c`（新增） | 用例数再增约 22 → 与 P1-3 合计约 470；套件 `proto/modbus_master` 独立可核验 | P0 |
| P1-8 | **自检示例**：`examples/ex_modbus_master.c` —— 内置确定性"模拟从站"应答器（含"丢首个应答"以驱动超时重发、异常应答、广播） | `examples/ex_modbus_master.c`（新增）、`Makefile`（`ex` 目标第 5 例） | `make ex` 四例 → 五例；examples 计数回刷；纯虚拟时间，零耗时断言 | P0 |
| P1-9 | **工具从站仿真器**：`tools/modbus_master.py` 增 `--slave`（串口监听应答 0x03/04/06/10、`--slave-drop N` 丢包注入、`--slave-exc` 异常注入、`--slave-regs` 初值）；`--selftest` 扩展覆盖仿真器 | `tools/modbus_master.py` | 板侧走单不再依赖第二台设备（绕开 CO-2/CO-11 阻塞）；python 与 C 组帧/解析跨实现对拍 | P1 |
| P1-10 | **CO-8 范式修正**：API_GUIDE 5.7 示例循环改为"每轮末复查 `et_modbus_response()`（tick 路径应答必须发送）"，正文写明**应答有两条产生路径**；`examples/ex_modbus_slave.c` 增/确认 tick 路径应答断言 | `docs/API_GUIDE.md`（5.7）、`examples/ex_modbus_slave.c` | 纯文档+示例断言，无 API 变化；把 v2.4 咬人的坑变成 CI 守护的断言 | P1 |
| P1-11 | 文档：API_GUIDE 新增 5.8（主站协议表/状态机/超时换算/异常与重试语义/统计）+ 11.14（主站轮询多从站：与 et_sched 协作的应用侧范式，显式声明库不提供调度器）；architecture 选型表与数据流更新 | `docs/API_GUIDE.md`、`docs/architecture.md`、`README.md`（特性表/结构树） | 数字回刷面（模块 34/用例/examples）；docsync 断言需同步 | P1 |

### 3.4 P2 板侧同步与真机走单（约 1.5 天，G474）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P2-1 | 板侧 `Core/et/` 同步至 v2.5（跨 v2.4→v2.5）：`diff -rq` 校验、0 warning、FLASH/RAM 读数 | 板侧工程 `Core/et/**`（库外）、`移植stm32实机记录.md` | 库内零回归应保持（-gc-sections 丢弃未调用模块）；体积增量记档 | P0 |
| P2-2 | demo 挂主站命令组：`AT+MBRD`/`AT+MBWR`/`AT+MBPOLL`（走单期间抑制 shell 回显与提示符，避免与主站 RX 流争用同一 USART1） | `examples/stm32g474_demo.c` | demo 体积增长；分流规则需处理"从站应答首字节"路由（应答首字节 = 从站地址 → 走 modbus） | P0 |
| P2-3 | **真机走单（逐字节）**：(a) 主站读/写经 PC 从站仿真器；(b) `--slave-drop` 注入 → 超时重发与统计；(c) 异常应答；(d) 广播写；(e) `et_log` 域宽输出行原样比对 | `移植stm32实机记录.md`（新增 §12）、`tools/modbus_master.py`（消费方） | HC-10 的落点；两版能力（主站 + 日志）同场验证 | P0 |
| P2-4 | 既有回归：`AT+SELFTEST`、`AT+SELFSTOR`、`AT+SIMUPGRADE 3` CONFIRMED 不破；Renode smoke 断言行按 HC-9 处置 | `移植stm32实机记录.md`、`tools/smoke.sh`（若扩套件） | 若 selftest 维持 20 则 smoke 断言行零改动 | P0 |

### 3.5 P3 可选（时间盒各 0.5 天，不阻塞发布）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P3-1 | bench v2.5 行：主站"请求组帧"与"应答解析"ns/op（既有行无回归） | `tools/bench.c`、`docs/bench.md` | 纯增量；环境注记沿 v1.7 基线 | P2 |
| P3-2 | selftest 20 → 21（modbus 从站+主站纯逻辑冒烟，host/板上同套件）+ **数字回刷全链** | `debug/et_selftest.c`、`tools/smoke.sh`、`README.md`、`docs/API_GUIDE.md` 8.4、`移植stm32实机记录.md`、`port/*/README.md` | 回刷面最大的一项（HC-9）；与板档期冲突则不做并显式声明维持 20 | P2 |
| P3-3 | API_GUIDE 11.13 补"32 位参数组合读写"4 行最小说明（仅文档，不做库级封装） | `docs/API_GUIDE.md` | 落实 CO-5 的"文档已覆盖"结论，避免下次再被提为候选 | P2 |

---

## 4. 验收标准（每条可客观核验，标注对应任务）

**契约与治理**

- **AC-1**（P0-1）`sh tools/apidump.sh --diff` 在发版点输出"新增 N 项 / 修改删除 0 项"且退出码 0；`docs/API_INVENTORY_v2.4.md` 存在，v2.3/v2.2 归档文件未被改动（`git diff --stat` 无它们）。
- **AC-2**（P0-2）`docs/v3-candidates.md` #8 行含"v2.5 复评完成 + 维持排队 + 下次触发=具体频响指标"；`sh tools/apidump.sh --check` 清单中 `biquad` 相关符号计数为 0；32 位组合封装与主站边界措辞在 `v3-candidates.md` 与交付文档 §8 一致（HC-11）。
- **AC-3**（P0-3）`grep ET_VERSION et_config.h` 得 `0x020500` 与 `"2.5.0"`；板上横幅与 `AT+VER` 显示 2.5.0。
- **AC-4**（P0-4）`docs/评审记录模板.md` 存在且含 CO 编号规则、处置四选项（纳入/暂缓/拒绝/已关闭）、触发条件字段；`README.md` 开发流程 checklist 含"每版交付后产出评审记录"一行。

**et_log 加固（HC-2/HC-3 的门）**

- **AC-5**（P1-1/P1-3）`mingw32-make test` 中 `debug/log` 套件用例数 ≥ 23（现基线 **7 例**，新增 ≥16），fail=0；其中**每条规格用例都含"后随哨兵实参取值正确"断言**（错位检测）。
- **AC-6**（P1-1）对受测子集（`%04u` `%02x` `%-5u` `%+d` `% d` `%#x` `%*u` `%.3d` `%.Ns` `%.*s` `%lld` `%zu` `%hu`）逐字节等于 host `snprintf` 输出（同一用例内对拍断言）。
- **AC-7**（P1-2）`%f`/`%e`/`%g`/`%n`/`%lc` 输出**不含**字面 `%f` 等原样回显，而是文档固定的可见占位，且其后随哨兵实参取值正确；`%n` 用例断言未对目标变量写入（哨兵值不变）。
- **AC-8**（P1-1）域宽上限：`%9999u` 输出字符数 ≤ 上限常量值，且 `-fsanitize=address` 构建下该用例无越界报告（host）。
- **AC-9**（P1-4）`grep -c "手工拼\|dt_line" examples/stm32g474_demo.c` = 0；该处改为域宽格式串；全库 ET_LOG 调用点 grep 无"不支持规格"用法（`%0`/`%-`/`%.`/`%z`/`%+` 之外无命中，或命中处即为 P1-3 的有意用例）。
- **AC-10**（P1-5）`sh tools/sizecheck.sh` 输出 `pass=3 fail=0`，且 `port/stm32f103/README.md`、`port/stm32g474/README.md` 含 v2.5 表行（数值与实测逐值相等）。

**主站**

- **AC-11**（P1-6）`apidump --diff` 新增项含主站函数 ≥ 8、类型 ≥ 4（`et_modbus_master_t`/`cfg_t`/`stats_t`/`et_mb_status_t`）、宏 ≥ 1；修改删除 0；`ET_MODBUS_*` 既有宏值无变更行。
- **AC-12**（P1-6）`grep -c "0x03\|0x04\|0x06\|0x10" protocol/et_modbus_master.c` 中**无裸数值常量**（功能码必须经 `ET_MODBUS_FC_*` 引用）——HC-5 的机械核验。
- **AC-13**（P1-7）`mingw32-make test` 中 `proto/modbus_master` 套件 pass ≥ 20，fail=0。
- **AC-14**（P1-7）超时重发用例断言：`retry_max = 2` 且全程不应答时，重发请求字节与首帧**逐字节相同**（含 CRC），重发次数 = 2，`et_modbus_master_poll()` 终态返回 `ET_MB_TIMEOUT`，统计中请求数 = 3。
- **AC-15**（P1-7）迟到应答用例断言：终态后喂入上一事务应答字节 → 统计丢弃计数 +1，且**下一事务**的读回值与期望逐值相等（不污染）。
- **AC-16**（P1-7）异常应答用例断言：返回 `ET_MB_EXC`，`et_modbus_master_exc()` 等于注入的异常码，重发次数 = 0。
- **AC-17**（P1-7）广播写用例断言：`addr=0` + `sent()` 后 `poll()` 立即 `ET_MB_OK`，且未进入等待（以 `now` 未推进为条件断言）。
- **AC-18**（P1-8）`mingw32-make ex` 五例 PASS；`ex_modbus_master` 的断言均为字节级/逐值比对（用例内含"丢首应答→重发成功"与异常路径）。
- **AC-19**（P1-9）`python tools/modbus_master.py --selftest` 全项 PASS（含从站仿真器回环与丢包注入）；C 库组帧字节 == python 组帧字节（对拍值写入交付文档）。
- **AC-20**（P1-10）API_GUIDE 5.7 示例循环在 `tick()` 之后**复查并发送**应答（正文明确"应答有 feed 快路径与 tick 静默路径两条来源"）；`make ex` 中 `ex_modbus_slave` 含 tick 路径应答断言。

**板侧与回归**

- **AC-21**（P2-1/P2-3）`移植stm32实机记录.md` 新增 §12，含：`Core/et` 同步 `diff -rq` 全 OK 结论、0 warning、FLASH/RAM 读数；主站请求/应答**逐字节**、重发序列字节、异常码、广播写、`et_log` 域宽行原样输出。
- **AC-22**（P2-4）板上 `AT+SELFTEST` 为 20/20（或 21/21，视 P3-2）且 `AT+SELFSTOR` PASS、`AT+SIMUPGRADE 3` → CONFIRMED；若扩套件，`tools/smoke.sh` 断言行与之一致。
- **AC-23**（全版）`sh tools/docsync.sh` 输出 `pass=N fail=0`，且当前交付文档行首声明的 docsync 计数 = 实测（自指断言）。
- **AC-24**（全版）`mingw32-make test`、`test-g4`、`test-tab`、1K 变体（`-DET_XM_1K=1`）全部 `ALL PASS (fail=0)`；`gcovr` 行覆盖 ≥85%（记实测值）；CI 七 job 全绿（含 renode-smoke）。
- **AC-25**（P3-1/P3-2/P3-3）P3 三项：做了的给出证据（bench.md v2.5 行与实测 ns、`AT+SELFTEST` 21/21、11.13 新说明）；未做的在交付文档 §8 显式声明"不做 + 理由"，并记 v2.6 候选（禁静默略过）。

---

## 5. 可裁量项（开发者可自主决策，其余不得偏离）

1. `et_modbus_master` 的内部字段布局、枚举成员顺序、统计字段命名与计数口径细节（须在 API_GUIDE 5.8 写明并测）。
2. 主站缓冲最小校验值、`txcap`/`rxcap` 下限（≥ `ET_MODBUS_ADU_MAX` 为硬底线）。
3. `et_log` 可见占位的**具体字形**（如 `<?f>`），一经选定须全库一致并写进 `et_log.h` 头注与 API_GUIDE。
4. 域宽上限常量的**具体数值**与命名（须文档化并有用例覆盖）。
5. 主站示例中"模拟从站"的实现方式（表驱动应答器 vs 脚本化字节表），以及 `--slave` 模式的命令行参数拼写。
6. bench 行测量粒度（仅组帧 / 组帧+解析一轮）。
7. 板侧 demo 命令名与分流判定的具体实现细节（前提：不与 shell 争用、走单可复现）。
8. P3 三项做与不做的取舍（时间盒各 0.5 天；不做须如实声明）。
9. 文档章节编号（5.8 / 11.14）与措辞；`docs/评审记录模板.md` 的字段排布。
10. 若新增测试源文件，`Makefile` 中登记位置与顺序。

**不可偏离**：主站文件归属（新增 `protocol/et_modbus_master.c/.h`，复用 `et_modbus.h` 常量，不新增模块开关）、主站 API 形状（`tx`/`sent`/`feed`/`poll` 模型与单事务语义）、HC-1…HC-11 全部、§1.1 CO 处置结论。

---

## 6. 基线

- **起始分支/commit**：`main` @ **`b0fc5d8`**（= tag `v2.4`；`origin/main` 同步；工作区 clean）。
- **动手前必须先打快照**（回滚点，先于任何代码改动）：

  ```sh
  git tag v2.5-start b0fc5d8
  git push origin v2.5-start        # 本机推送偶发 443 阻断 → 重试即可
  ```

  注：单次 push 多个 tag 不触发 workflow，快照 tag 无副作用；本地无网络时至少保留本地 tag，并在交付文档记录快照 tag 名。
- **计划文档入库**：本文件随首个提交入库（沿既有"计划归档"惯例）。
- **基线偏离申报**：若开发启动时 `main` 已前移，须以实际起点为准并在交付文档首行声明实际基线 commit；计划本身一经交付即冻结，不回改。

---

## 附 A：里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 |
|---|---|---|
| M1 | P0 全部：apidump 基线滚动 + 候选池落档 + 2.5.0 + 评审记录模板 | D+0.5 |
| M2 | `et_log` 加固：解析器 + 占位 + ≥16 例（含对拍/错位检测）+ demo 调用点清理 + 体积表回刷 | D+2.0 |
| M3 | `et_modbus_master` + ≥20 例 + `ex_modbus_master`（`make ex` 五例）+ `--slave` 仿真器 + API_GUIDE 5.8/11.14 | D+4.5 |
| M4 | 板侧同步 + 主站命令组 + 真机走单（含重发/异常/广播/日志行） | D+6.0 |
| M5 | 全量回归（本地三脚本 + CI 实跑）→ 交付文档 → **tag v2.5** | D+6.5 |

## 附 B：风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| et_log 加固改变输出/体积，连带 sizecheck 与板面日志变化 | 表行红 / 走单结论被格式变化干扰 | 既有规格语义不变（HC-1）；体积表与代码同批（HC-7）；P2-3 把日志行原样纳入走单证据 |
| 主站超时阈值与波特率/帧长耦合 | 误判超时、重发风暴 | 阈值由调用方换算注入（HC-4）；host 用虚拟时间全速跑超时/重发/迟到矩阵；真机走单用 `--slave-drop` 显式注入 |
| 板侧单串口上主站与 shell 争用 | 走单不稳、结论不可信 | 走单期间抑制 shell 回显/提示符（P2-2）；若仍不稳，按 CO-2 如实挂账并只保留 host 证据，不伪造板侧结论 |
| PC 从站仿真器与 C 主站实现漂移 | 走单"自证"失效 | 跨实现对拍（同一请求帧两实现字节相等）+ `--selftest` 覆盖仿真器自身（沿 v2.4 先例） |
| 范围蔓延（轮询调度器/多从站表/TCP/浮点格式化） | 时间盒失守 | HC-4 + §1.3 Non-goals 显式排除；相关诉求一律记 v2.6 候选 |
| 评审记录持续缺失 | 遗留项无人跟踪、逐版丢失 | P0-4 建模板 + checklist 行；交付文档必须"引用评审记录或声明缺失" |
| 数字回刷面变大（模块/用例/examples/docsync/体积表/selftest） | 漏刷即 CI 红 | 交付前以三脚本为门：`docsync.sh` + `sizecheck.sh` + `apidump.sh --check`，逐条对照 checklist |
| 板档期 | M4 受阻 | 库内项（M1–M3）先合入；走单项如实挂账，不阻塞 M5 之外的进度 |

> 环境注记（沿既有记录）：Windows 侧 `make` → `mingw32-make`；`apidump.sh` 已锁 `LC_ALL=C`（gawk 字节模式）；smoke.sh 含字面 NUL 历史（v1.5 起已修，勿回退）；体积读数以本机 CubeCLT 工具链为准并附环境注记。

---

> 维护约定：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v2.5 后本文档归档不再改动。
