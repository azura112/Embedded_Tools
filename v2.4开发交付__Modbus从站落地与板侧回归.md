# v2.4 开发交付：Modbus 从站落地与板侧回归

> 交付日期：2026-09-12 ｜ 版本：v2.4.0（`ET_VERSION = 0x020400`，冻结下纯增量 MINOR）｜ 基线：v2.3（tag `v2.3` → `8c12579`，Release Latest）
> 计划文档：[v2.4 开发计划__Modbus 从站落地与板侧回归](./v2.4开发计划__Modbus从站落地与板侧回归.md)（状态：✅ 已交付归档）
> 远程：https://github.com/azura112/Embedded_Tools（**本工作区尚未提交/打 tag**；`v2.4` tag 由发布 checklist 核对后执行）
> 本版 docsync：**259/259**（`sh tools/docsync.sh` 实测,含本自指断言;机制与自证见 §7）

## 1. 重点概况（TL;DR）

v2.4 给协议层装上了**第一个标准应用协议**：`et_modbus` —— Modbus RTU 从站（0x03/0x04 读、0x06/0x10 写、三类异常码、广播、分片/粘包/静默界定），复用 v2.2 铺好的 CRC16-MODBUS 查表与字节流状态机范式。配套交付**自检式示例**（第 4 例进 `make ex`）与**主站工具** `tools/modbus_master.py`（`--selftest` 内置从站仿真器 + 串口读写 + 逐字节 verbose），并**在 G474 真机上完成字节级走单**（含 kv 参数经 Modbus 读写的复位保持验证）。全程零破坏性变更：`apidump --diff` 对 v2.3 基线 **新增 22 项 / 修改删除 0 项**。biquad 立项判定按预期**维持排队**（v2.5 复评），不消耗版本容量。

- **P0 治理与候选判定**：基线滚动至 `docs/API_INVENTORY_v2.3.md`（395 项比较单元，v2.2 归档保留）；**biquad 判定落档**——`docs/v3-candidates.md` #8 记"维持排队, v2.5 复评"+ 判定依据（现有 IIR 需求已被 et_lpf1 覆盖 / 无真实陷波-高通用例 / 条件未触发不消耗容量）+ 复评触发信号（用户提交具体频响指标）+ 实现预案；版本宏 2.4.0。
- **P1 `et_modbus`（第 33 模块，★ 核心，23 例）**：
  - **协议覆盖**：0x03/0x04 读保持/输入寄存器、0x06/0x10 写单/多寄存器；异常 0x01 非法功能 / 0x02 非法地址 / 0x03 非法数据值；**广播（addr 0）写执行不应答、读忽略**；数量边界 125/123、ADU 256。
  - **双路径帧判定**：已知功能码**长度域快路径**（0x10 = 9+bytecount）收齐即处理；未知功能码/畸形帧/残帧由**帧间静默 3.5 字符**界定（`silence_ms` 按波特率换算由调用方注入，`tick(now)` 周期调用，不内部取时基）。
  - **粘包安全**：`rxbuf`/`txbuf` **双缓冲**（应答构建不覆盖缓冲中后续帧），`feed` 产生一应答即停 → `feed(NULL,0)` 排空后续帧；新增只读 `et_modbus_rx_pending`（共享串口分流判"半帧延续"）。
  - **寄存器值域语义由应用钩子决定**（库只管协议与访问控制）；配方 **11.13 = kv 参数暴露为保持寄存器**（掉电参数直接被上位机读写）。
  - 单测 23 例：正常流四种、异常三种、CRC 坏/地址不符静默、广播写/读、分片、粘包、静默丢弃重同步、qty 边界 125/123、txcap 不足、统计累积、多实例、rx_pending。
- **P2 板侧回归与真机走单（★ 真机）**：`Core/et` 跨 **v2.2→v2.4** 两版同步（`diff -rq` 全 OK，0 warning，FLASH **37428 B**）；Modbus 从站挂 demo 并**逐字节走单**——0x03 读（`11 03 08 00 01 00 02 00 03 00 04 59 D4`）、0x06/0x10 写与回读、异常 0x02/0x03/0x01（0x63 的 `11 E3 01 A9 35` 与 host 示例期望**逐字节一致**）、CRC 坏帧静默、**kv 直通寄存器写→复位→读回保持**；既有 SELFTEST 20/20、SELFSTOR、升级链 CONFIRMED 回归不破。**挂载方案偏差如实记录**：计划预填 USART2 独立，bench 仅一路 USB-TTL → 实测用 **USART1 帧首字节分流**，其"地址不符静默"局限（其它从站地址的帧被分流到 shell 回显）在记录中明示，该路径由 host 单测与示例覆盖。
- **P3 可选**：bench v2.4 行（`modbus 0x03 读 4 寄存器` **115 ns/帧 ≈ 8.7 M 帧/s**；相对 115200 线速的 0.7ms/8B 可忽略）；**selftest 未扩套件**（维持 20，Non-goals 既定）；主站模式不做（记 v2.5 候选）。
- **过程自省（板侧暴露 3 个问题，全部当轮修复）**：① **静默路径应答漏发** —— `tick()` 内产生的应答（未知功能码异常帧）在 demo 无 flush 调用点 → 现象"0x63 请求无应答"，修法 tick 后补 flush；② **et_log 不支持宽度/补零修饰** —— `0x%02x` 输出字面量并错位消费参数，改回 `%x`；③ **工具自身三处 bug** 由 `--selftest` 首跑抓出（寄存器字节序取反、自检载荷地址拼错、期望帧字节错），修后与 C 库组帧**跨实现逐字节一致**（`11 03 00 00 00 04 46 99`）。

## 2. 逐项验收对照（v2.4 计划 §3）

| # | 任务 | 验收结果 |
|---|---|---|
| P0-1 | apidump 基线滚动 | ✅ v2.3 基线归档（395 项）；发版点 0/0；对当前头文件新增 22/0 |
| P0-2 | biquad 立项判定（不实现） | ✅ `v3-candidates.md` #8 记"维持排队, v2.5 复评"（依据/触发信号/实现预案三件套）；库内零实现 |
| P0-3 | 版本宏 2.4.0 | ✅ `0x020400`；板面横幅 `Embedded_Tools v2.4.0 (0x20400)` |
| P1 | `et_modbus` 合入 | ✅ 第 33 模块 23 例；API_GUIDE 5.7 + 11.13；示例 + 工具 + bench |
| P1 | 示例（覆盖正常+异常全路径） | ✅ `examples/ex_modbus_slave.c` 进 `make ex`；断言**逐字节比对**、零耗时断言 |
| P1 | 工具 `modbus_master.py` | ✅ `--selftest`（CRC 向量 + 内置从站仿真器 + 静默语义）与串口模式（`--read/--write/--write-multi/--raw/--raw-resp/--expect-exc`） |
| P2-1 | 板侧同步 v2.4（跨 v2.2→v2.4） | ✅ `diff -rq` 全 OK；0 warning；FLASH 36088 → **37428 B** |
| P2-2 | demo 挂 Modbus | ✅ 已挂（USART1 分流，偏差与理由见 §1/§5；生产建议 USART2 独立已文档化） |
| P2-3 | 真机走单（含 kv 掉电验证） | ✅ 逐字节记录见实机记录 §11；写→复位→读回保持 |
| P2-4 | Renode smoke 回归 | ✅ selftest 未动 → 断言行零改动（CI 以发布 run 为准） |
| P3 | 时间盒三项 | ✅ bench 行做；selftest 套件不做（非-do，维持 20）；主站不做（v2.5 候选） |

## 3. 冻结证明（本版 diff 摘要）

`sh tools/apidump.sh --diff`（默认基线 `docs/API_INVENTORY_v2.3.md` ↔ 当前头文件）：

```
新增     22 项  (只追加 = OK)
修改/删除 0 项  (冻结面破坏 = 红)
apidump: OK —— 纯新增 (22 项, 修改/删除 0)
```

**diff 摘要：新增 22 项，修改 0 项。** 函数 6（`et_modbus_init/feed/response/tick/stats/rx_pending`）；类型 5（`et_modbus_t`、`et_modbus_cfg_t`、`et_modbus_stats_t`、`et_modbus_rd_fn`、`et_modbus_wr_fn`）；宏 10（功能码 4 + 异常码 3 + 数量边界/ADU 3）；模块开关 1（`ET_MODULE_MODBUS`）。

**结构体字段追加登记区：本版零项**——et_modbus 全部为新增类型，未对既冻结结构体追加/改动字段。清单口径：`docs/API_INVENTORY.md` 共 **424 项**（`--diff` 比较单元 417 项 = v2.3 基线 395 + 22）。

## 4. DoD 对照（v2.4 计划 §4）

| DoD | 状态 | 证据 |
|---|---|---|
| 1 沿用 v2.3 全部 DoD | ✅ | docsync **259/259**；432×双几何 + 1K 433 + test-tab 全绿；`make ex` 四例 PASS；docbuild 0 warning；sizecheck 3/3 |
| 2 **示例即证据** | ✅ | `ex_modbus_slave` 覆盖正常+异常全路径（含静默/粘包/分片），字节级断言 + 进 CI `make ex` |
| 3 **真机走单记录** | ✅ | `tools/modbus_master.py` + 实机记录 §11 逐字节请求/应答 + 耗时无关；含 CRC 坏帧静默与 kv 复位保持 |
| 4 数字回刷四处 | ✅ | docsync 计数 / examples 计数（四例）/ README 特性表与结构树 / architecture 选型表与数据流（同批更新，docsync 断言把关） |
| 5 biquad 判定落档 | ✅ | `v3-candidates.md` #8"维持排队"；候选池状态与决议一致（docsync 断言"维持排队"） |

## 5. 真机走单摘要（全记录见实机记录 §11）

```
开机: Embedded_Tools v2.4.0 (0x20400) / modbus rtu slave addr=0x11 (kv regs 1000..1003)
[1] 0x03 读 0..3      tx 11 03 00 00 00 04 46 99   rx 11 03 08 00 01 00 02 00 03 00 04 59 D4 → 1,2,3,4
[2] 0x06 写 reg2=4660 tx 11 06 00 02 12 34 27 ED   rx 回显;  回读 → 4660
[3] 0x10 写 reg4..6   tx 11 10 00 04 00 03 06 ...   rx 11 10 00 04 00 03 C3 59
[4] 异常 0x02(越界)   rx 11 83 02 C1 34
    异常 0x03(qty=0)  rx 11 83 03 00 F4
    异常 0x01(0x63)   rx 11 E3 01 A9 35     ← 与 host 示例期望逐字节一致
[5] CRC 坏帧 → 静默(无应答)      [6] 地址不符 → 分流到 shell(局限已记录)
[7] kv 直通: 写 reg1000=4660 → 软复位 → RAM reg5 回默认 6、kv reg1000 仍 4660 ✓
```

## 6. 里程碑对照

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 | P0 基线滚动 + biquad 判定 + 2.4.0 | ✅ |
| M2 | `et_modbus` 合入（协议 + 23 例 + 异常矩阵） | ✅ |
| M3 | `ex_modbus_slave` + `make ex` + 工具回环自测 | ✅ |
| M4 | 板侧同步 + Modbus 挂载 + 真机走单记录 | ✅ |
| M5 | 全量回归 + 交付文档 | ✅（tag `v2.4` 待发布 checklist 执行） |

## 7. 机制自证记录

| 机制 | 绿向 | 红向（故意破坏） |
|---|---|---|
| 基线滚动 `--diff` | 对 v2.3 基线发版点 0/0；对当前头文件新增 22/0 | 沿 v2.3 记录（改签名 → 1 增 1 删红，exit 1） |
| docsync 自指断言 | 交付文档行首声明"本版 docsync 259/259" → `ok`，`pass=259 fail=0` | 计数手改 → `FAIL [自指] ≠ 实测` → exit 1；改回即绿 |
| 示例/工具双自检 | `make ex` 四例 PASS；`modbus_master.py --selftest` 15 项 PASS | 示例：字节级断言（改一个应答字节即红）；工具：CRC 破坏帧被拒 |
| 跨实现对拍 | C 库组帧 == Python 工具组帧（`11 03 00 00 00 04 46 99`） | ——（一致性由同日两次独立实现互证） |

## 8. 挂账与后续

1. **tag `v2.4` 待执行**：本工作区未提交、未推送；发布 checklist 核对后打 tag，`release.yml` 注入 CHANGELOG 2.4.0 节。Renode smoke 断言行零改动，以发布 CI run 为准。
2. **USART2 独立串口方案**：生产建议（免分流歧义），本版未实现（bench 无第二路串口）；文档已给分流备选与局限说明。若后续需要，记 v2.5 候选。
3. **v2.5 候选池**：Modbus 主站模式（轮询调度协同，单独立项）、定点 biquad（复评触发=具体频响指标）、单向/双向 32 位寄存器组合的库级封装（当前归应用钩子）。四项破坏性候选维持需求触发。
4. **Non-goals 维持**：Modbus TCP/ASCII/主站不做、selftest 维持 20 套件、无浮点/动态内存/RTOS/i2c 抽象/签名启动/通配/多行编辑（连续十四版）。
5. **已知限制（如实挂账）**：共享串口分流下"发往其它从站地址的帧"被分流到 shell（回显），故地址不符静默仅由 host 侧覆盖；板侧该路径需 USART2 独立方案才能实测。

## 9. 量化数据复现（环境注记）

| 项 | 复现命令 | 结果 | 环境 |
|---|---|---|---|
| 单元全量 | `mingw32-make test` / `make test-g4` RESULT 行 | **432×2** 全绿；1K 变体（`-DET_XM_1K=1`）**433**；`make test-tab` 同套件数 | MinGW-w64 gcc **16.1.0** / Windows 11 Git Bash；CI ubuntu make 同绿 |
| 配方示例 | `mingw32-make ex` | 四例 **PASS**（含 ex_modbus_slave 全路径） | 同上 |
| 主站工具自测 | `python tools/modbus_master.py --selftest` | 15 项 **PASS**（CRC 向量/组帧字节序/从站回环/异常/广播/静默） | Python 3（pyserial 仅串口模式需要） |
| 覆盖率 | `mingw32-make coverage-build` + `gcovr --print-summary` | **lines 97.0%（8914 / 9190）**，门 ≥85%；et_modbus 90% | gcovr **8.6** + MinGW gcc 16.1（`--coverage`） |
| docsync | `sh tools/docsync.sh` | **pass=259 fail=0**（含 apidump `--check` 与自指断言） | Git Bash |
| API 清单/冻结 | `sh tools/apidump.sh --check`；`sh tools/apidump.sh --diff` | 清单 424 项（比较单元 417）；**新增 22 / 修改删除 0** | 同上；脚本内锁 `LC_ALL=C` |
| ARM 体积 | `sh tools/docbuild.sh` + `arm-none-eabi-size`；`sh tools/sizecheck.sh` | F103 **21236**(默认)/**30796**(selftest)、G474 **21608**；sizecheck **3/3** | GNU Tools for STM32 **13.3.rel1**（STM32CubeCLT 1.18.0），`-Os -g` 零警告 |
| CubeMX 板侧 | `cmake --preset Release && cmake --build --preset Release`；`STM32_Programmer_CLI` + COM12 | 0 warning；FLASH **37428 B**；SELFTEST 20/20 + SELFSTOR + 升级链 CONFIRMED + Modbus 走单全过 | 同上工具链 + ST-LINK(SWD) + CH343 115200-8-N-1 |
| bench | `make bench` | `modbus 0x03 读 4 寄存器` **115 ns/帧（8.7 M 帧/s）**；既有行无回归 | 同 v1.7 基线注记（Ryzen 9 7945HX / gcc 16.1 / `-O2`） |

---

> 维护约定：本文档随 v2.4 交付定稿；后续范围变更不再回改，走下一版计划。
