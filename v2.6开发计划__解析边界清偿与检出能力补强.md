# v2.6 开发计划：解析边界清偿与检出能力补强

> 起草日期：2026-09-19 ｜ 状态：⏳ 待开发（交付后归档）｜ 版本号 **2.6.0**
> 输入：`v2.5评审记录__Modbus主站补齐与日志规格解析加固.md`（2026-09-19，结论 **返工**，CO-1…CO-14 共 14 条）
> 上一版被评 tree：`e61ccd7`（`v2.5-start`=`b0fc5d8`..`e61ccd7`）；**`v2.5` tag 尚未打**（`gh release list` 最新仍为 v2.4 —— 起草期实测）
> 起草日实测基线：用例 **474**（×2 几何）/ docsync **291/291** / `apidump --diff` **16:0** / 五例配方 PASS / sizecheck 3:3 / 模块 34
> 结构说明：§1–§6 为规约结构；附 A（里程碑）、附 B（风险）为工程惯例补充

---

## 1. 目标与范围

### 1.1 输入说明与 CO 逐条处置

上一版评审记录**存在且可用**（v2.5 计划 P0-4 建立的评审载体首次生效）。记录给出 14 条 CO，并把下一版输入显式分为三段：**A = v2.5-r2 返工轮**（补齐后即可发 tag，无需二次开发）、**B = v2.6 必含项**、**C = v3 窗口**。本计划**全部承接**，逐条处置如下（"暂缓/拒绝"均附触发条件，条件未触发即不消耗版本容量）。

| # | 问题摘要 | 来源 | 处置 | 理由 / 触发条件 |
|---|---|---|---|---|
| **CO-1**(v2.5) | HC-10 未满足：板上逐字节证据缺失（P2-2/P2-3/P2-4） | AC-21/AC-22 | **纳入前置门 G0（v2.5-r2 返工轮）** | 硬件门不可代偿，但返工轮**不含新开发**（库内成果 M1–M3 已被评审判定可接受）。本计划 §3.0 给出可执行清单；若板在 v2.6 窗口内才可用，则 G0 走单与 §3.4 板侧任务**合并为同一次板侧会话**（见 §6 基线规则 3） |
| **CO-2**(v2.5) | 交付文档缺 §8（HC-11 第三落点不存在，AC-2 自指空小节） | HC-11/AC-2/AC-25 | **纳入 G0**（纯文本）；**本版机制化** | G0 补 §8；本计划自身**立即遵守**（§1.1 即 CO 对账节），且 §2 HC-11 把"交付文档必须含 §8 且三处措辞并置对账"定为不合格条件，防复发 |
| **CO-3**(v2.5) | `docs/architecture.md:111` 仍「四例」（本版 4→5） | HC-7 | **纳入 G0**（数字）；**本版装门** | 数字修订属返工轮；根因是**无门守护** → 本版 P0-2 加 docsync 断言（HC-4） |
| **CO-4**(v2.5) | `architecture.md:112`「docsync 210」、`API_GUIDE.md:1406/1419`「398」漏刷且无门 | HC-7 | **纳入 G0**（数字）；**本版装门** | 同 CO-3；另起草期自查发现**同族第四处**：`architecture.md:118`「连续十一版的 Non-goals」（v2.6 为第十六版）—— 一并回刷并入断言 |
| **CO-5**(v2.5) | 交付文档 §4 的 commit/diffstat 滞后自身提交（D-10），无门 | D-10 | **纳入 G0**（回填）；**本版机制化** | G0 按实测回填并写明"§4 记录范围"定义；本版 P0-3 建**本机**自指门（CI 不跑，沿 sizecheck 先例） |
| **CO-6**(v2.5) | **主站定长误判**：应答定长取自 `rx[2]` 字节数域且**先于 CRC 校验**被信任；噪声形如 `[addr][合法读功能码]`（板侧单线半双工下主站自身请求回显即属此形）→ 真应答被判 CRC 坏丢弃，只能靠超时+重发收场；`crc_err` 计数被污染 | 评审探针 | **纳入本版**（P0 ★ 核心） | 起草期读码确认：`protocol/et_modbus_master.c:339` `need = 3u + rx[2] + 2u`，`bc` 与 `2*exp_qty` 的比对在 `:157` 才发生（**帧已按噪声长度切片并过 CRC 之后**）→ 缺陷成立。既有用例 `mbm.garbage_resync` 的噪声是 `{FF,00}`（fc 非法）未覆盖此路径 |
| **CO-7**(v2.5) | **`%c` 的域宽/精度被解析后丢弃**（`[%5c]`→`[A]`，host 为 `[    A]`），而文档承诺是全域「与 C printf 一致」 | 评审探针 | **纳入本版**（补实现 + 用例，不收窄文档） | 读码确认：`debug/et_log.c:443` 的 `case 'c'` 直接 `port_putc`，`sp.width`/`sp.prec` 未用；`emit_pad` 已在位（`:126`）→ 修法成本极低。**拒绝"收窄文档"**：例外表比修实现更易踩（承诺全域时用户不会读例外） |
| **CO-8**(v2.5) | 未知转换字符**不消费**：`%jd`/`%tu`/`%Lf` 走未知分支 → 其后实参仍会错位（"错位"类缺陷的残留口子） | 交付 §5.2 N-2 | **纳入本版** | 纳入"已知不支持→消费"集合（消费 `intmax_t`/`ptrdiff_t`/`long double`），消灭 HC-2 的最后口子；与 CO-7 同批同文件 |
| **CO-9**(v2.5) | `data()` 结果缓冲有效窗口是隐含契约（指向内部 RX 缓冲） | 交付 §5.2 N-1 | **拒绝入库 → v3 窗口** | 改为 `read_into(m,…, dst, cap)` 触碰既有签名，属破坏性窗口（`docs/v3-candidates.md` 首行纪律）；本版为 MINOR，**不触碰**。窗口已在 API_GUIDE 5.8 明示 |
| **CO-10**(v2.5) | selftest 维持 20 → 板侧冒烟对主站与日志零覆盖 | 交付 §5.1 P3-2 | **纳入本版**（P2） | 评审指定"与 CO-1 同批（板侧可复验是扩套件的前提）"；本版有板侧会话（§3.4），扩至 21 并**全链回刷**（HC-12） |
| **CO-11**(v2.5) | USART2 独立串口 / 共享口地址不符静默的板侧验证 | 计划 §1.1（承接自 v2.4） | **再次声明：暂缓** | 评审要求"不得静默消失"，故显式重述：硬件阻塞同前（bench 仅一路 USB-TTL）。触发 = 第二路 USB-TTL 或双板对连；届时单独立项（含 CO-11 原项的板侧验证） |
| **CO-12**(v2.5) | 计划模板三处起草期缺陷：① 快照 tag 与 `release.yml` 的 `v*` 触发面冲突；② 板侧落点写成库内文件；③ 缺"AC↔冻结形状互洽"检查 | 交付 §5.2 N-5/N-6/N-8、D-9 | **纳入本版**（P0-4） | ① 起草期**已实测复现**：run `35378057777`（head=`b0fc5d8`=v2.5-start）→ `workflowName=Release, conclusion=cancelled`，`gh release list` 最新仍 v2.4 → **`v*` 确会触发 Release workflow**（未产出 Release）。本版起内部 tag 一律 `snap-*`；②③ 写入新建的 `docs/开发计划模板.md` 起草期核验清单 |
| **CO-13**(v2.5) | 本机对 ABI/内存类缺陷是盲区（MinGW 无 libasan、clang ASan 运行时失败、`va_list` 形态只在 Linux 报警） | 交付 §5.2 N-4/N-7 | **纳入本版**（采评审的"或"分支：host 零警告门入 CI）+ 环境安装**需用户决策** | 起草期实测本机环境：`wsl -l -v` = **未安装 WSL**、`/c/msys64` 不存在、`clang 17.0.1` 在位但 ASan 运行时不可用（与 N-4 一致）→ 装环境属系统级变更，需用户决策；本版先做**CI 侧硬门**（host 零警告）+ 把"本机检出边界"文档化。触发 = 用户安装 MSYS2/WSL 时补 `test-asan` 本机执行路径（P3-3） |
| **CO-14**(v2.5) | bench 基线无负载控制：同机同日复跑与文档值差幅最大 59%（40.0 vs 60.0 ns/op），逐版数值对比无判别力 | 评审复跑 | **纳入本版**（P3 可选，时间盒 0.5 天） | 二选一：改为相对比值（以稳定基线操作为 1.0）+ 预热/固定迭代，或明确标注"仅供量级参考，不作回归判据"。**若不做的处置**：必须在 `docs/bench.md` 与交付文档显式声明不作判据，并记 v2.7 |

**非 CO 项的处置声明**（评审 §5.2 的 N 清单中未被 CO 覆盖者，防静默丢失）：**N-3**（`et_log` 无浮点支持）**维持 Non-goal**（本计划 §1.3 重述）；**N-9**（既有测试 stack-buffer-overflow，v2.5 已修）**已关闭**，但其"同法复查 port/host 与 tools"的延伸纳入本版 P1-4；**N-1/N-2/N-4/N-5/N-6/N-7/N-8** 分别对应 CO-9/CO-8/CO-13/CO-12/CO-12/CO-13/CO-12。

### 1.2 本版做什么

1. **主站应答解析边界清偿（CO-6，★ 核心）**——定长前先做字节数域合理性校验，噪声不得伪造帧长、不得污染 `crc_err`；补用例 + 板上注入验证。
2. **`et_log` 修饰符承诺与实现对齐（CO-7/CO-8）**——`%c` 的域宽/精度真正生效；`j/t/L` 纳入"已知不支持→消费"，消灭错位残留口子。
3. **机制门补强（CO-3/CO-4/CO-5 的机制化 + CO-12 模板）**——把三处漏刷点变成**跨文档一致性断言**；交付文档 §4 自指加本机门；建立 `docs/开发计划模板.md` 与起草期核验清单。
4. **本机检出能力补强（CO-13）**——CI 侧 host 零警告硬门；`make test-asan` 常设并覆盖 1K 变体；"本机检出边界"文档化。
5. **板侧收口（CO-1 前置门 + CO-6 板上验证 + CO-10 selftest 21）**——一次板侧会话完成 v2.5 走单与 v2.6 新项验证；v2.5 tag 在**未含 v2.6 代码改动**的提交上定着。

### 1.3 明确不做什么（Non-goals）

- **不做** `data()` → `read_into()` 形态变更（CO-9 拒绝入库，属 v3 破坏性窗口）；
- **不做**任何触碰既有签名的变更（`apidump --diff` 必须纯增；本版预期**新增 0 项 / 修改删除 0 项**）；
- **不做** `et_log` 浮点支持（N-3 维持 Non-goal：`%f` 族仍为"消费 + 可见占位"）；
- **不做** USART2 独立串口（CO-11 再次暂缓，硬件阻塞）；
- **不做** Modbus TCP/ASCII、主站轮询调度器与多从站表（v2.5 既定边界维持）；
- **不做** biquad、32 位寄存器库级封装、四项破坏性候选（v2.5 CO-4/CO-5/CO-6 结论维持）；
- **不做**本机 ASan 环境安装（需用户决策；本版只做 CI 侧硬门 + 边界文档化）；
- **不做**动态内存 / RTOS / 内部取时基 / i2c 抽象 / 签名启动 / 通配 / 多行编辑（连续十六版永久边界）；
- **不做**新模块（本版无新增模块，模块数维持 34）。

---

## 2. 硬性约束（违反任一条即交付不合格）

- **HC-1 契约零破坏**：`sh tools/apidump.sh --diff` 对 v2.5 基线必须"修改删除 0 项"；本版预期**新增 0 项**（若确有新增，须逐项说明理由且仍为纯增）；既有 API 语义与既有宏值不变。
- **HC-2 主站不得信任噪声字节**：应答帧长在**定长前**须经字节数域合理性校验（与 `2*exp_qty` 一致，或落在 `≤ 2*ET_MODBUS_RD_QTY_MAX` 的合法域内）；不符即逐字节重同步。`crc_err` 只统计**构成帧**的 CRC 失败，不得计入从未成帧的字节。
- **HC-3 修饰符承诺与实现一致**：文档承诺的规格集合内，域宽/精度必须**实际生效**（含 `%c`）；`j/t/L` 等仍不支持的修饰必须**消费实参**并输出可见占位。文档承诺与实现的差集必须为空，或由 `et_log.h` 头注 + API_GUIDE 显式列出**且有用例覆盖**。
- **HC-4 漏刷点必须有门**：`architecture.md` 的机制门行（examples 数 / docsync 断言数）与 `API_GUIDE` 的用例数必须由 `tools/docsync.sh` 断言守护，且断言**与 README/实测计数交叉一致**（非各自写死）；门须自证可红（故意改一处数字 → 脚本红）。
- **HC-5 §4 自指有门**：交付文档 §4 必须写明"记录范围 = `snap-2.6-start`..`<最后一次代码提交>`"，其 commit 与 diffstat 须与实测一致，并由**本机**门（CI 不跑，沿 sizecheck 先例）核验。
- **HC-6 库不变式**：零动态内存、多实例句柄化、`core/`+`algorithm/` 零硬件依赖、不内部取时基、不新增模块开关、不追加/改动既有冻结结构体字段。
- **HC-7 数字回刷同批**：用例数、examples 数、docsync 计数、模块数、README（特性表/结构树/质量门）、`architecture.md`、`API_GUIDE`、CHANGELOG、`port/*/README` 体积表（sizecheck 逐值）必须与代码同批更新。
- **HC-8 板侧落点必须写明库外路径**：板侧任何改动一律标注为**板侧工程私有文件（库外，不在本仓 git）**，路径写全（`D:\code\STM32CubeMX\G474VET6_ET_TEST\Core\Src\et_demo.c`、同步目录 `...\Core\et\`），禁止写成库内 `examples/`。
- **HC-9 真机证据与 tag 完整性**：v2.5 的走单证据必须在**不含 v2.6 库内改动**的提交上取得（否则 `tag v2.5` 的证据不自洽）；v2.6 新项（CO-6 修复、selftest 21）的板上证据须单独取得。编译通过不算验收。
- **HC-10 内存门**：`make test-asan` 常设并**覆盖 1K 变体**；本机不可执行须在文档显式声明为已知边界，**不得声称本机已跑**（沿 v2.5 如实性纪律）。
- **HC-11 CO 一致性三落点**：本计划 §1.1 / `docs/v3-candidates.md` / **交付文档 §8** 三处 CO 措辞必须一致；交付文档缺 §8 即不合格（v2.5 正因此不通过）。
- **HC-12 selftest 扩套件全链**：若由 20 扩至 21，`smoke.sh` 断言行、README、API_GUIDE 8.4、实机记录、体积表须同批，且**板上复跑**结果可比对；不扩则须显式声明维持 20 并给理由。

---

## 3. 任务分解

### 3.0 前置门 G0：v2.5-r2 返工轮（无新开发，约 1.5 天）

> 交付物命名带 `-r2` 后缀：`v2.5开发交付__Modbus主站补齐与日志规格解析加固-r2.md`（评审 §0 约定）。G0 完成且 HC-10 满足后**立即打 `tag v2.5`**。

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| G0-1 | **板侧走单补齐（CO-1）**：板侧私有 `Core/Src/et_demo.c` 增主站命令组（`AT+MBRD`/`AT+MBWR`/`AT+MBPOLL`，执行期间抑制 shell 回显与提示符）；与 PC 侧 `tools/modbus_master.py --slave/--slave-drop N/--slave-exc 0x02/--slave-regs` 走单；取得逐字节记录（请求/应答/重发序列/异常码/广播写/`et_log` 域宽行原样） | 板侧工程私有（库外）：`...\G474VET6_ET_TEST\Core\Src\et_demo.c`；`移植stm32实机记录.md`（新增 §12）；工具 `tools/modbus_master.py`（消费方，库内只读） | 改板侧共享 USART1 分流路径 → **必须同会话复跑 v2.4 的从站分流走单项**（`--read/--write` 等）作为"未改坏已验证路径"的证据（评审 D-1a 的关注点）；库内零改动 | P0 |
| G0-2 | **交付文档 §8 新增（CO-2）**：把计划 §1.1 / `v3-candidates.md` / 交付文档三处 CO 措辞并置对账；修正 AC-2 指向空小节的证据 | `v2.5开发交付__...-r2.md`（§8 新增） | 纯文本；HC-11 第三落点补齐 | P0 |
| G0-3 | **三处（+1）数字回刷（CO-3/CO-4）**：`architecture.md` 机制门行「四例」→「五例」、「docsync 210 断言」→ 实测值、「连续十一版」→ 实际版序；`API_GUIDE.md:1406/1419`「398」→ 实测用例数 | `docs/architecture.md`、`docs/API_GUIDE.md` | 纯文本；**根因（无门）由本版 P0-2 装门根治** | P0 |
| G0-4 | **§4 回填（CO-5）**：按实测 `HEAD` 与 `git diff --stat` 回填 commit 清单与数字，并写明"§4 记录范围"定义 | `v2.5开发交付__...-r2.md`（§4） | 纯文本；机制化由本版 P0-3 承接 | P0 |
| G0-5 | **打 `tag v2.5` 并核验发布**：确认 G0-1 证据齐备后打 tag（触发 `release.yml` 出 Release 四附件）；核验 Release 正文与附件 | git tag / `gh release list` / `gh run list` | 发布动作；**tag 指向的 commit 不得含 v2.6 库内改动**（HC-9） | P0 |
| G0-6 | 诊断性注入（为 v2.6 取证，**不改代码**）：在板上用主站自身请求回显形态的噪声喂入，记录现象（预期退化为超时 + `crc_err` 污染） | `移植stm32实机记录.md`（§12 内小节） | 只记录现象；修复归本版 P1-1 | P1 |

### 3.1 P0 治理常规与机制门（约 1 天）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P0-1 | apidump 基线滚动：归档 `docs/API_INVENTORY_v2.5.md`，`--diff` 默认基线切至 v2.5（依赖 G0-5 打 tag；若未完成则延后补做并记录在案） | `docs/API_INVENTORY_v2.5.md`（新增）、`tools/apidump.sh`（默认基线变量）、`docs/API_STABILITY.md`（§5 基线规则行） | 只动治理脚本与归档，不改头文件；v2.3/v2.4 归档只读保留 | P0 |
| P0-2 | **三处漏刷点装门（CO-3/CO-4 机制化，HC-4）**：`tools/docsync.sh` 增"跨文档计数一致性"断言——从 README 提取用例数/examples 数，断言 `architecture.md` 机制门行与 `API_GUIDE` 的对应数字**与之相等**（而非各自写死）；docsync 断言数对 `architecture.md` 机制门行做自指断言（沿 v2.1 自指先例） | `tools/docsync.sh`、`docs/architecture.md`、`docs/API_GUIDE.md`、`README.md` | 断言数增加 → docsync 自指计数全链回刷（HC-7）；**门须自证可红**（改一处数字即红） | P0 |
| P0-3 | **§4 自指门（CO-5 机制化，HC-5）**：新增本机发布前门脚本（或并入 sizecheck 同族），核验交付文档 §4 的 commit 与 diffstat = 实测（`git diff --stat <snap-tag>..<last-code-commit>`）；CI 不跑（checkout 无 tag 历史，沿 sizecheck "CI 只上传读数"先例） | `tools/docref.sh`（新增，或并入 `tools/sizecheck.sh` 同族）、`README.md`（checklist）、`v2.5开发交付__...-r2.md`（§4 范围定义引用） | 纯治理；避免 v2.5 D-10 的"自指滞后"复发 | P1 |
| P0-4 | **计划模板落地（CO-12）**：新建 `docs/开发计划模板.md`——六节骨架 + **起草期核验清单四项**：① 内部 tag 一律 `snap-<ver>-start`（不带 `v` 前缀，附触发面实测要求）；② 任何"无副作用/不会触发"类断言**起草期必须实测**（给命令）；③ 板侧落点必须写"库外私有文件"全路径；④ **AC ↔ 冻结 API 形状互洽检查**（逐条核对 AC 断言与 §3 形状是否可同时成立，v2.5 的 `result()` 单值 vs "逐值相等"即反例） | `docs/开发计划模板.md`（新增）、`README.md`（发布 checklist 增一行指向计划模板）、`docs/评审记录模板.md`（核验清单补"AC↔形状互洽"项） | 纯治理；本计划已按四项起草（见文末"起草期合规声明"） | P0 |
| P0-5 | 版本宏升 2.6.0 + 数字回刷启动 | `et_config.h`、`CHANGELOG.md` | 触发体积表新增 v2.6 行（sizecheck 逐值门） | P0 |

### 3.2 P0 库内缺陷清偿（★ 核心，约 1.5 天）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P1-1 | **主站定长合理性校验（CO-6）**：读应答在定长前校验字节数域——与 `2*exp_qty` 相符（读路径可完全以 `exp_qty` 定长，不信任 `rx[2]`）或落在 `≤ 2*ET_MODBUS_RD_QTY_MAX` 合法域；不符即 `drop_head` 重同步；`crc_err` 只计构成帧者 | `protocol/et_modbus_master.c`（`:327-366` 解析循环）、`protocol/et_modbus_master.h`（若需注明语义） | 内部逻辑修复，**API 零变化**（HC-1）；行为变化：噪声形态下由"超时+重发"变为"重同步后正确解析"；`crc_err` 语义收紧 | P0 |
| P1-2 | 主站新增用例：噪声形如 `[addr][合法读fc]+垃圾` 后跟真应答 → 真应答被正确解析；`crc_err` 不因未成帧字节增长；重同步后 `exp_qty` 边界（125/126）与 `0x10` 写路径不回退；既有 25 例全绿 | `test/test_modbus_master.c` | 用例数 +≥6；docsync/README 计数回刷（HC-7） | P0 |
| P1-3 | **`%c` 域宽/精度生效（CO-7）**：`case 'c'` 复用既有 `emit_pad` 路径，域宽/左对齐/精度语义与 C 一致 | `debug/et_log.c`（`:443` 附近）、`debug/et_log.h`（头注更新） | 内部修复；与 host `snprintf` 对拍断言（AC-6 手法） | P0 |
| P1-4 | **`j/t/L` 纳入"已知不支持→消费"（CO-8）**：消费 `intmax_t`/`ptrdiff_t`/`long double` 并输出可见占位；未知转换字符例外在头注与 API_GUIDE 保持明示 | `debug/et_log.c`（长度修饰解析 + 未知分支）、`debug/et_log.h`、`docs/API_GUIDE.md`（8.1 规格表） | 内部修复；消灭 HC-2 最后口子；文档规格表需与实现差集一致（HC-3） | P0 |
| P1-5 | `et_log` 新增用例：`%c` 域宽/精度/左对齐对拍；`j/t/L` 占位 + 后随哨兵实参不错位；负向断言无字面回显 | `test/test_log.c` | 用例数 +≥6；`debug/log` 套件由 24 增至 ≥30 | P0 |
| P1-6 | **ASan 门常设化 + 覆盖 1K 变体（N-9 延伸）**：`make test-asan` 增 1K 变体目标并纳入 CI；用同法复查 `port/host` 与 `tools/`（bench、xmodem_host_recv）是否有同类越界 | `Makefile`、`.github/workflows/ci.yml`、`port/host/port_host.c`、`tools/*.c` | CI job 数不变（步骤内追加）；体积表与 CI 步骤清单需回刷文档 | P1 |
| P1-7 | 文档与数字回刷：API_GUIDE 8.1 规格表、CHANGELOG 2.6.0 节、`port/*/README` v2.6 体积行、README 用例数 | `docs/API_GUIDE.md`、`CHANGELOG.md`、`port/*/README.md`、`README.md` | sizecheck 逐值门（HC-7）；docsync 断言同批 | P0 |

### 3.3 P1 本机检出能力（CO-13，约 0.5 天）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P2-1 | **host 零警告硬门**：CI 的 host 构建（Linux `make test`/`make ex` 与 Windows 纯 gcc 步骤）加 `-Werror`（或在 Makefile 增 `WERROR=1` 变体目标供 CI 调用）；首跑若红则修至零警告，并把抓到的问题条数记入交付文档 | `Makefile`（变体目标）、`.github/workflows/ci.yml`（Linux + Windows job） | 把"仅 ARM 交叉编译有零警告门"扩到 host 侧；若暴露既有告警 → 修复属本版范围（可裁量：允许以 `-Wno-*` 逐条豁免但须记理由） | P1 |
| P2-2 | **"本机检出边界"文档化**：把本机对 ASan/ABI 类缺陷的盲区、CI 侧补偿门清单、触发条件（安装 MSYS2/WSL 时补本机执行路径）写入文档固定位置 | `docs/getting-started.md` 或 `README.md`（新增"质量门与检出边界"小节）、`docs/评审记录模板.md`（核验方法节引用） | 纯文档；起草期实测结论（WSL 未装 / MSYS2 无 / clang ASan 运行时不可用）作为事实基础，避免下版重复踩 | P1 |

### 3.4 P2 板侧收口（CO-6 板上验证 + CO-10 selftest 21，约 1.5 天）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P2-3 | 板侧同步 v2.5→v2.6：`Core/et/` 全量重拷 + `diff -rq` 校验 + 0 warning + FLASH/RAM 读数 | 板侧工程私有（库外）：`...\G474VET6_ET_TEST\Core\et\**`；`移植stm32实机记录.md` | 库内零回归（`--gc-sections` 丢弃未调用模块）；体积增量记档 | P0 |
| P2-4 | **CO-6 板上验证**：修复后复跑 G0-6 的注入形态 → 由"超时+重发"变为"重同步后正确解析"；记录统计（`crc_err`/重发次数）对照 | `移植stm32实机记录.md`（新增 §13）；板侧私有 `Core/Src/et_demo.c`（注入命令） | HC-9 的落点；这是 CO-6 唯一"只有板上能证伪"的证据 | P0 |
| P2-5 | **selftest 20→21（CO-10）**：新增 modbus（从站+主站纯逻辑冒烟）+ `et_log` 修饰符冒烟套件；**全链回刷**：`smoke.sh` 断言行、README、API_GUIDE 8.4、实机记录、体积表 | `debug/et_selftest.c`、`tools/smoke.sh`、`README.md`、`docs/API_GUIDE.md`、`移植stm32实机记录.md`、`port/*/README.md` | 回刷面最大项（HC-12）；板上 `AT+SELFTEST` 21/21 为验收证据 | P1 |
| P2-6 | 既有回归：`AT+SELFTEST`/`AT+SELFSTOR`/`AT+SIMUPGRADE 3` CONFIRMED；v2.4 从站分流走单项复跑（证明 G0-1 的板侧改动未破坏已验证路径） | `移植stm32实机记录.md` | 回归不破是发布前提 | P0 |

### 3.5 P3 可选（时间盒各 0.5 天，不阻塞发布）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P3-1 | **bench 可判别化（CO-14）**：改为相对比值（以稳定基线操作为 1.0）+ 预热与固定迭代；或明确标注"仅供量级参考，不作回归判据"（二选一，不做的处置见 §1.1 CO-14 行） | `tools/bench.c`、`docs/bench.md` | 影响历史数值可比性 → 文档须写明口径变更 | P2 |
| P3-2 | **CO-11 重新声明落地**：在交付文档 §8 与 `v3-candidates.md` 重述"USART2 独立口 / 共享口地址不符静默板侧验证"维持暂缓 + 触发条件 | `docs/v3-candidates.md`、交付文档 §8 | 纯文本；满足评审"不得静默消失"的要求 | P2 |
| P3-3 | 本机 ASan 执行路径（**触发 = 用户安装 MSYS2 或 WSL**）：补 `make test-asan` 的本机执行说明与验证记录 | `Makefile`、`docs/getting-started.md` | 未触发则不做；触发时属增量，不改变 CI 侧门 | P2 |

---

## 4. 验收标准（每条可客观核验，标注对应任务）

**前置门 G0**

- **AC-1**（G0-1）`移植stm32实机记录.md` §12 含逐字节记录：主站请求/应答字节、`--slave-drop` 触发的**重发序列字节**、异常码应答、广播写、`et_log` 域宽行原样输出（`%04u-%02u-%02u %02u:%02u:%02u`）；且同节含 v2.4 从站分流走单的复跑记录（证明板侧改动未破坏既有路径）。
- **AC-2**（G0-2/G0-4）`v2.5开发交付__...-r2.md` 含 §8，其中并置列出计划 §1.1 / `docs/v3-candidates.md` / 交付文档三处的 CO 措辞（逐条可比对）；§4 的 commit 与 diffstat 与 `git diff --stat v2.5-start..<该文档所述 commit>` 实测一致。
- **AC-3**（G0-3）`grep -n "四例\|docsync 210\|连续十一版" docs/architecture.md` 命中数为 0；`grep -c "398" docs/API_GUIDE.md` 的命中处不再是"PC 单测(398)/host 398 用例"（改为实测用例数）。
- **AC-4**（G0-5）`git tag -l v2.5` 有输出；`gh release list` 显示 v2.5 为 Latest 且含四个附件；`git show v2.5:protocol/et_modbus_master.c` 中**不含**本版 CO-6 的修复（HC-9 证据完整性）。

**机制门与治理**

- **AC-5**（P0-1）`sh tools/apidump.sh --diff` 在发版点输出"修改/删除 0 项"且退出码 0；`docs/API_INVENTORY_v2.5.md` 存在且 v2.3/v2.4 归档未被改动（`git diff --stat` 无它们）。
- **AC-6**（P0-2，HC-4）`tools/docsync.sh` 断言数 ≥ 291 + 新增条数，`pass=N fail=0`；**门自证可红**：故意把 `docs/architecture.md` 的 examples 数改成"四例" → docsync 退出码非 0，改回即绿（证据记入交付文档 §7 机制自证表）。
- **AC-7**（P0-3，HC-5）本机门脚本对交付文档 §4 的核验通过；**门自证可红**：把 §4 的 diffstat 数字改 1 → 门红。
- **AC-8**（P0-4）`docs/开发计划模板.md` 存在且含起草期核验清单四项（快照 tag 命名 / 无副作用断言须实测 / 板侧落点库外全路径 / AC↔冻结形状互洽）；`README.md` 发布 checklist 含指向它的条目；`docs/评审记录模板.md` 核验清单含"AC↔形状互洽"项。
- **AC-9**（P0-5）`grep ET_VERSION et_config.h` 得 `0x020600` 与 `"2.6.0"`；板上横幅与 `AT+VER` 显示 2.6.0（P2-3 走单证据内）。

**库内缺陷**

- **AC-10**（P1-1/P1-2，HC-2）新增用例断言：先喂 `[addr][0x03]` + 垃圾字节（含"自身请求回显"形态），再喂**真应答** → `feed()` 返回 1、读回值逐值正确；且该路径下 `stats.crc_err` **不增长**（对照 v2.5 行为：返回 0 / 保持 BUSY / 仅超时收场）。核验命令：`mingw32-make test` 中 `proto/modbus_master` 套件 pass ≥ 31，fail=0。
- **AC-11**（P1-1）重同步后既有语义不回退：`0x10` 写路径、qty 边界（125/126、123/124）、异常应答不重试、广播写不等应答、迟到字节不污染下一事务**全部既有用例仍绿**。
- **AC-12**（P1-3/P1-5）`%5c`/`%-3c`/`%3.1c` 输出与 host `snprintf` **逐字节相等**（同一用例内对拍）；`debug/log` 套件 pass ≥ 30，fail=0。
- **AC-13**（P1-4/P1-5，HC-3）`%jd`/`%tu`/`%Lf` 后随哨兵实参取值正确（不错位）且输出为文档固定的可见占位；负向断言：输出中无字面 `%j`/`%t`/`%L` 残留。
- **AC-14**（P1-3/P1-4）`et_log.h` 头注与 `docs/API_GUIDE.md` 8.1 的规格表所列"支持集合/不支持集合"与实现**差集为空**（逐项比对；以 P1-5 用例逐条覆盖为证）。
- **AC-15**（P1-6，HC-10）`make test-asan` 与新增的 1K 变体 ASan 目标在 CI 中执行且绿（`gh run view` 逐步骤核验）；交付文档声明"本机 ASan 不可执行"并给出 CI 侧补偿门清单，且未声称本机已跑。
- **AC-16**（P2-1）CI 的 host 构建步骤含 `-Werror`（Linux 与 Windows 两个 job 均可核验），且该步骤绿；若修复了既有告警，交付文档记录条数与内容。

**板侧与回归**

- **AC-17**（P2-3/P2-4）`移植stm32实机记录.md` §13 含：`Core/et` 同步 `diff -rq` 全 OK、0 warning、FLASH/RAM 读数；CO-6 注入形态修复前后对照（重发次数/`crc_err` 变化）。
- **AC-18**（P2-5，HC-12）板上 `AT+SELFTEST` **21/21 PASS**；`tools/smoke.sh` 断言行、README、API_GUIDE 8.4、实机记录、`port/*/README` 体积表五处同批回刷（`sh tools/docsync.sh` 全绿为证）。
- **AC-19**（P2-6）`AT+SELFSTOR` PASS、`AT+SIMUPGRADE 3` → CONFIRMED；v2.4 从站分流走单复跑逐字节记录在位。
- **AC-20**（全版）`sh tools/docsync.sh` `pass=N fail=0`，且交付文档首行声明的计数 = 实测（自指断言）。
- **AC-21**（全版）`mingw32-make test`、`test-g4`、`test-tab`、1K 变体全绿；`make ex` 五例 PASS；`gcovr` 行覆盖 ≥85%（记实测值）；`sh tools/sizecheck.sh` `pass=3 fail=0` 且 v2.6 表行逐值相等；CI 七 job 全绿（含 renode-smoke）。
- **AC-22**（P3-1/P3-2/P3-3）P3 三项：做了的给出证据（bench 口径变更说明 + 新旧数值对照，或"不作判据"声明；CO-11 重述落在 §8 与 `v3-candidates.md`；本机 ASan 路径仅当环境安装触发）；未做的在交付文档 §8 显式声明"不做 + 理由"并记 v2.7（禁静默略过）。

**AC ↔ 冻结形状互洽检查（CO-12 第④项，起草期已完成）**：本版**不新增/不修改任何公开 API 形状**（P1-1/P1-3/P1-4 均为内部实现修复），故 AC-10…AC-14 的断言对象（既有函数返回值、统计字段、日志输出字节）与冻结形状**无冲突面**；唯一需注意者为 AC-10 的 `stats.crc_err` 语义收紧（由"计入未成帧字节"改为"只计构成帧者"）—— 该字段为既有公开结构体成员，**语义收紧属行为修正而非签名变更**，须在交付文档 §2 登记区显式说明（HC-1 的语义面）。

---

## 5. 可裁量项（开发者可自主决策，其余不得偏离）

1. P1-1 的具体校验判据（读路径"完全以 `exp_qty` 定长"或"`rx[2]` 合理性区间校验"二选一，或两者并用）——前提：噪声形态不得伪造帧长、`crc_err` 语义符合 HC-2。
2. P0-2 断言写法（跨文档提取比对 vs 逐处字面断言）与断言措辞。
3. P0-3 门脚本的归属（独立 `tools/docref.sh` vs 并入 `tools/sizecheck.sh` 同族）与调用时机（发布前手动 / `make` 目标）。
4. P2-1 的 `-Werror` 施加方式（Makefile 变体目标 vs CI 内联 flags）与逐条豁免（`-Wno-*`）的取舍，豁免须记理由。
5. P1-6 中 ASan 1K 变体与 `test-tab` 形态的覆盖取舍（至少覆盖 1K 变体）。
6. P2-5 的 selftest 新套件内容粒度（modbus 从站+主站是否合为一套件）与命名。
7. 板侧命令名（`AT+MBRD`/`AT+MBWR`/`AT+MBPOLL` 之类）与回显抑制的实现细节。
8. P3-1 bench 的形态选择（相对比值 vs 明示不作判据）。
9. 文档章节位置与措辞（"本机检出边界"放 getting-started 还是 README；`docs/开发计划模板.md` 的排布）。
10. G0 与 §3.1–3.3 的**执行顺序**：若板在 v2.6 窗口内不可用，允许库内项（§3.1–3.3）先行于 `dev/v2.6` 分支，板侧会话到达后再按 §6 基线规则 3 合流。

**不可偏离**：§1.1 的 CO 处置结论（尤其 CO-9 拒绝入库、CO-11 暂缓、CO-7 选"补实现"而非"收窄文档"）、HC-1…HC-12 全部、v2.5 tag 的证据完整性规则（HC-9）、板侧落点的库外标注（HC-8）。

---

## 6. 基线

- **首选基线**：**`tag v2.5`**（= G0 完成后的 commit，含 v2.5 全部成果与返工文本修订）。**该 tag 当前不存在**（起草期实测：`git tag -l` 仅至 v2.4 + `v2.5-start`；`gh release list` 最新 v2.4）。
- **当前可用起点**：`e61ccd7`（`origin/main`，v2.5 库内成果终态）+ G0 的文本修订。
- **动手前必须先打快照**（先于任何代码改动）：

  ```sh
  git tag snap-2.6-start <起点 commit>        # 内部 tag 一律不带 v 前缀
  git push origin snap-2.6-start
  ```

  **为什么用 `snap-` 而非 `v2.6-start`（起草期实测，非推断）**：`v*` 会命中 `release.yml` 的 tag 触发面 —— run `35378057777`（head=`b0fc5d8`= `v2.5-start`）的 `workflowName=Release`、`conclusion=cancelled`，`gh release list` 最新仍为 v2.4（**未产出 Release，但 workflow 确被触发**）。`snap-*` 不匹配 `v*`，故无该副作用；**快照后仍须复核**：`gh run list --limit 3` 无新 run、`gh release list` 无新增。
- **分支规则**：v2.6 库内改动在 `dev/v2.6` 分支上进行；**`tag v2.5` 必须在 v2.6 库内改动合入 `main` 之前定着**（HC-9：v2.5 的走单证据须取自不含 v2.6 代码的树）。
- **顺序规则（板不可用时的合法路径）**：① G0 文本修订（G0-2/3/4）先做；② 库内项在 `dev/v2.6` 推进；③ 板到达后，在 `main`（= e61ccd7 + G0 文本修订）上完成 G0-1 走单 → 打 `tag v2.5` → 再把 `dev/v2.6` 合入 `main` → 做 §3.4 的 v2.6 板上项。若开发启动时 `v2.5` tag 已存在，则基线直接取该 tag，跳过 ③ 的前半段。
- **基线偏离申报**：若实际起点与上述不符，须在交付文档首行声明实际基线 commit；本计划一经交付即冻结，不回改。

---

## 附 A：里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 |
|---|---|---|
| G0 | v2.5-r2 返工轮：文本修订 → 板侧走单 → **tag v2.5** | D+1.5 |
| M1 | P0-4 计划模板 + P0-1 基线滚动 + 2.6.0 + P0-2/P0-3 两门 | D+2.5 |
| M2 | CO-6/CO-7/CO-8 修复 + 用例 + ASan 1K 门 + 数字回刷 | D+4.0 |
| M3 | host 零警告门 + 检出边界文档化 | D+4.5 |
| M4 | 板侧同步 + CO-6 板上验证 + selftest 21 + 既有回归 | D+6.0 |
| M5 | 全量回归（三脚本 + CI）→ 交付文档（含 §8）→ **tag v2.6** | D+6.5 |

## 附 B：风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| CO-6 的修复改变既有用例语义（`crc_err` 收紧） | 既有 25 例可能红 | AC-11 明确"既有语义不回退"；若必须改断言，须在交付文档登记为**行为修正**并说明理由（HC-1 语义面） |
| `-Werror` 引入后暴露大量既有告警 | M3 超时盒 | 允许逐条 `-Wno-*` 豁免但须记理由；豁免清单入交付文档，禁止整体关掉该门 |
| 板侧改动破坏 v2.4 已验证的分流路径 | 已验证路径回归且不易察觉 | G0-1 与 P2-6 均要求**同会话复跑从站分流走单项**并留逐字节记录；改动限于"MBPOLL 执行期间抑制回显" |
| v2.5 tag 与 v2.6 代码顺序错乱 | tag 证据不自洽（AC-4 直接红） | §6 分支/顺序规则 + AC-4 的 `git show v2.5:...` 核验 |
| selftest 21 的回刷面（5 处文档 + 体积表）漏刷 | CI 红 / HC-12 不满足 | 交付前以 `docsync.sh` + `sizecheck.sh` + `apidump.sh --check` 三脚本为门；smoke 断言行同批 |
| docsync 新增断言与自指计数互为前提（改断言数即改自指数字） | 门自身难落地 | 沿 v2.1 自指断言既有模式：先加断言 → 跑一次取实测计数 → 回刷文档数字（该模式已在 v2.1/v2.5 两次验证） |
| 本机 ASan 仍不可用（环境安装需用户决策） | 内存类缺陷本机仍盲 | 本版采"CI 侧硬门"分支（CO-13 的"或"）；边界文档化 + 触发条件写明；**不得声称本机已跑**（HC-10） |
| 板档期 | G0/P2 受阻 | G0 文本项与库内项先推进（§6 顺序规则）；走单项如实挂账，不伪造结论（沿 v2.5 正确取舍） |
| bench 口径变更使历史数值不可比 | 文档误导 | P3-1 须同时给"口径变更说明 + 新旧数值对照"，或明示不作判据 |

> 环境注记（沿既有记录）：Windows 侧 `make` → `mingw32-make`；`apidump.sh` 锁 `LC_ALL=C`；本机 `wsl` 未安装、`/c/msys64` 不存在、`clang 17.0.1` 的 ASan 运行时不可用（起草期实测）；体积读数以本机 CubeCLT 工具链为准并附环境注记。

---

> **起草期合规声明（CO-12 四项自检）**：① 快照 tag 命名已改 `snap-2.6-start`，且"`v*` 触发 Release"已**实测**（run `35378057777`）而非推断；② 本计划不含任何未实测的"无副作用/不会触发"类断言；③ 板侧落点一律标注库外私有文件全路径（HC-8）；④ AC↔冻结形状互洽检查已执行，结论见 §4 末段。
>
> 维护约定：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v2.6 后本文档归档不再改动。
