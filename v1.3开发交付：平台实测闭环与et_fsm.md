# v1.3 开发交付：平台实测闭环与 et_fsm

> 交付日期：2026-09-03 ｜ 版本：v1.3.0（`ET_VERSION = 0x010300`）｜ 基线：v1.2（`b2ab3cc`）
> 计划文档：[v1.3 开发计划：平台实测闭环与 et_fsm](./v1.3开发计划：平台实测闭环与et_fsm.md)（状态：✅ 已交付）

## 1. 重点概况（TL;DR）

v1.3 是清账版本，把悬了两版的"真机可用"从**声明变成证据**，并交付表驱动状态机 `et_fsm`：

- **Renode 仿真闭环（本版最大价值）**：本机 Renode 1.16.1 headless 跑通 F103 demo 全功能，串口日志断言全绿（横幅 / `kv: seq=…` / UTC 心跳 / `boot #1` / **复位后 `boot #2`**）——并**当场揪出 v1.2 遗留的 F103 flash 驱动严重 bug**（详见 §2 与 §5）：宿主机 191 用例全绿未能暴露，仿真首跑即现形。这正是计划 §7"host 模拟与真实 flash 行为偏差"风险的实证，也宣告 P1 实测闭环不是形式工程。
- **CI 常设门**：`renode-smoke` job 进驻 ci.yml（Renode 固定 1.16.1，失败保留 uart 日志 artifact）；`release.yml` 拆为 verify（全量单测 + 仿真 smoke）→ release 两段，**验证门不过不出版本**。仿真验收从"本地截图"升级为永久闭环。
- **新模块 et_fsm**（algorithm/，第 19 模块）：const 迁移表可驻 flash、guard 回退链、零分配；15 例单测全绿，行覆盖 100%，头文件附 ASCII 状态图。
- **P0 一致性收口**：修复"v1.2 交付声称 README 已同步而实际未同步"的脱节（本版 README 补齐后逐条 grep 验收），并落地**开发者发布 checklist**（每项"已完成"声明必须附复现命令）防再犯。
- **真机政策切换（P1-3）**：Renode CI 门作为 F103 功能验收线；真机记录转**常设挂账**（待硬件，不阻塞发布），不再随版本顺延。

## 2. 逐项验收对照

### P0 一致性收口（M1，`d15ae56`）

| # | 任务 | 验收结果 |
|---|---|---|
| 1 | README 补齐 v1.2 实际状态 | ✅ `grep et_kv / et_softclock / storage / 1.2.0` 命中 13 处；版本行 v1.2.0 与 tag 一致（现随版本演进至 v1.3.0） |
| 2 | 发布 checklist | ✅ 落于 README"开发者发布 checklist"小节：文档逐文件核对 / 版本钉三方一致 / 量化声明附复现命令 / 本地回归 / 交付文档命名 |
| 3 | 版本宏 1.3.0 + 删 exp/kv | ✅ `ET_VERSION=0x010300`；`git branch -d exp/kv` 完成（其使命已全部合入 main） |

### P1 平台实测闭环（M3，`3089ea6` + `0516742`）

**本机实测**（Windows，Renode 1.16.1 portable）：headless 运行 `build/stm32f103_demo.elf`，关键日志（smoke 断言 5/5 通过）：

```
[0][I][demo]   Embedded_Tools v1.3.0 (0x10300)
[3][I][demo]   kv: seq=1 free=996 rec=1 key=1
[2004][I][demo] alive 2004 ms | boot #1 | 2026-01-01 00:00:02 (UTC)
[2050][I][demo] alive 2050 ms | boot #2 | 2026-01-01 00:04:32 (UTC)   ← cpu Reset 后
```

复位后 `boot #2` = kv 掉电持久化的功能证据；时间 00:04:32 = kv 保存的 UTC 秒 + tick 续走。

**仿真资产**（`port/stm32f103/renode/`）：`smoke.sh`（本机/CI 共用，5 组断言）+ `flash_erased_64k.bin`（0xFF 擦除态填充）。关键技术点：

| 项 | 处理 |
|---|---|
| F103 flash 控制器无寄存器模型 | `sysbus Tag <0x40022000,0x40022013> "FLASH" 0x0`（读 0 = BSY 恒闲/无错误位），存储体可写 |
| 有界运行 / 复位 | `emulation RunFor`（不可先 `start`）；`sysbus.cpu Reset` 软复位（内存保持，等效复位键） |
| 已知差异 | 擦除瞬时完成、无 PGERR 真实时序 —— smoke 只断言功能日志，时序项沿用"以真机为准" |

**CI 门**：`renode-smoke` job（ubuntu，Renode 固定 1.16.1 portable）+ `release.yml` verify 段。🟡 计划要求的"连续 3 次 CI run 稳定绿"须在首次推送 GitHub 后核对（本仓库无 git remote，无法本地执行 CI）——已列入交付文档待办，不阻塞 tag。

**真机政策**（P1-3）：硬件未到位，按计划切换政策：Renode CI 门 = 功能验收线，真机 = 常设挂账（`port/stm32f103/README.md` 平台表已改标"待硬件，不阻塞发布"）。

### P2 et_fsm（M2，`a4c711d`）

- **API**：`et_fsm_init / et_fsm_dispatch / et_fsm_state`，迁移表 `const` 传入（可驻 flash，API 无任何改表入口）；头文件附 ASCII 状态图（DoD #2）。
- **语义决议逐条落地**：表序首匹配、guard 失败继续向后扫（回退链）、无匹配忽略事件、自迁移合法且 action 照常、无 entry/exit 独立钩子、重复 init 防护。
- **单测 15 例**（≥14 达标）：首匹配顺序、guard 回退链、guard 全拒、未知事件、自迁移、单条目自环边界、user 透传、三态行走、const 表驻留、重复 init 防护、未初始化派发等。**行覆盖 100%（27/27）**。
- 开关：`ET_MODULE_FSM`（`#ifndef` 保护，可 `-D` 裁剪）。

### P3 工程化

1. `release.yml` 前置 Renode smoke（verify job `needs` 依赖）✅
2. 覆盖率门槛维持 85%：v1.3 基线**行 97.0%（4200/4330）/ 函数 99.6%** —— 高于"不低于 96.9%−2pt=94.9%"的要求 ✅（复现：`gcovr --print-summary`，构建命令见 Makefile/README）

## 3. 质量数据（每项附复现命令）

| 项 | 数值 | 复现 |
|---|---|---|
| 模块 | 19（+algorithm/et_fsm） | `ls algorithm/` |
| 主机单测 | **206 例 ALL PASS** | `mingw32-make test`（或 `make test`）→ RESULT 行 |
| 行覆盖率 | **97.0%**（函数 99.6%），门槛 85% | `gcovr --print-summary`（`--coverage` 构建） |
| ARM 编译 | 零警告，`text=11208 data=4 bss=276`（Flash 17%） | `arm-none-eabi-gcc -Wall -Wextra -pedantic -Os ...`（命令见 port README） |
| Renode smoke | 5/5 断言通过 | `sh port/stm32f103/renode/smoke.sh ...`（参数见脚本头注） |
| 内存 | 依旧零动态分配 | `grep -rn malloc core/ algorithm/ sys/ ...` 无命中 |

## 4. 文档同步

- `README.md`：v1.3.0 版本行、19 模块特性表（et_fsm）、206 用例、CI 门控（Renode smoke）与发布流（verify 门）、发布 checklist；
- `docs/API_GUIDE.md`：3.2 et_fsm（语义/API/示例）、配置表 `ET_MODULE_FSM`、平台表 206 用例；
- `port/stm32f103/README.md`：Renode 仿真小节（用法/要点/曾揪出的 bug）、平台表政策切换；
- 计划文档状态行与里程碑结果已归档。

## 5. 开发中发现并修复的问题

1. **【严重】F103 flash 驱动写偏移 bug**（v1.2 遗留，`port_stm32f103.c`）：`port_flash_write` 循环误用固定 `src[0..3]`（应为 `src[done..done+3]`），每个 4B 块都写入缓冲区首 4 字节——页头 4 块全变 magic、记录 vcrc 槽位变 `[key|len]`，**kv 在真机上必然全坏**。宿主机 191 用例全绿（host 模拟器独立实现）未暴露；Renode 首跑即现形（内存转储定位）。已修复并由 smoke 断言覆盖回归。
2. **demo 心跳时间占位符原样打印**：et_log 不支持域宽（`%04u`），心跳行输出 `%04u-%02u-…` 字面量。改为栈上手工补零拼串。
3. **Renode 工程适配三坑**（记录于 smoke.sh 头注与 port README）：FLASH 寄存器区无模型需 `sysbus Tag`；`start` 后 `RunFor` 不可用（有界运行直接 `RunFor`）；`CreateFileBackend` 带 `$true` 参数会挂死（用单参形式）。

## 6. 范围外事项 / 常设挂账

| 项 | 状态 | 去向 |
|---|---|---|
| F103 真机实测 | ⏳ 常设挂账（**不阻塞发布**，政策见 port README） | 硬件到位后按 checklist 补录，重启计数 `boot #n` 递增即最直观验收 |
| CI Renode job 连续 3 绿 | 🟡 待首次推送 GitHub 后核对 | 本仓库无 remote；job 本身已交付且本机等价流程全绿 |
| 磨损均衡 v2 / 加密 / 压缩 | Non-goal（沿 v1.2） | — |

## 7. 提交与版本

| 里程碑 | 提交 | 说明 |
|---|---|---|
| M1 | `d15ae56` | README 收口 + 发布 checklist + 1.3.0 + 删 exp/kv |
| M2 | `a4c711d` | et_fsm 模块 + 15 例单测 + 文档同步 |
| M3 | `3089ea6` / `0516742` | Renode 实测闭环 + **flash 驱动修复** / CI 门 + 发布 verify + 政策切换 |
| M4 | 本文本 | 回归 206 全绿 + 交付文档 + **tag v1.3** |

版本钉：`et_config.h` → 1.3.0 / `0x010300`；tag **v1.3**（三方一致，见 checklist 第 2 条）。