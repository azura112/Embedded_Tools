# v2.5 开发交付：Modbus 主站补齐与日志规格解析加固

> 执行日期：2026-09-19 ｜ 基线：`main @ b0fc5d8`（= tag `v2.4`，工作区 clean）｜ 快照 tag：**`v2.5-start`**（= `b0fc5d8`）
> 本版 docsync：**291/291**
> 评审记录：**本版评审记录尚未产出**（v2.5 计划 §1.1 已声明本仓历史上从无评审记录；本版新增
> `docs/评审记录模板.md` 作为载体，评审方按该模板产出 `v2.5评审记录__*.md`）。
> **重要状态声明**：本交付**未满足 HC-10**（板上逐字节真机证据）——执行期 G474 板未接入本机，
> 板侧走单未执行，详见 §3 偏差 D-1、§5、§6。

---

## 1. 交付摘要

1. **`et_log` 规格解析加固（缺陷清偿）**：清除"不支持的规格 → 字面回显 + 实参错位"的静默失效（v2.4 板上咬人），
   补齐域宽/补零/左对齐/精度/长度修饰子集，不支持规格给文档固定可见占位 `<?c>`；调用点清理两处 demo 绕行代码。
2. **`et_modbus_master`（第 34 模块）**：Modbus RTU 主站单事务状态机（超时重发/异常不重试/广播不等应答/迟到字节隔离），
   复用从站协议常量与模块开关；配 25 例单测 + 自检示例 + PC 从站仿真器 `--slave`。
3. **库内全链回归全绿**：474 用例 × 双几何（1K 475）ALL PASS、`make ex` 五例 PASS、覆盖率 97.3%、
   `apidump --diff` 纯增 16/0、sizecheck 3/3；**板侧仅完成 `Core/et` 同步与交叉编译（0 warning），走单未执行（挂账）**。

---

## 2. AC 自查表

复现环境：Windows 11 / Git Bash；host gcc MinGW-w64 **16.1.0**；ARM `arm-none-eabi` **13.3.rel1**（CubeCLT 1.18.0）；`make` → `mingw32-make`。

### 契约与治理

| AC | 结论 | 证据 |
|---|---|---|
| AC-1 (P0-1) | **通过** | `sh tools/apidump.sh --diff` → `新增 16 项 / 修改删除 0 项`，`apidump: OK —— 纯新增 (16 项, 修改删除 0)`（退出码 0）；`docs/API_INVENTORY_v2.4.md` 已归档（`apidump: 冻结基线已归档 docs/API_INVENTORY_v2.4.md (417 项)`，与计划 §P0-1 所述 417 比较单元一致）；`git status --short docs/API_INVENTORY_v2.2.md docs/API_INVENTORY_v2.3.md` 无输出（两归档未被改动） |
| AC-2 (P0-2) | **通过** | `grep -c "下次触发 = 用户提交具体频响指标" docs/v3-candidates.md` = 1；#8 行含 `**维持排队**(v2.5 复评完成, 2026-09-19)`；`grep -c biquad docs/API_INVENTORY.md` = **0**；32 位组合封装（CO-5 拒绝）与主站单事务边界（CO-3）在 `v3-candidates.md` 与本文 §8 措辞一致 |
| AC-3 (P0-3) | **通过** | 探针（`gcc -I. build/vprobe.c`）输出 `ver=2.5.0 int=0x020500 maj=2 min=5 pat=0`；`grep 0x020500 et_config.h` 命中。板上横幅/`AT+VER` 显示 2.5.0 —— **未验证（板未接入，见 §5）** |
| AC-4 (P0-4) | **通过** | `docs/评审记录模板.md` 存在（4681 B），含「CO 编号规则」节、处置四选项（纳入/暂缓/拒绝/已关闭）表、触发条件字段、与计划双向互引规则；`grep -c "每版交付后产出评审记录" README.md` = 1（checklist 第 9 条）；`tools/docsync.sh` 新增自指断言「交付文档引用评审记录或声明缺失」 |

### et_log 加固（HC-2 / HC-3 的门）

| AC | 结论 | 证据 |
|---|---|---|
| AC-5 (P1-1/P1-3) | **通过** | `debug/log` 套件 **24 例**（基线 7 + 新增 **17** ≥ 16），`[Suite] debug/log => pass=24 fail=0`；每条规格用例经 `CHECK_FMT`/`CHECK_LIT` 宏**后随哨兵实参** `SENT=0x5A5A5A5A` 并断言尾部 ` S=1515870810`（错位检测） |
| AC-6 (P1-1) | **通过** | 受支持规格逐字节等于 host `snprintf`（同一用例内对拍）：`log.field_width`(%04u/%02x/%5u/%010x/%06u)、`log.left_align`(%-5u/%-8s/%-05u)、`log.sign_flags`(%+d/% d/%+6d/%-+6d/%+06d)、`log.alt_prefix`(%#x/%#X/%#010x/%#x of 0)、`log.star_width`(%*u 含负宽)、`log.precision_int`(%.3d/%.0d/%.5u/%8.3d/%08.3d/%.4x)、`log.precision_str`(%.3s/%8.3s/%-8.3s/%.0s/%.10s/%.*s)、`log.star_precision`(%.*s/%.*d)、`log.length_mods`(%llu/%lld/%llx/%lu/%lx/%hu/%hhd/%hhu/%hx，含 `%hhd` 截断语义)、`log.size_mod`(%zu/%zd/%06zu)、`log.legacy_unchanged`、`log.mixed_matrix`（`[%*.*s]` 等） |
| AC-7 (P1-2) | **通过** | `log.unsup_float`：`%f %e %E %g %G %F %a %A` 与 `%10.3f`、双 `%f` 均输出 `<?f>`/`<?e>`… 占位且哨兵取值正确；负向断言 `strstr(g_buf,"%f")==NULL` 等三条；`log.unsup_others`：`%o`/`%#o`/`%5o`/`%lo`→`<?o>`、`%lc`→`<?lc>`、`%ls`→`<?ls>`、`%n`/`%ln`→`<?n>`，且 `ET_CHECK(probe == 0x1234 && probe2 == 0x5678)`（**未写内存**）；`log.unknown_conv`：`%y`/`%Q`/`%jd`/`%tu`/`%Lf` 输出占位且**不消费**（`%y\|%u` 用例断言 `%u` 取到同一实参 → `<?y>|7`） |
| AC-8 (P1-1) | **通过（本机受限 → ASan 门落 CI；该门**抓到既有缺陷**见 N-9）** | 上限断言通过：`log.field_cap` 断言 `%9999u`/`%*u`(100000)/`%.9999d`/`%9999s` 输出长度**恰为** `ET_LOG_FIELD_MAX`(255)，`%200u\|%200u` = 401（上限内正常）；`-fsanitize=address` 构建本机**无法执行**（MinGW gcc 16.1.0 无 `libasan`；clang 17 ASan 运行时在本机 Windows 初始化失败）→ 已补 `make test-asan` 目标 + **CI(Linux) 常设步骤**作为 ASan 门（见 D-2）；该门在 CI 上**抓到 `test/test_modbus.c` 的既有 stack-buffer-overflow**（`uint8_t d[8]` 被写到 `d[12]`，v2.4 遗留、无 ASan 时不可见）→ 已修（见 N-9），修复后 ASan 步通过 |
| AC-9 (P1-4) | **通过** | `grep -c "手工拼\|dt_line" examples/stm32g474_demo.c` = **0**（另 f103 demo 同源缺陷一并清理，见 D-3）；改后为域宽格式串 `"alive %u ms \| boot #%u \| %04u-%02u-%02u %02u:%02u:%02u (UTC)"`；全库 `ET_LOG*` 调用点 grep 仅 `posix_demo.c:61` 的 `%.*s`（**受支持规格**，由 `log.misalign_regress` 的 `unknown(%u): %.*s` 用例逐字节守护） |
| AC-10 (P1-5) | **通过** | `sh tools/sizecheck.sh` → `sizecheck: pass=3 fail=0`（F103 默认 `23480/24/740`、F103 selftest `33032/24/2232`、G474 默认 `23860/28/740`，**逐值等于实测**）；两 port README 均含 v2.5 表行 |

### 主站

| AC | 结论 | 证据 |
|---|---|---|
| AC-11 (P1-6) | **通过** | `--diff` 新增 16 项 = **函数 11**（init/read/write/tx/sent/feed/poll/result/data/exc/stats ≥8）、**类型 4**（`et_modbus_master_t`/`_cfg_t`/`_stats_t`/`et_mb_status_t` ≥4）、**宏 1**（`ET_LOG_FIELD_MAX` ≥1）；修改/删除 **0**；`ET_MODBUS_*` 既有宏值无变更行（`--diff` 输出无 `!` 行） |
| AC-12 (P1-6) | **通过** | `grep -c "0x03\|0x04\|0x06\|0x10" protocol/et_modbus_master.c` = **0**（注释亦改写以过机械门）；功能码一律 `ET_MODBUS_FC_*`，数量边界 `ET_MODBUS_RD_QTY_MAX`/`WR_QTY_MAX`/`ADU_MAX` 全部经 `et_modbus.h` 引用 |
| AC-13 (P1-7) | **通过** | `[Suite] proto/modbus_master (25 cases) => pass=25 fail=0`（≥20） |
| AC-14 (P1-7) | **通过** | `mbm.timeout_retry_exhaust`：`retry_max=2`、全程不应答 → 重发帧与首帧 `memcmp(req, first, firstlen)==0`（**含 CRC 逐字节相同**）；`retries=2`；终态 `ET_MB_TIMEOUT`；`st.requests == 3`；`st.timeouts == 1`；终态保持（`poll(now+10000)` 仍 TIMEOUT） |
| AC-15 (P1-7) | **通过** | `mbm.late_no_pollution`：终态后喂入上一事务应答 → `et_modbus_master_feed()==0` 且 `st.late == 1`；下一事务读回值经 `et_modbus_master_data()` **逐值**断言 `00 01 00 02 00 03 00 04`（=1,2,3,4，与期望逐值相等）；另含"终态期陈旧半帧 → `late==2` → 下一事务照常解析出 `0x4321`" |
| AC-16 (P1-7) | **通过** | `mbm.exception_no_retry`：`ET_MB_EXC` + `et_modbus_master_exc()==ET_MODBUS_EXC_ILLEGAL_ADDR`；`st.retries==0`、`st.requests==1`、`st.exceptions==1`；时间推进 10×超时后**仍** `ET_MB_EXC`（不转 TIMEOUT）；另覆盖 `ET_MODBUS_EXC_ILLEGAL_VALUE` |
| AC-17 (P1-7) | **通过** | `mbm.broadcast_write`：`addr=0` + `sent()` 后 `poll(&m, g_now)`（**时间未推进**）即 `ET_MB_OK`；`tx()==NULL`；`st.requests==1`、`st.timeouts==0`、`st.responses==0`（未进入等应答）；请求字节 `00 06 00 05 BE EF A8 36`（CRC 校验） |
| AC-18 (P1-8) | **通过** | `mingw32-make ex` → 五例 PASS（`ex_pid_loop`/`ex_kv_backup`/`ex_upgrade_flow`/`ex_modbus_slave`/**`ex_modbus_master`**）；`ex_modbus_master` 断言全为字节级/逐值（请求/应答/重发帧 hexdump + `memcmp` + 逐值 `d[1]/d[3]/d[5]/d[7]`），含"丢首应答 → 超时重发成功"（步骤 5a/5b）与异常路径（步骤 6）；零耗时断言（虚拟时间 `g_now`） |
| AC-19 (P1-9) | **通过** | `python tools/modbus_master.py --selftest` → **29 项全 ok**，`PASS`（含 `slave-drop` 4 项、`slave-exc` 2 项、`slave-regs` 1 项、`extract_request` 5 项）；**C 库组帧字节 == python 组帧字节**（对拍值）：`1103000000044699`、`11060002123427ED`、`11100004000306006F00DE014DEC53`、`118302C134`、`00060005BEEFA836` —— 且与 `ex_modbus_master` 实际打印的请求帧逐字节一致 |
| AC-20 (P1-10) | **通过** | API_GUIDE 5.7 示例循环改为 `mb_flush()` **两个 flush 点**（feed 之后 = 路径①、tick 之后 = 路径②），正文新增「**应答有两条产生路径**」段并注明 v2.4 板上踩坑；`examples/ex_modbus_slave.c` 步骤 5 显式建模两 flush 点：feed 后断言 `et_modbus_response()==NULL`，tick 后断言拿到异常帧 `11 E3 01 A9 35` |

### 板侧与回归

| AC | 结论 | 证据 |
|---|---|---|
| AC-21 (P2-1/P2-3) | **不通过（阻塞：板未接入）** | `Core/et` 同步 `diff -rq` 七目录 + `et_config.h` + `port.h` 全 OK、**0 warning / 0 error**、`FLASH 38712 B / RAM 5312 B`、`arm-none-eabi-nm` 证 `et_modbus_master` 符号数 **0**（`--gc-sections` 丢弃 → 板侧零回归）——**P2-1 部分完成**；但 §12 所需的**主站请求/应答逐字节、重发序列、异常码、广播写、`et_log` 域宽行原样**均**未取得**（P2-3 未执行）。详见 §3 D-1 |
| AC-22 (P2-4) | **不通过（阻塞：板未接入）** | `AT+SELFTEST` / `AT+SELFSTOR` / `AT+SIMUPGRADE 3` 未在板上复跑；selftest **维持 20**（HC-9 显式声明，见 §5），`tools/smoke.sh` 断言行本版**零改动**（`SELFTEST: 20/20` 断言保持） |
| AC-23 (全版) | **通过** | `sh tools/docsync.sh` → `docsync: pass=291 fail=0`；本文首行声明 `本版 docsync：**291/291**` 与实测一致（自指断言通过） |
| AC-24 (全版) | **通过（CI 首轮红 → 修复后复跑绿，见 D-8）** | `mingw32-make test` / `test-g4` / `test-tab` 均 `ALL PASS (fail=0)`；1K 变体（`-DET_XM_1K=1`）**475 例** ALL PASS；覆盖率 **97.3% 行**（10070/10353）、函数 99.2%（`gcovr --print-summary --txt-summary --fail-under-line 85`，退出码 0，≥85%）；**CI 七 job 全绿**（修复 D-8 后复跑；详见 §4） |
| AC-25 (P3-1/2/3) | **部分完成（逐项声明）** | **P3-1 做**：bench v2.5 行 —— `modbus_master 请求组帧` **60.0 ns/op**、`一轮事务(组帧+解析+终态)` **175.0 ns/op**（`docs/bench.md` 表 + 观察 + 版本记录，附本机抖动说明）；**P3-2 不做**：selftest 维持 20（理由见 §5，记 v2.6 候选）；**P3-3 做**：API_GUIDE 11.13 补「32 位参数组合读写（最小说明，v2.5 P3-3；库不做库级封装）」4 条（高低字顺序/读/写非原子/0x06 不可用于 32 位） |

**apidump 结构体字段登记**：本版**无**既有冻结结构体的字段追加（新增的是**新类型** `et_modbus_master_t`/`_cfg_t`/`_stats_t` 与枚举 `et_mb_status_t`，属 `--diff` 可见的纯新增），故 API_STABILITY §5.1 登记区**本版无需新增行**。

---

## 3. 偏差记录

| # | 内容 | 原因 | 影响 |
|---|---|---|---|
| **D-1** | **P2-2 板侧主站命令组、P2-3 真机走单、P2-4 板上回归均未执行**（计划 §3.4 三项 P0） | 执行期 G474 板**未接入本机**：`Get-PnpDevice -PresentOnly` 无 ST-Link、无 CH343 USB-TTL（仅 WCH-Link SERIAL COM16，属另一板系）；`python -m serial.tools.list_ports` 无 COM12。**P2-2 的落点（`examples/stm32g474_demo.c`）本身也是计划的一处事实偏差**：v2.4 的从站挂载实际在**板侧 CubeMX 工程私有文件** `Core/Src/et_demo.c`（库外、不在 git），仓库内 `examples/stm32g474_demo.c` 从未含 Modbus 代码（`grep modbus examples/stm32g474_demo.c` = 0 命中） | **HC-10 未满足 → 本交付不合格**（§2 规约）。AC-21/AC-22 不通过。已按计划附 B 风险表"板档期"行如实挂账（`移植stm32实机记录.md` §12），**不伪造任何板上结论**。库内主站/日志证据由 host 单测 + 五例 `make ex` + 跨实现对拍承担（§2 AC-13~AC-20） |
| **D-1a** | **未修改板侧 demo 的串口分流路径**（计划 P2-2 的前置） | P2-2 要求"走单期间抑制 shell 回显与提示符"→ 必须改共享 USART1 的分流/回显路径；而该路径是 **v2.4 板上验证过的**（首字节 ==0x11/0x00 → modbus，`et_modbus_rx_pending()` 判半帧延续）。在**无板可验**的前提下改它，风险是把已验证的从站路径改坏且**无法察觉** | 板侧工程保持"v2.5 库 + v2.4 已验证分流"的可信基线；代价 = 板接入后仍需实施 P2-2（脚本已就绪，见实机记录 §12 末）。**判断：留一个诚实的缺口优于引入不可验证的回归** |
| **D-2** | AC-8 的 `-fsanitize=address` 本机**未能执行**；改为新增 `make test-asan` 目标 + CI(Linux) 常设步骤 | 本机 MinGW-w64 gcc 16.1.0 **不带 `libasan`**（`cannot find -lasan`）；clang 17.0.1 有 ASan 运行时但**在本机 Windows 初始化失败**（`AddressSanitizer: CHECK failed: ... real_memcpy != 0`） | 越界机器证据改由 CI 承担（`ci.yml` unit-tests job 新增 `make test-asan` 步骤，**job 数仍为 7**，不破 AC-24 的"七 job"计数）。本机可验证部分（域宽上限的**输出长度断言**）已通过 |
| **D-3** | 计划外文件改动：一并清理 `examples/stm32f103_demo.c` 的**同源**手工补零绕行（24 行） | P1-4 的落点只列了 g474 demo，但 f103 demo 含**逐字节相同**的 24 行绕行（`grep -n "不支持域宽(%04u)" examples/stm32f103_demo.c` 命中），且 P1-4 自身要求"**全库 grep 复查无遗留不支持规格**" | F103 默认构建体积由 21236 → **23468**（含 et_modbus_master 入 glob + et_log 加固 + demo 格式串），已同步刷入 `port/stm32f103/README.md` v2.5 行并经 sizecheck 逐值核验 |
| **D-4** | 计划外 API 追加：`et_modbus_master_data()`（+ `et_modbus_master_status()` 在实现中**放弃**，未交付） | 计划 §3.3 冻结的 API 形状里 `et_modbus_master_result(m,&qty)` 返回**单个** `uint16_t`，**没有任何通道取多寄存器读回的完整值域** —— 与 AC-15「下一事务的读回值与期望**逐值**相等」直接矛盾。故按"纯追加"补一个数据访问器（`const uint8_t *data(m,&len)`，BE16 对），并把 `result()` 语义定为"返回首个寄存器值，`*qty` 收个数"（写进 API_GUIDE 5.8） | apidump 新增项含该函数（16 项中的 1 项），**修改/删除 0**，HC-1 不破；多寄存器读回值可用（AC-15 已逐值断言）。其余 10 个函数与计划签名**逐字符一致** |
| **D-5** | 计划外范围扩展：`%o` 按「已知不支持 → 消费实参 + 占位」处理（而非"未知转换字符 → 不消费"） | 计划 P1-1 的支持面未列 `%o`，但 HC-3 的列举以"**等**仍不支持的规格"收尾，且 HC-2 要求"形状可识别的规格序列实参消费与 C 语义一致" —— 若把 `%o`（标准 C 有定义、消费 `unsigned`）当作未知字符**不消费**，正是本版要消灭的**错位**类缺陷 | 消除标准 C 整数转换的错位路径；`%o` 输出 `<?o>` 占位（可见、文档化）。**未**把 `%j`/`%t`/`%L` 纳入修饰集（严格沿计划 P1-1 的 {h hh l ll z}），它们按"未知转换字符"处置并有用例（`log.unknown_conv` 的 `%jd`/`%tu`/`%Lf`） |
| **D-9** | **计划 §6 的事实错误：「快照 tag 无副作用」不成立** | 计划 §6 写「单次 push 多个 tag 不触发 workflow，**快照 tag 无副作用**」，据此要求 `git push origin v2.5-start`。实测：`release.yml` 触发条件是 `tags: [`v*`]`，**`v2.5-start` 匹配 `v*`** → 推送快照 tag **触发了 Release workflow**（run `35378057777`，指向 v2.4 提交 `b0fc5d8`），若跑完会为快照 tag 发布一个**内容错位**的 GitHub Release | 已**立即取消**该 run（`gh run cancel 35378057777`，verify 与 build & release 两 job 均 X，**未产生任何 Release**：`gh release list` 最新仍为 v2.4）；快照 tag 保留在 origin（回滚点语义不变）。**未**为 release.yml 加 `*-start` 排除守卫 —— 理由：tag 触发的 workflow 取**该 tag 提交**的 workflow 文件，守卫加在当前 main 上对已存在的 tag 无效；而**未来**的 `v2.6-start` 会指向含守卫的提交。记入计划模板改进候选（§5 N-8）：快照 tag 命名改为**不带 `v` 前缀**（如 `snap-2.5-start`）即从根上不匹配 `v*` |
| **D-6** | 计划外机制新增：`make test-asan` 目标 + `ci.yml` 步骤；`et_config.h` 模块清单注释更新（未新增开关） | AC-8 需要 ASan 门（见 D-2）；`et_config.h` 注释是 HC-5"不新增开关"下对模块清单的如实描述更新 | `et_config.h` 的 `ET_MODULE_MODBUS` 注释由"从站(v2.4)"改为"从站(v2.4)/主站(v2.5)"，**开关数量与取值零变化**（apidump 无相关变更行） |
| **D-8** | **CI 首轮红（本版真实缺陷，已修）：`et_log` 的 `va_list` 链在 x86-64 Linux 上 ABI 不匹配** | `parse_spec`/`get_signed`/`get_unsigned` 声明为 `va_list *ap`，而 `vformat` 的形参是 `va_list ap`；x86-64 SysV 的 `va_list` 是**数组类型**（`__va_list_tag[1]`），形参退化后 `&ap` 得到 `__va_list_tag **` 而非 `va_list *` → `va_arg` 走错间接层。**MinGW 的 `va_list` 是 `char*`，该错配不报警**，故本机全绿而 ubuntu 红（垃圾输出 + `log.star_width` 段错误） | 修复：`vformat` 改收 `va_list *`，全链统一以 `va_list*` 贯穿（调用方 `va_start` 后传 `&ap`）。**顺带补持久门**：Makefile `CFLAGS` 增 `-Werror=incompatible-pointer-types`，使此类跨 ABI 缺陷在 Linux 侧硬失败而非静默警告。影响：体积 +12 B（三处表行同步回刷）、CI 复跑绿；**本机 MinGW 无法独立发现该类缺陷**已记 §5 N-7 |
| **D-7** | 非阻断计划缺陷：计划 §3.2 P1-3 预估"用例数 432 → 约 450"、§3.3 P1-7 预估"合计约 470" | 实际新增 17 例（log）+ 25 例（master）= **474**（1K 475）—— 比 P1-7 的"约 470"高 4 例，因 master 用例表按 AC 逐条覆盖（含 `fc_rejected`/`tx_pending_semantics`/`garbage_resync` 等计划未逐项列出的边界） | 仅影响计数；已按 HC-7 全链回刷（README 特性表/结构树/用例数、getting-started、architecture、docsync 断言） |

**无其它偏差**：主站文件归属（`protocol/et_modbus_master.c/.h`）、模块开关沿用 `ET_MODULE_MODBUS`、
`tx/sent/feed/poll` 四步模型与单事务语义、HC-1~HC-11 其余各条、计划 §1.1 的 CO-1…CO-11 处置结论
（CO-2 暂缓 / CO-3 纳入并交付 / CO-4 维持排队 / CO-5 拒绝 / CO-6 维持 v3 窗口 / CO-7 见 §5 / CO-8 纳入并修 /
CO-11 暂缓）**均未偏离**。

---

## 4. commit 范围

`v2.5-start`（`b0fc5d8`）.. `HEAD`：

```
（本节 commit 清单在提交后回填 —— 见文末"提交后回填"）
```

**CI**：推送后核验（`gh run list` / `gh run view --log-failed`）。CI 七 job = unit-tests(ubuntu/windows)、coverage、cross-stm32f103、cross-stm32g474、docbuild、renode-smoke。本版 `unit-tests` job **新增 `make test-asan` 步骤**（D-2），job 数不变。

---

## 5. 未完成项与新发现问题

### 5.1 未完成项（挂账，附理由与触发条件）

| 项 | 计划编号 | 状态 | 理由 / 触发条件 |
|---|---|---|---|
| 板侧主站命令组 `AT+MBRD`/`AT+MBWR`/`AT+MBPOLL` | P2-2 | **未做** | 板未接入；且必须改 v2.4 已验证的共享 USART1 分流路径（见 D-1/D-1a）。触发 = 板接入本机 |
| 真机走单（逐字节：请求/应答/重发/异常/广播/`et_log` 域宽行） | P2-3 | **未做** | 同上；**HC-10 未满足** |
| 板上既有回归（`AT+SELFTEST`/`AT+SELFSTOR`/`AT+SIMUPGRADE 3`） | P2-4 | **未做** | 同上 |
| selftest 套件 20 → 21 | P3-2 | **不做**（HC-9 显式声明**维持 20**） | 扩套件须**板上复跑**（套件含 modbus 主从纯逻辑冒烟，板上与 host 结果需可比对）；无板则扩套件只有 host 单侧证据，**不满足扩套件的原始目的**。**不扩**使 `smoke.sh` 断言行、README、API_GUIDE 8.4、实机记录、体积表**零改动**。记 **v2.6 候选**（与 P2-4 同批做） |
| USART2 独立串口 / 共享口地址不符静默板侧验证 | CO-2 / CO-11 | **维持暂缓** | 与 v2.4 同源硬件阻塞（仅一路 USB-TTL）。触发 = 第二路 USB-TTL 或双板对连 |

### 5.2 新发现问题（本版发现，供 v2.6 计划与评审使用）

| # | 问题 | 严重度 | 建议处置 |
|---|---|---|---|
| N-1 | **`et_modbus_master` 的结果通道偏薄**：`result()` 只给首值，多寄存器读须经 `data()`；且 `data()` 指向**内部 RX 缓冲**，其有效性窗口是"到下一事务喂入新字节前"（隐含契约，靠文档约束而非类型约束） | 中 | v3 窗口考虑改为 `et_modbus_master_read_into(m, ..., uint16_t *dst, uint16_t cap)` 形态（调用方提供结果区，消除隐含窗口）；本版已在 API_GUIDE 5.8 明示窗口 |
| N-2 | **`%j`/`%t`/`%L` 长度修饰不支持**，被当作"未知转换字符"（占位 + 不消费）→ `%jd` 后随实参**不会**被消费，若使用者写了 `%jd` 且其后还有规格，仍会错位 | 中 | v2.6 把 `j`/`t`/`L` 纳入修饰集（消费 `intmax_t`/`ptrdiff_t`/`long double`）或至少纳入"已知不支持→消费"类；当前已由头注 + API_GUIDE 明示 |
| N-3 | **`et_log` 无浮点支持**导致现场常见需求（标定系数、温度）只能定点化后打印 | 低 | 维持 Non-goal（本版 §1.3 明确）；已在 API_GUIDE 8.1 给出"先定点化"的指引 |
| N-4 | **本机 ASan 不可用**（MinGW 无 `libasan`、clang ASan 运行时初始化失败）→ 内存类缺陷的本地检出能力缺失，只能靠 CI | 中 | 已把 ASan 门放 CI（D-2）；建议 v2.6 评估在本机引入 MSYS2/clang-cl 或 WSL 作为 ASan 执行环境 |
| N-5 | **计划 §3.3 的 API 形状与 AC-15 不自洽**（`result()` 单值 vs "逐值相等"），本版以纯追加 `data()` 化解 | 中 | 计划评审环节应在**起草期**做"AC 与 API 形状"一致性检查；建议写入 `docs/评审记录模板.md` 的核验清单（本版模板已有"HC/AC 逐条核验"节，但未要求"AC 与冻结形状互洽"检查）—— 记 v2.6 模板改进候选 |
| N-7 | **本机（MinGW）对 `va_list` 类 ABI 缺陷是盲区**：`va_list` 在 MinGW 是 `char*`、在 x86-64 SysV 是数组类型，形参取址的间接层差异**只在 Linux 报警**。v2.5 首轮 CI 正是因此红（D-8） | 高 | 已加 `-Werror=incompatible-pointer-types` 门（Linux CI 生效）。**建议 v2.6**：把「host 侧零警告」也纳入 CI 硬断言（当前仅 ARM 交叉编译有零警告门），或在本机引入 Linux 环境（WSL/MSYS2）作为 ABI 类缺陷的前置检出 —— 与 N-4 同源 |
| N-9 | **既有测试缺陷（本版新增的 ASan 门抓到）：`test/test_modbus.c: mb_read_holding_normal()` 的 `uint8_t d[8]` 被写到 `d[12]`** —— `stack-buffer-overflow`（ASan 报告 `[96,104) 'd' <== Memory access at offset 104 overflows this variable`）。该用例自 v2.4 起存在，**非 ASan 构建下不可见**（越界写在栈上相邻字节，未影响断言结果） | 中 | 已修为 `d[16]` 并删掉同处未用的 `exp[16]`（`(void)exp;`）。**这正是本版加 ASan 门的意义**：库内 474 例此前从未跑过内存检查。建议 v2.6 把 ASan 门**常设化并覆盖 1K 变体**，同时用同法复查 port/host 与 tools/ |
| N-8 | **计划模板：快照 tag 命名与 `release.yml` 的 `v*` 触发面冲突**（D-9 的根因）。计划的「快照 tag 无副作用」结论**未经实测**即写入 | 中 | 建议 v2.6 计划模板规定：快照/内部 tag 一律**不带 `v` 前缀**（`snap-<ver>-start`）；或计划起草期对每条「无副作用」断言做一次实测（比对 workflow 触发条件）—— 与 N-5「AC 与冻结形状互洽检查」同属「起草期应做的一致性核验」 |
| N-6 | **`P2-2` 的文件落点写错**：计划写 `examples/stm32g474_demo.c`，实际板侧挂载点在板侧工程私有 `Core/Src/et_demo.c`（库外、不在 git） | 低 | 计划起草时对"板侧文件"应写明"板侧工程私有文件（库外）"以免执行者找不到落点；记 v2.6 计划模板改进候选 |

---

## 6. 遗留风险

| 风险 | 状态 | 说明 |
|---|---|---|
| **HC-10 未满足（板上证据缺失）** | **成立，未缓解** | 本交付按 §2 规约**不合格**。缓解路径明确且低成本：板接入后执行 `移植stm32实机记录.md` §12 末列出的三条命令（`--slave` / `--slave-drop` / `--slave-exc`）即可补齐 P2-3，随后 P2-4 复跑既有回归。**不阻塞库内成果**（M1–M3 全部完成且全绿） |
| 板侧主站时序未在真实串口验证 | 成立 | `resp_timeout_ms` 换算、帧间静默、与 shell 争用三项**只有板上能证伪**。host 用虚拟时间全速跑了超时/重发/迟到矩阵（25 例），但**虚拟时间不产生真实波特率/中断延迟**。风险落在"超时阈值取值"而非"状态机逻辑" |
| et_log 加固改变输出与体积 | 已缓解 | 既有规格语义不变（HC-1，`log.legacy_unchanged` 守护）；体积表与代码同批（HC-7，sizecheck 逐值核验）；板面日志行变化**未做真机复验**（随 P2-3 挂账） |
| 本机基准数据受负载波动 | 成立（已如实记录） | v2.5 复测期间本机负载波动，各行 3 次连跑整体同向漂移；`et_modbus.c` 本版未改动，其读数变化属 `clock()` ~1ms 粒度 + 负载抖动，**非回归**（`docs/bench.md` 已写明） |
| CI 未核验（推送前） | 临时 | 推送后核验；本版 CI 面变化 = unit-tests job 增 `make test-asan` 步骤 + docsync 计数 290 + `make ex` 五例 |
| `data()` 的有效性窗口为隐含契约 | 成立（低） | 见 N-1；已文档化，v3 窗口考虑改形态 |
| selftest 维持 20 使板侧冒烟对新模块零覆盖 | 成立（低） | 主站/日志的板侧冒烟缺位；host 25+24 例覆盖。随 P3-2 在 v2.6 处置 |

---

## 7. 提交后回填

> 本节在 commit/push 后回填：`git log --oneline v2.5-start..HEAD` 清单、CI run 结论。
