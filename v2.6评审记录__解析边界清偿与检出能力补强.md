# v2.6 评审记录

> 评审对象：`v2.6开发交付__解析边界清偿与检出能力补强.md` ＋ `v2.5开发交付__Modbus主站补齐与日志规格解析加固-r2.md`（G0 返工轮）
> 判据来源：`v2.6开发计划__解析边界清偿与检出能力补强.md` §2（HC-1…HC-12）、§4（AC-1…AC-22）
> 基线：`snap-2.6-start` = `02b1592`；被评终态 `HEAD` = `c865f69`（`tag v2.6` = `2e57b16`）
> 评审日期：2026-09-25 ｜ 评审方式：**先 diff 后文档**，关键「修复前为红」全部由评审方在隔离目录独立复现（不采信交付贴出的输出）
> **编号体系声明**：本文 `CO-n(v2.6)` 为**本次评审新发现**，与计划/交付中表示「上一版遗留」的 `CO-n(v2.5)` **不是同一体系**；计划文档内亦用 `CO-n` 表示 v2.4 遗留（见 `-r2` §8.1 表头），三套编号互不相通，引用时必须带版本后缀。

---

## 1. 结论 —— **返工**（下一轮文件加 `-r2` 后缀）

**库内代码改动（CO-6 / CO-7 / CO-8）全部通过，无需修改**；返工范围**仅限文本、门与证据链**（与 v2.5 的 G0 同构，不含新开发）。

判为返工的硬依据（不是「证据不足」，而是**已被证伪**）：

| # | 事实 | 依据 |
|---|---|---|
| 1 | **AC-20 被证伪**：交付声明 `docsync pass=296 fail=0`，评审方实跑 `sh tools/docsync.sh` → **`pass=294 fail=2`，退出码 1**（两个独立 shell 均复现） | §2 AC-20 行、§7 表格第 8 行 |
| 2 | **HC-4 的门在交付平台上恒红**：`apidump --check` 报"公开面漂移"——成因是**行尾敏感**（工作树 `docs/API_INVENTORY.md` 为 CRLF，生成件为 LF；`core.autocrlf=true` 检出所致），与 API 面无任何关系 | 实验 E5/E6/E7 |
| 3 | **已发布产物含不实内容**：`CHANGELOG.md` v2.6.0「挂账」段与 **Release v2.6 正文**（Latest，公开）声称"板侧项**均未执行**、`tag v2.5` **亦未打**"，而事实是板侧已执行（实机记录 §13/§14）、`tag v2.5` 已打并已出 Release（`gh release view v2.5` 四附件在位） | 实验 E8 |
| 4 | **交付文档自相矛盾**：§1 状态声明/§4「CI 待推送后核验」/§5.1「远端待推送」/文末「本交付不合格（HC-9 未满足）」 与 §2 AC-4/AC-17/AC-21、§7 的终态结论**互斥** | 文档内部比对 |
| 5 | **HC-9 的 v2.6 侧未满足**（板上 CO-6 对照未取得）；**AC-18 字面未满足**（selftest 21/21 未做）；**HC-7 部分未满足**（3 处同族漏刷，见 CO-6/CO-7） | §3、§4 |

**同时必须明确记录（避免误伤）**：本版的核心技术成果是真实且可复现的 —— CO-6 的「修复前为红」在基线回退下**独立跑出 4 例 FAIL**，CO-7/CO-8 的「修复前为红」跑出 **6 例 FAIL**（含错位的直接证据）；设备冻结面、用例矩阵、覆盖率、体积、CI 七 job 均与声称逐值一致（见 §2）。返工不涉及任何 `core/ algorithm/ sys/ protocol/ drivers/ debug/ storage/` 源文件。

---

## 2. AC 核验表

复现环境：Windows 11 / Git Bash（PortableGit 1.2.0 usr/bin，另以 `D:\software\Git\usr\bin\sh.exe` 交叉复核）；host gcc MinGW-w64 16.1.0；`make` → `mingw32-make`。

### 前置门 G0

| AC | 交付声称 | 核验结果 | 证据或缺口 |
|---|---|---|---|
| AC-1 (G0-1) | 通过 | **通过（附注）** | `移植stm32实机记录.md` §13 确有逐字节记录（主站读 `11 03 00 00 00 04 46 99` / 应答 `11 03 08 …59 D4`、重发 ×3 同字节、异常 `11 83 02 C1 34`、广播 `00 06 00 02 00 55 E9 E4`、域宽心跳行）+ §13.8 从站分流复跑。**附注①**：计划 AC-1 指向 **§12**，实际证据落在 **§13**（§12 仍是旧占位节），AC 落点未回改；**附注②**：板上字节不可离线独立复验，本项按"证据在案且与 tag 事实自洽"接受，非评审方复现所得 |
| AC-2 (G0-2/G0-4) | 通过 | **通过（独立复现）** | `-r2` 文档 §8 存在且含三处 CO 措辞并置对账（§8.1）+ `CO-n(v2.5)` 去向表（§8.2）+ 数字回刷表（§8.3）；`sh tools/docref.sh "…-r2.md"` → **OK**，实测 `35 files / 4769 insertions / 179 deletions` 与文档声明逐值相等 |
| AC-3 (G0-3) | 通过 | **通过（独立复现）** | `grep -c "四例\|docsync 210\|连续十一版" docs/architecture.md` = **0**；`grep -c 398 docs/API_GUIDE.md` = **0**；新值为 `architecture.md:111` 「五例」、`:112` 「docsync 296 断言」、`:118` 「连续十六版」、`API_GUIDE.md:1411/1424` 「487」 |
| AC-4 (G0-5) | 通过 | **通过（独立复现）** | `git rev-parse v2.5` = `25f3316`；`git merge-base --is-ancestor 7077592 v2.5` → **否**（v2.5 树**不含** CO-6 修复，HC-9 满足）；`git show v2.5:debug/et_log.c \| grep -c emit_char_field` = 0；`gh release list` → v2.5 已在，四附件大小**逐值一致**：`demo.exe 131032` / `SHA256SUMS 245` / `stm32f103_demo.bin 24240` / `stm32f103_demo.elf 254772` |

### 机制门与治理

| AC | 交付声称 | 核验结果 | 证据或缺口 |
|---|---|---|---|
| AC-5 (P0-1) | 通过 | **通过（附注）** | `sh tools/apidump.sh --diff` → **新增 0 / 修改删除 0，exit 0**（复现，耗时 6m29s）；`docs/API_INVENTORY_v2.5.md` 在案且 `ET_VERSION_MINOR 5` 定着正确，条目 **433 = 440 − 7 个 `ET_VERSION*` 元数据宏**（与 API_STABILITY §5 忽略规则吻合）；v2.0~v2.4 归档 `git status` 无输出。**附注**：同族 `--check` 恒红（见 HC-4 / CO-1(v2.6)） |
| AC-6 (P0-2，HC-4) | 通过 | **不通过** | 门自证可红**已独立复现**：改 `architecture.md` 的「五例」→「四例」后 `docsync` 输出 `FAIL 跨文档 examples 数不一致: 实测=5 arch=四→4 README=五→5`（与交付 §7 自证表逐字一致）。**但 `pass=N fail=0` 不成立**：实跑 `pass=294 fail=2`（未变异状态），exit 1 |
| AC-7 (P0-3，HC-5) | 通过 | **通过（独立复现）** | `docref` 默认（v2.6 文档）→ OK `28/2233/57`；`-r2` 文档 → OK `35/4769/179`；把副本 `4769`→`4768` → **FAIL exit 1**，还原后 OK。**附注**：门存在设计性空隙（见 CO-4(v2.6)） |
| AC-8 (P0-4) | 通过 | **通过（独立复现）** | `docs/开发计划模板.md` 存在（5554 B），§7「起草期核验清单」四项齐备（`snap-*` 命名 / 无副作用断言须实测 / 板侧落点库外全路径 / AC↔冻结形状互洽），并附 v2.5 反例；`README.md` checklist **第 10 条**指向它；`docs/评审记录模板.md` 新增 **§3.1 AC ↔ 冻结 API 形状互洽（复核项）** |
| AC-9 (P0-5) | 通过 | **通过（附注）** | `et_config.h` → `ET_VERSION_MAJOR 2` / `MINOR 6` / `PATCH 0`、注释 `0x020600`；`et_config.h` 全文件 diff 仅版本 3 行。板上横幅/`AT+VER` 属不可离线复验项，同 AC-1 附注 |

### 库内缺陷（★ 核心）

| AC | 交付声称 | 核验结果 | 证据或缺口 |
|---|---|---|---|
| AC-10 (P1-1/P1-2，HC-2) | 通过 | **通过（独立复现，含红证据）** | `et_tests.exe` → `[Suite] proto/modbus_master (32 cases) pass=32 fail=0`（声称 32 ✓）；**基线回退实验 E1**：仅把 `protocol/et_modbus_master.c` 换回 `snap-2.6-start` 版 → **`pass=28 fail=4`**（`mbm.noise_self_echo` / `noise_bare_head` / `noise_odd_bc` / `noise_oversize_bc`），失败点 `feed(resp)` expected=1 actual=0 —— 即"真应答被噪声切走、只能超时"，与 CO-6 描述**逐字吻合** |
| AC-11 (P1-1) | 通过 | **通过（独立复现）** | `test` / `test-g4` 均 **487 ALL PASS**；既有 25 例（0x10 写、qty 边界、异常不重试、广播不等应答、迟到不污染、CRC 坏/地址不符丢弃、`read_bc_mismatch`）全绿 |
| AC-12 (P1-3/P1-5) | 通过 | **通过（独立复现，含红证据）** | `debug/log` → `(30 cases) pass=30 fail=0`（声称 ≥30 ✓）；**E2（回退 `debug/et_log.c`）**：6 例 FAIL，失败明细 `lib: [[A]]` vs `ref: [[    A]]` —— 正是 CO-7「`%5c` 域宽被丢弃」；且 `CHECK_FMT` 确为与 host `snprintf` **逐字节对拍**（`strcmp(g_buf, ref_)`）并校验哨兵尾部 |
| AC-13 (P1-4/P1-5，HC-3) | 通过 | **通过（独立复现，含红证据）** | E2 中 `log.len_mod_jt` 失败明细 `lib: [[<?j>d][<?j>u][<?j>x] S=7]` vs `ref: [[<?jd>][<?ju>][<?jx>] S=1515870810]` —— 同时给出**字面残留**与**实参错位**（`S=%u` 吃到了哨兵本应占的值 7）的直接证据；负向断言 `log.len_mod_no_literal` 亦在 E2 中红 |
| AC-14 (P1-3/P1-4) | 通过 | **通过（独立比对本机源码）** | 逐项列举实现的实际集合：支持 `{d i u x X c s p %}`（域宽/精度/`*`/`h hh l ll z`）；消费+占位 `{o f F e E g G a A n lc ls}` ＋ `{j t L}`；未知不消费 `default`。与 `et_log.h` 头注、`API_GUIDE` 8.1 规格表**差集为空**；剩余唯一例外（未知转换字符不消费）在头注+规格表+`log.unknown_conv` 用例三处固定 |
| AC-15 (P1-6，HC-10) | 通过（CI 侧） | **通过（独立核验 CI）** | `gh run view 35975885148 --job 107556134741` → ubuntu job 步骤 `Run unit tests under AddressSanitizer (make test-asan)` ✓、`Run 1K variant under AddressSanitizer (make test-asan-1k)` ✓；README「本机（Windows/MinGW）检出边界」小节如实声明 ASan 本机不可执行，**未见任何"本机已跑"字样** |
| AC-16 (P2-1) | 通过 | **通过（独立复现）** | `ci.yml` Linux 步骤改 `make WERROR=1 test/ex`、Windows 纯 gcc 步骤加 `-Werror`（diff 已核）；`gh run view … --job 107556134661`（windows）→ `Run unit tests (pure gcc, no make, WERROR host gate)` **✓**；本机 `mingw32-make WERROR=1 test` → ALL PASS 且 `grep -Ei "warning\|error"` 无命中 |

### 板侧与回归

| AC | 交付声称 | 核验结果 | 证据或缺口 |
|---|---|---|---|
| AC-17 (P2-3/P2-4) | 部分通过（如实） | **有条件通过（偏差已如实声明；按 AC 字面为不通过）** | P2-3 证据在 §14（`diff -rq` 全 OK / 0 warning / FLASH 75144 B）；P2-4「修复前/后对照」确实不可得。评审方独立佐证 §14.2 的推论：**若板收到自身 8 字节回显，统计应为 `disc≈6`**（见实验 E9，实测 `disc=6`），而板上实测 `disc=0`（不应答）/`disc=1`（正常读）→ "无回显"结论成立，但**该 AC 的字面判据（板上对照）未满足**，属 HC-9 未满足项 |
| AC-18 (P2-5，HC-12) | 通过（HC-12 的「不扩」分支） | **不通过（字面）／偏差成立（HC-12 分支）** | 计划 AC-18 字面要求「板上 `AT+SELFTEST` **21/21 PASS**」＋五处同批回刷，本版**未做**（维持 20）。HC-12 的「不扩则须显式声明维持 20 并给理由」分支**已满足**（§3 D-5、§2 AC-18、§8.1 三处声明）。**另附注**：AC-18 证据行称「`tools/smoke.sh` 断言行、README、API_GUIDE 8.4、实机记录、体积表 **本版零改动**」——其中**实机记录**（新增 §13/§14）与**体积表**（新增 v2.6 行）本版**均有改动**，表述不实（应为"selftest 相关行零改动"） |
| AC-19 (P2-6) | 通过 | **通过（附注）** | §14.1 载 `SELFTEST 20/20 PASS`、`STORAGE SELFTEST PASS`、`slot 1 CONFIRMED (self-check ok)`、从站分流复跑 `1,2,3,4` PASS；不可离线复验（同 AC-1 附注） |
| AC-20 (全版) | 通过 | **不通过（被证伪）** | 交付文档首行声明 `本版 docsync：**296/296**`；实跑 `sh tools/docsync.sh` → **`pass=294 fail=2`，exit 1**；两条 FAIL 为 `FAIL API 清单漂移…` 与由它级联的 `FAIL [自指] … 声明 docsync [296] ≠ 实测 295`。**断言总数 296 本身自洽**（294 pass + 2 fail = 296），失败点是 `apidump --check` 的行尾敏感（非 API 面漂移） |
| AC-21 (全版) | 通过 | **通过（附注：docsync 子项红）** | 独立复现：`test` **487**、`test-g4` **487**、`test-tab` **494**、`test-1k` **488**（全部 ALL PASS fail=0）；`make ex` 五例 PASS（末例 `[ex_modbus_master] PASS`）；`gcovr` 行 **97.3%**（10261/10550）、函数 **99.2%**（1046/1054）；`sizecheck` **pass=3 fail=0**（33272/24/2232、24100/28/740 逐值）；`gh run view 35975885148`（headSha `2e57b16`）**7 job 全绿**。**唯一缺口 = docsync** |
| AC-22 (P3-1/2/3) | 部分完成（逐项声明） | **通过（独立复现）** | `docs/bench.md` 顶部新增「口径声明（v2.6 P3-1/CO-14(v2.5)）」明确"仅供量级参考、不作回归判据"并附 59% 差幅依据；`docs/v3-candidates.md` 新增「v2.6 复评记录」节重述 CO-9 维持 v3 / CO-11 再次暂缓+触发条件；P3-3 未触发且未静默略过（§5.1 + README 均声明） |

---

## 3. HC 核验（逐条结论）

| HC | 结论 | 依据 |
|---|---|---|
| **HC-1 契约零破坏** | **满足** | `apidump --diff` 对 v2.5 基线 **新增 0 / 修改删除 0**（独立复现，exit 0）；`et_config.h` diff 仅版本 3 行；无签名变更 |
| **HC-2 主站不得信任噪声字节** | **满足** | 定长前判据在位（`et_modbus_master.c:362`，严格 `== 2*exp_qty`）；`crc_err` 门槛在位（`:382` `is_expected_shape`）。独立探针 E1/E3/E4 证明该判据**有判别力且非冗余**：E1（整文件回退）4 例红、E3（仅去掉 `is_expected_shape`）4 例红（含**既有** `mbm.read_bc_mismatch`）、探针 E4 显示去掉门槛后噪声会以 `drop_n(5)` 吃掉真应答前缀 |
| **HC-3 修饰符承诺与实现一致** | **满足** | 实现集合与 `et_log.h` 头注、`API_GUIDE` 8.1 逐项比对差集为空；唯一例外（未知转换字符不消费）三处明示且有用例覆盖；`%c` 域宽经 `emit_char_field` 真正生效（`et_log.c:277`），`%s`/`%p` 域宽原本即生效（`:259` / `emit_number :218`），**无同族残留** |
| **HC-4 漏刷点必须有门** | **不满足（作为交付状态）** | 门已建、断言写法为**跨文档提取比对**（非各自写死）、**自证可红已独立复现**（E5）。但交付状态下 `docsync` 恒红（`apidump --check` 行尾敏感），"与 README/实测计数交叉一致"无法在交付平台取得**可复现的绿** |
| **HC-5 §4 自指有门** | **满足（字面），附设计性空隙** | `docref` OK（默认文档 `28/2233/57`，独立复现）+ 自证可红（`4769→4768` → FAIL）。**空隙**：门的锚点是 `<base>..<最后一次代码提交>`，本版 §4 锚定 `2376a11`，而**其后 5 个提交**（`f3303a0 / 188e73f / 3182302 / 2e57b16 / c865f69`，含 CHANGELOG 与交付文档 §2/§7 的结论回填）**全部落在门的视野之外** —— 本版最严重的两处文本错误（CO-2/CO-3）恰好都在这个盲区里 |
| **HC-6 库不变式** | **满足** | 零动态内存（`grep malloc\|calloc\|realloc` 于 `et_modbus_master.c`/`et_log.c` 无命中）；`et_config.h` 无新增 `ET_MODULE_*` 开关；无模块新增；头文件仅版本宏变化 |
| **HC-7 数字回刷同批** | **部分不满足** | 门覆盖到的数字**全部正确**（用例数 487/494/488、examples 五例、docsync 296、体积表 3 值、README 结构树 487/32/30）；但 3 处同族漏刷不在门内：① `README.md:237` 冻结面 checklist 仍写 `当前 docs/API_INVENTORY_v2.4.md`；② `port/stm32g474/README.md` v2.6 行写"板侧工程本版**未同步**"（与 §14 矛盾）；③ `CHANGELOG` / Release 正文挂账段未随板接入回刷 |
| **HC-8 板侧落点库外路径** | **满足** | 实机记录 §13/§14 一律写全库外路径（`D:\code\STM32CubeMX\G474VET6_ET_TEST\Core\Src\et_demo.c`、同步目录 `...\Core\et\`），并显式标注"库外私有、不在本仓 git"；库内 `examples/` 无对应改动 |
| **HC-9 真机证据与 tag 完整性** | **部分满足** | v2.5 侧：**满足**（`tag v2.5` = `25f3316`，不含 CO-6 修复，独立复现）。v2.6 侧："CO-6 修复的板上证据须单独取得"——**未取得**（受害形态在本板不复现，已如实声明）。按字面 **不满足**；交付给出的理由成立且未伪造结论，判为**有条件接受** |
| **HC-10 内存门** | **满足** | `make test-asan` **常设** + 新增 `test-asan-1k` 覆盖 1K 变体，CI ubuntu job 两步 ✓；本机不可执行已在 README「检出边界」与交付 §5 显式声明，**未声称本机已跑** |
| **HC-11 CO 一致性三落点** | **满足** | 交付文档 §8 存在（v2.5 正因此不通过）；三处 = 计划 §1.1 / `docs/v3-candidates.md`「v2.6 复评记录」/ §8，逐条比对结论一致；**差集仅 CO-10(v2.5) 一处**（计划"纳入本版" vs 实际"维持 20"），已在 §8.1 显式标 ⚠️ 并给理由 —— 不属静默丢失 |
| **HC-12 selftest 扩套件全链** | **满足（"不扩"分支）** | 显式声明维持 20 + 理由（扩套件须板上复跑，属新功能面而非本版范围）+ 记 v2.7 候选（§2 AC-18、§3 D-5、§8.1 三处）；`tools/smoke.sh` 本版确实零改动（不在 diff 清单内） |

---

## 4. 偏差裁决表

| 偏差 | 裁决 | 理由 |
|---|---|---|
| **D-1** 板侧项挂账 → 板接入后消项（仅 P2-4 对照不可得） | **接受** | 由"证据缺失"转为"证据齐备 + 一项对照不可得"，且不可得的原因被**对照实验证伪了前提**（本板无自身回显）而非"没做"；未伪造结论。评审方独立佐证：回显形态的统计特征应为 `disc≈6`（E9 实测 6），板上实测 0/1 → 前提确不成立 |
| **D-2** `crc_err` 统计口径收紧（行为修正） | **接受** | 字段类型/结构体布局未动，属语义修正；HC-1 的语义面已在 `API_GUIDE` 5.8 口径表 + 交付 §2 登记区双重登记；既有 `mbm.crc_bad_discarded` 仍绿（该帧与在途事务同形，`crc_err==1` 成立） |
| **D-3** 定长判据取"严格等于 `2*exp_qty`"而非计划 §5 的"合法域" | **接受** | 有实测依据（合法域判据会把噪声里恰为偶数的字节当帧长 → 停等永不到齐）；HC-2 的措辞本身给出"或"分支；边界行为由 `mbm.noise_expected_shape` 固定。**该判据比计划更严，方向正确** |
| **D-4** 新增 `test-1k` / `test-asan-1k` 两个 Makefile 目标 | **接受** | 计划仅要求 ASan 覆盖 1K；把手工 gcc 命令常设化使 AC-21 的"1K 变体全绿"可一键复现，属正向增益；CI 步骤 +1 已同步 |
| **D-5** selftest 维持 20（与 CO-10(v2.5) 的"v2.6 扩至 21"不一致） | **接受（附条件）** | HC-12 明文提供"不扩须显式声明并给理由"分支且已被正确援引；但须承认 **CO-10(v2.5) 未关闭**，且计划 AC-18 字面未满足 → 记 CO-14(v2.6)，v2.7 **必办**（不得再次援引本分支） |
| **D-6** `tools/apidump.sh` TMPD 归一 + 取消逐文件 `rm` | **有条件接受** | 修复方向正确（消除了"无参数运行覆盖成残缺清单"与小时级劣化）。**两处条件**：① 所称"修复后本机 `--check` **21 秒**"**不可复现** —— 评审方实测 `--check` **4m39s**（PortableGit sh）/ **4m53s**（系统 Git sh），`--diff` **6m29s**；② 修复**未触及行尾敏感性**，导致 `--check` 在交付平台恒红（→ CO-1(v2.6)）。须重新测量并如实记数 |
| **D-7** `docs/API_INVENTORY_v2.5.md` 按"v2.6 工作树生成 + 回改版本宏"定着 | **有条件接受** | 事实层面成立：评审方实测 `--diff` = 0/0，且 `docs/API_INVENTORY.md` 相对 v2.5 归档的差异确为**唯一一行**（`ET_VERSION_MINOR 5→6`）；433 = 440 − 7 个 `ET_VERSION*` 与忽略规则吻合。**但**"基线清单 = 快照树的生成物"这一语义被人工等价性论证替代，属证据链降级（该结论无法由机器复核）。建议 v2.7 用 `git worktree add` 在 `v2.5` 树上真生成一次并逐字节比对 |

---

## 5. 遗留问题清单

> 严重度：**高** = 影响发布物正确性/门不可用；**中** = 正确性相邻或证据链断裂；**低** = 代码卫生/表述。

| # | 问题 | 严重度 | 建议处置版本 |
|---|---|---|---|
| **CO-1(v2.6)** | **`docsync` 在交付平台恒红**：`apidump --check` 用 `diff -q` 做**字节级**比对，工作树 `docs/API_INVENTORY.md` 经 `core.autocrlf=true` 检出为 CRLF、生成件为 LF → 报"公开面漂移"（与 API 面无关）。后果：HC-4 的门不可用，AC-6/AC-20 不可复现。修复选项：① `gen_body` 输出统一行尾（或比对前归一化）；② 比对改"条目集合比对"（`--diff` 用的 `item_lines` 手法，已天然抗行尾）；③ 提交 `.gitattributes` 对 `docs/*.md` 显式 `text eol=lf` | **高** | **v2.6-r2** |
| **CO-2(v2.6)** | **已发布产物含不实内容**：`CHANGELOG.md` v2.6.0「挂账（如实声明）」段称板侧项**均未执行**、`tag v2.5` **亦未打** —— 与实机记录 §13/§14、交付文档 §2 AC-1/AC-4/AC-17/AC-19 及 `gh release view v2.5` 的事实**相反**。该段经 `release.yml` 注入，故 **Release v2.6 正文（Latest，公开）同样含此错误** | **高** | **v2.6-r2**（CHANGELOG 改写；Release 正文以 `gh release edit v2.6 --notes` 重注，或在新版 Release 说明中更正；**不得**靠重打 tag 解决） |
| **CO-3(v2.6)** | **交付文档自相矛盾**：§1 状态声明（"CI 未核验"/"板侧 tag 待推送"）、§4（"CI：待推送后核验"）、§5.1（G0-5 "远端待推送"、CI "待推送后核验"）、文末纪律（"本交付**不合格**（HC-9 未满足）"）与 §2 AC-4/AC-21、§7 终态结论互斥。成因是终态回填只改了 §2/§7，未同步其余章节 | **中** | **v2.6-r2** |
| **CO-4(v2.6)** | **HC-5 门的设计性空隙**：`docref` 只核验 `<base>..<end>` 的 commit 与三个 diffstat 数字，`<end>` 由作者自选为"最后一次**代码**提交"（`2376a11`），其后的**任意数量文档提交不受检**。本版 5 个后续提交正是全部文本缺陷的来源（CO-2/CO-3/CO-6/CO-7 均在其中）。建议：门的锚点改为 `<base>..HEAD`，或增补"交付文档 §2/§7/§8 与 CHANGELOG 的终态一致性"断言 | **中** | **v2.6-r2**（或 v2.7） |
| **CO-5(v2.6)** | **从站侧同源缺陷未改**（改 A 漏 B）：`protocol/et_modbus.c` 的 `expected_len()` 对 `FC_WRITE_MULTIPLE` 直接 `return b[6] + 9u` —— **字节数域先于 CRC 被信任**，与 CO-6 同一类；且 `process_frame()` CRC 失败后**仍按该（未受信）长度整段前进**（`memmove` 消费 `need` 字节），不是逐字节重同步。**评审方实测复现**（探针 E10）：先喂 11 字节噪声 `11 10 00 00 00 01 04 DE AD BE EF`（`bc=4` → 伪造长度 13），再喂真请求 `11 03 00 00 00 04 46 99` → `et_modbus_feed` 返回 **0**（无应答）、`crc_err=1`、`rxlen=6` —— **真请求被吞掉前 2 字节**，残段 `b[1]=0x00` 使 `expected_len=0` 永久停等；该事务丢失，只能靠主站超时重发恢复。修复方向与主站对称：切片前校验 `b[6] == 2*((b[4]<<8)|b[5])`，不符即逐字节重同步，且 CRC 失败的"非成帧"序列不计 `crc_err` | **中** | **v2.7**（建议单独立项，勿在 -r2 中文档轮夹带代码改动） |
| **CO-6(v2.6)** | **README 冻结面 checklist 漏刷**：`README.md:237` 第 8 条仍写"最近已发布 MINOR 的冻结基线（当前 `docs/API_INVENTORY_v2.4.md`）"，而默认基线已滚动至 v2.5。**该处不在新门（跨文档计数断言）覆盖范围内** —— 与 CO-3/CO-4(v2.5) 同族，说明"装门"只盖住了被点名的三处 | **中** | **v2.6-r2** |
| **CO-7(v2.6)** | **`port/stm32g474/README.md` v2.6 行叙述与事实冲突**：写"板侧工程本版**未同步**（板未接入，随交付文档 §5.1 挂账）"，而 §14 明载已同步（`diff -rq` 全 OK、0 warning、FLASH 75144 B）。体积数字本身正确（宿主侧读数），错误仅在叙述 | **中** | **v2.6-r2** |
| **CO-8(v2.6)** | **板上"正常读恒有 `disc=1`"未进交付文档**：实机记录 §14.3 已记为 v2.7 排查项，但交付文档 §5.2「新发现问题」与 §6「遗留风险」**均未登记**（§5.2 仅 N-1…N-4）。评审方已把该现象**机械定位**：单个前导噪声字节即产生 `disc=1 / crc=0 / st=2`（探针 E11，v2.5 与 v2.6 **同值**），与板上记录**逐值吻合**；且 8 字节自身回显应产生 `disc≈6`（E9）→ **反证板未收到自身 TX**（独立佐证 §14.2）。剩余未知仅"该字节的物理来源"（建议读 UART 错误标志或抓一次波形） | **中** | **v2.7**（含交付文档登记） |
| **CO-9(v2.6)** | **主站读路径出现死代码/恒真子句**：因 `feed()` 在读分支已强制 `rx[2] == 2*exp_qty`，故 ① `handle_frame()` 的 `bc != 2*exp_qty` 分支**永不可达**（探针 E12：在该分支注入 `printf` 后跑全量 487 例，命中 **0** 次）；② `is_expected_shape()` 读分支的 `rx[2]==2*exp_qty` 子句在到达点**恒真**（有效判别力只剩 addr/fc 检查）。建议改为断言/注释说明或删繁 | **低** | **v2.7** |
| **CO-10(v2.6)** | **`L` + 整数转换（UB 域）无覆盖**：`%Lu/%Ld/%Lx` 走 `j/t/L` 分支按 `long double` 消费。本机（Windows x64，所有参数同槽宽）实测**不错位**（探针 E13：哨兵 `S=1515870810` 正确）；但 x86-64 SysV 下 `long double` 属 memory-class，`va_arg` 可能不推进整数槽 → 后继实参仍错位，**本机无法验证**且 `test_log.c` 无用例。建议文档限定 `L` 仅对浮点转换有意义，或补 Linux 侧用例 | **低** | **v2.7** |
| **CO-11(v2.6)** | **计划 AC 落点过时**：AC-1 指向 `移植stm32实机记录.md` **§12**，实际证据在 **§13**（§12 仍是"未执行的走单（如实挂账）"占位节）。属 `docs/开发计划模板.md` 起草期清单未覆盖的项，建议增第 5 项"证据落点章节号与占位节清理" | **低** | **v2.6-r2**（模板）/ **v2.7**（计划） |
| **CO-12(v2.6)** | **AC-18 证据行表述不实**：称"实机记录、体积表**本版零改动**"，而二者本版均有改动（§13/§14、v2.6 体积行）。应为"selftest 相关行零改动" | **低** | **v2.6-r2** |
| **CO-13(v2.6)** | **D-6 的"21 秒"读数不可复现**：实测 `--check` 4m39s／4m53s、`--diff` 6m29s。须重测并如实记数（数字类声明漏环境注记即无效，README 发布纪律第 6 条） | **中** | **v2.6-r2** |
| **CO-14(v2.6)** | **`CO-10(v2.5)` 未关闭**：selftest 仍 20，主站/日志在板上零冒烟覆盖。本版援引 HC-12「不扩」分支合法，但**v2.7 不得再次援引**；须新增 modbus（从站+主站）与 `et_log` 修饰冒烟套件并完成五处回刷 + 板上复跑 | **中** | **v2.7 必办** |
| **CO-15(v2.6)** | **bench 无回归判别力**：仅以文档声明封住误用面（P3-1 轻量分支），跨版数值仍不可比 | **低** | **v2.7** |

---

## 6. 下一版计划必须处理的输入（从第 5 章精选）

> 规则沿用 v2.5：**返工轮（`-r2`）不含新开发**；代码类问题一律进下一版正式窗口。

### 6.1 前置门 G0′（v2.6-r2 返工轮，纯文本/门，发布前必须全绿）

1. **CO-1(v2.6)** 修 `apidump --check` 的行尾敏感（三选一：归一化输出 / 改用条目集合比对 / `.gitattributes` 显式 `eol=lf`），并在**本机**复跑 `docsync` 至 `fail=0`、`apidump --check` 至 exit 0；把"本机 `--check`/`--diff` 实测耗时"重新测量后写入文档（覆盖 CO-13(v2.6)）。
2. **CO-2(v2.6)** 重写 `CHANGELOG.md` v2.6.0「挂账」段（板侧已完成、`tag v2.5` 已打并发布、仅 P2-4 对照不可得），并**同步更正已发布的 Release v2.6 正文**；声明更正方式与时间。
3. **CO-3(v2.6)** 统一交付文档终态：§1 状态声明、§4「CI」行、§5.1 的 G0-5/CI 行、文末纪律句，全部改为与 §2/§7 一致的终态表述（"未能核验者不按通过计"的原则保留，但不再自判"不合格"）。
4. **CO-6(v2.6) / CO-7(v2.6) / CO-12(v2.6)** 三处同族漏刷与表述：`README.md:237` → `API_INVENTORY_v2.5.md`；`port/stm32g474/README.md` v2.6 行改为"板侧已同步（见实机记录 §14）"；AC-18 证据行改为"selftest 相关行零改动"。
5. **CO-11(v2.6)** 计划模板增补第 5 项（证据落点章节号 + 占位节清理），并把 `移植stm32实机记录.md` §12 的占位节标注为"已由 §13 承接"。
6. **CO-4(v2.6)** 收窄 HC-5 的空隙：`docref` 锚点改为 `<base>..HEAD`，或增补"交付文档 §2/§7/§8 与 CHANGELOG 终态一致性"断言（否则 r2 轮自身仍会被同一盲区放过）。
7. `-r2` 完成后按既有纪律重新出 `docsync`/`apidump`/`sizecheck`/`docref` 四门读数与 CI run 链接，并补齐 `本版 docsync：N/N` 自指声明。

### 6.2 v2.7 必含项（代码/功能）

1. **CO-5(v2.6)** 从站侧同源缺陷清偿：`et_modbus.c` 的 `expected_len()` 与 `process_frame()` 前进策略对称化（切片前校验字节数域 ↔ qty、CRC 失败逐字节重同步、`crc_err` 只计成帧者），补用例并**板上注入验证**（与 CO-14 同批板侧会话）。
2. **CO-14(v2.6)** 关闭 `CO-10(v2.5)`：selftest 20→21（modbus 主从 + `et_log` 修饰冒烟），五处文档回刷 + 板上 `AT+SELFTEST 21/21` 复跑。
3. **CO-8(v2.6)** 定位板上 `disc=1` 的物理来源（UART 错误标志 / 波形），并在交付文档 §5.2/§6 正式登记（不得只留在实机记录）。
4. **CO-9(v2.6)** 清理主站读路径的死代码与恒真子句。
5. **CO-10(v2.6)** `L` + 整数转换：文档限定或补 Linux 侧用例（消除 x86-64 SysV 的未验证面）。

### 6.3 v2.7 可选（时间盒）

- **CO-15(v2.6)** bench 相对比值化（+ 预热 + 固定迭代）。
- **D-7** 用 `git worktree` 在 `v2.5` 树上真生成基线清单并与归档逐字节比对，消除"人工等价性论证"。
- **CO-11(v2.6)** 若模板第 5 项在 r2 轮落地，v2.7 起 AC 一律带章节号。
- `CO-9(v2.5)`（`data()` → `read_into()`）**维持 v3 窗口**，不动。

---

## 附：评审方独立复现实验索引

**隔离目录**：`build/review/`（`build/` 已 gitignore；实验产物**不清理**，作下一轮探针）

| 编号 | 目的 | 命令/手法 | 产物 | 结果 |
|---|---|---|---|---|
| **E1** | AC-10 红证据（整文件回退） | `git show snap-2.6-start:protocol/et_modbus_master.c > build/review/et_modbus_master_base.c`；以新版 `test/*.c` 链接基线源编译后运行（cwd=`build/review/cwd`） | `t_mb_base.exe` / `run_mb_base.log` | `proto/modbus_master pass=28 fail=4`：`mbm.noise_self_echo` / `noise_bare_head` / `noise_odd_bc` / `noise_oversize_bc`，失败点均为 `feed(resp) expected=1 actual=0` |
| **E2** | AC-12/AC-13 红证据（整文件回退） | 同上，回退 `debug/et_log.c`（`et_log_base.c`，自检 `emit_char_field` 出现 0 次） | `t_log_base.exe` / `run_log_base.log` | `debug/log pass=24 fail=6`；明细含 `lib: [[A]]` vs `ref: [[    A]]`、`lib: [[<?j>d]…S=7]` vs `ref: [[<?jd>]…S=1515870810]`（字面残留 + 实参错位双重证据） |
| **E3** | 定向注入：**只**去掉 `is_expected_shape` 门槛（保留 `rx[2]` 严判） | Python 定向替换（`build/review/inject_e3.py`，自检残留 `is_expected_shape(m, fc)` = 0） | `et_modbus_master_e3.c` / `t_e3.exe` / `run_e3.log` | `pass=28 fail=4`（含**既有** `mbm.read_bc_mismatch`）→ 证明该门槛**有判别力且非冗余** |
| **E4** | 三版源码逐步对拍（机制解释） | `probe_mb.c` 分别链接 新版 / E3 注入版 / 基线版 | `probe_new.exe` `probe_e3.exe` `probe_base.exe` | 新版：`feed(echo)→rxlen=2 disc=6 crc_err=0`，随后真应答 `ret=1 resp=1`（成功）；E3：`feed(echo)` 同为新版结果，但真应答到达后 `crc_err=1`、`disc=14`、`ret=0`、残留 `59 D4` —— 因 CRC 失败分支按 `drop_n(5)` **整段前进**（把残留 `46 99` + 真帧前 3 字节当一帧丢弃）→ 错位；基线：`crc_err=3`、残留 `D4`，同样失败 |
| **E5** | docsync 门自证可红（真实变异） | `cp docs/architecture.md build/review/arch_backup.md` → `sed -i 's/五例/四例/'` → 跑全量 `docsync` → **自动还原** | `gates5_docsync_mut.log` | `FAIL 跨文档 examples 数不一致: 实测=5 arch=四→4 README=五→5`（与交付 §7 自证表逐字相同）；`docsync pass=293 fail=3` exit 1 |
| **E6** | HC-4 门恒红（本机，PortableGit sh） | `sh tools/apidump.sh --check` | `gates1.log` | **FAIL**：`@@ -1,757 +1,757 @@`，`diff` 报全文件差异；real **4m39s** |
| **E7** | 排除 shell 差异 + 计时复核 | `"/d/software/Git/usr/bin/sh.exe" tools/apidump.sh --check` | `gates4_sysgit.log` | 同样 **FAIL**；real **4m53s**（与 D-6 所称 21 秒差 ~13×） |
| **E8** | 行尾根因定位 | `tr -cd '\r' <f \| wc -c`；`git show HEAD:docs/API_INVENTORY.md \| grep -c $'\r'`；`git config core.autocrlf` | 命令输出 | 工作树 757 CR / 生成件 **0 CR** / HEAD blob **0 CR** / `core.autocrlf=true` → 检出即 CRLF，生成件 LF |
| **E9** | 回显形态的统计指纹 | `probe_mb.c`（喂 8 字节主站自身请求回显后再喂真应答）+ `probe_disc.c`（单字节前导噪声） | `probe_mb.exe`(→`probe_new/e3/base.exe`) / `probe_disc_new.exe` / `probe_disc_base.exe` | 8 字节回显形态（v2.6 实现）→ `disc=6`；单字节前导噪声 → `disc=1`（v2.5 与 v2.6 同值）→ 独立佐证 §14.2「板未收到自身 TX」，并定位 CO-8(v2.6) |
| **E10** | 从站侧同源缺陷复现 | `probe_slave.c`：先喂 `11 10 00 00 00 01 04 DE AD BE EF`，再喂真请求 `11 03 00 00 00 04 46 99` | `probe_slave.exe` | `feed_req=0`（**无应答**）、`crc_err=1`、`rxlen=6`（真请求被吞前 2 字节）→ CO-5(v2.6) 成立 |
| **E11** | `disc=1` 机械定位 | `probe_disc.c` 变体：无噪声 / 前导 `0x11` / `0x00` / `0x0A` / `0x03` 各 1 字节，随后真应答 | 同上 | 基准 `disc=0`；任一单字节前导 `disc=1 crc=0 st=2` → 与板上 §14.1/§14.3 逐值吻合 |
| **E12** | 死代码判定 | Python 定向在 `handle_frame()` 的 `bc` 不符分支插入 `printf("[PROBE] …")`（`inject_dead.py`），跑全量 487 例 | `et_modbus_master_dead.c` / `t_dead.exe` | `grep -c PROBE` = **0**，且 `ALL PASS` → 该分支不可达 |
| **E13** | `L`+整数转换消费行为 | `probe_log_L.c`：`"[%Lu][%Ld][%Lx] S=%u"` | `probe_L.exe` | 输出 `[<?Lu>][<?Ld>][<?Lx>] S=1515870810` → **本机不错位**（Linux/x86-64 SysV 未验证） |
| **E14** | 全量门槛复跑 | `mingw32-make {test,test-g4,test-tab,test-1k,ex,coverage-build}`、`WERROR=1 test`、`sizecheck.sh`、`gh run view` | `gates3_tests.log` `gates6_cov.log` `gates1.log` | 487/487/494/488 全 ALL PASS；五例 PASS；覆盖率 97.3%/99.2%；WERROR 零告警；sizecheck 3/3；CI run `35975885148`（headSha `2e57b16`）**7 job 绿** |

**工作区完好性复核（实验后）**

- `docs/architecture.md`：实验前 `c0d0d1ea…30e90` → 实验后同值，且与备份 `diff -q` 字节级一致 ✓
- 关键源文件 sha256 与实验前 `build/review_pre_sha.txt` **逐值相同**（`et_modbus_master.c` `33cbf1ff…`、`et_log.c` `96a0ac7a…`、`et_log.h` `83723b52…`、`test_modbus_master.c` `259474c3…`、`test_log.c` `3d4a3a2e…`）✓
- `git status --short` 仅 `?? .workbuddy/`（本工作区既有未跟踪目录）；`git diff --stat HEAD` 为空 ✓
