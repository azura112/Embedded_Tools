# v2.1 开发交付：定点控制与治理收口

> 交付日期：2026-09-11 ｜ 版本：v2.1.0（`ET_VERSION = 0x020100`，冻结后**首个 MINOR**）｜ 基线：v2.0（tag `6bb4f48`，API freeze）
> 计划文档：[v2.1 开发计划__定点控制与治理收口.md](./v2.1开发计划__定点控制与治理收口.md)（状态：✅ 已交付归档）
> 远程：https://github.com/azura112/Embedded_Tools（**本工作区尚未提交/打 tag**；`v2.1` tag 由发布 checklist 第 1~8 条核对后执行）
> 本版 docsync：**186/186**（`sh tools/docsync.sh` 实测,含本自指断言;机制与自证见 §7）

## 1. 重点概况（TL;DR）

v2.1 是**冻结后的第一个 MINOR**，主题两件：**把"测量→滤波→控制→输出"环路的最后两块补上**（定点 PID 控制器 + 运行统计），以及**把"冻结不虚"从人工承诺变成机检事实**（apidump 基线 diff + docsync 自指断言）。全程只增不改——`apidump --diff` 对 v2.0 基线实测 **新增 27 项 / 修改删除 0 项**，本版不存在任何破坏性变更。

- **P0 治理收口与板侧同步**：`tools/apidump.sh` 新增 `--snapshot`（固化冻结基线）与 `--diff`（**纯新增=绿；签名删改/宏值变更=红**，`ET_VERSION*` 元数据宏忽略），基线归档为 `docs/API_INVENTORY_v2.0.md`，规则写入 API_STABILITY 附则（含"结构体字段追加 apidump 不可见 → 须人工登记"）。**docsync 自指断言**：当前版本交付文档声明的"本版 docsync N/N"与脚本实测断言数对账，写错即红——v2.0 验收发现的"交付文档计数无门"自指盲区（同族第三次）就此终局。版本宏升至 2.1.0；CubeMX 板侧同步 v2.1（跨 v1.9→v2.1 两版）并完成上板复验。
- **P1 `et_pid`（★ 核心）**：位置式定点 PID，补齐闭环控制环节。Q15 增益（`kp` 无量纲 / `ki` 1/s / `kd` s）、**int64 饱和中间量**（极端增益/误差/dt 下无有符号溢出 UB，输出恒被限幅钳定）、**积分限幅抗饱和**（钳位法，无回灌）、`d_on_measure` 微分作用于测量值（推荐默认开，免除设定值跳变冲击）、`dt_ms` 由调用方给（不内部取时基，复用 stimer/sched 时基）、非 ISR-safe 单上下文标注。配套整定配方入 API_GUIDE 11.10（`et_lpf1 → et_pid → et_spwm` 三段式链路 + Ziegler-Nichols 定性 + 抗饱和/常见坑）。18 例单测含手算阶跃序列、积分钳位、d-on-measure 开关对照、dt 变化、reset、多实例、饱和边界。
- **P2 `et_stats`**：Welford **整数增量**流式统计（`min/max/均值/方差 Q10`），不累加 Σx²（避免大数相减失真与溢出）；与 et_pid 联动做判稳/超调记录。11 例含双精度参照对拍、顺序无关、负值、恒定输入、极值饱和。
- **P3 时间盒**：`et_bytes`（BE/LE u16/u32 打包解包，边界检查即唯一面）经评估**不冗余**（`et_frame_pack` 只管自身协议字段，通用载荷拼装此前无对应工具）→ 实现并交付 8 例；`docs/getting-started.md` 端到端教程（裁剪 → 单测 → `port/_template` 移植 → 串口 shell → selftest → 闭环示例 → 提交自查）；bench v2.1 行（`pid step` 15.0 ns/op、`stats push` 6.0 ns/op）。
- **过程自省（机制抓到的问题）**：板侧零警告构建抓到 `et_bytes.h` 缺 `<stddef.h>`（host 侧因头文件间接包含而未显形）——**板侧同步的价值实例**，已修。apidump 基线 diff 上线首日即完成双向自证（对自身 0/0 绿、对故意改签名 1 增 1 删红）。

## 2. 逐项验收对照（v2.1 计划 §3）

| # | 任务 | 验收结果 |
|---|---|---|
| P0-1 | apidump 基线存档 + `--diff` | ✅ `docs/API_INVENTORY_v2.0.md` 归档（347 项比较单元）；`--diff` 对当前头文件 **新增 27 / 修改删除 0**；自证：对基线自身 0/0 绿、对故意改 `et_lpf1_init` 签名 → **1 增 1 删红**；规则入 API_STABILITY §5 附则 |
| P0-2 | docsync 自指断言 | ✅ 交付文档"本版 docsync 186/186"与实测一致；自证：手改 186→185 → 本断言**红**（记录见 §7） |
| P0-3 | CubeMX 板侧同步 v2.1 | ✅ `Core/et/` 全量重拷 + `diff -rq` 七目录三文件全 OK；0 warning；FLASH **32100 B**（新模块被链接器 GC，与 v1.9 等值）；上板复验见 §5 |
| P0-4 | 版本宏 2.1.0 | ✅ `ET_VERSION = 0x020100`；板面横幅 `Embedded_Tools v2.1.0 (0x20100)`；README/API_GUIDE/CHANGELOG 三方对齐（docsync 动态版本链断言） |
| P1 | `et_pid` 合入 | ✅ algorithm/ 第 28 模块，18 例；整定配方 11.10；bench 行；Q 标度与溢出语义文档化 |
| P2 | `et_stats` 合入 | ✅ algorithm/ 第 29 模块，11 例（含双精度对拍）；联动配方入 11.10 |
| P3-1 | `et_bytes` 评审判冗余 | ✅ **判不冗余**（通用载荷字节序读写无既有归属，`et_frame_pack` 只管自身协议）→ protocol/ 第 30 模块，8 例 |
| P3-2 | getting-started 端到端教程 | ✅ `docs/getting-started.md`（7 步、一步一命令）+ README/API_GUIDE 链入 |
| P3-3 | bench v2.1 行 | ✅ `pid step` 15.0 / `stats push` 6.0 ns/op（docs/bench.md 结果表 + 版本记录） |

## 3. 冻结证明（本版核心 DoD）

`sh tools/apidump.sh --diff`（基线 `docs/API_INVENTORY_v2.0.md` ↔ 当前头文件）：

```
新增     27 项  (只追加 = OK)
修改/删除 0 项  (冻结面破坏 = 红)
apidump: OK —— 纯新增 (27 项, 修改/删除 0)
```

**diff 摘要：新增 27 项，修改 0 项。**

| 类别 | 新增 | 明细 |
|---|---|---|
| 函数 | 21 | `et_pid_*` ×5、`et_stats_*` ×8、`et_bytes_*` ×8 |
| 类型 | 3 | `et_pid_cfg_t`、`et_pid_t`、`et_stats_t` |
| 宏 | 3 | `ET_MODULE_PID`、`ET_MODULE_STATS`、`ET_MODULE_BYTES` |

**结构体字段追加登记区（--diff 附则要求）：空（本版零项）。** 说明：本版三类新增均为**全新类型**，未对任何既冻结结构体追加/改动字段；既有 `et_*_t` 字段布局与 v2.0 完全一致，故无人工登记项。

`--diff` 规则（入 API_STABILITY §5）：新增=OK；删除/修改签名（含宏值变更）一律红；`ET_VERSION*` 版本元数据宏忽略（随版本必然变化、不属冻结面）；结构体字段追加 apidump 不可见，须在交付文档登记理由与破坏半径。

清单口径：`docs/API_INVENTORY.md` 共 **381 项**（函数 212 / 类型 69 / 宏 100，其中 `ET_VERSION*` 元数据 7 项）；`--diff` 比较单元 **374 项**（v2.0 基线 347 + 27）。

## 4. DoD 对照（v2.1 计划 §4）

| DoD | 状态 | 证据 |
|---|---|---|
| 1 沿用 v2.0 全部 DoD（docsync/sizecheck/双几何/docbuild 单源/量化环境注记） | ✅ | docsync **186/186**（含 apidump `--check`）；sizecheck **3/3**（19636/28100/20008）；382×双几何 + 1K 383 + test-tab 全绿；docbuild 原样执行 0 warning |
| 2 **冻结证明**：`--diff` 对 v2.0 基线纯新增 | ✅ | §3：新增 27 / 修改 0；自证红绿双向 |
| 3 自指断言生效（计数机制保证） | ✅ | 交付文档计数 = 实测 186；故意写错 → 红（§7 自证记录） |
| 4 板侧回最新验证态 | ✅ | §5：同步 + 0 warning + FLASH 32100B；板上 `AT+SELFTEST 17/17`、`AT+SELFSTOR` kv+bootctl PASS、升级链 confirm/rollback 双路径 |
| 5 新模块均定点实现（无浮点依赖）、Q 标度文档化 | ✅ | 三模块仅依赖 stdint/stdbool/stddef + et_config.h，无 `<math.h>`/float；Q15/Q10/Q 标度见各头注释与 API_GUIDE 3.3/3.4/5.6 |

## 5. 板侧同步与上板复验（G474VET6_ET_TEST）

- **同步**：`Core/et/` 全量重拷 `core/algorithm/sys/protocol/drivers/debug/storage` + `et_config.h` + `port/port.h`（`et_port/`、`et_demo.c` 工程私有不动）；`diff -rq` 校验全 OK。
- **构建**：`cmake --preset Release && cmake --build --preset Release` → **0 warning**；FLASH **32100 B** / text 32032 / data 68 / bss 4560。
- **零足迹事实**：新模块未被 demo 调用 → 链接器丢弃其 **59 个 section**（map "Discarded input sections"），FLASH 与 v1.9 记录**逐字节等值 32100 B** → 板端行为零变化。
- **板上复验**（烧录 + COM12 115200 采集）：

```
[0][I][demo] Embedded_Tools v2.1.0 (0x20100)        # 版本宏板面自证
AT+VER      -> ver=2.1.0 boot=14
AT+SELFTEST -> 17/17 PASS (59ms)                     # kv/bootctl SKIP(非存储门)
AT+SELFSTOR -> kv PASS + bootctl PASS -> STORAGE SELFTEST PASS
AT+SIMUPGRADE 3 (奇=自检过) -> STAGED slot B -> 复位 -> slot 1 CONFIRMED (self-check ok)
AT+SIMUPGRADE 4 (偶=自检败) -> STAGED slot B -> 复位 -> self-check FAILED, not confirmed
                             -> 再复位 -> boot slot 1 attempt 2 + ROLLBACK: attempts exhausted
                             -> BOOTINFO: staged=-1 confirmed=-1 attempts=0 (回干净态)
```

完整记录与复现命令见仓库根 [`移植stm32实机记录.md`](./移植stm32实机记录.md) §9。

## 6. 里程碑对照

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 | P0 全部（基线快照/diff 门/自指断言/板同步/2.1.0） | ✅ |
| M2 | `et_pid` 合入（含整定配方） | ✅ |
| M3 | `et_stats` 合入 | ✅ |
| M4 | P3 时间盒（et_bytes/getting-started/bench）+ 文档同步 | ✅ |
| M5 | 全量回归 + apidump 纯增证明 + 交付文档 | ✅（tag `v2.1` 待发布 checklist 执行） |

## 7. 机制自证记录（本版新增两门的红绿双向验证）

| 机制 | 绿向 | 红向（故意破坏） |
|---|---|---|
| `apidump.sh --diff` | 对基线自身 0/0；对当前头文件新增 27 / 修改 0 | 临时把基线中 `et_lpf1_init(..., uint16_t k_q15)` 改成 `uint32_t k_q15` → 输出"新增 1 / 修改 1"并 **exit 1** |
| docsync 自指断言 | 交付文档行首声明"本版 docsync 186/186" → `ok`，最终 `pass=186 fail=0` | 把该行计数手改为 185/185 → `FAIL [自指] 声明 docsync [185] ≠ 实测 186` → docsync **exit 1**；改回 186 即绿 |

## 8. 挂账与后续

1. **tag `v2.1` 待执行**：本工作区未提交、未推送；发布时按 README checklist（含第 8 条"冻结面机检 + 自指断言"）核对后打 tag，`release.yml` 将注入 CHANGELOG 2.1.0 节为 Release 正文。
2. **v3.0 评审窗口素材池不变**：`docs/v3-candidates.md` 四项破坏性候选（回调 user 位/foreach 形状/动词命名等）**无到期义务**；本版零破坏，不动候选。
3. **Non-goals 维持**：不做浮点 PID/统计、不做 FOC/自动整定、不做 i2c/spi 抽象/RTOS/安全启动/动态内存/多行编辑（连续八版）；smap 通配与 u32 全键域维持 v2.0 终局关闭。
4. **新模块未进 et_selftest 套件**（有意）：selftest 是板上冒烟子集（维持 17 套件），et_pid/et_stats/et_bytes 由 host 单测覆盖；板侧零行为变化的性质也要求 selftest 计数不变（数字回刷面最小化）。
5. **板侧同步可复现**：`Core/et` 非 git 管理（仅磁盘），同步方法与 G4 构建要点见 `Core/et/README.md` 与实机记录 §9。

## 9. 量化数据复现（环境注记）

| 项 | 复现命令 | 结果 | 环境 |
|---|---|---|---|
| 单元全量 | `mingw32-make test` / `make test-g4` RESULT 行 | **382×2** 全绿；1K 变体（`-DET_XM_1K=1`）**383**；`make test-tab` 同套件数 | MinGW-w64 gcc **16.1.0** / Windows 11 Git Bash；CI ubuntu make 同绿 |
| 覆盖率 | `mingw32-make coverage-build` + `gcovr --print-summary` | **lines 96.9%（7808 / 8054）**，门 ≥85%；functions 99.1%；新模块 et_pid 93% / et_stats 90% / et_bytes 93% | gcovr **8.6** + MinGW gcc 16.1（`--coverage`） |
| docsync | `sh tools/docsync.sh` | **pass=186 fail=0**（内嵌 apidump `--check`；含自指断言） | Git Bash |
| API 清单/冻结 | `sh tools/apidump.sh --check`；`sh tools/apidump.sh --diff` | 清单 381 项（比较单元 374）；**新增 27 / 修改删除 0** | 同上；脚本内锁 `LC_ALL=C` |
| ARM 体积 | `sh tools/docbuild.sh` + `arm-none-eabi-size`；`sh tools/sizecheck.sh` | F103 **19636**(默认)/**28100**(selftest)、G474 **20008**；sizecheck **3/3** | GNU Tools for STM32 **13.3.rel1**（STM32CubeCLT 1.18.0），`-Os -g` 零警告 |
| CubeMX 板侧 | `cmake --preset Release && cmake --build --preset Release`；`STM32_Programmer_CLI` + COM12 | 0 warning；FLASH **32100 B**；板上 17/17 + SELFSTOR PASS + 升级链 confirm/rollback | 同上工具链 + ST-LINK(SWD) + CH343 115200-8-N-1 |
| bench | `make bench` | `pid step` **15.0 ns/op**、`stats push` **6.0 ns/op**（既有行在 `clock()` 1ms 粒度抖动内） | 同 v1.7 基线注记（Ryzen 9 7945HX / gcc 16.1 / `-O2`） |

---

> 维护约定：本文档随 v2.1 交付定稿；后续范围变更不再回改，走下一版计划。
