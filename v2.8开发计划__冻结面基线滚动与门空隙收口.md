# v2.8 开发计划：冻结面基线滚动与门空隙收口

> 起草日期：2026-09-26 ｜ 状态：⏳ 待开发（交付后归档）｜ 版本号 **2.8.0**
> 输入：`v2.7评审记录__从站解析对称化与板上自测收口.md`（2026-09-26，结论 **有条件通过**，`CO-1(v2.7)`…`CO-13(v2.7)` 共 13 条 + 三项随轮修订条件）
> 上一版被评 tree：`snap-2.7-start` = `724d089`（= v2.6-r2 终态）；被评终态 `HEAD` = **`e7c79f7`**（`tag v2.7` = `e7c79f7`，**范围内唯一提交**，评审 E9 实测）
> 起草日实测基线：用例 **497**（Tab **504** / 1K **498**）/ 内建 selftest 套件 **22** / docsync 断言 **304**（评审实测 304/0）/ 模块 **34** / **API 归档最新 = v2.5（基线滞留已跨两版，`CO-2(v2.7)`）** / `apidump` 默认基线 = `docs/API_INVENTORY_v2.5.md`（`tools/apidump.sh:218`）
> 结构说明：§1–§6 为规约结构；附 A（里程碑）、附 B（风险）为工程惯例补充

---

## 1. 目标与范围

### 1.1 输入说明与 CO 逐条处置

上一版评审记录**存在且可用**。结论为**有条件通过**（非返工，**无 `-r2` 轮**）：三项条件均为纯文本修订，评审已指定"随本轮修订、与评审记录同批提交"（§6.1）——本计划将其收为 **§3.0 前置门 G0**（沿 v2.6-r2 收进 v2.7 计划 G0′ 的同构先例），由本版开发对话与**评审记录入仓同一提交**完成。13 条 CO 逐条处置如下：

| # | 问题摘要 | 来源 | 处置 | 理由 / 触发条件 |
|---|---|---|---|---|
| **CO-1**(v2.7) | **§4 的 commit 清单错指悬空提交**（列 `838edb1`，实际范围内唯一提交 = `e7c79f7`；CI run 锚在悬空的 `23c0d3f`）；且 `docref` **不核清单**（只核范围 + 三个数字） | 评审 §1 条件 1、§5 CO-1、E9 | **拆两半**：文本半 → **G0-1**（清单改 `e7c79f7` + 注记悬空提交与 CI 差异面）；装门半 → **P1-1**（"清单中每个 SHA 须为 RANGE 可达提交"断言） | 起草期实测：`git log --oneline snap-2.7-start..HEAD` = 仅 `e7c79f7`；交付文档 `:96` 仍列 `838edb1`。门空隙属 **HC-5 的残留面**——v2.7 修了"终点覆盖 HEAD"，没修"清单逐项可达" |
| **CO-2**(v2.7) | **冻结面基线滞留 v2.5 已跨两版**：`docs/API_INVENTORY_v2.6.md` 从未归档（v2.6 发布时未做、v2.7 计划亦漏列该任务），HC-1 的"对 v2.6 基线"字面不可满足；本次 0/0 实质成立但属"事实巧合而非门保证" | 评审 §3 HC-1、§5 CO-2、§6.2 第 1 条 | **纳入本版（P0 ★ 治理核心，P0-1…P0-5）** | **根因裁决**：这不是执行疏忽，是**计划模板缺陷**——v2.6/v2.7 两版计划都把基线滚动隐含在"P0-1 基线滚动"旧任务名下而未列显式任务，v2.7 计划甚至 HC-1 写了"对 v2.6 基线"却无归档任务。故本版除补归档外，**必须把"基线滚动"写进模板起草期清单**（P0-4），把这类缺陷变成起草期可拦 |
| **CO-3**(v2.7) | §7.3 复跑预期数字不准（预测 `pass=23 fail=10`，实测 `proto/modbus pass=25 fail=8` + `debug/selftest` 2 例连锁红；2 例新用例基线即绿=零判别力） | 评审 §1 条件 3、§5 CO-3 | **G0-1**（文本） | 起草期实测：交付文档 `:177` 仍写"期望: pass=23 fail=10"；按评审 §6.1 第 3 条改写为实测值并标注两例回归钉的属性 |
| **CO-4**(v2.7) | 覆盖率分支计数 5618 ≠ 实测 5612（同为 57.4%） | 评审 §5 CO-4 | **G0-1**（文本） | 一行修订 |
| **CO-5**(v2.7) | `README.md:192`"API 冻结机检（v2.1）：…对 **v2.0** 基线"——同族漏刷又一处（checklist #8 已有门，此行没有） | 评审 §5 CO-5、§6.2 第 5 条 | **纳入本版（P0-3）** | 起草期实测：`README.md:192` 确含"对 v2.0 基线"。**与 CO-2 合并装门**：docsync 的"基线名交叉断言"（`:467-470`，现只盖 checklist 第 8 条）扩展覆盖"测试与质量门"小节的基线行 |
| **CO-6**(v2.7) | **`docref` 的 `END==HEAD` 副作用**：交付文档一经提交，其后任何提交（评审记录、下一版计划、修订）都使门红——门在仓库历史大部分时间"设计性红" | 评审 §5 CO-6、§6.2 第 4 条 | **纳入本版（P1-2）**：改**白名单式判定** | 设计约束见 P1-2 与可裁量项 4（反滞后意图保留 + G0 类"评审授权修订"必须可放行 + 三场景红绿矩阵作为 AC）；这是 v2.7 G0′-6 断言的**迭代**而非回退 |
| **CO-7**(v2.7) | **能力损失待恢复**：`bc`/`qty` 不自洽的 `0x10` 帧不再回异常 0x03（v2.7 D-2 的代价）。恢复路径 = "双假设定长"（CRC 通过才接受 `9+b[6]`） | 评审 §4 D-2 条件、§5 CO-7、§6.2 第 6 条 | **纳入本版（P2-1，评估后二选一）** | 评审明示"不得静默消失"。**安全前提（HC-6）**：恢复实现中 CRC 失败**不得按不可信长度整帧前进**、长度域自洽**不得单独**作为 `crc_err` 同形判据——否则重开 `CO-5(v2.6)` 的口子。走"实现"分支须附红证据用例；走"论证维持"分支须交付文档记录论证 |
| **CO-8**(v2.7) | **`item_lines` 的"抗行尾"依赖 GNU sed 剥 CR 的隐式行为**（交付自报 N-2）：换不剥离的实现会静默返回 0 条 → `--diff` 误红。修法一行：显式 `tr -d '\r'` | 评审 §5 CO-8、§6.2 第 2 条 | **纳入本版（P1-3）** | 起草期读码确认：`tools/apidump.sh` `item_lines()` 仅有 `sed -n 's/^- `\(.*\)`$/\1/p'`，无显式行尾处理。**把"抗行尾"从副作用升级为契约**，一行、零风险 |
| **CO-9**(v2.7) | **`apidump.sh` 的 `TMPD_REL` 回退静默 + 无参数运行无守卫**（交付自报 N-3）：mkdir 失败静默退回 `mktemp -d`（回到 v2.6 修掉的"Windows 路径 → gawk 转义 → 提取为空"陷阱）；无参数生成可把清单覆盖成残缺版 | 评审 §5 CO-9、§6.2 第 3 条 | **纳入本版（P1-4）** | 交付文档 N-3 已实测复现（v2.5 时代工具在 worktree 内生成 0 条目）；v2.6 D-6 的遗留建议，两版未做 |
| **CO-10**(v2.7) | **从站"读帧无长度域"的 8 字节窗口**（交付自报 N-4）：噪声伪造 `[addr][读fc]` 2 字节即可让坏读帧按整帧丢弃（可能吃掉真请求前缀）；"读帧 CRC 失败改逐字节重同步"的替代方案有更高风险（坏帧残字节可重组假帧 = 从站误写） | 评审 §5 CO-10、§6.2 第 7 条 | **纳入本版（P2-2，裁决项，默认维持现状）** | 评审明示"默认维持"。本版任务 = **对比分析落档**（两种取舍的代价矩阵入 API_GUIDE 5.7），除非分析翻转结论，不改行为；翻转则须用例 + HC-6 同款安全论证 |
| **CO-11**(v2.7) | 板上 `log` 套件**不校字符内容**（v2.7 D-4 的边界）：板上修饰面冒烟只验"生效性"（字符数） | 评审 §4 D-4 条件、§5 CO-11、§6.3 | **纳入本版（P3-1，拆两半）**：明示半必做（API_GUIDE 8.4 写明边界，纯文档）；钩子半可选评估（`port.h` 增捕获钩子 = **公开契约变更**，须走破坏性窗口评估，本版**不实现**） | D-4 的"附条件"要求边界明示；实现半触碰 HC-1，维持 v2.7 裁决的不可行性结论，只评估 |
| **CO-12**(v2.7) | 板侧 demo 命令行 `\n` 残留未清（`CO-8(v2.6)` 已定位，保留证据原样是评审认可的合理选择） | 评审 §5 CO-12、§6.3 | **可选（P3-2，库外）** | 库外一行级修法已在实机记录 §15.4 给出。**若做**：清理后须复跑从站走单确认 `disc=0` 基线并更新实机记录（`disc=1` 现象的"已知现象"登记同步改写）；**不做**须显式声明 |
| **CO-13**(v2.7) | §15.3 的 `MBSLAVE` 读数为**会话累计值**但表内以绝对值呈现 | 评审 §5 CO-13 | **G0-1**（文本：表头注明口径） | 一行修订 |

**非 CO 项的处置声明**（评审 §4 偏差裁决中未被 CO 覆盖者，防静默丢失）：**D-1**（快照推送重试 + 探针 tag 实测无副作用）已接受，其"探针 tag"手法值得沿用到本版 §6；**D-3**（`crc_err` 门槛取"仅本站地址"）已接受，5.7 口径表已登记，本版 P2-1/P2-2 的任何改动**不得回退该口径**；**D-5/D-6/D-7**（板侧诊断命令、用例数下限、A/B 对照升级）已接受，无遗留动作；**D-2 的条件** → `CO-7(v2.7)`（P2-1）；**D-4 的条件** → `CO-11(v2.7)`（P3-1）；**D-9**（docsync 字面量断言改"单一来源取值"）→ **P3-4**（可选项）；**`CO-15(v2.6)`**（bench 相对比值化，跨版第三次）→ **P3-3**（可选项，触发条件未变）。

### 1.2 本版做什么

1. **冻结面基线滚动清偿（CO-2，★ 治理核心）**——用 `git worktree` 在 `tag v2.6` 树上**真生成**并归档 `docs/API_INVENTORY_v2.6.md`（沿 v2.7 AC-19 已验证的手法）；发版点归档 v2.7；`apidump` 默认基线前滚；"基线滚动"写入计划模板起草期清单（**根治**连续两版漏列的结构性原因）。
2. **门空隙收口（CO-1/CO-6/CO-8/CO-9）**——`docref` 增"清单 SHA 可达性"断言并把 `END==HEAD` 迭代为白名单式判定；`apidump` 的 `item_lines` 显式抗行尾 + TMPD 回退告警 + 无参数生成守卫。每条门改动**自证可红**。
3. **解析边界复评（CO-7/CO-10）**——"双假设定长"恢复异常 0x03 面的评估（实现或论证，二选一）；从站读帧 8 字节窗口的取舍裁决（默认维持）。结论落 API_GUIDE 5.7。
4. **随轮修订（评审三项条件 + CO-3/4/13）**——v2.7 交付文档五处文本修订，**与评审记录入仓同一提交**（G0-1）。
5. **同族漏刷清零（CO-5）**——README:192 修正并纳入 docsync 交叉断言（至此基线表述全库有门）。
6. **可选**：板上 log 冒烟边界明示（CO-11 明示半）、板侧 `\n` 残留清理（CO-12）、bench 相对比值化（`CO-15(v2.6)`）、docsync 单一来源取值（D-9）。

### 1.3 明确不做什么（Non-goals）

- **不做**任何触碰既有签名的变更（`apidump --diff` 必须纯增；本版预期**新增 0 项 / 修改删除 0 项**）；`port.h` 捕获钩子（CO-11 实现半）属公开契约变更，**本版只评估不实现**；
- **不做** `data()` → `read_into()` 形态变更（`CO-9(v2.5)` 维持 v3 破坏性窗口）；
- **不做** `et_log` 浮点支持（`%f` 族维持"消费 + 可见占位"；`L` + 整数 = UB 的文档限定维持 v2.7 结论）；
- **不做** USART2 独立串口 / 共享口地址不符静默的板侧验证（`CO-11(v2.5)` **再次暂缓**，硬件阻塞：bench 仅一路 USB-TTL；触发 = 第二路 USB-TTL 或双板对连）；
- **不做**从站读帧 8 字节窗口的行为变更（CO-10 默认维持，除非 P2-2 分析翻转并满足 HC-6 同款论证）；
- **不做**新模块（本版无新增模块，模块数维持 **34**）；
- **不做** Modbus TCP/ASCII、主站轮询调度器与多从站表、biquad、32 位寄存器库级封装、四项破坏性候选（v2.5/v2.6/v2.7 结论维持）；
- **不做**本机 ASan 环境安装（需用户决策；触发 = 安装 WSL 或 MSYS2）;
- **不做**动态内存 / RTOS / 内部取时基 / i2c 抽象 / 签名启动 / 通配 / 多行编辑（**连续十八版**永久边界）。

---

## 2. 硬性约束（违反任一条即交付不合格）

- **HC-1 契约零破坏（基线滚动后字面可满足）**：`sh tools/apidump.sh --diff` 在 P0-2 完成后对 **v2.6 基线**必须"新增 0 项 / 修改删除 0 项"；**发版点**归档 v2.7 清单并把默认基线切至 v2.7 后，须对 **v2.7 基线**复跑一次并记录（两轮读数都入交付文档）。既有 API 语义与既有宏值不变。
- **HC-2 归档件必须真生成**：`docs/API_INVENTORY_v2.6.md` 与 v2.7 归档件必须用 `git worktree add --detach <dir> <tag>` 检出对应树 + **当前** `tools/apidump.sh` 生成（v2.7 交付 N-3 的教训：旧工具在 worktree 内产出 0 条目），附 sha256 与生成命令、环境注记；**禁止手工编辑归档件**；评审方须能按文档记载独立复现（沿 v2.7 AC-19 口径）。
- **HC-3 门改动自证可红**：P1-1（清单 SHA 断言）、P1-2（白名单判定）、P1-3（`tr -d '\r'` 的抗行尾验证）、P1-4（TMPD 告警 + 条目数守卫）各自给出**红证据**（构造违规输入 → 门红 → 还原 → 绿），证据入交付文档 §7 机制自证表。
- **HC-4 门改动零行为漂移**：门修复完成后，对 v2.7 终态的既有输入，`apidump --check` / `--diff` / `docref` / `docsync` 的判定结果与修复前一致（绿仍绿）；差异仅限新增的告警行/守卫拒绝路径。全量用例（497 量级）不回退。
- **HC-5 CO-7 恢复路径的安全前提（若走实现分支）**：① CRC 校验失败时**不得**按不可信的字节数域整帧前进（必须逐字节重同步）；② "长度域自洽"**不得单独**构成 `crc_err` 的同形判据（同形判据须仍以帧首地址 + 功能码为主，沿 v2.7 D-3 口径）；③ 须有用例固定"噪声 bc/qty 碰巧自洽 + CRC 坏 → 不整帧前进、不吞真请求"；④ 与 `CO-5(v2.6)` 修复判据（`et_modbus_master.c` 严判定长）并读后不得互相矛盾。
- **HC-6 库不变式**：零动态内存、多实例句柄化、`core/`+`algorithm/` 零硬件依赖、不内部取时基、不新增模块开关、不追加/改动既有冻结结构体字段；本版库内代码改动**仅限** P2-1 若走实现分支的 `protocol/et_modbus.c`（+ 用例），其余任务均为工具/文档/板侧。
- **HC-7 数字回刷同批**：docsync 断言数（304 → 新值）、README（结构树/质量门/checklist）、`architecture.md` 机制门行、`API_GUIDE`、`CHANGELOG`、`port/*/README` 体积表（sizecheck 逐值）与代码/门同批更新；基线名改动涉及的多处文档**同一提交**回刷。
- **HC-8 板侧落点必须写明库外路径**：板侧任何改动（P3-2）一律标注**库外私有文件**全路径（`D:\code\STM32CubeMX\G474VET6_ET_TEST\Core\Src\et_demo.c`、同步目录 `...\Core\et\`、走单脚本），禁止写成库内 `examples/`。
- **HC-9 真机证据与 tag 完整性**：P3-2 若做，板上复跑证据须在**不含 v2.8 之后代码**的提交上取得；`tag v2.8` = 交付终态 `HEAD`；编译通过不算验收。
- **HC-10 内存门**：`make test-asan` / `test-asan-1k` CI 绿（若 P2-1 走实现分支，新代码路径随之纳入）；本机不可执行维持显式声明，**不得声称本机已跑**。
- **HC-11 CO 一致性三落点**：本计划 §1.1 / `docs/v3-candidates.md`（新增「v2.8 复评记录」节）/ **交付文档 §8** 三处 CO 措辞一致（含 CO-7/CO-10 的"二选一"结论、CO-11/CO-12/CO-15(v2.6)/D-9 的可选处置）；交付文档缺 §8 即不合格。
- **HC-12 起草期核验清单六项**：`docs/开发计划模板.md` §7 扩至**六项**（新增⑥"基线滚动"，见 P0-4）后，本计划文末合规声明须按六项逐项自检；此后各版计划按六项起草。

---

## 3. 任务分解

### 3.0 前置门 G0：v2.7 随轮修订 + 评审记录入仓（纯文本，约 0.5 天）

> 评审 §6.1 执行提示原文："上述修订会使 `docref`（END==HEAD）转红——**须与评审记录入仓同一提交完成**，并在提交前重跑 `docref` 至 OK"。G0-1 的验收时点 = **提交前的工作树状态**（此时 `END_SHA == HEAD_SHA` 成立）；提交后 `docref` 转红属 `CO-6(v2.7)` 所述副作用，由 P1-2 的白名单式判定**根治**（该提交本身即 CO-6 的活体样本，须记入交付文档）。

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| G0-1 | **五处文本修订 + 评审记录入仓（同一提交）**：① v2.7 交付文档 §4 的 commit 清单 `838edb1` → `e7c79f7`，并注记"`838edb1`/`23c0d3f` 为修订前悬空提交、CI run `36157294575` 与 HEAD 差异面 = 纯本文档 12/4 行"（CO-1 文本半）；② §2 AC-20 与 §7.1 基线名统一为"v2.5（v2.6 归档缺失，见 `CO-2(v2.7)`）"（CO-2 文本半）；③ §7.3 复跑预期改为实测值（`proto/modbus pass=25 fail=8` + `debug/selftest` 2 例连锁红；`mb.unknown_fc_unicast_retained`/`mb.wr_truncated_then_completed` 标注"基线即绿（回归钉）"）（CO-3）；④ 分支计数 5618 → **5612**（CO-4）；⑤ §15.3 表头注明"MBSLAVE 读数为会话累计值，结论以增量表述"（CO-13）。同提交将 `v2.7评审记录__*.md` 入仓 | `v2.7开发交付__从站解析对称化与板上自测收口.md`（§2/§4/§7.3/§15.3）、`v2.7评审记录__从站解析对称化与板上自测收口.md`（入仓） | 纯文本；**提交前** `sh tools/docref.sh` 须 OK（HC-4 时点）；提交后门转红 = CO-6 实证，记录在案 | P0 |

### 3.1 P0 冻结面基线滚动（CO-2 ★ + CO-5，约 1 天）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P0-1 | **归档 v2.6 清单**：`git worktree add --detach build/wt-v26 v2.6` → 拷入**当前** `tools/apidump.sh` → 生成 → sha256 → 与 `docs/API_INVENTORY_v2.5.md` 比对（**预期差异**：v2.6 相对 v2.5 公开面 0 增改 + `ET_VERSION_MINOR` 等版本宏行；逐行列出）→ `worktree remove --force` | `docs/API_INVENTORY_v2.6.md`（新增归档）、交付文档（生成记录：命令 + sha256 + 差异说明） | `tag v2.6` = `2e57b16`（起草期实测在位）；sha256 须与评审 E7 手法可交叉复核；**预期差异若不为 0 增改** → 说明 v2.6 公开面有未登记变化，**立即停下报备**（HC-1 语义面） | P0 |
| P0-2 | **默认基线切至 v2.6 + 断言同批**：`tools/apidump.sh:218` 默认基线 → `docs/API_INVENTORY_v2.6.md`；`tools/docsync.sh:301/:374/:375` 的基线断言同批改指 v2.6 归档；复跑 `--diff` 对 v2.6 基线（HC-1 第一轮读数） | `tools/apidump.sh`（`:15` 注释、`:218`）、`tools/docsync.sh`（3 处）、交付文档（读数） | docsync 断言数变化 → 自指计数回刷（HC-7）；**v2.5 归档只读保留** | P0 |
| P0-3 | **README:192 修正 + 装门（CO-5）**：`README.md:192`"对 v2.0 基线" → 动态表述（"最近已发布 MINOR 的冻结基线（当前 `docs/API_INVENTORY_v2.6.md`）"）；docsync 既有"README 基线名 ↔ apidump 默认基线"交叉断言（`:467-470`）**扩展**覆盖"测试与质量门"小节的该行（提取规则与 checklist 第 8 条同构） | `README.md`（`:192`）、`tools/docsync.sh`（交叉断言扩展） | 至此**全库基线表述有门**；自证可红：把 `:192` 改回"v2.0" → docsync 红 | P0 |
| P0-4 | **模板第 6 项：基线滚动（CO-2 根因根治）**：`docs/开发计划模板.md` §7 增第 ⑥ 项——"**每版必须显式列出：① 发版点归档本版 API 清单（worktree 真生成 + sha256）；② `apidump` 默认基线切至本版归档；③ docsync 基线断言同批**。反例：v2.6/v2.7 连续两版未归档（v2.7 计划 HC-1 写'对 v2.6 基线'却无归档任务 → `CO-2(v2.7)`）"；`docs/评审记录模板.md` 核验清单同步引用 | `docs/开发计划模板.md`（§7）、`docs/评审记录模板.md` | 结构性根治；**本计划文末合规声明按六项自检**（HC-12） | P0 |
| P0-5 | **版本宏 2.8.0 + 发版点归档 v2.7**：`et_config.h` 升 2.8.0；**发版点**（tag v2.8 前）用同法归档 `docs/API_INVENTORY_v2.7.md`（worktree 于 `tag v2.7`）→ 默认基线切 v2.7 → `--diff` 对 v2.7 基线复跑（HC-1 第二轮读数）→ docsync 断言再同批 | `et_config.h`、`docs/API_INVENTORY_v2.7.md`（新增）、`tools/apidump.sh`、`tools/docsync.sh`、`CHANGELOG.md`、体积表 | **时序注意**：P0-1/P0-2 在 `dev/v2.8` 上先行（修 CO-2 滞留）；P0-5 的 v2.7 归档在发版点执行（§6 顺序规则）；两轮基线读数都入交付文档 | P0 |

### 3.2 P1 门空隙收口（CO-1 装门半 + CO-6 + CO-8 + CO-9，约 1 天）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P1-1 | **`docref` 清单 SHA 可达性断言（CO-1 装门半）**：解析交付文档 §4 的 commit 清单（v2.6 起为逐 SHA 列表形态），断言**每个 SHA 满足 `git merge-base --is-ancestor <sha> <END>`**（即 RANGE 可达提交）；不可达 → FAIL 并列出悬空 SHA。**自证可红**：清单临时写入悬空 SHA（如 `838edb1`）→ 红 | `tools/docref.sh`（清单解析段）、`docs/评审记录模板.md`（HC-5 核验项引用） | 封死 v2.7 `CO-1` 的门空隙；注意清单的**形态解析**要兼容"单提交"与"多提交"两种历史文档（旧文档不回溯校验，沿 docsync"历史文档引用不在校验范围"先例） | P0 |
| P1-2 | **`docref` END==HEAD → 白名单式判定（CO-6）**：**反滞后意图保留**——`<END>..HEAD` 间的提交若触及 **CHANGELOG.md 或交付文档本身**（且交付文档无对应修订登记）→ FAIL；只触及白名单外文件（实机记录/评审记录/下一版计划等）→ **OK**，但打印提示"`<END>` 后有 N 个提交未计入 diffstat（清单）"。三场景红绿矩阵 = AC-8。**设计难点与两个候选机制见可裁量项 4** | `tools/docref.sh`（END==HEAD 段 `:78-88` 迭代）、`docs/评审记录模板.md`、README checklist（若命令行为变化需注记） | 消除"门在仓库历史大部分时间设计性红"；**G0-1 修订提交是活体验证样本**（P1-2 完成后对含修订登记的文档复跑应绿）；自证可红见 AC-8 | P0 |
| P1-3 | **`item_lines` 显式抗行尾（CO-8）**：`item_lines()` 管道中显式加 `tr -d '\r'`（一行），把"GNU sed 剥 CR 的副作用"升级为契约；验证：人工构造 CRLF 与 LF 两个清单副本 → `item_lines` 产出**同条目集**（命令入交付文档） | `tools/apidump.sh`（`item_lines()`） | `--diff` 跨实现/跨环境稳定性；零行为变化（现有环境本来就绿） | P0 |
| P1-4 | **`apidump` 双守卫（CO-9）**：① Windows 分支 `mkdir -p "$TMPD_REL"` 失败退回 `mktemp -d` 时**打印显式告警**（提示可能回到"路径转义 → 提取为空"陷阱）；② 无参数生成（写 `docs/API_INVENTORY.md`）前加守卫：生成条目数 < 现有清单（或最近归档）条目数的**一半** → **拒绝写入** + exit 非 0 + 打印诊断。**自证可红**：构造空 `HDRS` 环境 → 0 条目 → 拒绝 | `tools/apidump.sh`（`:36-52` TMPD 段、写 `OUT` 前的守卫点） | 关闭"清单被覆盖成残缺版"的静默路径（v2.6 D-6 遗留建议清偿）；守卫阈值 ½ 沿交付 N-3 建议，可裁量（见可裁量项 6） | P0 |

### 3.3 P2 解析边界复评（CO-7 + CO-10，约 1 天，可能含库内代码改动）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P2-1 | **"双假设定长"恢复评估（CO-7，二选一）**：**分支 A（实现）**——`expected_len()` 的 `FC_WRITE_MULTIPLE` 分支恢复返回 `9+b[6]`（不再先验 `b[6]==2*qty`），**但** CRC 失败一律逐字节重同步（HC-5 全部四项前提）；补用例：① `bc/qty` 不自洽但 CRC 正确的真请求 → 回异常 0x03（能力恢复证据）；② 噪声 `bc/qty` 碰巧自洽 + CRC 坏 → 不整帧前进、不吞后续真请求（防重开口子）；③ `crc_err` 口径不回退（沿 D-3）。**分支 B（论证维持）**——交付文档记录论证：恢复收益（主站免一次超时） vs 风险（同形误判面扩大）与既有 33 例覆盖的判据，结论"维持 v2.7 行为"并记 v3 候选 | `protocol/et_modbus.c`（`:48` `expected_len`、`:121` `frame_is_expected_shape`）、`test/test_modbus.c`（分支 A：+≥3 例）、`docs/API_GUIDE.md`（5.7）、交付文档 | 分支 A 触碰 v2.7 D-2 裁决过的行为面 → 交付文档 §2 登记"能力恢复"；分支 B 零代码。**两分支都须在 §8 与 `v3-candidates.md` 登记结论**（HC-11） | P1 |
| P2-2 | **从站读帧 8 字节窗口裁决（CO-10，默认维持）**：把两种取舍的代价矩阵写入 `API_GUIDE` 5.7——现状（坏读帧按整帧 8B 丢弃，可能吃真请求前缀，主站超时重发兜底） vs 替代（读帧 CRC 失败逐字节重同步，坏帧残字节可能重组假帧 → **从站误执行写**，风险更高）；结论默认"维持现状"，写明"受害形态 = 前导 `[addr][读fc]` 2 字节伪造 + 主站超时重发可恢复，无静默错误"。**仅当**分析翻转让出替代方案时才改行为（须 HC-5 同款论证 + 用例） | `docs/API_GUIDE.md`（5.7）、交付文档 | 纯文档（默认分支）；把 N-4 的"已写明判据"升级为"已裁决并落档"，防下版重复评估 | P1 |

### 3.4 P3 可选（不阻塞发布；未做须显式声明 + 记下版）

| # | 描述 | 涉及文件 | 影响分析 | 优先级 |
|---|---|---|---|---|
| P3-1 | **板上 log 冒烟边界明示（CO-11 明示半，必做；钩子半可选评估）**：`API_GUIDE` 8.4 写明"板上 `log` 套件只验修饰面**生效性**（字符数），**不校字符内容**；内容级断言由 host 30 例承担"；可选：评估 `port.h` 增可选捕获钩子的代价（公开契约变更 → 结论记 v3 破坏性窗口候选，**不实现**） | `docs/API_GUIDE.md`（8.4）、交付文档（评估结论） | 明示半为 D-4 条件项；钩子半维持 v2.7 不可行性结论 | P2 |
| P3-2 | **板侧 `\n` 残留清理（CO-12，库外）**：按实机记录 §15.4 的一行修法清理板侧 demo 命令行残留 → 复跑从站走单（`AT+MBSLAVE` 等）确认 `disc=0` 基线 → 更新实机记录（`disc=1`"已知现象"登记改写为"已清除"及日期） | 板侧工程私有（库外）：`...\Core\Src\et_demo.c`；`移植stm32实机记录.md` | 库内零改动；板上证据时点受 HC-9 约束；**不做**须显式声明（保留现状也是合法选择——评审已认可"保留证据原样"） | P2 |
| P3-3 | **bench 相对比值化（`CO-15(v2.6)`，跨版第三次）**：相对比值（稳定基线操作 = 1.0）+ 预热 + 固定迭代；或再次显式声明"不做 + 理由 + 记 v2.9" | `tools/bench.c`、`docs/bench.md` | 口径变更须附新旧数值对照 | P2 |
| P3-4 | **docsync 字面量断言改"单一来源取值"（D-9）**：把随版本滚动的字面量断言（基线名等）改为从单一来源（如 `apidump.sh` 的 BASE 变量）提取比对，消除"改值须改多处"的耦合 | `tools/docsync.sh` | 沿 v2.6 跨文档计数门同构思路；改动须自证可红（HC-3） | P2 |

---

## 4. 验收标准（每条可客观核验，标注对应任务）

**前置门 G0**

- **AC-1**（G0-1）v2.7 交付文档：`grep -c "838edb1" 该文档` 在 §4 记录范围内为 **0**（仅存于修订注记的历史引述）；§4 清单含 `e7c79f7` 且注记段在案；§2 AC-20 与 §7.1 的基线名含"v2.5"与"v2.6 归档缺失"字样、互不一致的"v2.6 基线"终态表述为 0；§7.3 含 `pass=25 fail=8` 与两例"基线即绿（回归钉）"标注、"pass=23 fail=10" 为 0；分支计数 5612；§15.3 表头含"累计值"；`v2.7评审记录__*.md` 已被 git 跟踪。**提交前** `sh tools/docref.sh` → OK（时点证据入交付文档）。

**基线滚动**

- **AC-2**（P0-1，HC-2）`docs/API_INVENTORY_v2.6.md` 在案；交付文档载生成命令（`git worktree add --detach … v2.6` + 当前 `apidump.sh`）、sha256、与 v2.5 归档的逐行差异说明；评审方可按记载独立复现 sha256 一致；`git diff --stat` 显示 v2.0~v2.5 归档零改动。
- **AC-3**（P0-2，HC-1）`grep -n "API_INVENTORY_v2.6.md" tools/apidump.sh` 命中默认基线行（`:218`）；`sh tools/apidump.sh --diff` 对 v2.6 基线 → **新增 0 / 修改删除 0，exit 0**（读数 + 耗时入交付文档）；docsync 基线断言已同批（`grep -c "API_INVENTORY_v2.5" tools/docsync.sh` 的基线断言行 → 仅存于历史注记）。
- **AC-4**（P0-3，CO-5）`grep -c "对 v2.0 基线" README.md` = **0**（`README.md:192` 改为动态基线表述）；docsync 交叉断言覆盖该行，**自证可红**：把该行临时改回"v2.0" → docsync exit 1 且 FAIL 信息指向该行，还原后绿。
- **AC-5**（P0-4，HC-12）`docs/开发计划模板.md` §7 含**六项**（⑥"基线滚动"附 v2.6/v2.7 双版反例）；`docs/评审记录模板.md` 核验清单含基线滚动复核项；本计划文末合规声明为六项。
- **AC-6**（P0-5，HC-1）`grep ET_VERSION et_config.h` 得 `0x020800` 与 `"2.8.0"`；发版点 `docs/API_INVENTORY_v2.7.md` 在案（worktree 生成 + sha256）；`--diff` 对 **v2.7 基线** → 0/0 exit 0（第二轮读数在案）；`CHANGELOG.md` 含 `[2.8.0]` 节。
- **AC-7**（P1-1，HC-3）`docref` 对 v2.8 交付文档 §4 清单逐 SHA 校验通过；**自证可红**：清单临时写入悬空 SHA → FAIL 且列出该 SHA，还原后 OK。
- **AC-8**（P1-2，HC-3）**三场景红绿矩阵**（证据入交付文档 §7）：场景 A——`<END>..HEAD` 仅新增实机记录/评审记录类提交 → **OK**（打印未计入提示）；场景 B——临时 amend/构造触及 `CHANGELOG.md` 的提交 → **FAIL**；场景 C——触及交付文档且无修订登记 → **FAIL**、含修订登记（G0-1 型）→ **OK**。
- **AC-9**（P1-3，HC-3/HC-4）`item_lines` 含显式 `tr -d '\r'`；CRLF/LF 双副本产出同条目集（命令与输出在案）；`--check`/`--diff` 对 v2.7 终态判定结果与修复前一致。
- **AC-10**（P1-4，HC-3）TMPD 回退路径打印告警（代码 + 触发方式在案）；守卫自证可红：构造空 `HDRS` → 生成被拒绝、exit 非 0、现有清单**未被覆盖**；正常路径（433+ 条目）不受守卫影响。

**解析边界复评**

- **AC-11**（P2-1，CO-7）**二选一可核验**：分支 A——`test/test_modbus.c` 新增 ≥3 例（0x03 恢复 / 防重开 / `crc_err` 口径）且全绿，`mingw32-make test` 全量不回退，交付 §2 登记"能力恢复"；分支 B——交付文档含论证记录（收益/风险/判据三要素）且 §8 与 `v3-candidates.md` 登记"维持 + 记 v3 候选"。
- **AC-12**（P2-2，CO-10）`docs/API_GUIDE.md` 5.7 含读帧窗口的**代价矩阵与裁决结论**（两取舍的受害形态、恢复手段、为何维持）；若翻转行为则另附 HC-5 同款论证 + 用例。

**可选项与全量回归**

- **AC-13**（P3-1，CO-11）`docs/API_GUIDE.md` 8.4 含"板上 log 冒烟不校字符内容"边界明示；钩子评估结论（若做）记入 §8 / `v3-candidates.md`。
- **AC-14**（P3-2，CO-12）做了 → 实机记录载清理后 `disc=0` 基线复跑记录 + "已知现象"登记改写；不做 → 交付文档 §8 显式声明。
- **AC-15**（P3-3/P3-4）P3-3/P3-4：做了给证据（bench 口径变更 + 新旧对照；单一来源改造 + 自证可红）；未做在 §8 显式声明 + 记下版（禁静默略过）。
- **AC-16**（全版）四门读数：`docsync` `pass=N fail=0`（自指计数一致）、`apidump --check` exit 0、`--diff` 对 v2.7 基线 0/0、`sizecheck` `pass=3 fail=0`（v2.8 表行逐值）、`docref` OK（白名单式）；CI 全 job 绿（含 renode smoke `SELFTEST: 22/22`）。
- **AC-17**（全版，HC-4）`mingw32-make test`、`test-g4`、`test-tab`、`test-1k` 全绿（497/497/504/498 量级，P2-1 分支 A 则按新增用例顺延并逐值记录）；`make ex` 五例 PASS；`WERROR=1` 零诊断；覆盖率 ≥85% 记实测值；模块数维持 34。
- **AC-18**（HC-11）三落点一致：计划 §1.1 / `docs/v3-candidates.md`「v2.8 复评记录」/ 交付 §8，逐条比对差集为空（含 CO-7/CO-10 二选一结论、四个可选项的处置）。

**AC ↔ 冻结形状互洽检查（模板 §7 第④项，起草期已完成）**：本版库内代码改动**仅限** P2-1 分支 A 的 `protocol/et_modbus.c` 内部实现（不改任何签名/结构体/宏），故 AC-11 的断言对象（异常应答字节、统计字段、用例计数）与冻结形状**无冲突面**；其余任务全部落在工具脚本（`tools/*.sh` 非公开 API）、文档与归档件。**本版预期 `apidump --diff` 两轮读数均为新增 0 项 / 修改删除 0 项。**

---

## 5. 可裁量项（开发者可自主决策，其余不得偏离）

1. **P0-1/P0-5 的生成环境与目录**：worktree 目录名、sha256 记录格式、差异说明的粒度——前提：HC-2 全部满足（真生成、可独立复现、禁止手改）。
2. **P0-2/P0-5 的断言改法**：docsync 基线断言是"改字面量"还是"改为从 `apidump.sh` 提取"（后者与 P3-4 合流，若合流则 P3-4 标记"已并入 P0-2"）。
3. **P0-3 的提取规则**：docsync 交叉断言对 README `:192` 行的解析写法（与 checklist 第 8 条同构即可，措辞不限）。
4. **P1-2 白名单式的实现机制**（设计难点在此）：难点 = "修订交付文档的提交"本身无法预知自身 SHA。两个候选机制——**甲**：交付文档设"修订登记"段（列出修订提交 SHA），`<END>..HEAD` 触及交付文档时检查这些 SHA 是否已登记，已登记 → 放行；**乙**：触及交付文档一律 FAIL，但允许"修订轮"用 `docref --amend <sha>` 类参数显式把终点推进到修订提交（推进动作本身记入 §4 修订注记）。**任选其一或第三种，前提：AC-8 三场景矩阵成立 + 反滞后意图保留（CHANGELOG 滞后必红）+ G0 型评审授权修订可放行。**
5. **P2-1 的分支选择**（A 实现 / B 论证维持）——评审仅要求"评估不静默"；选 A 必须 HC-5 四项全过，选 B 必须三要素论证在案。**本计划不预设结论。**
6. **P1-4 守卫阈值**（½ 为建议值，可按归档件条目数分布调整为其他比例，须记录理由）与告警措辞。
7. **P3 各项的做/不做**（P3-1 明示半除外——该项必做）。
8. **文档章节位置与措辞**（5.7 代价矩阵的小节位置、§8 的排布）。
9. **G0 与 §3.1–§3.3 的执行顺序**：G0-1 必须最先（评审条件随轮落实）；P0-1/P0-2 可在 `dev/v2.8` 先行；P0-5 严格在发版点；P1-2 建议**先于** G0-1 的"提交后转红"期过长之前落地（缩短门红窗口），但不强制。

**不可偏离**：§1.1 的 CO 处置结论（尤其 **CO-2 纳入本版且模板第 6 项必落**、CO-7 二选一不得静默、CO-10 默认维持、CO-11 实现半不进本版、`CO-11(v2.5)` 再次暂缓）、HC-1…HC-12 全部、G0-1 与评审记录入仓同一提交的时序规则、归档件真生成纪律（HC-2）。

---

## 6. 基线

| 字段 | 内容 |
|---|---|
| **首选基线** | **`tag v2.7`**（= **`e7c79f7`**，起草期实测存在且 = `HEAD`；评审 E9 确认范围内唯一提交、无悬空残留） |
| **快照（动手前必打）** | `git tag snap-2.8-start <起点 commit>` ＋ `git push origin snap-2.8-start`（**不带 `v` 前缀**；`v*` 会命中 `release.yml` 的 `tags: [v*]`——该结论已由 v2.6 起草期 run `35378057777` 实测坐实，v2.7 D-1 又以探针 tag `snap-probe-nosideeffect` 二次实测"无 run / 无 Release"）。打完快照后复核：`gh run list --limit 3` 无新 run、`gh release list` 无新增 |
| **分支规则** | G0-1（修订 + 评审记录入仓）在 `main` 完成；库内/工具改动在 `dev/v2.8`；`tag v2.8` 必须在下一版代码合入 `main` **之前**定着 |
| **顺序规则** | ① G0-1（同一提交：五处修订 + 评审记录入仓；提交前 docref OK）→ ② 打 `snap-2.8-start` → ③ P0-1/P0-2（v2.6 归档 + 基线切换）与 P1 门收口在 `dev/v2.8` 推进 → ④ P2 复评 → ⑤ 全量回归 → ⑥ **发版点**：P0-5（v2.7 归档 + 基线再切 + 两轮读数齐）→ 交付文档 → `tag v2.8`。板不可用不影响本版主线（唯一板侧项 P3-2 为可选；若做，须在 P0-5 之前的提交上取证，HC-9） |
| **偏离申报** | 实际起点与上述不符时，须在交付文档首行声明；计划一经交付即冻结 |

---

## 附 A：里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 |
|---|---|---|
| G0 | 五处修订 + 评审记录入仓（同一提交，提交前 docref OK） | D+0.5 |
| M1 | v2.6 归档 + 默认基线切 v2.6 + README:192 装门 + 模板第 6 项 + 2.8.0 | D+1.5 |
| M2 | docref 两断言（清单可达 + 白名单式）+ apidump 双守卫 + `tr -d '\r'` | D+2.5 |
| M3 | CO-7 评估落定 + CO-10 裁决落档（5.7） | D+3.5 |
| M4 | 发版点 v2.7 归档 + 两轮基线读数 + 全量回归（四门 + CI）→ 交付文档（含 §8）→ **tag v2.8** | D+4.5 |

## 附 B：风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| **v2.6 归档生成结果 ≠ 预期**（与 v2.5 差异不止版本宏行） | 说明 v2.6 公开面有未登记变化 → HC-1 语义面存疑 | P0-1 明确"立即停下报备"；评审 E7 已证当前工具对 v2.5 树生成与归档逐字节一致，方法可信 |
| **白名单式判定设计过繁或误放行** | 门空隙换了形态而非收口 | 可裁量项 4 给两候选机制 + AC-8 三场景矩阵作为**行为判据**（不锁实现）；CHANGELOG 滞后必红是底线 |
| **P2-1 分支 A 重开"长度域被信任"口子** | 从站误写风险回归（v2.6 CO-5 复辟） | HC-5 四项前提全部强制；②号用例（噪声自洽 + CRC 坏不整帧前进）为硬门 |
| **守卫阈值误伤合法生成**（条目数自然波动） | 正常路径被拒 | 阈值可裁量（可裁量项 6）+ AC-10 含正常路径不受影响断言 |
| **G0-1 提交后 docref 转红窗口过长** | 门不可用期拉长（CO-6 原问题） | 可裁量项 9：P1-2 尽早落地；窗口期如实记录（本版本身就是 CO-6 的清偿版） |
| **两轮基线读数（v2.6 → v2.7）时序混乱** | HC-1 读数不可比 | P0-5 固定在发版点；§6 顺序规则 + 交付文档分轮记录 |
| 板档期 | 仅影响 P3-2（可选） | 不做则显式声明；本版主线无板侧硬依赖（与 v2.7 相比是重大减负） |
| docsync 断言数变化与自指计数互为前提 | 门自身难落地 | 沿 v2.1/v2.5/v2.6 三次验证的既有模式：先加断言 → 跑一次取实测 → 回刷文档 |

> 环境注记（沿既有记录）：Windows 侧 `make` → `mingw32-make`；`apidump.sh` 锁 `LC_ALL=C`；本机无 WSL/MSYS2、MinGW 无 `libasan`（起草期实测，与 v2.6/v2.7 声明一致）；`apidump --check`/`--diff` 本机分钟级（评审实测 5m35s / 5m25s）、`docsync` ~10 分钟，计划等待窗口按此预留；worktree 手法已在 v2.7 AC-19 / 评审 E7 双向验证（**必须用当前工具**，旧工具在 worktree 内产出 0 条目——交付 N-3 实测）。

---

> **起草期合规声明（`docs/开发计划模板.md` §7 六项自检；第 ⑥ 项为本版 P0-4 新增，本计划先行遵守）**：
> ① **内部 tag 命名**：快照一律 `snap-2.8-start`（不带 `v` 前缀）；"快照 tag 无副作用"**不写入本计划作为断言**——`v*` 触发 Release workflow 已由 v2.6 起草期实测（run `35378057777`）、`snap-*` 无副作用已由 v2.7 D-1 探针 tag 实测，本计划引用的是**既有实测记录**而非新的未实测断言，且 §6 写明打完快照后的复核命令。
> ② **"无副作用/不会触发"类断言**：本计划不含未经实测的此类断言。文中"exit 0""fail=0"等均为**待开发方实测**的目标值。起草期亲自实测的事实限于：`HEAD = tag v2.7 = e7c79f7`、`snap-2.7-start..HEAD` 仅含 `e7c79f7`、`docs/API_INVENTORY_v2.6.md` **不存在**、`tools/apidump.sh:218` 默认基线仍为 v2.5 归档、`tools/apidump.sh` 的 `item_lines()` **无**显式行尾处理、`tools/docref.sh:78-88` 的 `END==HEAD` 断言在位、`tools/docsync.sh:467-470` 交叉断言存在但未覆盖 `README.md:192`、`README.md:192` 仍写"对 v2.0 基线"、v2.7 交付文档 `:96` 仍列 `838edb1` 且 `:177` 仍写 `pass=23 fail=10`、`docs/开发计划模板.md` §7 现为五项、`et_modbus.c` 新实现落点（`:48/:90/:121/:289/:369-392`）。
> ③ **板侧落点标注**：P3-2 的板侧落点标注为**库外私有文件**并写全路径，未写成库内 `examples/`（HC-8）；本版主线无板侧硬依赖。
> ④ **AC ↔ 冻结 API 形状互洽**：已执行，结论见 §4 末段——库内改动仅限 P2-1 分支 A 的内部实现，**预期两轮 `--diff` 读数均为新增 0 项 / 修改删除 0 项**；无语义面登记项（v2.7 的 `crc_err`/`discarded` 口径维持，P2-1 分支 A 若实现亦不得回退 D-3 口径）。
> ⑤ **证据落点章节号 + 占位节清理**：实机记录落点指向**既有 §15**（v2.7 已建，P3-2 若做在其中追加小节，追加前先 `grep -n '^## \|^### '` 确认节号未占用）；`docs/v3-candidates.md` 指向**新增「v2.8 复评记录」节**；无"待补"类占位指向。
> ⑥ **基线滚动**（本版新增项）：本计划已显式列出 P0-1（归档 v2.6）、P0-5（发版点归档 v2.7 + 默认基线切至 v2.7）与 docsync 断言同批任务——**直接清偿 `CO-2(v2.7)` 指出的"连续两版漏列"**；此后各版计划按本项起草。
>
> **维护约定**：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v2.8 后本文档归档不再改动。计划一经交付开发即**冻结**，§1.1 的 CO 处置结论、HC-1…HC-12、§5 的"不可偏离"清单不得单方修改。
