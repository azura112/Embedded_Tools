# v2.1 开发计划__定点控制与治理收口

> 起草日期：2026-09-09 ｜ 基线：v2.0（tag `6bb4f48`，API freeze 生效）｜ 状态：✅ 已交付归档（v2.1.0，2026-09-11；P3-1 et_bytes 评审判"不冗余"→实现，见交付文档 §2）
> 上一版本：[v2.0] 27 模块 / 345 用例 × 2 几何 / API 冻结 + 构建单一来源 + sizecheck/apidump 双门
> 输入：v2.0 验收发现（交付文档 docsync 计数 142 实测 143——同族问题第三次，自指盲区）；v2.0 交付已知后续（CubeMX 板侧停 v1.9 待同步）；`docs/v3-candidates.md`（无到期义务）；库环路缺口（有 filter/spwm 无控制器）

---

## 1. 版本定位

- **版本号 v2.1**：**冻结后的首个 MINOR**，全程只增不改——本版把"冻结不虚"变成可机检事实。
- **本版主题**：**定点控制补齐 + 治理收口**——交付定点 PID 控制器与运行统计（补上"测量→滤波→控制→输出"环路的最后两块），并以 apidump 基线 diff 机制化证明零破坏；顺手关闭 v2.0 验收发现的自指盲区、把板侧拉回最新验证态。
- **不变式**（继续遵守）：零动态内存、多实例句柄化、core/algorithm 层零硬件依赖、ISR-safe 范围显式标注、**MINOR 只追加**（API_STABILITY 契约）。

## 2. v2.0 现状盘点

| 项 | 状态 |
|---|---|
| 模块 | 27 个，345 用例 × 2 几何（1K 346 / test-tab 8），覆盖 96.9%，docsync 143，sizecheck 3/3，apidump 清单 341 项锁 |
| 契约 | v2.0 起冻结：MINOR 只增，破坏性变更走 `ET_DEPRECATED` → ≥2 MINOR → MAJOR；`docs/v3-candidates.md` 存 4 项破坏性候选（无到期义务） |
| 环路缺口 | 有 `et_filter`（平滑/限幅）与 `et_spwm`（执行输出），**无控制器**——闭环需用户手写 PID；有 `et_log`/`et_selftest`，**无运行统计**（min/max/均值/方差） |
| 治理遗留 | ① 交付文档 docsync 计数无门（142 vs 实测 143，同族第三次）② CubeMX 板侧停 v1.9，未同步 v2.0（新开关默认关=零变化） |
| 机制资产 | docbuild/apidump/sizecheck/docsync 四门可用——本版新增机制应优先复用它们 |

## 3. 开发范围

### P0 治理收口与板侧同步（约 0.5 天，M1 先行）

| # | 任务 | 验收标准 |
|---|---|---|
| 1 | **apidump 基线存档与 diff 机制**：`sh tools/apidump.sh --snapshot docs/API_INVENTORY_v2.0.md` 固化冻结基线；新增 `--diff`（新增行=OK；**删除/修改签名行=红**；结构体追加字段需在 diff 说明区登记） | `--diff` 对故意改签名变红、对纯新增变绿（自证）；入 docsync 与发布 checklist |
| 2 | **docsync 自指断言**：交付文档中出现的 docsync 计数必须等于本轮实测值（关闭 v2.0 验收发现的自指盲区，同族第三例终局） | 故意写错计数→红；自证记录入交付文档 |
| 3 | **CubeMX 板侧同步 v2.0**：`Core/et/` 重拷 + 新开关（`ET_MODULE_SMAP`/`SHELL_TAB`）默认关验证 + 板上 `AT+SELFTEST` 与升级链烟测 | 板侧回最新验证态并记录；同步方法按 `Core/et/README.md` |
| 4 | 版本宏升至 2.1.0 | 发布时与 tag 核对一致 |

### P1 `et_pid` —— algorithm/ 定点 PID 控制器（★ 核心，1.5 人日）

补齐"测量→滤波→控制→输出"闭环。**定点实现**（与 et_filter 的 Q15 一脉相承，库无浮点定位不变）：

```c
typedef struct {
    int32_t kp, ki, kd;        /* Q15 增益 (32768 = 1.0) */
    int32_t out_min, out_max;  /* 输出限幅(与被控量同标度) */
    int32_t i_min,  i_max;     /* 积分限幅(抗饱和) */
    uint8_t d_on_measure;      /* 1=微分作用于测量值(免除设定值跳变冲击) */
} et_pid_cfg_t;

bool et_pid_init(et_pid_t *p, const et_pid_cfg_t *cfg);
void et_pid_set_gains(et_pid_t *p, int32_t kp, int32_t ki, int32_t kd);
void et_pid_reset(et_pid_t *p);                            /* 清积分/微分历史 */
int32_t et_pid_step(et_pid_t *p, int32_t sp, int32_t pv, uint32_t dt_ms);
```

**默认决议**（评审时间盒 0.5 天）：位置式；积分与微分项 **int64 中间累加防溢出**（溢出语义文档化）；抗饱和 = 积分限幅（钳位法），不做反算回灌；微分作用于测量值可选（默认开）；`dt_ms` 由调用方给（复用 stimer/sched 时基，不内部取时基）；非 ISR-safe（🏠MAIN 单上下文，与 mempool 同级标注）。

- 单测 ≥ 14 例：阶跃响应数值（手算序列）、积分限幅（持续误差不饱和失控）、输出钳位、d-on-measure 无设定值冲击 vs 关断对照、增益为 0/满量程边界、dt 变化（相同增益不同采样）、reset 语义、多实例、int64 溢出防护边界、被控量=设定值稳态（输出归零）
- 文档：API_GUIDE 新章 + **整定配方**（与 et_filter 平滑、et_spwm 输出的三段式链路图 + Ziegler-Nichols 定性说明）；bench 行（step 耗时）

### P2 `et_stats` —— algorithm/ 运行统计（约 1 天）

调试与整定配套：定长窗口的运行统计，零分配、定点化。

```c
bool et_stats_init(et_stats_t *s);                          /* 清零 */
void et_stats_push(et_stats_t *s, int32_t v);               /* 流式喂入 */
uint32_t et_stats_count(const et_stats_t *s);
int32_t  et_stats_min/max/mean(const et_stats_t *s);        /* 定点/整数 */
int32_t  et_stats_var_q10(const et_stats_t *s);             /* 方差(Q10), Welford 整数版 */
void et_stats_reset(et_stats_t *s);
```

- Welford 增量算法整数版（避免 Σx² 溢出与大数相减失真）；方差以 Q10 定点输出，文档给标度换算；
- 与 et_pid 联动配方：`AT+TUNE` 场景 = stats 采集被控量 → 判稳（方差阈值）→ 记录超调；
- 单测 ≥ 12 例：与双精度参照对拍（整数集/大步长/负值）、单样本、空集、方差为 0/恒定输入、reset、极值（INT32_MIN/MAX 输入不溢出）、多实例

### P3 可选（时间盒 0.5 天，不阻塞发布）

| # | 任务 | 说明 |
|---|---|---|
| 1 | `et_bytes` 打包解包（protocol/ 或 core/ 小件） | BE/LE u16/u32 读写 + 边界检查，服务于帧/传输场景；确认不属于已有模块（et_frame 组帧只管自身协议）→ 若评审判冗余则关闭 |
| 2 | `docs/getting-started.md` 端到端教程 | 从零到板上：裁剪 → 移植（`port/_template`）→ 单测 → selftest → 串口调试，一步一命令 |
| 3 | bench v2.1 行 | `et_pid_step` / `et_stats_push` 耗时与 v2.0 基线并列 |

## 4. 交付标准（DoD）

1. 沿用 v2.0 全部 DoD（docsync / sizecheck / 双几何 / docbuild 单源 / 量化声明附环境注记）；
2. **冻结证明（本版核心项）**：`apidump --diff` 对 v2.0 基线必须**纯新增**，任何删除/签名修改即红——交付文档附 diff 摘要（新增 N 项，修改 0 项）；
3. 自指断言生效：交付文档自身计数与实测一致（含 docsync 计数，机制保证而非人工）；
4. 板侧回最新验证态：CubeMX 同步 + 板上 selftest/升级链烟测记录；
5. 新模块均定点实现（无浮点依赖），Q 标度文档化。

## 5. 里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 |
|---|---|---|
| M1 | P0 全部（基线快照/diff 门/自指断言/板同步/2.1.0） | D+1 |
| M2 | `et_pid` 合入（含整定配方） | D+2.5 |
| M3 | `et_stats` 合入 | D+3.5 |
| M4 | P3 时间盒 + 文档/bench 同步 | D+4 |
| M5 | 全量回归 + **apidump 纯增证明** → **tag v2.1** | D+4.5 |

## 6. 明确不做（Non-goals）

- **不做任何破坏性变更**：`docs/v3-candidates.md` 四项（回调 user 位/foreach 形状/动词命名等）维持"需求触发才评"，本版不碰；
- 不做浮点 PID / 浮点统计（定点为库定位；浮点变体属 v3 候选，需先看真实需求）；
- 不做 FOC/换相/电机专用算法（超库定位）；不做自动整定（只给配方与定性方法）；
- 不做 i2c/spi 抽象、RTOS、安全启动、动态内存、多行编辑（永久边界，连续七版）；
- 不做 smap 通配、u32 全键域（v2.0 终局关闭，维持）。

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 定点 PID 溢出/比例失真 | 板上行为异常且难查 | int64 中间累加 + 用例覆盖饱和边界；Q15 标度与"增益上限=被控量标度"换算表入文档；先 host 用 `et_stats` 对拍响应曲线再上板 |
| 微分项噪声放大 | 板上抖动 | 文档给出"PID 前串 et_lpf1"的标准链路（本库既有模块正好配套）；d-on-measure 默认开 |
| `apidump --diff` 对结构体追加字段判定过严/过松 | 冻结证明失真或误红 | diff 规则明确：`et_*_t` 字段追加单列白名单区（需说明理由），删除/改签名一律红；规则入 API_STABILITY 附则 |
| 板侧同步引入回归（v1.9→v2.0 跨两版） | 板上异常 | 新开关默认关=零变化为第一道防线；同步后跑 selftest + 升级链烟测；异常按"库/板侧/接线"三分法定位 |
| docsync 自指断言与"文档归档冻结"冲突 | 历史文档被反复扫改 | 断言只校验**当前版本交付文档**（按版本号匹配），历史文档不追溯 |
| et_bytes 与现有模块职责重叠 | 冗余模块 | P3 评审判冗余即关闭（不为凑数建模块），结论入 v3-candidates 备查 |

---

> 维护约定：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v2.1 后本文档归档不再改动。
