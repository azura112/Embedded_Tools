# v2.4 开发计划__Modbus 从站落地与板侧回归

> 起草日期：2026-09-12 ｜ 基线：v2.3（tag `v2.3` → commit `8c12579`，Release Latest）｜ 状态：✅ 已交付归档（v2.4.0，2026-09-12；biquad 维持排队 v2.5 复评；板侧分流方案偏差与已知限制见交付文档 §5/§8）
> 上一版本：[v2.4 前身 v2.3] 32 模块 / 409 用例 × 2 几何 / 三例自检配方进 CI / apidump 纯增 11/0
> 输入：v2.3 §8.2 素材池（#8 定点 biquad 评估 + 四项破坏性候选）；协议层现状盘点——crc/frame/atcmd/xmodem/bytes 全是**通用零件**，无标准应用层协议；v2.2 已铺好的 CRC16-MODBUS 查表 + `et_frame` 解析范式与 ringbuf/event 的 ISR→主循环通路

---

## 1. 版本定位

- **版本号 v2.4**：冻结契约下纯增量 MINOR——新增 Modbus RTU 从站模块 + 示例/工具/板侧走单，无破坏性变更。
- **本版主题**：**给协议层装上第一个标准应用协议**——`et_modbus` RTU 从站（复用 CRC16-MODBUS 查表、字节流状态机范式、ringbuf 通路），配套自检示例与 python 主站工具，板上真机走单；biquad 立项条件判定（预期：维持排队，理由记录在案）。
- **不变式**（继续遵守）：零动态内存、多实例句柄化、core/algorithm 层零硬件依赖、ISR-safe 范围显式标注、MINOR 只追加。

## 2. v2.3 现状盘点

| 项 | 状态 |
|---|---|
| 模块 | 32 个，409 用例 × 2 几何（1K 410），覆盖 97.0%，docsync 237，sizecheck 3/3，CI 七 job 全绿（含 `make ex`） |
| 协议层 | crc（含 MODBUS 查表）/frame/atcmd/xmodem rx+tx/bytes——**通用零件齐备，缺标准应用协议**；工业现场最常见的 Modbus RTU 需用户自行拼装 |
| 配方资产 | 三例自检示例 + `make ex` + API_GUIDE 11.x 互链——**新模块配示例即可入 CI 守护**（本版沿此范式） |
| 板侧 | 停在 v2.2 验证态（v2.3 刻意无板依赖）；双平台同步链路与自动化烧录/串口工具链已就绪（pyserial/CubeProgrammer CLI） |
| 候选池 | #8 定点 biquad（立项条件：真实 IIR 频响需求 + 两级链不够用）——**至今无需求证据**；四项破坏性候选维持需求触发 |

## 3. 开发范围

### P0 治理常规与候选判定（约 0.5 天）

| # | 任务 | 验收标准 |
|---|---|---|
| 1 | apidump 基线滚动至 v2.3（395 项比较单元） | 对 v2.3 基线 0/0 绿；v2.2 归档保留 |
| 2 | **biquad 立项判定（记录决议，不实现）**：需求证据评估（库内两个 IIR 形态需求是否已被 lpf1 覆盖；陷波/高通是否出现真实用例） | 判定结论入 `v3-candidates.md` #8（本版预期：**维持排队，v2.5 复评**——条件未触发则不消耗版本容量） |
| 3 | 版本宏升至 2.4.0 | 发布时与 tag 核对一致 |

### P1 `et_modbus` —— protocol/ Modbus RTU 从站（第 33 模块，★ 核心，约 2 人日）

**字节流喂入 + 主循环处理**（与 `et_frame`/`et_xmodem` 同范式：ISR 只入 ringbuf，主循环喂处理）。

```c
typedef struct {
    uint8_t  slave_addr;            /* 1~247; 0=广播接受 */
    void    *user;
    /* 寄存器映射: 不透明区间 + 读写钩子; 钩子按异常码语义返回 */
    et_modbus_rd_fn rd;             /* 读 holding/input, 可返回异常 */
    et_modbus_wr_fn wr;             /* 写 single/multiple */
} et_modbus_cfg_t;

bool et_modbus_init(et_modbus_t *mb, const et_modbus_cfg_t *cfg, uint8_t *rxbuf, uint32_t rxcap);
uint32_t et_modbus_feed(et_modbus_t *mb, const uint8_t *data, uint32_t len);  /* 返回应答帧数 */
const uint8_t *et_modbus_response(const et_modbus_t *mb, uint32_t *len);     /* 待发应答(含 CRC) */
void et_modbus_tick(et_modbus_t *mb, uint32_t now_ms);                        /* 帧间静默超时复位 */
et_modbus_stats_t 只读统计: 帧数/异常数/CRC 错/地址不符/丢弃
```

**默认决议**（评审时间盒 0.5 天）：

- **功能码**：`0x03/0x04`（读保持/输入寄存器）、`0x06/0x10`（写单/多寄存器）——工业现场覆盖 90% 场景；其余回异常 `0x01 非法功能`；
- **异常码**：`0x01` 非法功能 / `0x02` 非法地址（寄存器区间外） / `0x03` 非法数据值（数量越界、字节数不符）——由映射钩子返回或框架判定；
- **帧判定**：长度域驱动（从机地址 + 功能码 + 数据 + CRC16-MODBUS **低前高后**——与帧层 MODBUS 约定一致），`rxcap` 超限丢弃并计数；**帧间 3.5 字符静默**由调用方按波特率换算后经 `tick(now)` 注入（不内部取时基，沿用库惯例）；
- **广播**（地址 0）：执行写、**不应答**；读请求广播忽略；
- 应答缓冲由调用方持有（`rxbuf` 兼作 tx 缓冲或独立 `txbuf`，两选一在评审定）；零分配、🏠MAIN；
- **寄存器映射**：不透明区间表 + `rd/wr` 钩子——值域语义（保持/输入寄存器、32 位双寄存器组合）由应用层定义，库只做协议与访问控制。

- 单测 ≥ 20 例：0x03/0x04/0x06/0x10 正常流、CRC 坏静默、地址不符静默、异常 0x01/0x02/0x03、广播写不应答、广播读忽略、分片喂入、粘包两帧、超长帧丢弃并重同步、数量边界（0/125/126）、`tick` 静默复位、统计计数、多实例
- **示例**：`examples/ex_modbus_slave.c`（脚本化主站帧序列 → 从站应答逐字节比对，含异常路径；自检式，进 `make ex`）
- **工具**：`tools/modbus_master.py`（pyserial；`--read/--write/--addr/--port`；回环自测模式与串口模式）——板侧走单不依赖串口助手手工操作（沿 xmodem_send.py 范式）
- **文档**：API_GUIDE 新章（协议表/API/异常码表/寄存器映射设计指引）+ 配方（**kv 参数暴露为保持寄存器**：`rd/wr` 钩子直通 et_kv，掉电参数直接可被上位机读写）+ architecture.md 选型表/数据流更新

### P2 板侧回归与真机走单（约 1 人日，G474）

| # | 任务 | 验收 |
|---|---|---|
| 1 | 板侧同步至 v2.4（跨 v2.2→v2.4） | `diff -rq` 校验 + 0 warning + FLASH 增量记录 |
| 2 | demo 挂 Modbus 从站：USART1 复用（shell 与 Modbus 共存分流：帧首字节判定/或独立 USART2） | 方案评审预填：**USART2 独立**（避免与 shell 争用，板上有闲置串口） |
| 3 | **真机走单**：`tools/modbus_master.py` 经 CH343 读/写寄存器，实测 0x03 读、0x06/0x10 写、异常码、CRC 坏帧静默 | 实机记录新增章节（逐命令请求/应答字节 + 耗时）；kv 参数经 Modbus 读写的掉电验证 |
| 4 | Renode smoke 回归（既有断言不受影响） | 20/20 维持；若 selftest 不动则断言行零改动（本版预期） |

### P3 可选（时间盒 0.5 天，不阻塞发布）

| # | 任务 | 说明 |
|---|---|---|
| 1 | bench v2.4 行 | modbus 帧处理吞吐（帧/s） |
| 2 | `et_modbus` selftest 套件 | 20→21 需**数字回刷全链**（smoke 断言/README/API_GUIDE/实机记录/体积表）——若时间盒与回刷清单冲突则本版不做，记 v2.5 |
| 3 | Modbus 主站模式 | **不做**，记 v2.5 候选（从站优先；主站涉及轮询调度协同，单独立项） |

## 4. 交付标准（DoD）

1. 沿用 v2.3 全部 DoD（docsync 全绿 / 双几何 / docbuild / sizecheck / `--diff` 纯增门 / `make ex` 进 CI / 量化声明附环境注记）；
2. **示例即证据**：`ex_modbus_slave` 覆盖正常+异常全路径并进 `make ex`；断言仅确定性数值（字节级比对）；
3. **真机走单记录**：modbus_master.py 工具 + 串口实测字节级记录（板侧真验证，非仅编译）；
4. 数字回刷：docsync 计数、examples 计数、README 特性表/结构树、architecture 选型表四处同批；
5. biquad 判定决议落档（`v3-candidates.md` #8），候选池状态与决议一致。

## 5. 里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 |
|---|---|---|
| M1 | P0 基线滚动 + biquad 判定 + 2.4.0 | D+0.5 |
| M2 | `et_modbus` 合入（协议 + 20 例 + 异常矩阵） | D+2.5 |
| M3 | `ex_modbus_slave` + `make ex` + `tools/modbus_master.py` 回环自测 | D+3.5 |
| M4 | 板侧同步 + USART2 接入 + 真机走单记录 | D+4.5 |
| M5 | 全量回归（CI 实跑）→ **tag v2.4** | D+5 |

## 6. 明确不做（Non-goals）

- 不做 Modbus TCP / ASCII / 主站模式（主站记 v2.5 候选；TCP 超库定位）；
- **不做 biquad 实现**（P0-2 判定：条件未触发则维持排队，不为"素材池有项"而实现）；
- 不做破坏性变更（四项候选维持需求触发）；不做浮点/动态内存/RTOS/i2c 抽象/签名启动/通配/多行编辑（永久边界，连续十三版）；
- 不做寄存器值域语义（32 位组合/浮点寄存器）的库级封装——由应用钩子决定（文档给两种组合配方）；
- selftest 维持 20（P3-2 若做需回刷，否则记 v2.5）。

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 3.5 字符静默与波特率耦合 | 帧边界误判/粘包 | 由调用方按波特率换算注入 `tick(now)`（公式入文档）；host 用虚拟时间全速测粘包/分片矩阵；板侧真机走单验证 |
| 寄存器映射回调设计争议（不透明区间 vs 值域语义） | 接口返工 | 预填决议：库管协议与访问控制，值域由应用；时间盒 0.5 天到期按默认执行并记录异议 |
| Modbus 与 shell 争用同一串口 | 板侧演示不可用 | 预填决议：**独立 USART2**（板上闲置）；文档给"帧首字节分流"的共用方案备选 |
| 示例/工具的字节级断言脆弱 | CI 偶发红 | 断言只对确定性字节序列；python 工具先回环自测再上串口（沿 xmodem_send.py 范式） |
| 板档期 | M4 真机走单受阻 | P1~P3 全部库内项先合入；走单项如实挂账（沿既有模式），不阻塞 tag 之外的进度 |
| 范围蔓延（主站/值域封装诱惑） | 时间盒失守 | Non-goals 显式排除；主站与 biquad 均记 v2.5 候选 |

---

> 维护约定：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v2.4 后本文档归档不再改动。
