# v1.3 开发计划：平台实测闭环与 et_fsm

> 起草日期：2026-09-03 ｜ 基线：v1.2（tag `b2ab3cc`）｜ 状态：草案
> 上一版本：[v1.2] 18 模块 / 191 用例全过 / 行覆盖 96.9% / CI 覆盖率门 + 发布工作流
> 输入：v1.2 交付挂账清单（Renode / 真机）；v1.2 验收发现（README.md 未同步，详见 §2）

---

## 1. 版本定位

- **版本号 v1.3**：新增 et_fsm + 清账收口，无破坏性变更（MINOR）。既有 18 模块 API 一律不动。
- **本版主题**：把悬了两版的"真机可用"从**声明变成证据**——Renode 仿真升级为 CI 门永久闭环，同时交付 v1.2 计划已预告的轻量状态机 `et_fsm`。
- **不变式**（继续遵守）：零动态内存、多实例句柄化、core/algorithm 层零硬件依赖、ISR-safe 范围显式标注。

## 2. v1.2 现状盘点（含 v1.2 验收实测发现）

| 项 | 状态 |
|---|---|
| 模块 | 18 个（core 4 / algorithm 1 / sys 4 / protocol 3 / storage 1 / drivers 3 / debug 2），191 用例全过 |
| CI | 单测矩阵 + 覆盖率门（≥85%）+ STM32 交叉编译 + tag 发布工作流 |
| 遗留 ① | **README.md 停在 v1.1**（验收实测：版本行仍 v1.1.0，全文无 et_kv/et_softclock/storage 字样，最后改动为 v1.1 提交 `472a2a9`；而 v1.2 交付文档 §4 声称已同步）——文档同步存在"声称与实际脱节"，需制度性防再犯 |
| 遗留 ② | Renode/QEMU 仿真未跑（v1.1 起挂账，v1.2 因本机未安装 Renode 顺延） |
| 遗留 ③ | F103 真机实测未做（无硬件，挂账已跨 v1.1→v1.2 两版） |
| 遗留 ④ | `exp/kv` 分支已完成使命（提案与决议均合入 main），分支本体未删 |

## 3. 开发范围

### P0 一致性收口（约 0.5 天）

| # | 任务 | 验收标准 |
|---|---|---|
| 1 | README.md 补齐至 v1.2 实际状态：版本行 v1.2.0、特性表补 et_kv 与 et_softclock 两行、目录树加 `storage/`、快速开始/Makefile 注释补 `-Istorage`、使用速查补 KV 掉电保存示例、质量门控（覆盖率/发布流）说明 | `grep et_kv` / `et_softclock` / `storage` / `1.2.0` 均有命中，版本行与 tag 一致 |
| 2 | 建立发布 checklist（写入 README 开发者小节或 CONTRIBUTING）：**交付文档中每一项"已完成"声明必须附可复现命令或文件位置**；文档同步类声明逐文件核对 | checklist 成为 release.yml 触发前的说明性门 |
| 3 | `et_config.h` 版本宏升至 1.3.0；删除已合并的 `exp/kv` 分支 | 发布时宏与 tag 核对一致 |

### P1 平台实测闭环（★ 本版核心，1~2 天）

| # | 任务 | 说明 |
|---|---|---|
| 1 | 本机安装 Renode（官方包或 dotnet tool），headless 跑通 `build/stm32f103_demo.elf` | 验收断言取串口日志：心跳行、`kv: seq=… free=… rec=… key=…`、重启计数 +1 写回后复位复读一致 |
| 2 | **CI 新增 Renode job** | ubuntu runner 安装 Renode → 运行 demo ELF → 断言上述关键日志行。仿真验收从"本地截图"升级为 CI 常设门，v1.1 以来的仿真挂账就此**永久闭环**（port README 中 Renode 参考命令已有，直接引用） |
| 3 | 真机实测 | 硬件到位：烧录 + 串口实测记录归档 `port/stm32f103/README.md` 平台表；**仍未到位则启用新政策**：Renode CI 门作为 F103 的功能验收线，真机记录转为常设挂账（表中状态"待硬件，不阻塞发布"），不再随版本顺延阻塞 |

**Renode job 验收**：连续 3 次 CI run 稳定绿（防抖动）后才算闭环；失败时保留完整串口日志 artifact。

### P2 新模块 `et_fsm` —— algorithm/ 表驱动状态机（约 1 天）

v1.2 计划 P3 已预告的 v1.3 候选，落位 algorithm/（纯逻辑层，禁 port.h，PC 可全测）。

```c
typedef uint16_t et_fsm_state_t;
typedef uint16_t et_fsm_event_t;

typedef struct {
    et_fsm_event_t  event;
    et_fsm_state_t  next;
    bool          (*guard)(void *user);   /* 可为 NULL=恒真 */
    void          (*action)(void *user);  /* 可为 NULL=无动作 */
} et_fsm_trans_t;

bool et_fsm_init(et_fsm_t *f, et_fsm_state_t init,
                 const et_fsm_trans_t *table, uint32_t count, void *user);
bool et_fsm_dispatch(et_fsm_t *f, et_fsm_event_t ev);  /* 事件被消费返回 true */
et_fsm_state_t et_fsm_state(const et_fsm_t *f);
```

**语义默认决议**（评审时间盒 0.5 天，确认或否决）：

- 表序扫描：首个 `event` 匹配且 guard 通过（或缺省）的迁移生效；guard 失败**继续向后扫**（允许同事件的回退迁移）；
- 无匹配迁移 → 事件被忽略，dispatch 返回 false（应用可据此做日志/默认处理）；
- 自迁移（next == 当前态）合法，action 照常执行；entry/exit 动作不做独立钩子，由应用在 action 内组合；
- 迁移表由调用方以 `const` 提供（可驻 flash），运行期不可变——这是"零分配 + 表可放 flash"的关键约束。

- 单测 ≥ 14 例：首匹配顺序、guard 回退链、自迁移、未知事件忽略、guard 全拒、重复 init 防护、空表/边界、状态查询只读性、表驻 const 段验证

### P3 工程化（可选，不阻塞发布）

| # | 任务 | 说明 |
|---|---|---|
| 1 | release.yml 前置 Renode smoke | tag 发布前先跑一次仿真门，失败不出版本 |
| 2 | 覆盖率门槛维持 85% | et_fsm 合入后整体不得低于现基线 96.9% − 2pt |

## 4. 交付标准（DoD）

1. 沿用 v1.2 全部 DoD（.c/.h 成对 + 上下文标注 + 开关 `#ifndef` 烟测 + 套件注册 + 零警告零 malloc）；
2. **et_fsm 附加**：头文件附 ASCII 状态图示例；迁移表 `const` 完整性（API 不提供任何改表入口）；
3. **README 收口即验收**：P0 表格逐条 grep 验证，杜绝"声称已同步"；
4. CI Renode job 连续 3 绿；交付文档中的量化声明（用例数/覆盖率/体积）全部附复现命令。

## 5. 里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 |
|---|---|---|
| M1 | P0 一致性收口合入 main | D+0.5 |
| M2 | `et_fsm` 合入（含文档同步） | D+2 |
| M3 | Renode 本机跑通 + CI job 3 连绿 | D+3 |
| M4 | 真机实测或政策归档 → 全量回归 → **tag v1.3** | D+4 |

（清账版本，节奏较 v1.1/v1.2 短；P3 可选项不计入关键路径。）

## 6. 明确不做（Non-goals）

- 不做层次状态机（HSM）/并行态/事件队列内置——et_fsm 定位是"扁平表驱动"轻量件，复杂状态编排由应用组合；
- 不做磨损均衡 v2 / 加密 / 压缩（沿 v1.2 Non-goals）；
- 不改既有 18 模块公开 API 签名与语义；RTOS 适配依旧不做；
- 不为迁就 Renode 修改库代码——仿真与真机的行为差异只记录在案，不引入条件编译。

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| Renode 对 F103 flash 擦写时序建模不完整 | 仿真中擦除瞬时完成，kv 时序行为失真 | CI 断言只覆盖**功能日志**（kv 记录正确性），明确不测时序；时序项沿用"以真机为准"标注 |
| Renode CI job 抖动（启动/串口超时） | 门禁不稳 | 充足超时 + 一次重试；失败保留日志 artifact 便于排查 |
| 真机持续缺位 | 实测记录永远缺席 | 政策切换：Renode CI 门为功能验收线，真机转常设挂账不阻塞发布（P1-3 已内置） |
| et_fsm guard 语义争议 | 返工 | 默认决议先行 + 时间盒 0.5 天，未决按默认执行并记录异议 |
| 发布 checklist 流于形式 | 再次出现"声称与实际脱节" | checklist 要求每项声明附复现命令/文件路径；验收按 §4-3 逐条 grep |

---

> 维护约定：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v1.3 后本文档归档不再改动。
