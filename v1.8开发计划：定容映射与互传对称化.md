# v1.8 开发计划：定容映射与互传对称化

> 起草日期：2026-09-06 ｜ 基线：v1.7（HEAD `64a1387`，tag v1.6/v1.7 顺序收口挂账中）｜ 状态：✅ 代码已交付（2026-09-06, 见 [v1.8开发交付：et_map定容映射与互传对称化](./v1.8开发交付：et_map定容映射与互传对称化.md)；板上顺序收口挂账见交付 §5）
> 上一版本：[v1.7] 24 模块 / 300 用例 × 2 几何全过 / 覆盖 97.0% / docsync 69 断言 / bench 基线 + CHANGELOG
> 输入：v1.7 计划将 `et_map` 记为 v1.8 候选；xmodem 仅接收器（发送端为 host 侧 python 工具，MCU 无法作发送方）；v1.6/v1.7 双 tag 顺序收口；v1.7 P3（tickless demo 实装、Renode smoke 增 selftest）顺延；验收治理项——交付复现表连续两版漏覆盖率行

---

## 1. 版本定位

- **版本号 v1.8**：新增 et_map（core/）与 et_xmodem_tx（protocol/）两模块 + 收口治理，纯增量无破坏性变更（MINOR）。
- **本版主题**：**容器补齐 + 传输对称化**——库缺的最后一个高频容器（定容开放寻址哈希表）落地；xmodem 补齐发送端，MCU 从"只会收"变为"能收能发"（对传/上报场景）；同时完成 v1.6/v1.7 顺序收口与覆盖率治理固化。
- **不变式**（继续遵守）：零动态内存、多实例句柄化、core/algorithm 层零硬件依赖、ISR-safe 范围显式标注。

## 2. v1.7 现状盘点

| 项 | 状态 |
|---|---|
| 模块 | 24 个（core 4 / algorithm 2 / sys 4 / protocol 4 / storage 2 / drivers 3 / debug 4），300 用例 × 2 几何，覆盖 97.0%，docsync 69，bench + CHANGELOG 就位 |
| 容器缺口 | core 有 ringbuf/queue/mempool/list，**无键值映射容器**——命令路由/传感器表/去重场景只能手写线性表 |
| 传输缺口 | et_xmodem 仅接收器；`tools/xmodem_send.py` 是 host 侧工具，**MCU 之间或 MCU 作发送方无库级支持** |
| 挂账 | ① v1.6 走单 5 条回填 → tag v1.6 ② 库化 selftest 板上记录（AT+SELFTEST/SELFSTOR）→ tag v1.7 ③ tickless demo 实装 ④ F103 Renode smoke 增 selftest 断言 |
| 治理项 | 交付复现表连续两版（v1.6/v1.7）漏覆盖率行——docsync 应能抓住这类"表格缺项" |

## 3. 开发范围

### P0 顺序收口与治理固化（约 0.5~1 天，板档期并行不互堵）

| # | 任务 | 验收标准 |
|---|---|---|
| 1 | v1.6 走单回填（runbook 5 条）+ **补打 tag v1.6** | 实机记录 §5 落数；走 release.yml 产出 Release |
| 2 | 库化 selftest 板上记录（`AT+SELFTEST` 15 PASS + 2 SKIP → `AT+SELFSTOR` 实跑）→ **补打 tag v1.7** | 实机记录补库化版结果表 |
| 3 | **覆盖率行固化**：docsync 增"交付复现表覆盖率行"断言（README 与交付模板必须含覆盖率行，否则红） | 故意删除该行脚本变红（自证有效） |
| 4 | `et_config.h` 版本宏升至 1.8.0 | 发布时与 tag 核对一致 |

### P1 `et_map` —— core/ 定容开放寻址哈希表（★ 核心，1.5 人日）

v1.7 预告的 v1.8 候选。定位：**小容量（≤ 数百槽）键值映射**，查询/更新 O(1) 均摊、零分配、表可驻任意存储。

```c
typedef struct {
    uint32_t key;                 /* 键(0 保留为"空槽", 键 0 不可用) */
    uint32_t val;                 /* 值(可存放指针位模式) */
} et_map_slot_t;

bool et_map_init(et_map_t *m, et_map_slot_t *storage, uint32_t cap, uint32_t probe_limit);
bool et_map_put(et_map_t *m, uint32_t key, uint32_t val);   /* 已存在则覆盖, 满表返回 false */
bool et_map_get(const et_map_t *m, uint32_t key, uint32_t *val);
bool et_map_del(et_map_t *m, uint32_t key);
uint32_t et_map_count(const et_map_t *m);
void et_map_clear(et_map_t *m);
bool et_map_foreach(const et_map_t *m, et_map_visit_fn fn, void *user); /* 遍历中删除安全 */
```

**语义默认决议**（评审时间盒 0.5 天，确认或否决）：

- 键空间：u32，**键 0 保留为空槽标记**（文档显式声明）；字符串键列为 v1.9 候选不展开；
- 探测：线性探测 + **探测上限 probe_limit**（满表/退化时提前拒绝而非无限探测——嵌入式无界查找不可接受）；文档给出负载因子指引（建议 ≤0.7）与容量选择（质数槽数）；
- 删除：tombstone 置空槽（避免破坏探测链），`foreach` 跳过 tombstone；遍历中删除安全（迭代游标独立）；
- 上下文：🏠MAIN 单上下文（与 mempool 同级标注，ISR 使用需调用方临界区）；
- 单测 ≥ 16 例：插查删改、碰撞链线性探测、同桶多键、删除后空洞复用、满表拒绝、probe_limit 退化拒绝、键 0 拒绝、重复键覆盖、count/clear、foreach 全遍历 + 遍历中删、多实例隔离、init 参数校验（cap/存储/probe 合法域）

### P2 `et_xmodem_tx` —— protocol/ 传输发送器（1 人日）

与接收器对称，MCU 作发送方（对传、镜像上报、日志回传）。**与 rx 共享协议常量与组帧助手**（128B/1K、CRC16-CCITT、NAK 重传 ≤10、CAN×2 中止、EOT 两段确认、超时注入 now）。

```c
void et_xmodem_tx_init(et_xmodem_tx_t *x, et_xmodem_cfg_t *cfg);  /* 块型/超时/重传上限 */
et_xm_act_t et_xmodem_tx_poll(x, ch, now);   /* 逐字节处理对端应答, 返回下一步动作 */
et_xm_act_t et_xmodem_tx_tick(x, now);       /* 无应答超时 → 重发/中止 */
/* 数据侧: et_xmodem_tx_write(x, buf, len) 逐块取数据(回调或轮询式) */
```

- 对端行为矩阵测试 ≥ 12 例：正常流逐字节一致、NAK 重传计数、超时重发 SOH、对端 CAN 中止、EOT 协商、1K 变体、发送中途数据源耗尽；
- **端到端自证**：tx(库) → rx(库) 内存环回，128B/1K 双块型逐字节一致（host 即可跑，不依赖串口）；
- 与 `tools/xmodem_send.py` 的关系：host 工具保留（调试便利），库发送器用于 MCU 侧。

### P2-2 v1.7 P3 顺延收编（各 0.5 天，时间盒）

| # | 任务 | 说明 |
|---|---|---|
| 1 | demo tickless 实装 | `stm32f103_demo` / `stm32g474_demo` 主循环改 `next_due()` + WFI + **RX 中断唤醒**（配方 11.8 落地）；Renode smoke 断言不回归，真机收口时一并验证 |
| 2 | F103 Renode smoke 增 selftest 断言 | 仿真中发 `AT+SELFTEST`，断言套件 PASS 日志行（selftest 全启用体积增量已入表，F103 预算是否允许按实测定） |
| 3 | bench v1.8 基线行 | `make bench` 复测补 docs/bench.md 版本行（沿环境注记纪律） |

## 4. 交付标准（DoD）

1. 沿用 v1.7 全部 DoD（docsync 全绿、双几何硬性项、量化声明附复现命令 + 环境注记）；
2. **新增：交付复现表覆盖率行**为 docsync 断言项（P0-3，自证有效）；
3. et_map 附加：容量指引与退化行为（probe_limit 触发条件）写头文件注释；表可驻 const 段以外的任意存储（调用方决定 SRAM/内存池）；
4. et_xmodem_tx 附加：与 rx 共享协议常量（单一事实来源，防两侧漂移）；端到端回环用例入套件；
5. 顺序收口：**tag v1.8 前置 v1.6/v1.7 双 tag 已补打**（沿 DoD #5 规则）。

## 5. 里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 |
|---|---|---|
| M1 | P0-3/P0-4 治理与版本；P0-1/P0-2 板上收口并行推进 | D+0.5 |
| M2 | `et_map` 合入 | D+2.5 |
| M3 | `et_xmodem_tx` 合入（含回环端到端） | D+3.5 |
| M4 | tickless demo + Renode selftest + bench 基线行 + 文档同步 | D+4.5 |
| M5 | 全量回归（CI 实跑）→ **tag v1.8** | D+5 |

## 6. 明确不做（Non-goals）

- 不做字符串键映射（v1.9 候选）、不做动态扩容/缩容（定容为定位）、不做开放寻址以外的桶+链实现；
- shell Tab 补全/多行编辑维持排除（三版边界）；层次 FSM 不做；
- 不做 i2c/spi 总线抽象、安全启动/加密、RTOS、浮点格式化、动态内存（全部沿既有边界）；
- F103 真机维持常设挂账；Renode G4 维持"不排期（真机覆盖）"政策。

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| et_map 高负载下线性探测退化 | 查询变慢/探测上限误伤 | 文档容量指引（负载 ≤0.7）+ 用例覆盖退化路径；probe_limit 语义明确（拒绝 vs 放宽由调用方参数决定） |
| 键 0 保留导致调用方数据不适配 | 使用困惑 | 头文件显式声明 + init 拒绝 + 文档示例；若真实需求强烈改 v1.9 支持键 0（改空槽标记方案） |
| tx/rx 协议语义漂移 | 互传失败 | 共享协议常量/组帧助手（单一事实来源）+ 内存回环端到端用例作为 CI 常规回归 |
| 板上收口依赖档期 | 双 tag 悬置 | P0 与 P1/P2 并行；顺序 tag 规则维持——收口不完成则 v1.8 tag 挂账（沿 v1.7 模式） |
| tickless demo 的 WFI 在 Renode 无功耗意义 | 演示失真 | 只断言功能日志（沿"仿真不测时序"政策）；真机收口时补功耗/唤醒行为备注 |

---

> 维护约定：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v1.8 后本文档归档不再改动。
