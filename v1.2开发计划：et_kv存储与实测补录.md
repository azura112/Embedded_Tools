# v1.2 开发计划：et_kv 存储与实测补录

> 起草日期：2026-09-02 ｜ 基线：v1.1（tag `472a2a9`）｜ 状态：✅ 已交付 v1.2（2026-09-02）
> 变更记录：M1~M5 完成；P3 可选模块 et_softclock 实际落地；P4 落地（gcovr 门 85% + 发布工作流）；P2（Renode/真机）无环境挂账，转 v1.3。
> 上一版本：[v1.1] 16 模块 / 150 用例全过 / host 与 ARM 构建零警告
> 技术输入：`exp/kv` 分支《et_kv 技术验证提案：port flash 契约演进》（契约扩展 + 5 评审要点 + 模块设计草案）

---

## 1. 版本定位

- **版本号 v1.2**：port 契约演进为**纯增量**（新增 flash 三件套，不改既有临界区/时基/putc 语义），et_kv 为全新模块，无破坏性变更，对应 semver MINOR。若评审迫使既有契约变更，升格 v2.0 另行评审。
- **本版主题**：把 v1.0 以来的"PC 可测 → 真机可用"再推进为"**掉电可靠**"——落地首个存储模块 et_kv（flash 参数掉电保存），同时清偿 v1.1 挂账的仿真/真机实测。
- **不变式**（继续遵守）：零动态内存、多实例句柄化、core 层零硬件依赖、ISR-safe 范围显式标注；**et_kv 与 mempool 同级标注 🏠MAIN 专用**。

## 2. v1.1 现状盘点

| 项 | 状态 |
|---|---|
| 模块 | 16 个（core 4 / algorithm 1 / sys 3 / protocol 3 / drivers 3 / debug 2），分层依赖单向 |
| 测试 | 150 用例全过；CI 已有（ubuntu+windows 单测矩阵 / gcovr 覆盖率 / STM32 交叉编译） |
| port 契约 | 临界区 + 毫秒时基 + putc，host 与 F103 双平台实现 |
| 挂账 | Renode/QEMU 仿真截图、F103 真机实测记录（v1.1 降级方案，挂账 v1.1.1，见 `port/stm32f103/README.md`） |
| 技术储备 | `exp/kv` 分支契约提案待评审——本版动工的直接依据 |

## 3. 开发范围

### P0 契约评审与冻结（约 1 天，et_kv 动工前的强制门槛）

对 `exp/kv` 提案 5 个评审要点**逐条形成决议**。为压缩评审周期，下表预先给出默认决议（评审时确认或否决，记录异议即可，不另起炉灶）：

| # | 评审要点 | 默认决议（待确认） |
|---|---|---|
| 1 | 写入粒度 | 统一 **4B 对齐**（F103 半字编程为其超集，port 内部负责补齐；host 模拟无粒度限制） |
| 2 | 擦除耗时与看门狗 | 契约声明"**调用方负责喂狗/停调度**"；port 增加常量 `PORT_FLASH_ERASE_MS_MAX` 供看门狗超时配置参考 |
| 3 | 擦写期间中断 | erase/write **仅限 🏠MAIN**，port 内部用 `PORT_CRITICAL_*` 包住整个擦写序列（禁止取指风险由单上下文 + 关中断双重兜底） |
| 4 | 乒乓与磨损均衡 | v1.2 只做**双扇区固定乒乓**，不做均衡算法；寿命估算（擦写次数 × set 频度指引）写入文档 |
| 5 | 契约冻结点 | 提案决议后，契约章节先合入 API_GUIDE 第 8 章，随后才允许 et_kv 动工（沿用提案原条款） |

评审新增点：**删除语义**——设计草案 API 预览无 delete；默认决议：增加 `et_kv_del(key)`（写入 tombstone 记录，读侧跳过，搬迁时不搬迁死记录）。

| # | 任务 | 验收标准 |
|---|---|---|
| 1 | 评审会议/书面评审，5+1 要点形成决议 | 决议追加到提案文档"决策记录"，`exp/kv` 合入 main（文档先行） |
| 2 | `port.h` 增补 flash 三件套 + `PORT_FLASH_SECTOR_SIZE/COUNT` | 契约标注"**仅在 `ET_MODULE_KV=1` 时为必选契约**"，关闭该模块的老平台零影响 |
| 3 | `et_config.h` 版本宏升至 1.2.0；`ET_MODULE_KV` 开关（`#ifndef` 保护） | 发布时与 tag 核对一致 |

### P1 et_kv 主体（★ 核心，3~4 人日）

| # | 任务 | 说明 |
|---|---|---|
| 1 | host 模拟 flash（`port/host/` 扩展） | RAM 模拟扇区，支持**异常注入**：半页写入截断、指定偏移后掉电、坏页标记、seq 乱序——异常注入是本模块测试的根基 |
| 2 | `et_kv` 模块（新目录 `storage/`，与 algorithm 同级） | 按设计草案：页头 `magic|seq|crc32` + 记录 `key|len|crc32(payload)|payload`；追加写、读侧取 key 最新有效记录、页满乒乓搬迁存活记录、初始化双扇区扫描 seq 仲裁、坏页弃用；复用 `et_crc32` |
| 3 | F103 flash 驱动（`port/stm32f103/` 扩展） | 寄存器级实现（KEYR 解锁 / PER 页擦 / 半字编程），不用 HAL；busy-wait 带超时 |
| 4 | demo 扩展 | `stm32f103_demo` 增加上电读参数 → 修改 → 写回 → 重启恢复演示（重启次数计数是天然演示场景） |

- API 定稿（依评审）：`et_kv_init(layout)` / `et_kv_get(key, buf, cap)` / `et_kv_set(key, buf, len)` / `et_kv_del(key)` / `et_kv_commit()` / `et_kv_stats()`（磨损可见性：各扇区擦写计数与剩余容量）
- 单测 ≥ 20 例：基本读写改删、同 key 多版本取最新、页满搬迁正确性（含存活/死记录筛选）、**掉电恢复矩阵**（页头半写/记录半写/搬迁中途，每类 ≥ 2 个断点）、CRC 坏记录跳过、seq 仲裁（新旧页乱序）、容量边界（key 表满/单记录超长拒绝）

### P2 v1.1 挂账补录（1 人日，与 P1 可并行）

| # | 任务 | 说明 |
|---|---|---|
| 1 | Renode 仿真跑通 F103 demo | `port/stm32f103/README.md` 已给参考命令；截图与日志归档该 README |
| 2 | 真机实测记录 | 若硬件到位：烧录 + 串口日志 + 按键/LED 实测记录归档 |

**发布策略**：若硬件提前到位，可随时以 **v1.1.1** 单独发布补录（不动 main 功能）；否则随 v1.2 一并清账。两种路径都消除挂账，不重复排期。

### P3 可选新模块（不阻塞发布，时间盒 1 人日）

**`et_softclock`（sys/，软时钟）**：毫秒 tick → 日历时钟（年月日时分秒，闰年/月长换算，`et_time_t` 编译期可裁剪为 32 位秒计数）。与 et_kv 协同（参数时间戳掉电保存），demo 可演示"重启后时间由 kv 恢复 + tick 续走"。单测 ≥ 10 例（闰年边界、月末进位、2038 前安全范围、回绕）。

备选 `et_fsm`（表驱动状态机）列为 v1.3 候选，本版不展开。

### P4 工程化（可选）

| # | 任务 | 说明 |
|---|---|---|
| 1 | CI 覆盖率门槛 | gcovr 报告 v1.1 已产出，本版设 **行覆盖 ≥ 85%** 门槛（先跑基线，若既有模块低于线则只约束新增代码） |
| 2 | 发布工作流 | tag 推送 → 自动构建 host 测试 + F103 ELF 并附到 GitHub Release |

## 4. 交付标准（DoD）

1. 沿用 v1.1 全部 DoD（.c/.h 成对 + 上下文标注 + `ET_MODULE_*` 开关烟测 + 套件注册 + 文档同步 + 零警告零 malloc）；
2. **et_kv 附加**：掉电恢复矩阵用例全绿（host 异常注入）；契约文档（API_GUIDE 第 8 章 flash 小节）先于实现合入；`et_kv_stats()` 可观测磨损；
3. F103 flash 驱动：与 ST 参考手册（PM0056）逐项核对解锁/擦除/编程时序，注释标明勘误依据；
4. README 目录树、API_GUIDE 章节号、Makefile 显式源列表三处同步（新增 `storage/`、`et_softclock` 时同批更新）。

## 5. 里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 | 结果 |
|---|---|---|---|
| M1 | P0 评审决议 + 契约冻结合入 main | D+1 | ✅ 5+1 点决议记入提案，`exp/kv` 合入，port.h 演进（见提交 `5f68f28`） |
| M2 | host 模拟 flash + et_kv 基本读写/搬迁全绿 | D+3 | ✅ 28 例单测全绿 + et_softclock 13 例（`7982d12`） |
| M3 | 掉电恢复矩阵全绿 + F103 flash 驱动 + demo 扩展编译通过 | D+5 | ✅ 矩阵 28 例；F103 驱动（PM0056 核对）；demo 重启计数 + 时钟持久化；ARM 零警告（`996ef43`） |
| M4 | P2 补录（仿真 + 真机）；P3 可选模块（时间盒内） | D+6 | 🟡 P3 完成（et_softclock 并入 M2/M3）；P2 无环境挂账 v1.3 |
| M5 | 全量文档同步 → 全量回归 → **tag v1.2** | D+7 | ✅ 191 用例回归 + 交付文档 + tag（本文档状态行同步） |

## 6. 明确不做（Non-goals）

- 不做通用文件系统 / FatFs 替代品；et_kv 定位是"参数掉电保存"，不是数据存储引擎；
- 不做磨损均衡 v2（多扇区动态映射）与坏块管理表（双扇区乒乓已覆盖参数场景寿命）；
- 不做加密、压缩、掉电事务的通用化抽象；
- 不改既有 16 模块公开 API 签名与语义；RTOS 适配依旧不做；
- `et_fsm` 等新模块候选延后 v1.3。

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| F103 同 bank 擦写期间取指风险 | 中断返回跑飞 | 契约双重兜底（🏠MAIN + port 内部关中断）；M3 编译期静态检查 erase/write 调用上下文；真机补录重点验证 |
| 掉电时序无法穷举 | 罕见断点数据损坏 | 覆盖三类关键断点（页头/记录/搬迁）+ CRC 与 seq 仲裁兜底；文档声明"任何单记录损坏不影响其他 key" |
| 双扇区磨损集中 | 参数高频写入寿命短 | 文档给出寿命估算公式与 set 频度指引；`et_kv_stats()` 暴露擦写计数 |
| Renode flash 时序与真机有差异 | 仿真通过≠真机通过 | 仿真只验功能逻辑，时序项标注"以真机为准"；真机补录不因仿真通过而豁免 |
| 评审僵持阻塞 M1 | 全线顺延 | 默认决议机制：时间盒 1 天，未决项按默认值执行并记录异议 |
| host 模拟 flash 与真实 flash 行为偏差（如 0→1 位写限制） | 测试盲区 | 模拟器按契约实现位写约束（写入前检查旧位），不做理想化写 |

---

> 维护约定：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v1.2 后本文档归档不再改动。
