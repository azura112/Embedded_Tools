# v2.3 开发交付：配方可执行化与直方图

> 交付日期：2026-09-12 ｜ 版本：v2.3.0（`ET_VERSION = 0x020300`，冻结下纯增量 MINOR）｜ 基线：v2.2（tag `v2.2` → `6ed9a83`，Release 已发布）
> 计划文档：[v2.3 开发计划__配方可执行化与直方图](./v2.3开发计划__配方可执行化与直方图.md)（状态：✅ 已交付归档）
> 远程：https://github.com/azura112/Embedded_Tools（**本工作区尚未提交/打 tag**；`v2.3` tag 由发布 checklist 核对后执行）
> 本版 docsync：**237/237**（`sh tools/docsync.sh` 实测,含本自指断言;机制与自证见 §7）

## 1. 重点概况（TL;DR）

v2.3 的主题一句话：**把"纸面配方"变成"可运行证据"**。API_GUIDE 11.x 的三个核心配方（闭环整定 / kv 备份恢复 / 升级流程）落地为 `examples/` 三个自检式示例——`make ex` 一键全跑、CI 常设、任一 FAIL 即红，配方的正确性从此由 CI 守护而非由文档维护者保证。算法面补上可观测性最后一块拼图 **`et_hist` 定容直方图**（分布视图：p99 长尾检测，与 et_stats 点估计、et_sched_task_stats 单任务 last/max 互补）；工程面新增 **`docs/architecture.md`**（分层图 + 模块选型表 + 验证金字塔）。全程 `apidump --diff` 对 v2.2 基线 **新增 11 项 / 修改删除 0 项**，零破坏性变更；**本版全部项无板上依赖**（刻意选择，板侧留喘息）。

- **P0 治理常规**：`--snapshot docs/API_INVENTORY_v2.2.md`（384 项比较单元）+ `--diff` 默认基线切至 v2.2（滚动规则沿用，v2.1 归档只读保留）；版本宏 2.3.0。
- **P1 `et_hist`（第 32 模块）**：闭区间 `[lo,hi]` 等宽分桶（**下含上含**——v==hi 恰落末桶，配"判稳带宽"类闭区间直觉）、`bin_count` 1~255、越界进 under/over **不丢弃**、percentile **桶内线性插值粗估**（桶宽=1 时精确；声明非精确分位数）、O(1) 入桶零分配、🏠MAIN。11 例单测（等宽映射/闭区间边界/百分位精确与插值/INT32 全值域不溢出/多实例）。配方 11.12：`et_sched_task_stats` 采样喂 `et_hist` → p99 长尾诊断。bench：push 2.0 / percentile 10.0 ns/op。
- **P2 配方可执行化（★ 核心）**：三例均为"跑完自检退出"的无人值守形态，断言只对**确定性数值**（定点仿真无抖动；`clock()` 粒度教训外推——不碰耗时断言）：
  - `ex_pid_loop.c`（11.10 host 版，v2.2 板上 demo 的收编）：模拟对象 10ms 节拍闭环 3s → **peak=500 / settle=1090ms / 稳态方差 0 与 host 同款定点公式预计算逐值一致**；et_stats 方差阈值判稳 + et_hist 记录控制量分布（桶 3/4 = 34/196，p50=455）；
  - `ex_kv_backup.c`（11.11）：kvA 8 key → `et_kv_iter` 导出帧（BE16 key/len + value + CRC16-MODBUS）→ `et_xmodem_tx/rx` 内存回环（256B 线上字节/144B 载荷逐字节一致）→ 解帧重灌 kvB → 8/8 key 比对一致；
  - `ex_upgrade_flow.c`（5.4×6.2 组合）：镜像经 xmodem 回环写 B 槽（落盘逐字节回读比对）→ verify/stage → 模拟重启 → **confirm 路径**（attempts=1 → confirmed）与**超次回滚路径**（attempts=2 → should_rollback → abandon 清态）全走。
  - `make ex` 进 Makefile 与 CI unit-tests job；API_GUIDE 11.10/11.11/6.2 三处"可执行载体"互链；示例只用公开 API + host port（API 演进即编译错，证据不腐化）。
- **P3 `docs/architecture.md`**：ASCII 分层图 + 两条典型数据流（UART 交互/升级流、测量→控制流）+ **32 模块选型表**（"要消尖峰→medfilt、要抗饱和控制→pid、要掉电参数→kv"式导航）+ 验证金字塔图；与 getting-started 分工（architecture 管是什么/怎么选，getting-started 管怎么跑起来）。
- **P4 可选全做**：bench v2.3 行（见上）；**v2.4 候选评估**——定点 Q15 双二阶（biquad）入 `docs/v3-candidates.md` #8（立项条件：真实 IIR 型频响需求 + 两级链不够用；直接 IIA 转置型 + int64 累加；不做浮点变体）。
- **过程自省**：apidump 基线滚动后红向自证抓到一个反直觉事实——**连头文件里参数改名都会被抓红**（清单登记签名原文，改名即"1 增 1 删"）。这是从严语义，符合"签名删改即红"的契约字面，已如实记录。

## 2. 逐项验收对照（v2.3 计划 §3）

| # | 任务 | 验收结果 |
|---|---|---|
| P0-1 | apidump 基线滚动 | ✅ v2.2 基线归档（384 项）；发版点 0/0 绿；故意破坏（改 `et_medfilt_init` 参数名）→ 12 增 1 删红（exit 1）；v2.1 归档保留 |
| P0-2 | 版本宏 2.3.0 | ✅ `0x020300`；README/API_GUIDE/CHANGELOG 对齐（docsync 动态版本链） |
| P1 | `et_hist` 合入 | ✅ 第 32 模块 11 例；区间/百分位语义头文件与 API_GUIDE 3.6 双处一致（DoD #5）；bench 两行；11.12 配方 |
| P2 | 三示例 + `make ex` + CI | ✅ 三例全 PASS；Makefile `ex` 目标 + ci.yml 常设步骤；API_GUIDE 三处互链；docsync 登记九条 |
| P3 | architecture.md | ✅ 分层图/数据流/选型表/金字塔四件套；docsync 断言在档 |
| P4 | 时间盒两项 | ✅ bench v2.3 行 + biquad v2.4 候选（零实现） |

## 3. 冻结证明（本版 diff 摘要）

`sh tools/apidump.sh --diff`（默认基线 `docs/API_INVENTORY_v2.2.md` ↔ 当前头文件）：

```
新增     11 项  (只追加 = OK)
修改/删除 0 项  (冻结面破坏 = 红)
apidump: OK —— 纯新增 (11 项, 修改/删除 0)
```

**diff 摘要：新增 11 项，修改 0 项。** 函数 8（`et_hist_*` 全族：init/push/clear/count/bin/under/over/percentile）；类型 1（`et_hist_t`）；宏 2（`ET_MODULE_HIST`、`ET_HIST_BIN_MAX`）。

**结构体字段追加登记区（--diff 附则要求）：本版零项**——`et_hist` 全部为新增类型，未对任何既冻结结构体追加/改动字段；v2.2 开立的登记区（`et_task_t`）无新增行。清单口径：`docs/API_INVENTORY.md` 共 **402 项**（`--diff` 比较单元 395 项 = v2.2 基线 384 + 11）。

## 4. DoD 对照（v2.3 计划 §4）

| DoD | 状态 | 证据 |
|---|---|---|
| 1 沿用 v2.2 全部 DoD | ✅ | docsync **237/237**；409×双几何 + 1K 410 + test-tab 全绿；docbuild 0 warning；sizecheck 3/3 |
| 2 **三示例自检通过并进 CI** | ✅ | `make ex` 三例 PASS（本机）；ci.yml unit-tests job 常设 `make ex` 步骤；断言全部为确定性数值（peak/settle/分布/逐字节），零耗时断言 |
| 3 基线滚动生效 | ✅ | `--diff` 默认对 v2.2；v2.1/v2.0 归档只读保留（docsync 断言在档） |
| 4 数字回刷 | ✅ | examples 目录数（README 结构树 6 文件）/ `make ex`（README 质量门 + getting-started + Makefile）/ docsync 计数（交付文档声明）/ API_GUIDE 互链三处——全部同批更新 |
| 5 et_hist 语义双处一致 | ✅ | 闭区间/下含上含/插值粗估语义在 `et_hist.h` 头注与 API_GUIDE 3.6 同文（docsync 断言头注关键词） |

## 5. 三示例自检内容一览（`make ex`）

| 示例 | 配方 | 自检点（全部确定性数值） |
|---|---|---|
| `ex_pid_loop.c` | 11.10 闭环整定 | peak==500、settle==1090ms、稳态方差 <1.0（50 样本窗）、hist 桶 34/196 与 p50==455、控制量零越界 |
| `ex_kv_backup.c` | 11.11 kv 备份恢复 | xmodem 回环载荷逐字节一致、8 帧 CRC 全过、kvB 逐 key 值/长度一致、key 计数一致 |
| `ex_upgrade_flow.c` | 升级流程 | 镜像落盘回读逐字节一致、confirm 路径（staged→confirmed）、回滚路径（attempts 1→2 → should_rollback → abandon 清态） |

## 6. 里程碑对照

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 | P0 基线滚动 + 2.3.0 | ✅ |
| M2 | `et_hist` 合入 | ✅ |
| M3 | 三示例合入 + `make ex` + CI 步骤 | ✅ |
| M4 | architecture.md + bench + 文档同步 | ✅ |
| M5 | 全量回归 + 交付文档 | ✅（tag `v2.3` 待发布 checklist 执行） |

## 7. 机制自证记录

| 机制 | 绿向 | 红向（故意破坏） |
|---|---|---|
| 基线滚动 `--diff` | 对 v2.2 基线发版点 0/0；对当前头文件新增 11/0 | 改基线中 `et_medfilt_init` 参数名 → 12 增 1 删 → **exit 1**（并发现：头文件参数改名同样被抓——清单登记签名原文） |
| docsync 自指断言 | 交付文档行首声明"本版 docsync 237/237" → `ok`，`pass=237 fail=0` | 计数手改 → `FAIL [自指] ≠ 实测` → exit 1；改回即绿 |
| 三示例自检 | `make ex` → `[ex_pid_loop] PASS`/`[ex_kv_backup] PASS`/`[ex_upgrade_flow] PASS` | 断言对确定性数值（如 settle 改一个采样即红）；过程问题（tx 循环提前退出、镜像头误用大端）首跑即被自检抓出——机制价值实证 |

## 8. 挂账与后续

1. **tag `v2.3` 待执行**：本工作区未提交、未推送；发布 checklist 核对后打 tag，`release.yml` 注入 CHANGELOG 2.3.0 节。
2. **v2.4 素材池**：`docs/v3-candidates.md` #8 定点 biquad（本版评估）+ 原四项破坏性候选；均为"需求触发才评"。
3. **Non-goals 维持**：selftest 维持 20 套件（不为扩充而扩充）、无浮点、无真实被控对象硬件、无 i2c/spi/RTOS/动态内存/通配/多行编辑（连续十二版）；示例不做交互式、不做配置文件驱动。
4. **板侧**：本版无板上依赖项（刻意）；板侧仍为 v2.2 验证态，下版涉及板侧改动时按 §9 复现链路同步。

## 9. 量化数据复现（环境注记）

| 项 | 复现命令 | 结果 | 环境 |
|---|---|---|---|
| 单元全量 | `mingw32-make test` / `make test-g4` RESULT 行 | **409×2** 全绿；1K 变体（`-DET_XM_1K=1`）**410**；`make test-tab` 同套件数 | MinGW-w64 gcc **16.1.0** / Windows 11 Git Bash；CI ubuntu make 同绿 |
| 配方示例 | `mingw32-make ex` | 三例 **PASS**（退出码 0） | 同上 |
| 覆盖率 | `mingw32-make coverage-build` + `gcovr --print-summary` | **lines 97.0%（8320 / 8574）**，门 ≥85%；et_hist 93% | gcovr **8.6** + MinGW gcc 16.1（`--coverage`） |
| docsync | `sh tools/docsync.sh` | **pass=237 fail=0**（含 apidump `--check` 与自指断言） | Git Bash |
| API 清单/冻结 | `sh tools/apidump.sh --check`；`sh tools/apidump.sh --diff` | 清单 402 项（比较单元 395）；**新增 11 / 修改删除 0** | 同上；脚本内锁 `LC_ALL=C` |
| ARM 体积 | `sh tools/docbuild.sh` + `arm-none-eabi-size`；`sh tools/sizecheck.sh` | F103 **20368**(默认)/**29976**(selftest)、G474 **20736**；sizecheck **3/3** | GNU Tools for STM32 **13.3.rel1**（STM32CubeCLT 1.18.0），`-Os -g` 零警告 |
| bench | `make bench` | `hist push` **2.0**、`hist percentile` **10.0** ns/op（既有行无回归） | 同 v1.7 基线注记（Ryzen 9 7945HX / gcc 16.1 / `-O2`） |

---

> 维护约定：本文档随 v2.3 交付定稿；后续范围变更不再回改，走下一版计划。
