# v1.5 开发交付：安全升级与运行防护

> 交付日期：2026-09-05 ｜ 版本：v1.5.0（`ET_VERSION = 0x010500`）｜ 基线：v1.4（`6f346b0`）
> 计划文档：[v1.5 开发计划：安全升级与运行防护](./v1.5开发计划：安全升级与运行防护.md)（状态：✅ 已交付）
> 远程：https://github.com/azura112/Embedded_Tools（tag **v1.5**，Release 附件含 SHA256SUMS）

## 1. 重点概况（TL;DR）

v1.5 补上 v1.4"固件可更新"的**安全**二字，并以脚本化断言根治文档脱节：

- **新模块 et_bootctl**（storage/，第 22 模块）：32B 版本化镜像头（头 CRC + 全镜像 CRC）+ A/B 试运行/确认/回滚状态机。状态扇区独立于 et_kv，append-only 事件记录（STG/CNF/ATT，val+inv 双字互斥）+ 0→1 位写——**任意断电只丢最后一步**；脏尾/坏头 init 自愈。**24 例全绿（含掉电矩阵三类断点各 ≥2）**。
- **新模块 et_wdt**（sys/，第 23 模块）：port 契约第二次演进（wdt 三件套，仅 `ET_MODULE_WDT=1` 必选）——enable 下限 = `PORT_FLASH_ERASE_MS_MAX×2`（与擦除喂狗指引闭环）、feed 🔒ISR-safe、disable 尽力而为（F103 IWDG 不可停恒 false）。host 软件模拟 + F103 IWDG 寄存器直驱（RM0008），`et_wdt_guard` 阻塞段保护。**10 例全绿**。
- **shell 可选历史**：`ET_SHELL_HISTORY_N`（默认 0=字节直透）+ 静态环形缓冲，上/下键回放、行内编辑后回车、环形覆盖最旧。**6 例全绿（双变体）**。
- **demo 全链路串联**（v1.4 挂账收编）：USART1 RX → shell（`ET>` 提示符）→ `AT+UPGRADE`（xmodem 写 B 槽）/ `AT+SIMUPGRADE <ver>`（合成镜像）→ 开机引导决策段（attempt 计数 / even ver 自检失败 / 超次回滚 + abandon），"升级→确认"与"超次回滚"两条叙事均可串口观测。
- **docsync.sh 根治条款**：42 条文档同步断言（版本链动态比对/体积表逐版本/构建清单/套件注册/章节锚点），发布 checklist 与 CI 均调用；**自证有效**（破坏→红→还原→绿）。v1.4 验收发现的 port README v1.4 体积行缺口在本批补上（`76e8803`）。
- **P4**：Release 附件新增 SHA256SUMS；覆盖率门槛维持（96.8% ≥ 96.1%）。

## 2. 逐项验收对照

### P0 销账与根治

| # | 任务 | 结果 |
|---|---|---|
| 1 | 远程 + CI 3 绿 + 补 tag v1.4 | ✅ 已随 v1.4 收尾完成（CI run `4e98a2b`/`d13bba2`/`6958578`；Release v1.4 实跑） |
| 2 | port README 体积表 v1.4 行 + 环境注记 | ✅ 补上（v1.4 替换锚失配静默失败所致），改为逐版本表格 + 测量环境注记，docsync 断言固化 |
| 3 | docsync.sh | ✅ 42 断言全绿 + 自证（破坏→红）；checklist 第 1 条改为"运行 docsync 必须全绿"；CI ubuntu job 常设步骤 |
| 4 | 版本宏 1.5.0 | ✅ `0x010500`，与 tag/交付文档三方一致 |

### P1 et_bootctl（M2，`cde1a7a`）

- 镜像头 5 项拒绝（magic/hdr_ver/hdr_size/reserved/头 CRC）+ img_size 合法域 + 全镜像流式 CRC；
- 状态机：stage 幂等 + **双槽互斥**、confirm 仅对 staged、attempt 仅 staged 未确认计数、should_rollback 阈值判定、abandon 重置；
- 掉电矩阵：stage（inv 未写/val 半字截断）、confirm（inv 损坏/val 半字）、attempt（中段损坏/val 半字）各 2 断点，语义 = 丢最后一步、前态完好；
- 状态扇区自愈：坏头 init 重建、半擦注入回安全默认、记录区满拒绝 + abandon 重建。
- 开发中修复：**rec_append 成功后未推进 rec_off**（修复重放双写同址触发位写违约，插桩定位）；verify img_crc 偏移 16→12；hdr_size 按 uint16 读。

### P2-1 et_wdt（M3，`6081dcc`）

- 契约冻结先行（DoD #4）：API_GUIDE §9 表格 + 决议先于实现合入；
- host 模拟：虚拟时间窗口检查 + 超时回调注入 + 喂狗计数；F103：IWDG KR/PR/RLR/SR 直驱（0x5555 解锁 → 分频选档 4~256 × 12 位 RLR → 等 PVU/RVU → 0xCCCC），超上限拒绝；
- `et_wdt_guard`：前后喂狗、fn 结果透传、失败路径同样喂狗、NULL 拒绝零喂狗。

### P2-2 shell 历史（M4，`9206d33`）

- 编译期 `ET_SHELL_HISTORY_N`（默认 0）+ 运行期 `et_shell_set_history` 挂接静态缓冲；
- ESC[A/B 回放、实时行/浏览游标、行内编辑后回车（编辑行入史）、环形覆盖最旧、超长条目截断；
- 关闭时字节直透（默认构建行为与 v1.4 完全一致）。
- 开发中修复：browse 上键 off-by-one（实时行首按应选最新）；push/replace 与 atcmd"输入中不补 NUL"语义的配合（按 pos 精确拷贝 + 回放后清缓冲尾）。

### P3 demo 串联（M4，`9206d33`）

`stm32f103_demo` 升级链路：`ET>` shell → `AT+UPGRADE`（xmodem 阻塞收，sink 擦写 B 槽）/ `AT+SIMUPGRADE <ver>`（合成 64B 镜像）→ `verify_image` + `stage` → AIRCR 复位 → 开机引导决策段（attempt 日志 / even ver 自检失败不确认 / 奇数 confirm / 超次 ROLLBACK + abandon）。Renode smoke 原 5 断言照常全过，日志可见 `ET>` 提示符与 `boot: no staged slot`。

### P4 工程化

1. release.yml 生成 SHA256SUMS ✅（v1.5 Release 已带 4 附件）；
2. 覆盖率 96.8% ≥ 96.1%（97.1−1pt）✅。

## 3. 质量数据（复现命令 + 环境注记）

| 项 | 数值 | 复现 / 环境 |
|---|---|---|
| 模块 | 23（+et_bootctl、+et_wdt） | `ls storage/ sys/` |
| 主机单测 | **279 例 ALL PASS** | `mingw32-make test`（MinGW gcc 16.1，Git Bash） |
| 变体回归 | XM_1K=1、CRC_TABLE=1+HISTORY_N=4 均 ALL PASS | `gcc -DET_XM_1K=1 ...` / `gcc -DET_CRC_TABLE=1 -DET_SHELL_HISTORY_N=4 ...` |
| 行覆盖率 | **96.8%**（5515/5699），函数 99.4% | `gcovr --print-summary`（MinGW gcc 16.1，`--coverage`） |
| ARM 编译 | 零警告，`text=15440 data=24 bss=596`（Flash 24%） | GNU Tools for STM32 13.3.rel1，`-Wall -Wextra -pedantic -Os -g`，`arm-none-eabi-size` |
| Renode smoke | 5/5 断言 + `ET>` 提示符 + boot 日志（Renode 1.16.1） | `sh port/stm32f103/renode/smoke.sh ...` |
| docsync | 42/42 断言全绿（CI ubuntu 常设门） | `sh tools/docsync.sh` |
| CI/Release | CI run success（`a88ebff`）；Release v1.5 verify→release success，附件 ELF/BIN/demo.exe/SHA256SUMS | GitHub Actions |

## 4. 文档同步

- `docs/API_GUIDE.md`：4.5 et_wdt、6.2 et_bootctl（含状态机与 API 表）、8.3 历史说明、配方 11.7（看门狗×flash）、配置表（WDT/BOOTCTL/HISTORY 开关 + 1.5.0）、平台表 279 用例；
- `README.md`：v1.5.0、23 模块特性表两行、279 用例、checklist 脚本化条款；
- `port/stm32f103/README.md`：体积表 v1.5 行、279 用例；
- `tools/docsync.sh`：42 断言（本版新增 bootctl/wdt/history/Makefile 各条）；
- 计划文档状态行与里程碑归档。

## 5. 开发中发现并修复的问题

1. **【模块】et_bootctl rec_append 不推进游标**：修复重放时两条记录写同一偏移，第二条触发 1→0 位写违约——插桩定位（`[append] val=… off=12 w=1`）。修复：追加成功即 `rec_off += 8`。
2. **【模块】verify 镜像 CRC 读错偏移**（16=img_ver，应为 12=img_crc32）；hdr_size 字段为 uint16 却按 32 位读。
3. **【模块】脏尾检测误报**：干净 FF 结束与半写记录未区分，导致 init 无谓修复——扫描先判全 FF。
4. **【模块】shell 历史 browse 上键 off-by-one**（实时行首按选中次新而非最新）；push/replace 与 atcmd"输入中不补 NUL"的配合（按 pos 拷贝 + 回放后清缓冲尾）。
5. **【CI】ARM 构建源清单漏 et_bootctl**（本机 `storage/*.c` 通配未暴露）——两工作流统一改通配，docsync 亦新增 Makefile 断言。

## 6. 范围外事项 / 常设挂账

| 项 | 状态 | 去向 |
|---|---|---|
| F103 真机实测 | ⏳ 常设挂账（不阻塞） | 硬件到位按 checklist 补录（升级链路为现成验收脚本） |
| 安全启动（签名/防回滚） | Non-goal | 显式排除 |
| Tab 补全 / 多行编辑 | Non-goal 维持 | — |
| et_kv 迁移到状态扇区 | Non-goal | bootctl 独立状态扇区 |

## 7. 提交与版本

| 里程碑 | 提交 | 说明 |
|---|---|---|
| M1 | `76e8803` | docsync 42 断言 + 体积表补正 + 1.5.0 |
| M2 | `cde1a7a` | et_bootctl + 24 例（掉电矩阵） |
| M3 | `6081dcc` | wdt 契约 + host/F103 + et_wdt + 10 例 |
| M4 | `9206d33` | shell 历史 + demo 升级链路 |
| M5 | `61c2ffe`/`a88ebff`/tag **v1.5** | 文档同步 / CI 源清单修复 / Release 两段式 success + 4 附件 |

版本钉：`et_config.h` → 1.5.0 / `0x010500` = tag v1.5 = 交付文档版本行（docsync 动态断言固化）。
