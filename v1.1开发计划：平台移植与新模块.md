# v1.1 开发计划：平台移植与新模块

> 起草日期：2026-09-02 ｜ 基线：v1.0（commit `6cb8d5e`）｜ 状态：草案
> 上一版本：[v1.0] 13 模块 / 103 用例全过 / `-Wall -Wextra -pedantic` 零警告

---

## 1. 版本定位

- **版本号 v1.1**：只做"新增 + 清理"，不修改既有 API 语义（无破坏性变更，对应 semver 的 MINOR）。若开发中被迫调整既有接口，则升格为 v2.0 另行评审。
- **本版主题**：把 v1.0 的"PC 可测"推进为"真机可用"，并补齐嵌入式开发中三个高频缺口（侵入式链表 / 数字滤波 / 软件_PWM）。
- **不变式**（继续遵守）：零动态内存、多实例句柄化、core 层零硬件依赖、ISR-safe 范围显式标注。

## 2. v1.0 现状盘点

| 项 | 状态 |
|---|---|
| 模块 | 13 个（core 3 / sys 3 / protocol 3 / drivers 2 / debug 2），分层依赖单向 |
| 测试 | 103 用例全过；白盒回绕、SPSC 压测、畸形输入覆盖 |
| 移植 | 仅 `port/host/`；`port.h` 契约（临界区/时基/putc）稳定 |
| 遗留问题 | ① `et_config.h` 9 处"(预留)"注释与实现不符；② 库无版本宏；③ Windows 下 `make` 不在 PATH，快速开始命令不可直接复制；④ 无 CI |

## 3. 开发范围

### P0 基建清理（约 0.5 天，直接进 main）

| # | 任务 | 验收标准 |
|---|---|---|
| 1 | 修正 `et_config.h` 中 9 处过时的"(预留)"注释 | 注释与实际模块能力一致 |
| 2 | `et_config.h` 新增 `ET_VERSION_STRING`（`"1.1.0"`）与 `ET_VERSION`（整数编码 `0x010100`） | 发布打 tag 前与 git tag 一致 |
| 3 | Makefile 补 Windows 无 make 环境的构建说明（`mingw32-make` 或等价 gcc 单行命令） | 照注释可在纯 gcc + Git Bash 环境完成 test/demo 构建 |
| 4 | README 头部声明当前版本并链接开发计划 | — |

### P1 真机移植：port/stm32f103（1~2 人日，视硬件到位情况）

目标目录 `port/stm32f103/`（README 与 port.h 注释均已预留该位置）。

| # | 任务 | 说明 |
|---|---|---|
| 1 | `port_stm32f103.c`：临界区实现 | PRIMASK 保存/恢复 + `CPSID/CPSIE`，验证可嵌套语义 |
| 2 | SysTick 1ms 时基 | 中断内累加 `g_tick_ms`，自然回绕由上层无符号减法消化 |
| 3 | `port_putc` | USART1 轮询阻塞发送（TXE 等待） |
| 4 | 最小真机 demo | blink + 日志 + 按键/LED 联动，走 `examples/` 风格 |

**验收**：真机（BluePill F103C8T6 类）跑通 demo；`docs/API_GUIDE.md` 第 7 章补充"已验证平台"清单与实测记录。
**降级方案**：若硬件暂缺，交付代码 + `arm-none-eabi-gcc` 编译通过 + Renode/QEMU 仿真截图，实测项顺延至 v1.1.1 补录。

### P2 新模块（每个约 1 人日，按价值排序）

#### P2-1 `et_list` —— core/ 侵入式双向链表

与 et_queue 的区别：queue 是值拷贝 FIFO，list 是零拷贝组织任意结构（节点嵌入用户结构体），O(1) 删除。

```c
typedef struct { et_list_node_t node; int id; } item_t;   /* 节点嵌入用户结构 */
et_list_init(&list);
et_list_push_back(&list, &item->node);
et_list_remove(&list, &item->node);                        /* O(1) */
et_list_foreach(&list, visit_fn, user);                    /* 遍历中自删安全 */
```

- 纯逻辑、无分配；单上下文约定（跨上下文由调用方加临界区，文档标注）
- 单测 ≥ 10 例：空表头尾插删、遍历中自删/删后继、重复 remove 防护

#### P2-2 `et_filter` —— 新目录 algorithm/（纯算法层，禁 port.h）

ADC 采样处理标配，全部定点无浮点依赖（浮点可作为可选扩展）。

| 滤波器 | 句柄 | 说明 |
|---|---|---|
| 滑动窗口均值 | `et_movavg_t` | 环形覆盖历史，O(1) 更新 |
| 一阶 IIR 低通 | `et_lpf1_t` | `y += k*(x-y)`，Q15 定点系数 |
| 限幅/斜率限制 | `et_slew_t` | 每步最大变化量限制 |

- 单测 ≥ 12 例：稳态收敛、阶跃响应数值、Q15 系数饱和边界、reset 语义

#### P2-3 `et_spwm` —— drivers/ 多通道软件 PWM

与 `et_led` 呼吸模式直接配套（write 回调直连）。基于 port 毫秒时基。

- `et_spwm_init(ch, write_cb, user)` + `et_spwm_set(ch, duty_0_255)` + `et_spwm_poll(now)`（建议挂进主循环既有 poll 链）
- 文档明确分辨率假设：1ms 时基下周期 ≥ 2ms（≤ 500Hz），高分辨率需求走硬件 PWM
- 单测 ≥ 8 例：duty 0/255 边界恒电平、虚拟时间注入下的翻转序列、多通道相位独立性

#### P2-4 `et_kv` —— flash 参数存储（★ 工作量最大，可顺延 v1.2）

- 需要 **扩展 port 契约**：`port_flash_read/write/erase`（按扇区）——这是 port.h 的首次契约演进，须在 API_GUIDE 立独立小节评审后再动手
- 方案：追加写 + 双扇区乒乓 + 复用 `et_crc32` 校验
- **决策**：先开独立分支 `exp/kv` 做技术验证，验证期不进 main；若 P0~P3 进度顺利再合入 v1.1，否则定版 v1.2 首个特性

### P3 工程化（可选，不阻塞发布）

| # | 任务 | 说明 |
|---|---|---|
| 1 | GitHub Actions CI | push/PR 触发：ubuntu gcc + windows msys 两矩阵跑 `make test` |
| 2 | gcov 覆盖率报告 | 生成并随 CI 产出，暂不设门槛 |

## 4. 每个新模块的交付标准（DoD）

1. `.c/.h` 成对，头文件带并发上下文标注（🔒ISR / 🏠MAIN），与既有模块风格一致；
2. `et_config.h` 增加对应 `ET_MODULE_*` 开关，置 0 时不参与编译且头文件内容被屏蔽；
3. `test/test_<模块>.c` 用例达标（见各节），接入 `test_main.c` 套件注册；
4. `docs/API_GUIDE.md` 增章节（签名表 + 示例 + 并发规则），README 特性表与目录结构图同步；
5. C99、`-Wall -Wextra -pedantic` 零警告、库内零 malloc、无隐藏全局状态。

## 5. 里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 |
|---|---|---|
| M1 | P0 基建清理合入 main | D+0.5 |
| M2 | P1 移植代码完成并编译通过 | D+2 |
| M3 | `et_list` + `et_filter` 合入 | D+4 |
| M4 | `et_spwm` 合入；全量文档同步 | D+6 |
| M5 | 真机实测（或降级方案归档）→ 全量回归 → **tag v1.1** | D+7 |
| 支线 | `exp/kv` 技术验证分支并行推进，结论决定 v1.1 还是 v1.2 收编 | — |

## 6. 明确不做（Non-goals）

- 不做 RTOS/线程安全适配（保持前后台模型定位）；
- 不引入动态内存、不做浮点 printf、不做域宽格式化；
- 不改动 v1.0 既有 13 模块的公开 API 签名与语义；
- 不做矩阵键盘/组合键等应用层特性（API_GUIDE 已声明由应用层组合实现）。

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| F103 开发板/调试器不在手 | P1 实测受阻 | 降级方案：编译验证 + Renode 仿真，实测顺延 v1.1.1 |
| `et_kv` 触发 port 契约演进 | 破坏"唯一硬件层"纪律 | 先评审契约文档再动手；独立分支隔离 |
| 软 PWM 受主循环抖动影响 | 灯效毛刺 | 文档显式声明抖动假设；重载场景引导用户用硬件 PWM |
| 新目录 `algorithm/` 需同步 Makefile | 构建遗漏 | Makefile 为显式源列表，M3 一并更新并跑全量回归 |

---

> 维护约定：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v1.1 后本文档归档不再改动。
