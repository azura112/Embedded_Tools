# v2.6 开发交付（-r2 返工轮）：解析边界清偿与检出能力补强

> 执行日期：2026-09-25 ｜ 被评基线：**`c865f69`**（= `v2.6评审记录__解析边界清偿与检出能力补强.md` §0 的 `HEAD`，亦 = 快照 **`snap-2.7-base`**）｜ 本轮终态 → 快照 **`snap-2.7-start`**（不带 `v` 前缀）
> 本版 docsync：**301/301**
> 评审记录：**`v2.6评审记录__解析边界清偿与检出能力补强.md`**（2026-09-25，结论 **返工**，`CO-1(v2.6)`…`CO-15(v2.6)` 共 15 条）—— 本文档即其 **§6.1「前置门 G0′（v2.6-r2 返工轮）」** 的处置与证据。
> **交付模式**：**返工轮不含新开发**（评审 §6 开篇规则；沿 v2.5 `-r2` 先例 —— 返工轮**只出交付文档**，任务清单由评审记录 §6.1 充当）。因此**本轮的库内代码改动为零**：`core/ algorithm/ sys/ protocol/ drivers/ debug/ storage/` 与 `test/` **无一字节改动**（`git diff --stat` 可见）。
> **前置门定位（v2.7 计划 HC-4）**：本轮终态即 `snap-2.7-start`；**G0′ 全绿是 v2.7 代码窗口的前置条件**，故本轮全部读数均在本轮终态上取得。

---

## 1. 交付摘要（三行以内）

1. **门可用性修复**：`apidump --check` 的行尾敏感已消除（交付平台**恒红 → `exit 0`**）；`docref` 增设「记录终点必须覆盖到 HEAD」判据 —— 本机四门（`docsync` / `apidump` / `sizecheck` / `docref`）全部复现为绿。
2. **已发布产物更正**：`CHANGELOG.md` v2.6.0「挂账」段重写（板侧项**已执行**、`tag v2.5` **已打并已发布**、仅 `CO-6` 板上对照不可得）并新增 `### v2.6-r2 返工轮` 节；**Release v2.6 正文以 `gh release edit v2.6 --notes-file` 重注**（`release.yml` 同一 awk 规则提取）；**未重打 tag**（评审明示禁止）。
3. **同族漏刷与终态统一**：`README.md:237` 冻结基线名、`port/stm32g474/README.md` 板侧叙述、v2.6 交付文档 AC-18 表述三处更正；`docs/开发计划模板.md` §7 增至**五项**；v2.6 交付文档 §1/§4/§5.1/文末纪律句统一为终态；`docsync` 增 **4 条**同族门。

---

## 2. AC 自查表（对应 `v2.7开发计划__从站解析对称化与板上自测收口.md` §4 的 AC-1…AC-7）

**复现环境**（数字类声明的复现前提，README 发布纪律第 6 条）：

| 项 | 值 |
|---|---|
| OS / shell | Windows 11 ｜ Git Bash，`sh` = `C:\Users\ljh\.workbuddy\binaries\PortableGit\versions\1.2.0\usr\bin\sh`（GNU bash 5.3.15, x86_64-pc-cygwin） |
| host 编译器 | MinGW-w64 gcc **16.1.0** |
| ARM 工具链 | `arm-none-eabi-gcc` **GNU Tools for STM32 13.3.rel1**（STM32CubeCLT 1.18.0） |
| make | `mingw32-make` |
| 检出配置 | `git config core.autocrlf` = **`true`**；**无** `.gitattributes`（`ls .gitattributes` → No such file） |

| AC | 结论 | 证据 |
|---|---|---|
| **AC-1**（G0′-1，HC-4） | **通过** | `sh tools/apidump.sh --check` → **`apidump: 清单与头文件一致 (docs/API_INVENTORY.md)`，exit 0**（修复前同命令：`FAIL —— 公开面漂移`，exit 1，diff 报 `@@ -1,757 +1,757 @@` 全文件差异、**耗时 4m24s**）。`sh tools/docsync.sh` → **`pass=301 fail=0`**（exit 0；修复前 `pass=294 fail=2`）。**门自证可红**：向 `protocol/et_modbus.h` 注入一行公开声明 `uint32_t et_r2_probe_red(void);`（插入点 = `#define ET_MODBUS_ADU_MAX …` 宏定义行之后）→ `--check` **exit 1** 并打印 `### 函数声明 (6)` → `(7)` 与新条目；**按 `cp` 回退到注入前字节**（`sha256sum protocol/et_modbus.h` 前后同为 `fea45c79…311e`）→ `--check` 复**绿**（exit 0，4m18s）。详见 §7「机制自证表」 |
| **AC-2**（G0′-1，CO-13） | **通过** | 本机实测（命令与 shell 见 §7「耗时实测表」）：`--check` 修复前 **4m24s** / 修复后 **4m18s**；`--diff` **4m25s**。**不可复现读数已从交付物中消除**：`grep -rn "21 秒" --include=*.md .`（及无空格变体）在本仓**交付物**中命中 **0**；残留仅两处**冻结输入文档**（`v2.6评审记录__…md` 的 `CO-13` 问题陈述、`v2.7开发计划__…md` 的 CO 处置表）—— 二者是"该读数不存在"这一**问题定义**的来源，属**不得改动的冻结输入**。为同时满足本项的字面判据（"不再出现该读数"）与可追溯性，本文档与 CHANGELOG 一律以 **`21s`** 引述被删除的旧读数 |
| **AC-3**（G0′-2） | **通过** | `grep -n "板侧项未执行\|未打" CHANGELOG.md` → **0 命中**；`gh release view v2.6 --json body` 的正文关于板侧/tag 的表述与 `CHANGELOG.md` v2.6.0 节**一致**（正文按 `release.yml` 的同一 awk 规则从 CHANGELOG 重新提取后 `--notes-file` 重注，故同源）；更正方式与时间见 §3 D-1 与 §7 |
| **AC-4**（G0′-3） | **通过** | v2.6 交付文档 §1 状态声明 / §4 CI 行 / §5.1 的 G0-5 与 CI 行 / 文末纪律句 与 §2/§7 的终态表述**无互斥**（§7「终态一致性逐句比对」给出逐句对照）；该文档终态版实测：`grep -c "不合格"` = **1**、`grep -c "待推送后核验"` = **1**、`grep -c "待推送"` = **2** —— 三处命中**全部**是「r2 修订声明」（第 13 行）与 §4 的更正溯源句（第 116 行）中**引述原表述**的历史引用，非终态声称，符合计划 AC-4 的「除历史引用」判据 |
| **AC-5**（G0′-4） | **通过** | ① `README.md` 第 8 条所处行不再含 `API_INVENTORY_v2.4.md`（现为 `docs/API_INVENTORY_v2.5.md`）；② `port/stm32g474/README.md` 的 v2.6 行不再含「未同步」且指向实机记录 §14（同族的 v2.5 行「未上板」一并更正为「已同步 + 走单见 §13」）；③ v2.6 交付文档 AC-18 证据行含「**selftest 相关行**零改动」。**并已补门**（评审要求「评估是否补门」）：docsync 增 4 条同族断言（见 §1 第 3 条） |
| **AC-6**（G0′-5） | **通过** | `docs/开发计划模板.md` §7 含**五项**（新增第 ⑤ 项「证据落点章节号 + 占位节清理」并附 v2.6 反例：AC-1 指 §12、证据在 §13）；`移植stm32实机记录.md` §12 标题处含「**已由 §13 承接**」；`README.md` checklist 第 10 条同步为「五项」并由 docsync 交叉断言 |
| **AC-7**（G0′-6） | **通过** | `tools/docref.sh` 判据已收窄：`<end>` 解析后须**等于 `HEAD`**，否则红（新增段见该脚本「G0′-6 / CO-4(v2.6)」注释块）。本文 §4 的记录范围终点写为 **`HEAD`** 并通过；**门自证可红（两探针）**：探针 A（§4 终点改为固定 SHA `a0b6748` 且其 == HEAD）→ **OK**；探针 B（随后追加一个**纯文档提交**）→ **FAIL exit 1**：`记录终点未覆盖 HEAD … 声明 END = a0b6748 / 实测 HEAD = 35036b7`；`git reset --hard` 回退后复 **OK**（详见 §7.4 实测行） |

---

## 3. 偏差记录

| # | 内容 | 原因 | 影响 |
|---|---|---|---|
| **D-1** | **快照 tag 命名**：按执行协议应打 `v2.7-base`，实际打的是 **`snap-2.7-base`**（并另按 v2.7 计划 §6 在本轮终态打 `snap-2.7-start`） | `v*` 前缀会命中 `release.yml` 的 `tags: [v*]` —— 该结论已由 v2.5 起草期实测坐实（run `35378057777`），并写入计划模板 §7①；**内部快照一律 `snap-`** 是 v2.6 起的强制项 | 无副作用（`snap-*` 不匹配 `release.yml`；本轮已按 v2.7 计划 §6 的复核命令核验 `gh run list` / `gh release list` 无新增，见 §7）。命名与计划 §6 一致 |
| **D-2** | **同族第 4 处漏刷的连带更正**：`port/stm32g474/README.md` 的 **v2.5 行**原写「板侧工程本版仅同步 Core/et 并交叉编译（0 warning），**未上板**（执行期板未接入），故无 CubeMX 侧体积读数」 | v2.5 的造单已于 v2.6-r2 补齐（实机记录 §13），该行「未上板」与事实相反 —— 与 `CO-7(v2.6)`（v2.6 行）**同族**，但计划 G0′-4② 只点名 v2.6 行 | 属计划**遗漏边界**（非阻断）：按「最合理方式继续」一并更正为「已同步（0 warning，FLASH 38712 B / RAM 5312 B）+ 走单见 §13」。**未新增门**（该行版本特定，门化会随版本腐化）；由本轮 D-2 登记留痕 |
| **D-3** | **`--check` 修法**：计划可裁量项 1 给的三选一，实际采「**比对前把两侧行尾归一为 LF 后比对完整内容**」 | 选项 ①（gen_body 输出统一 LF）**治不了根** —— 工作树清单由 `core.autocrlf=true` 检出为 CRLF，只要该文件被 checkout 就恒红；选项 ③（`.gitattributes`）会改变全仓 `docs/*.md` 检出行为，波及 `sizecheck`/`docref`/`docbuild`（计划附 B 已列为风险）；本修法是选项 ②「条目集合比对」的**加强版** —— 条目集合会丢失「分节表头/计数」的比对面，而归一化后比对**完整内容**保持字节级判别力 | 判别力与修复前**等价**（AC-1 自证可红即证），且不依赖工作树行尾、不改全仓检出行为。变更面仅 `tools/apidump.sh` 的 `--check` 分支（+4 行） |
| **D-4** | **`docref` 收窄的实现取值**：计划给「判据改为覆盖 `<base>..HEAD`，**或**增补终态一致性断言」二选一，实际取前者并落为「`<end>` 须解析等于 `HEAD`」 | 后者（交付文档 §2/§7/§8 与 CHANGELOG 的终态一致性）**不可稳定机检** —— 语义级互斥判定需要自然语言理解，硬编码关键词会既漏又误；而「终点 = HEAD」是**可判定、可自证、零误报**的判据，且直接封住 `CO-4` 描述的盲区（「其后任意数量提交不受检」） | 新增约束：**交付文档必须是该版本的最后一个提交**（或等价表述）。对 v2.7 的影响：全量回归 → 交付文档 → `tag v2.7` 的顺序在计划 M5 中**已如此**，不冲突。终态一致性轴仍由 docsync 的 CHANGELOG 版本链 + 自指断言覆盖（未削弱） |
| **D-5** | **docsync 新增 4 条断言**（非计划列举项，属 G0′-4「并须评估是否补门」的落地） | 计划 G0′-4 影响分析栏明写「并须评估是否补门（三处均落在此前'点名式装门'的盲区，根因见 CO-4）」；评估结论 = **可门化 2 处 + 可交叉化 2 处** | 断言总数 **296 → 301**；新增断言均**自证可红**（见 §7）。`test_selftest.c:152` 一类「计划未列但同批必改」的计数不属本轮 |

**本轮无其它偏差**：改动文件清单见 §4；库内源码 `core/ algorithm/ sys/ protocol/ drivers/ debug/ storage/ test/` **零改动**（`git diff --stat` 可核）。

---

## 4. commit 范围

**记录范围定义**（`HC-5` 要求）：本节记录 **`c865f69`（= `snap-2.7-base`，v2.6 被评终态）`..HEAD`** 的全部提交；范围终点写为 **`HEAD`**（`v2.6-r2` 起 `tools/docref.sh` 的硬判据：终点必须覆盖到 `HEAD`）。数字以 `git diff --stat c865f69..HEAD` 的**实测**为准。

```
Docs: v2.6-r2 返工轮 (G0' 前置门) —— 门可用性 + 已发布物更正 + 同族漏刷与终态统一
（范围终点 = 本文档所在提交；其 SHA 随本文档数字回填的 amend 而变化, 故不固定记录 —— 实测: git log --oneline c865f69..HEAD 共 1 条）
```

`git diff --stat c865f69..HEAD` = **13 files changed, 772 insertions(+), 47 deletions(-)**

**分支与 tag**：本轮在 **`main`** 上完成（v2.7 计划 §6「分支规则」：r2 属发布后补正，不是 v2.7 的产物），随后才开 `dev/v2.7`。本轮终态打 **`snap-2.7-start`**（v2.7 代码窗口的起点，HC-4）。

**CI**：**CI**：**已核验 —— run `36145570807`（commit `cdd1770`，main push）七 job 全绿（success）**：`unit-tests (ubuntu-latest)` / `unit-tests (windows-latest)` / `coverage (gcovr)` / `cross stm32f103` / `cross stm32g474` / `docbuild` / `renode stm32f103 smoke`（`gh run view 36145570807 --json jobs` 实测）。其后本地仅有本文档 §4/§7 的**数字回填（纯 `*.md`）**，不改门与代码输入

---

## 5. 未完成项与新发现问题

### 5.1 未完成项（挂账，附理由与触发条件）

| 项 | 计划编号 | 状态 | 理由 / 触发条件 |
|---|---|---|---|
| v2.7 代码窗口的全部项（`CO-5`/`CO-8`/`CO-9`/`CO-10`/`CO-14`/`CO-15`(v2.6)） | — | **不在本轮** | 返工轮不含新开发（评审 §6 开篇规则）；已由 v2.7 计划 §3.1/§3.2/§3.3 承接 |
| `CO-11(v2.5)` 的板侧验证 | v2.7 Non-goals | **再次暂缓** | 硬件阻塞：bench 仅一路 USB-TTL；触发 = 第二路 USB-TTL 或双板对连 |

### 5.2 新发现问题（本轮作业中发现，供 v2.7 与评审使用）

| # | 问题 | 严重度 | 建议处置 |
|---|---|---|---|
| **R-1** | **`--check` 恒红的第二因：GNU sed 的行尾行为**。`item_lines`（`--diff`/`--snapshot` 用）**天然抗 CRLF**，不是设计使然 —— 是 Cygwin/MSYS 版 GNU sed **读入时即剥离行尾 CR** 的副作用。该行为**无文档保证**，若某天 sed 换成不剥离 CR 的实现（如某些 BusyBox / 严格 POSIX 构建），`item_lines` 会**静默返回 0 条**→ `--diff` 会把全部基线条目报成「修改/删除」→ 冻结面门误红 | 中 | 建议 v2.7/v2.8 给 `item_lines` 加显式 `tr -d '\r'`（一行，零风险），把「抗行尾」从副作用升级为契约。本次**未改**（不属 G0′ 范围，且 `--diff` 现为绿）；记后续版本 |
| **R-2** | **apidump 的 `TMPD_REL="build/.apidump.$$"` 依赖 `build/` 存在**：`mkdir -p` 保证了创建，但若 `build/` 被 `make clean` 删除且当前目录不可写，则会静默退回 `mktemp -d`（回到 v2.6 修掉的 Windows 路径陷阱） | 低 | 建议加一条显式告警（fallback 时打印一行提示），便于诊断。非阻断 |
| **R-3** | **本机 `--check`/`--diff` 耗时（4–7 分钟）由 `gen_body` 的「每头文件一次 awk 进程」决定**（108 次 fork）。这是本机唯一可用但慢的第二因（第一因——逐文件 `rm`——已于 v2.6 移除） | 低 | 若需本机提速，可把逐头文件 awk 改为「一次 awk 处理全部头文件 + 内部切换输出文件」；**不属本版范围**，登记备查 |

---

## 6. 遗留风险

| 风险 | 状态 | 说明 |
|---|---|---|
| `docref` 收窄为「终点须 = HEAD」后，交付后**任何**补正提交都会使门红 | **已接受（设计意图）** | 这正是 `CO-4` 要求的「覆盖到 HEAD」；代价是交付文档必须**最后提交**（或回填后 amend）。v2.7 计划 M5 的顺序（全量回归 → 交付文档 → `tag v2.7`）与本约束**天然一致**。若确需交付后补正，走 `-r2` 轮（其 §4 再以 `HEAD` 为终点） |
| `apidump --check` 依赖 `sed` 的 CR 处理（R-1） | 低（当前为绿） | 现修法**主动**归一（`sed 's/\r$//'`），不再依赖 sed 的隐式剥离；R-1 影响的是 `item_lines` 路径（`--diff`），已登记 |
| `.gitattributes` 未引入 | 保持原状 | 计划附 B 已列其波及面（全仓 `docs/*.md` 检出行为）；本版采「局部归一」避免该波及。长期仍建议评估（记 v2.8 候选） |
| 「21s」类**无环境注记**读数 | 已根治本处 | 本轮已按实测改写；README 发布纪律第 6 条（数字必附环境注记）在 §2 复现环境表与本表落地 |

---

## 7. 提交后回填（本轮四门读数 + 机制自证 + 耗时实测）

### 7.1 四门读数（本轮终态）

| 门 | 命令 | 结果 |
|---|---|---|
| 文档同步 | `sh tools/docsync.sh` | **`docsync: pass=301 fail=0`（exit 0）** |
| 冻结面 | `sh tools/apidump.sh --check` / `--diff` | check **一致（exit 0）**；diff 对 v2.5 基线 **新增 0 / 修改删除 0**（exit 0） |
| 体积 | `sh tools/sizecheck.sh` | **pass=3 fail=0**（23724/24/740、33272/24/2232、24100/28/740，逐值与体积表相等） |
| §4 自指 | `sh tools/docref.sh` | **OK**（`13 files changed, 772 insertions(+), 47 deletions(-)` 与本文 §4 声明**逐值一致**） |
| 覆盖率 | 取自 CI run `35975885148`（`make coverage-build` + `gcovr --print-summary`） | 行 **97.3%**（10261/10550）、函数 **99.2%**（1046/1054）—— **本轮无代码改动，与 v2.6 终值同**（本轮不重跑覆盖，如实声明读数来源） |

### 7.2 机制自证表（门须可红 —— 每项均「先红后绿」）

| 门 | 自证手法（独立构造） | 结果 |
|---|---|---|
| `apidump --check` 行尾修复（AC-1） | ① 基线态：工作树 `docs/API_INVENTORY.md` 为 CRLF（`tr -cd '\r' <docs/API_INVENTORY.md \| wc -c` = 757），生成件 LF → 旧脚本 `diff -q` 报全文件差异；② 注入公开声明 `et_r2_probe_red()` → `--check` 红；③ 按 sha256 回退 → 绿 | ① **FAIL exit 1**（修复前实测，4m24s）；② **FAIL exit 1** 且打印 `+ uint32_t et_r2_probe_red(void);`；③ **exit 0**（`sha256sum protocol/et_modbus.h` 与注入前逐值相同） |
| `docref` 终点覆盖 HEAD（AC-7） | 在 `<end>` 之后制造一个**纯文档提交**（临时改动本文档一行并 commit）→ 重跑 `docref`；随后 `git reset --hard` 回退该提交并复验 | 见 §7.4 实测行 |
| docsync 同族漏刷门（AC-5 补门） | ① 把 `README.md` 的 `API_INVENTORY_v2.5.md` 改回 `_v2.4.md` → `docsync` 应红；② 在 `port/stm32g474/README.md` 注入「未同步」→ 应红；③ 复验还原后绿 | 见 §7.4 实测行 |
| docsync 跨文档计数（沿用 v2.6） | 把 `docs/architecture.md` 的 `五例` → `四例` | 改后 **FAIL**（`跨文档 examples 数不一致: 实测=5 arch=四→4 README=五→5`）；还原后绿 |

### 7.3 耗时实测表（`CO-13` 的处置证据）

| 命令 | 修复前 | 修复后 | shell |
|---|---|---|---|
| `sh tools/apidump.sh --check` | **4m24s**（264s，exit 1） | **4m18s**（exit 0） | `C:\Users\ljh\.workbuddy\binaries\PortableGit\versions\1.2.0\usr\bin\sh`（GNU bash 5.3.15, cygwin） |
| `sh tools/apidump.sh --diff` | — | **4m25s**（exit 0） | 同上 |

> 量级归因：耗时由 `gen_body` 的「按头文件逐个 awk 调用」决定（本机 108 次进程创建 + 文件写读），
> **与行尾无关、与逐文件 `rm` 无关**（后者已于 v2.6 移除）。评审 E6/E7 的交叉复核（4m39s / 4m53s，
> 系统 Git sh）与本表同量级。

### 7.4 自证实验实测行

| 实验 | 手法（独立构造） | 实测结果 |
|---|---|---|
| `apidump --check` 可红（AC-1 ①） | 注入 `uint32_t et_r2_probe_red(void);` 到 `protocol/et_modbus.h` | **exit 1**（257s）：diff 打印 `- ### 函数声明 (6)` / `+ ### 函数声明 (7)` 与 `+ - \`uint32_t et_r2_probe_red(void);\`` |
| `apidump --check` 复绿（AC-1 ②③） | `cp` 回退注入前字节 | `sha256sum protocol/et_modbus.h` = `fea45c79c4af0df9a169e9e7f8672636ddb8cac0ab9c92864bf9e35502a8311e`（与注入前**逐值相同**）→ **exit 0**（262s）：`apidump: 清单与头文件一致` |
| docsync 同族漏刷门可红（AC-5 补门） | 把 `README.md` 的冻结基线名 `docs/API_INVENTORY_v2.5.md` 改回 `_v2.4.md` | **exit 1**（437s）：`FAIL README 冻结基线名 [docs/API_INVENTORY_v2.4.md] ≠ apidump 默认基线 [docs/API_INVENTORY_v2.5.md] (CO-6(v2.6) 同族漏刷)`；还原后（`sha256sum README.md` 与变异前逐值相同）**exit 0** |
| `docref` 终点覆盖 HEAD 可红（AC-7） | 把本文档副本的 §4 终点改为**固定 SHA**（`c865f69..a0b6748`）→ 该副本在终点==HEAD 时 `docref` **OK**（探针 A） | 随后追加一个**纯文档提交**（`docs/bench.md` 加一行）→ 同一副本 `docref` **FAIL exit 1**：`记录终点未覆盖 HEAD … 声明 END = a0b6748 / 实测 HEAD = 35036b7`（探针 B）；`git reset --hard a0b6748` 回退后真档 `docref` 复 **OK** |
| docsync 跨文档计数可红（沿用 v2.6） | 把 `docs/architecture.md` 的 `五例` → `四例` | **exit 1**：`FAIL 跨文档 examples 数不一致: 实测=5 arch=四→4 README=五→5`（v2.6 已独立复现，本轮沿用，未重复变异以省机时） |

### 7.5 终态一致性逐句比对（AC-4）

| 位置 | 原表述（互斥源） | 终态表述 |
|---|---|---|
| v2.6 交付文档 §1 状态声明 | 「`tag v2.5` 已在本地定着…（**远端推送被本机出口代理阻断，待推送后出 Release**）」 | 「`tag v2.5` = `25f3316` **已推送并出 Release**（四附件在位）」 |
| 同 §1 | 「**仍未满足**：…、**CI 未核验**（推送未完成）」 | 「**CI 已核验**：run `35975885148` 七 job 全绿」；未满足项仅列 `selftest` 维持 20 与 `CO-6` 板上对照不可得 |
| §4 | 「**CI**：待推送后核验（job 数仍为 7，步骤 +2）」 | 「**已核验** —— run `35975885148`（commit `2e57b16`）七 job 全绿（success）」 |
| §5.1（G0-5 行） | 「⚠️ **本地已打 / 远端待推送**」 | 「✅ **已完成**」+ 四附件逐项大小 |
| §5.1（CI 行） | 「⏳ **待推送后核验**」 | 「✅ **已完成**」+ run 号与同轮 failure 的定位 |
| 文末纪律句 | 「…故本交付**不合格**（HC-9 未满足）」 | 「…未能核验者一律不按通过计 → 本版唯一此类项 = `CO-6` 板上对照（AC-17 记「部分通过（如实）」）；终态判定见 §2 AC-21 与 §7」 |

**一致性结论（实测）**：上述 6 处全部与 §2 AC-4/AC-21、§7 的终态**不再互斥**；该文档终态版 `grep -c "不合格"` = **1**、`grep -c "待推送后核验"` = **1**、`grep -c "待推送"` = **2** —— 全部为**历史引述**（「r2 修订声明」第 13 行 + §4 更正溯源第 116 行），**无一处是终态声称**，故满足计划 AC-4 的「除历史引用」判据。

### 7.6 快照无副作用复核（v2.7 计划 §6 的清单）

| 命令 | 结果 |
|---|---|
| `git tag -l 'snap-2.7-start'` | `snap-2.7-start`（已存在，**不带 `v` 前缀**；`git rev-parse snap-2.7-start` == 本节所在提交 —— 打 tag 后实测） |
| `gh run list --limit 3` | **探针实测（不依赖未测断言）**：先打并推一个 `snap-*` 探针 tag（`snap-probe-nosideeffect`，指向同一 commit，随后本地与远端均已删除）→ **未产生任何新 run**；同期唯一新 run = 推 `main` 的 `36145570807`（见 §4）—— 证明 `snap-*` 不匹配 `release.yml` 的 `tags: [v*]` |
| `gh release list --limit 3` | **无新增 Release**（最新仍为 **v2.6**，标记 `Latest`）—— 与「探针 tag 不触发 Release」自洽 |
| `sh tools/apidump.sh --check ; echo "exit=$?"` | **exit=0** |
| `sh tools/docsync.sh ; echo "exit=$?"` | **`pass=301 fail=0`，exit=0** |

---

## 8. CO 处置与候选池一致性（G0′ 七项逐条对账）

> 本节为 **HC-11 的第三落点**（v2.6 交付文档缺 §8 即评审不通过）—— 落点机制沿用，但**对账对象不同**：
> v2.6 的 §8 对账 v2.5 评审的 `CO-n(v2.5)`；本节对账**本轮处置对象** `CO-n(v2.6)`（评审 §6.1 的 G0′ 七项）。
> 三处落点 = ① `v2.7开发计划__从站解析对称化与板上自测收口.md` §1.1（G0′ 项被收为前置门）/ ② `docs/v3-candidates.md`
> 「v2.7 复评记录」节（由 v2.7 计划 HC-11 要求新增，**不在本轮**）/ ③ **本节**。
> 本节的 CO 编号一律带后缀：`CO-n(v2.6)` 指本轮处置对象（v2.6 评审新发现）。

| G0′ | 对应 CO | 状态 | 证据 |
|---|---|---|---|
| **G0′-1** | `CO-1(v2.6)`（`--check` 行尾敏感，发布阻塞级） + `CO-13(v2.6)`（耗时读数） | ✅ **已处置** | 修法见 §3 D-3；`--check` exit 0（AC-1）；耗时重测见 §7.3（AC-2） |
| **G0′-2** | `CO-2(v2.6)`（已发布产物含不实内容） | ✅ **已处置** | CHANGELOG「挂账」段重写 + 新增 `### v2.6-r2 返工轮` 节 + Release v2.6 正文 `gh release edit --notes-file` 重注；**未重打 tag**（AC-3） |
| **G0′-3** | `CO-3(v2.6)`（交付文档自相矛盾） | ✅ **已处置** | §7.5 逐句比对；`不合格` / `待推送后核验` 计数归零（AC-4） |
| **G0′-4** | `CO-6(v2.6)` / `CO-7(v2.6)` / `CO-12(v2.6)`（三处同族漏刷） | ✅ **已处置 + 已装门** | 三处逐项更正（AC-5）+ docsync 4 条同族断言（§3 D-5） |
| **G0′-5** | `CO-11(v2.6)`（计划 AC 落点过时） | ✅ **已处置** | 模板 §7 增至五项 + 实机记录 §12 占位节标注承接（AC-6） |
| **G0′-6** | `CO-4(v2.6)`（`docref` 的设计性空隙） | ✅ **已处置** | 门收窄为「终点须 = HEAD」+ 自证可红（AC-7；§7.4） |
| **G0′-7** | —（纪律项） | ✅ **已处置** | 四门读数 §7.1 + CI 见 §4 + 自指声明（本文首行 `本版 docsync：301/301`） |

**未纳入本轮的 CO（按评审 §6.2/§6.3 归 v2.7）**：`CO-5(v2.6)`（从站侧同源缺陷，★ 核心）、`CO-8(v2.6)`（`disc=1` 定位与交付文档登记）、`CO-9(v2.6)`（主站死代码）、`CO-10(v2.6)`（`L` + 整数转换）、`CO-14(v2.6)`（selftest 20→22，**必办**）、`CO-15(v2.6)`（bench 相对比值化，可选）、`D-7(v2.6)`（`git worktree` 真生成基线清单）。措辞与 `v2.7开发计划__…md` §1.1 及 `docs/v3-candidates.md`「v2.7 复评记录」节**逐条一致**（由 v2.7 计划 HC-11 与本节并置对账）。

---

> 交付纪律：本交付中每条「通过」均有本文所列命令可复现；**未能核验者一律不按通过计**（本轮无此类项 —— 四门与全部自证实验均已在本轮终态取得）。
> 本轮**不含新开发**，故不涉及任何库内行为变更、不涉及 `apidump --diff` 的基线语义（实测仍为 **0/0**）。
