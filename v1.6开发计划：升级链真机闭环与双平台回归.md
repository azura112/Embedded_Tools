# v1.6 开发计划：升级链真机闭环与双平台回归

> 起草日期：2026-09-06 ｜ 基线：v1.5（tag `a88ebff`；G4 适配在工作区待提交）｜ 状态：草案
> 上一版本：[v1.5] 23 模块 / 279 用例全过 / 覆盖 96.8% / 远程 CI + Release 全通 / G474 真机 13/13 自测
> 输入：G474 实机记录挂账（升级链走单、IWDG 真超时、LED/按键补录、轨 A 上板、Renode G4）；v1.5 验收（G4 适配未入库）；v1.5 开发教训（8B 槽适配曾被单一 flash 几何掩盖）

---

## 1. 版本定位

- **版本号 v1.6**：G4 成果入库 + 升级链真机走单 + 回归制度化 + sched/stimer tickless 增量 API，无破坏性变更（MINOR）。
- **本版主题**：把"组件可用"收敛为"**链路可证**"——升级链在 G474 真机走完并记录；同时把 8B 槽适配的教训制度化（**双几何回归**：任何 storage 布局假设必须在 F1/G4 两种 flash 几何下都过全量），并给出 tickless 休眠的库侧支撑。
- **不变式**（继续遵守）：零动态内存、多实例句柄化、core/algorithm 层零硬件依赖、ISR-safe 范围显式标注。

## 2. v1.5 现状盘点

| 项 | 状态 |
|---|---|
| 模块 | 23 个，279 用例全过，覆盖 96.8%；CI（单测矩阵 + Renode smoke + 覆盖率门 + docsync + G4 待补）与 Release 两段式全通 |
| 平台 | host / F103（编译+仿真验证）/ **G474（真机 13/13 自测，常设挂账已清）** |
| 未入库 | G4 8B 槽适配（et_kv/et_bootctl/测试×2/docsync/port.h，9 文件）+ `port/stm32g474/` + `stm32g474_demo.c` + 实机记录——工作区待提交（docsync 已备 7 条新断言） |
| G474 挂账 | 升级链真机走单未记录；IWDG 真超时未实测；LED 极性/按键事件未正式记录；轨 A 未单独上板；Renode G4 未验证 |
| 结构性缺口 | 回归矩阵只有功能开关变体（XM_1K/CRC_TABLE/HISTORY），**无 flash 几何变体**——8B 槽适配正是被"host 单一几何全绿"掩盖后由真机暴露的 |

## 3. 开发范围

### P0 G4 成果入库（约 0.5 天，**首日必做**）

| # | 任务 | 验收标准 |
|---|---|---|
| 1 | 提交工作区 G4 适配（9 改动 + 3 未跟踪），推送并观察 CI | CI 全绿含 docsync 49 断言；`origin/main` 与本地一致 |
| 2 | port README v1.5 体积行确认为适配后复测值 15468（随 P0-1 入库） | docsync 体积表断言绿 |
| 3 | `et_config.h` 版本宏升至 1.6.0 | 发布时与 tag 核对一致 |

### P1 升级链真机闭环（★ 核心，1.5 人日，G474 板）

把 v1.5 组件在真机上走完三条叙事并固化为记录（实机记录文档新增"升级链"章节）：

| # | 走单内容 | 验收证据（串口日志） |
|---|---|---|
| 1 | `AT+SIMUPGRADE <even_ver>` → 复位 → 自检失败不确认 → 超次 **ROLLBACK** | attempt 计数递增 + 回滚日志 + 回滚后旧镜像 boot 续走 |
| 2 | `AT+SIMUPGRADE <odd_ver>` → 复位 → 自检通过 → **confirm** | stage→confirm 状态迁移 + 确认后 attempt 不再累计 |
| 3 | `AT+UPGRADE` **xmodem 真传输** → B 槽 → verify → stage | xmodem 收包字节数/块数与发送端一致，镜像 CRC 通过 |
| 4 | IWDG 真超时复位（短超时 200ms + 不喂狗） | 复位发生 + 复位后 boot#n+1 + 自检正常（看门狗复位原因可读则一并记录） |
| 5 | LED 极性 / 按键短按长按补录 | 实机记录表格补行（PC0 极性实测结论） |

- **可复现工具**：xmodem 发送端入库 `tools/xmodem_send.py`（pyserial，参数化端口/文件/1K），真机走单不依赖"串口助手手工操作"；
- docsync 新增断言：实机记录升级链章节锚点 + 关键日志行存在性；
- 风险控制：IWDG 演示先确认调试器在手（可复位恢复），超时选档避开擦扇区路径。

### P2 双几何回归制度化（约 1 天）

**8B 槽教训的制度化**：storage 布局代码必须在 F1/G4 两种几何下都过全量。

| # | 任务 | 说明 |
|---|---|---|
| 1 | host 测试增加**几何变体**：`-DPORT_FLASH_SECTOR_SIZE=1024 -DPORT_FLASH_SECTOR_COUNT=16`（F1）与 `=2048/=16`（G4）各跑全量 279+ | 布局差异（页满/压实/乒乓/掉电矩阵在双几何下）全绿；发现用例的几何假设即修用例 |
| 2 | CI 增 **G474 交叉编译 job**：`-mcpu=cortex-m4` 编译库 + `stm32g474_demo`（带 `-D` 三宏，port 层 #error 守卫验证） | 零警告；与 F103 job 并行 |
| 3 | Makefile/README 记录双几何变体命令；docsync 增补断言 | 变体成为发布 checklist 固定项 |

### P2-2 tickless 增量 API（约 1 天，纯增量）

G474 记录的 WFI 教训（RX 丢字节）说明休眠需要库侧配合；本轮只交付"算出还能睡多久"，不改既有语义：

```c
/* sched: 最近到期任务还需多少 ms; 无注册任务返回 PORT_TICK_WAIT_FOREVER */
port_tick_ms_t et_sched_next_due(void);
/* stimer: 最近到期定时器同款 */
port_tick_ms_t et_stimer_next_due(void);
```

- 语义决议（预填）：返回**相对毫秒**（≤ 则已到期应立即 poll）；跨回绕用无符号减法；ISR 启停定时器后主循环侧结果自然生效（下次调用重算）；
- API_GUIDE 配方：`WFI + UART RX 中断唤醒`——**RX 中断必须为唤醒源**（引 G474 RX 问题记录为反例），投喂流程 = 中断置事件 → WFI 醒 → poll；
- 单测 ≥ 10 例：空表、单/多任务取最近、已到期返回 0、回绕边界、运行中注销后重算、与 poll_once 语义一致性。

### P3 可选（不阻塞发布，时间盒各 0.5 天）

| # | 任务 | 说明 |
|---|---|---|
| 1 | host 基准套件 `tools/bench.c` | ringbuf 吞吐 / crc16 MB/s / xmodem 块速率，数字进 README（**环境注记强制**，沿 checklist） |
| 2 | Renode G4 模型探索 | cortex-m4 + G4 flash 双字时序建模若不可行，记录差异即关闭 |
| 3 | F103 真机 | 维持常设挂账（无硬件）；G474 已证平台可行性，F103 不再单独排期 |

## 4. 交付标准（DoD）

1. 沿用 v1.5 全部 DoD（含 docsync 全绿门、量化声明附复现命令与环境注记）；
2. **双几何变体全绿作为发布硬性项**：storage 布局类改动（et_kv/et_bootctl/port flash）必须在 F1/G4 双几何下回归；
3. 升级链走单记录入实机记录文档 + docsync 断言；xmodem 发送脚本可复现（命令行参数化）；
4. tickless API 头文件注明与 WFI/RX 唤醒的配对约束（防止 G474 RX 教训复发）；
5. CI G474 job 连续 3 run 绿。

## 5. 里程碑（按顺序推进）

| 里程碑 | 内容 | 预估累计 |
|---|---|---|
| M1 | P0 G4 入库 + CI 绿 | D+0.5 |
| M2 | P1 升级链三条叙事 + IWDG + LED/按键真机记录 | D+2 |
| M3 | P2 双几何变体 + CI G4 job | D+3 |
| M4 | P2-2 tickless + 文档同步 | D+4 |
| M5 | P3 时间盒（可选）→ 全量回归 → **tag v1.6** | D+5 |

## 6. 明确不做（Non-goals）

- 不做 bootloader 参考工程 / 安全启动（签名/防回滚版本强制）/ 固件加密——沿 v1.5 边界；
- 不做 USB、不做 RTOS、不做浮点格式化、不引入动态内存；
- shell Tab 补全 / 多行编辑维持排除；
- tickless 不做自动休眠（库不替应用决定何时 WFI，只提供"还能睡多久"）；
- F103 真机不再单独排期（常设挂账，G474 已承担真机验证职责）。

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| IWDG 真超时测试可能陷入复位循环 | 演示"变砖"观感 | 短超时仅用于专测用例，验证完即恢复常规配置；调试器在手（ST-Link）随时重烧；复位原因寄存器佐证 |
| xmodem 真传依赖上位机脚本质量 | 走单失败排查困难 | `tools/xmodem_send.py` 先对 host 模拟端到端自测，再上板；脚本带 `--verbose` |
| 双几何变体暴露既有用例的几何硬编码 | 回归红 | 用例假设显式化（容量经 `et_mempool_bytes_needed` 类 API 计算）；修用例不放宽产品代码 |
| G4 几何下页满/压实行为与 F1 有差异 | 隐藏 bug 再现 | 变体矩阵含掉电注入子集（kv 28 例 + bootctl 24 例在双几何下各跑） |
| tickless 与串口 RX 唤醒配置遗漏 | 复现 G474 RX 丢包 | 配方强制"RX 中断 = 唤醒源"检查单；demo tickless 示例默认带 RX 唤醒 |
| Renode 无 G4 精确模型 | P3-2 不可行 | 时间盒探索，建模不可行则以"差异记录"关闭，不阻塞主线 |

---

> 维护约定：开发过程中若范围变更，直接修订本文档并在文首"状态"行记录变更日期；发布 v1.6 后本文档归档不再改动。
