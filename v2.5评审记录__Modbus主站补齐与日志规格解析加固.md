# v2.5 评审记录

> 评审对象：`v2.5开发计划__Modbus主站补齐与日志规格解析加固.md` + `v2.5开发交付__Modbus主站补齐与日志规格解析加固.md`
> 评审日期：2026-09-19 ｜ 评审基线：`tag v2.5-start` = `b0fc5d8`（= `tag v2.4`）
> 实际被评 tree：`v2.5-start..HEAD` = `e61ccd7`（`origin/main` = `e61ccd7`，工作区 clean）
> 结论：**返工**（下一轮文件加 `-r2` 后缀）

## 0. 核验方法与范围

**手段（本机实跑，非引用交付文档）**：Windows 11 / Git Bash / MinGW-w64 gcc 16.1.0 / gawk / gcovr 8.6 / `gh`。

| 手段 | 命令 | 用途 |
|---|---|---|
| 改动清单 | `git diff --stat v2.5-start..HEAD`（35 文件 / +4769 / −179） | 先 diff 后文档；逐文件细读 `et_log.c/.h`、`et_modbus_master.c/.h`、两份新测试、示例、工具、全部文档 diff |
| 机制门复跑 | `mingw32-make test` / `test-g4` / `test-tab` / `ex` / `coverage-build` + `gcovr` | 用例数、五例、覆盖率 |
| 治理脚本复跑 | `apidump.sh --diff` / `--check`、`sizecheck.sh`、`docsync.sh` | 冻结面、体积逐值、文档同步 |
| 变体复跑 | `gcc -DET_XM_1K=1 …`（同源清单） | 1K 变体计数 |
| 工具复跑 | `python tools/modbus_master.py --selftest` / `--help` | 跨实现对拍与 CLI 面 |
| CI 侧核验 | `gh run view 35380622040` / `--job 105715651287` / `gh run list` | 七 job 与 ASan 步骤、首轮红/取消的如实性 |
| 板侧独立复核 | `diff -rq` 七目录 + 两文件（对 `D:\code\STM32CubeMX\G474VET6_ET_TEST\Core\et`）；`arm-none-eabi-size` / `nm` 对既有 Release ELF | P2-1 同步声称、体积读数、符号归因 |
| 定向探针 | `build/probe_log.c`、`build/probe_mbm.c`（评审期临时，已删） | 两处交付文档未提及的边界（见 CO-6 / CO-7） |

**未能核验**：板上真机走单（P2-2/P2-3/P2-4）—— 本机无 ST-Link / 无 CH343 USB-TTL，与交付文档 D-1 所述一致；`make test-asan` 本机不可执行（MinGW 无 libasan，与 D-2 一致），改由 CI 步骤核验（已实测通过）。**未核验项一律不按通过计**。

**关于编号**：本记录的 `CO-n` 属 **v2.5**（`CO-n(v2.5)`），与 v2.5 计划 §1.1 中承接自 v2.4 的 `CO-1…CO-11` 是两套编号（后者按模板规则应写全 `CO-n(v2.4)`）。

---

## 1. 结论

**返工。**

- **不通过的直接依据**：计划 §2 规定「HC-1…HC-11 违反任一条即交付不合格」。本版 **HC-10（真机证据）未满足**，交付文档 §3 D-1 与 §2 AC-21/AC-22 均已如实声明不通过 —— 事实成立，且**不是**虚假声称（交付文档主动宣告不合格并**未打 `v2.5` tag**，该决策正确，本记录予以确认）。
- **附加不通过项（本记录新发现，交付文档未记载）**：
  - **HC-7 数字回刷漏 3 处**（`docs/architecture.md:111` 仍写「四例」（examples 本版 4→5）、`:112` 仍写「docsync 210 断言」（实测 291）、`docs/API_GUIDE.md:1406/1419` 仍写「398」（实测 474））；
  - **HC-11 的第三落点缺失**：`交付文档 §8` 不存在（交付文档末节为 §7），而 HC-11/AC-2/AC-25 都以「交付文档 §8」为对账落点，AC-2 的证据因此自指一个不存在的小节；
  - **交付文档 §4 的 commit/diffstat 与实际 tree 不符**（详见 D-10）。
- **返工范围的界定**：**库内成果（M1–M3）判定为可接受，不需要重做**。本记录对 AC-1、AC-4~AC-20、AC-23~AC-25 逐条复跑并全部复现（用例 474×2 / 1K 475 / 覆盖率 97.3% / 五例 PASS / apidump 16:0 / sizecheck 3:3 / docsync 291:0 / CI 七 job 全绿）。返工 = **① 板接入后补齐 P2-2/P2-3/P2-4（硬件门，不可代偿）+ ② 三处纯文本修订**（HC-7 数字、交付文档 §8、§4 回填）。
- **发布状态**：维持「未打 tag」。补齐 ① 与 ② 后即可发版，无需二次开发。

---

## 2. AC 核验表

判定口径：**通过** = 声称与 diff/实跑一致；**不通过** = 声称无证据或与事实冲突；**部分通过** = 子项混合（逐项列明）。

| AC | 交付声称 | 核验结果 | 证据或缺口 |
|---|---|---|---|
| AC-1 | 通过 | **通过** | 复跑 `apidump.sh --diff` → `新增 16 项 / 修改删除 0 项`，退出码 0；`docs/API_INVENTORY_v2.4.md` 为**真 v2.4 面**（`grep -c et_modbus_master` = 0、`ET_VERSION_MINOR 4`），417 比较单元；`v2.2/v2.3` 归档未出现在 `diff --stat` 中 |
| AC-2 | 通过 | **不通过（部分）** | #8 行「维持排队(v2.5 复评完成)」+「下次触发 = 用户提交具体频响指标」`grep` = 1 ✓；`grep -c biquad docs/API_INVENTORY.md` = 0 ✓；**但证据列的「与本文 §8 措辞一致」所指的 §8 不存在** → 三处对账只完成两处（计划 §1.1 / v3-candidates），HC-11 第三落点缺失 |
| AC-3 | 通过 | **部分通过** | 探针实测 `ver=2.5.0 int=0x020500 maj=2 min=5 pat=0` ✓；**板上横幅与 `AT+VER` 未验证**（交付证据列自述「未验证（板未接入）」）→ 该子项无证据，与 AC-21 同源 |
| AC-4 | 通过 | **通过** | `docs/评审记录模板.md` 含「CO 编号规则」节、处置四选项（纳入/暂缓/拒绝/已关闭）、触发条件字段、双向互引规则（已读全文）；`README.md` checklist 第 9 条实测在位；`docsync` 自指断言「交付文档引用评审记录或声明缺失」实测生效（291/291） |
| AC-5 | 通过 | **通过** | 复跑 `[Suite] debug/log (24 cases) => pass=24 fail=0`（基线 7 + 新增 17 ≥ 16）；读码确认 `CHECK_FMT`/`CHECK_LIT` 每条规格后随 `SENT=0x5A5A5A5A` 并断言尾部 ` S=1515870810`（错位检测手法真实存在） |
| AC-6 | 通过 | **通过** | 读码确认 13 类规格组（field_width/left_align/sign_flags/alt_prefix/star_width/precision_int/precision_str/length_mods/size_mod/legacy_unchanged/mixed_matrix…）均在**同一用例内**由 host `snprintf` 生成 ref 并 `strcmp` 逐字节对拍。**缺口**：对拍子集不含 `%c` 的域宽/精度（见 CO-7），而文档的修饰符承诺是全域的 |
| AC-7 | 通过 | **通过** | `log.unsup_float`：8 个浮点转换 + `%10.3f` + 双 `%f` → `<?f>` 等占位且哨兵正确，含负向 `strstr(g_buf,"%f")==NULL` 三条；`log.unsup_others`：`%o/%#o/%5o/%lo → <?o>`、`%lc/%ls/%n/%ln` 占位，且 `probe==0x1234 && probe2==0x5678` 实证 **`%n` 未写内存**；`log.unknown_conv` 含 `%y|%u → <?y>|7` 不消费断言 |
| AC-8 | 通过（本机受限→ASan 门落 CI；该门抓到既有缺陷 N-9） | **通过** | 本机：`log.field_cap` 断言 `%9999u`/`%*u`(100000)/`%.9999d`/`%9999s` 长度**恰为** `ET_LOG_FIELD_MAX`(255)、`%200u\|%200u`=401 —— 属 24 例之一，复跑 fail=0；本机 ASan 确不可执行（`gcc -fsanitize=address` 不可用，与 D-2 一致）；**CI 侧已独立核验**：`gh run view --job 105715651287` 显示步骤 `Run unit tests under AddressSanitizer (make test-asan, v2.5 AC-8)` ✓ |
| AC-9 | 通过 | **通过** | `grep -c "手工拼\|dt_line" examples/stm32g474_demo.c` = 0 ✓；两 demo 改为 `%04u-%02u-%02u %02u:%02u:%02u`（读 diff 确认 24 行绕行删除）；**全库 `ET_LOG*` 调用点规格扫描**（含多行格式串）结果仅 `%u %s %d %02u %x %c %04u %lx %llu %ld %X %.*s %%` —— 全部为受支持规格，无遗留 |
| AC-10 | 通过 | **通过** | 复跑 `sizecheck: pass=3 fail=0`（23480/24/740、33032/24/2232、23860/28/740 逐值 = 实测）；两 port README 的 v2.5 行在位且逐值相等 |
| AC-11 | 通过 | **通过** | `--diff` 16 项 = 函数 11（含 `_data`）+ 类型 4 + 宏 1（`ET_LOG_FIELD_MAX`）；修改/删除 0；输出无 `!` 行 |
| AC-12 | 通过 | **通过** | 复跑 `grep -c "0x03\|0x04\|0x06\|0x10" protocol/et_modbus_master.c` = **0**；读码确认功能码一律经 `ET_MODBUS_FC_*`，数量边界经 `RD_QTY_MAX/WR_QTY_MAX/ADU_MAX` |
| AC-13 | 通过 | **通过** | 复跑 `[Suite] proto/modbus_master (25 cases) => pass=25 fail=0` |
| AC-14 | 通过 | **通过** | 读码 + 复跑：`mbm.timeout_retry_exhaust` 断言 `memcmp(req, first, firstlen)==0`（含 CRC）、`retries=2`、终态 `ET_MB_TIMEOUT`、`st.requests=3`、终态保持（`poll(now+10000)` 仍 TIMEOUT） |
| AC-15 | 通过 | **通过** | `mbm.late_no_pollution`：终态后喂入上一事务应答 → `feed()==0` 且 `late=1`；下一事务 `data()` 逐值 `00 01 00 02 00 03 00 04`；另有「终态期陈旧半帧 → `late=2` → 下一事务照常解析 `0x4321`」 |
| AC-16 | 通过 | **通过** | `mbm.exception_no_retry`：`ET_MB_EXC` + 异常码 `ET_MODBUS_EXC_ILLEGAL_ADDR`、`retries=0`、`requests=1`、时间推进 10×超时后**仍** `ET_MB_EXC`（不转 TIMEOUT） |
| AC-17 | 通过 | **通过** | `mbm.broadcast_write`：`addr=0` + `sent()` 后 `poll(&m, g_now)`（时间未推进）即 `ET_MB_OK`、`tx()==NULL`、`requests=1/timeouts=0/responses=0`，请求载荷 `00 06 00 05 BE EF` + CRC 校验 |
| AC-18 | 通过 | **通过** | 复跑 `make ex` → 五例 PASS；读码确认 `ex_modbus_master` 断言全为 hexdump + `memcmp` + 逐值（`d[1]/d[3]/d[5]/d[7]`），含丢首应答重发（5a/5b）与异常（6），时间由 `g_now` 虚拟推进、**零耗时断言**，仅用公开 API |
| AC-19 | 通过 | **通过** | 复跑 `--selftest` → 全项 `ok` + `PASS`（含 slave-drop 3 项、slave-exc 2 项、slave-regs、extract_request 5 项、cross-impl 5 项）；`--help` 确认 `--slave/--slave-drop/--slave-exc/--slave-regs/--slave-nregs/--slave-frames` 全部注册 |
| AC-20 | 通过 | **通过** | API_GUIDE 5.7 改为 `mb_flush()` 两个 flush 点 + 正文「应答有两条产生路径」段（读 diff 确认）；`ex_modbus_slave` 步骤 5 显式建模两 flush 点（feed 后断言 `response()==NULL`、tick 后断言拿到 `11 E3 01 A9 35`），`make ex` 通过 |
| AC-21 | **不通过（阻塞：板未接入）** | **确认（不通过成立）** | **P2-1 独立复核通过**：`diff -rq` 七目录 + `et_config.h` + `port.h` 对板侧工程**全 OK**（逐字节同步）；既有 Release ELF `arm-none-eabi-size` = text 38640 + data 72 = **FLASH 38712 B**、data+bss = **RAM 5312 B**（与声称逐字节一致）；`arm-none-eabi-nm \| grep -c et_modbus_master` = **0**（`--gc-sections` 丢弃未调用模块 → 板侧零回归成立）；`emit_number` = 0x1AC = 428 B（与归因一致）。**但 P2-3 的逐字节走单记录、重发序列、异常码、广播写、`et_log` 域宽行原样均未取得** → HC-10 未满足 |
| AC-22 | **不通过（阻塞：板未接入）** | **确认（不通过成立）** | `AT+SELFTEST`/`AT+SELFSTOR`/`AT+SIMUPGRADE 3` 无板上记录；selftest **维持 20** 已按 HC-9 显式声明并给理由（§5.1）；`tools/smoke.sh` 本版**零改动**（`git diff --stat` 空）→ 与「维持 20」自洽 |
| AC-23 | 通过 | **通过** | 复跑 `docsync: pass=291 fail=0`；交付文档首行 `本版 docsync：**291/291**` 与实测一致（自指断言通过） |
| AC-24 | 通过（CI 首轮红→修复后复跑绿，见 D-8） | **通过** | 复跑 `test`/`test-g4`/`test-tab` 均 `ALL PASS (fail=0)`，用例合计 **474**（`grep -cE "PASS$"` = 474）；1K 变体 `-DET_XM_1K=1` 实测 **475**；`gcovr --fail-under-line 85` → **行 97.3%（10070/10353）、函数 99.2%（1029/1037）**，退出码 0；CI **七 job 全绿**（`gh run view 35380622040` 逐 job ✓）；首轮红与取消的如实性见 D-8/D-9 |
| AC-25 | 部分完成（逐项声明） | **通过（如实）** | P3-1 做：`docs/bench.md` 有 v2.5 行 + 抖动说明（我复跑得 40.0/110.0 ns/op，与文档值 60.0/175.0 及所述 155~210 带不重合 → 见 CO-14）；P3-2 不做并显式声明「维持 20」+理由 + 记 v2.6；P3-3 做：API_GUIDE 11.13 补 4 条 32 位组合说明 |

**「声称通过但无证据」清单（按评审规则默认判不通过）**：AC-2 的 §8 对账（小节不存在）、AC-3 的板上子项（无板侧记录）、AC-21/AC-22（交付已自认不通过，非虚假声称，但客观未达标）。

---

## 3. HC 核验

| HC | 结论 | 证据 |
|---|---|---|
| HC-1 契约零破坏 | **通过** | `apidump --diff` 16/0 退出码 0；`--check` 一致；无 `!` 行；`ET_MODBUS_*` 既有宏值无变更；`et_config.h` 仅注释行 + 版本宏（版本宏属白名单） |
| HC-2 et_log 实参消费正确性 | **通过** | 读码：`parse_spec` 完整吃 标志{`-0+ 空格#`} + 域宽（含 `*`，负值转左对齐并防 `INT_MIN` 溢出）+ 精度（`.N`/`.*`，负精度按未给出）+ 长度{h hh l ll z}；每条规格用例带哨兵实证不错位；`log.misalign_regress` 复现 CO-9 三处受害点并断言无字面 `%` 残留 |
| HC-3 不支持规格必须可见 | **通过** | `%o`/浮点族（按 `double` 消费）/`%n`（消费指针、**禁写内存**，由 `probe` 哨兵断言实证）/`%lc`/`%ls` → 固定占位 `<?c>`；未知转换字符占位不消费且**在 `et_log.h` 头注与 API_GUIDE 8.1 明示为例外**；无字面回显（负向 `strstr` 断言） |
| HC-4 主站职责边界 | **通过** | 头注「单事务」显式；`grep -c "port_tick_get_ms\|port_"` = **0**（不内部取时基）；无轮询表/调度器代码；`resp_timeout_ms` 由 cfg 注入且 `init` 拒绝 0；`read/write` 在 `ET_MB_BUSY` 期间返回 false（用例 `mbm.qty_bounds_read` 覆盖） |
| HC-5 协议常量单一来源 | **通过** | `et_modbus_master.h` `#include "et_modbus.h"`；`.c` 中 `grep -c "0x03\|0x04\|0x06\|0x10"` = 0（注释亦改写以过机械门）；引用 `ET_MODBUS_FC_*`/`RD_QTY_MAX`/`WR_QTY_MAX`/`ADU_MAX`；`grep -c ET_MODULE_MODBUS_MASTER` = 0（未新增开关，沿用 `ET_MODULE_MODBUS`） |
| HC-6 库不变式 | **通过** | `grep -c "malloc\|calloc\|realloc\|free("` = 0（零动态内存）；多实例用例 `mbm.multi_instance`（两实例并发在途互不干扰）；新模块在 `protocol/`，零 port 依赖；头注「并发约定: 全部 API 仅限 🏠MAIN」；新增的 4 类型均为**新类型**，无既有冻结结构体字段追加（交付 §2 末已登记） |
| HC-7 数字回刷同批 | **不通过** | 已刷：模块 33→34、用例 432→474、examples 4→5（README 特性表/结构树/质量门、getting-started、API_GUIDE 5.7/5.8、architecture 选型表、CHANGELOG、port 体积表、docsync 断言与自指计数）。**未刷 3 处**：`docs/architecture.md:111` 仍「四例」、`:112` 仍「docsync 210 断言」（实测 291）、`docs/API_GUIDE.md:1406/1419` 仍「398」（实测 474）。无门守护（docsync 未断言这三处）→ 见 CO-3/CO-4 |
| HC-8 示例即证据 | **通过** | `ex_modbus_master` 进 `make ex`（五例 PASS）；断言为字节级/逐值比对，无耗时断言（虚拟时间 `g_now`）；只用公开 API |
| HC-9 selftest 若扩必须全链回刷 | **通过** | 未扩（维持 20），交付 §5.1 显式声明 + 给理由（扩套件需板上复验，无板则只有 host 单侧证据，不满足扩套件原始目的），并记 v2.6 候选；`smoke.sh` 零改动，与「维持 20」自洽（`SELFTEST: 20/20` 断言未破） |
| HC-10 真机证据 | **不通过** | 主站与 `et_log` 域宽两项**均无板上逐字节记录**。P2-1（同步+交叉编译）已由本记录独立复核通过，但「编译通过不算验收」——HC-10 明文如此。板上证据缺失是本次返工的唯一硬件门 |
| HC-11 候选池一致性 | **不通过（第三落点缺失）** | 计划 §1.1 与 `docs/v3-candidates.md`「v2.5 复评记录」两处措辞**一致**（CO-2 暂缓 / CO-3 纳入并交付且限单事务 / CO-4 维持排队 / CO-5 拒绝（库级）/ CO-6 维持 v3 窗口 / CO-7 见 §5 / CO-8 纳入并修 / CO-11 暂缓）；**但交付文档 §8 不存在**，而 AC-2/AC-25 的落点写的是「交付文档 §8」→ 三处对账缺一处 |

---

## 4. 偏差裁决表

| 偏差 | 裁决 | 理由 |
|---|---|---|
| **D-1** P2-2/P2-3/P2-4 未执行（板未接入） | **接受（挂账成立）** | 硬件事实可复核（板侧工程在位但本机无 ST-Link/无 CH343，与 `Get-PnpDevice` 结论一致）；处置依据是计划附 B 风险表「板档期」行的明文对策；**不伪造结论**是正确取舍。**但接受挂账 ≠ 交付合格**：HC-10 仍未满足，故结论为返工（返工范围即本项） |
| **D-1a** 未改板侧共享 USART1 分流路径 | **接受** | 论证成立且方向正确：v2.4 的分流规则是**板上验证过的**，在无板可验时改动它，风险（把已验证的从站路径改坏且无法察觉）大于收益（一个诚实的缺口）。属「留缺口优于引入不可验证的回归」 |
| **D-2** 本机 ASan 不可用 → 落 CI + `make test-asan` | **接受** | 本机限制已复核（MinGW 无 libasan）；补偿手段是**真门**而非声明：`gh run view --job 105715651287` 实测该步骤执行且绿，且它确实抓到了既有越界（N-9）。job 数仍为 7，不破 AC-24 计数 |
| **D-3** 计划外清理 f103 demo 同源绕行 | **接受** | 属 P1-4 自身要求（「全库 grep 复查无遗留不支持规格」）的必然延伸；体积表已同批回刷并经 sizecheck 逐值核验（23480 实测 = 表行） |
| **D-4** 计划外 API 追加 `_data()` | **接受（且为必要）** | 计划 §3.3 冻结的 `result(m,&qty)` 单值与 AC-15「逐值相等」**不自洽**（交付 N-5 已自省）。以纯追加 `data()` 化解：`--diff` 修改/删除 0，HC-1 不破；其余 10 函数与计划签名逐字符一致（已读码比对）。`_status()` 放弃交付属收窄而非偏离 |
| **D-5** `%o` 按「已知不支持→消费+占位」处理 | **接受** | 与 HC-2/HC-3 的**目标**一致（消灭错位）；`%o` 是标准 C 有定义、消费 `unsigned` 的转换，若按未知字符不消费则**制造**本版要消灭的缺陷。未顺带纳入 `%j/%t/%L`（严格沿计划 P1-1 的 {h hh l ll z}）—— 该保守取舍成立，但残留见 CO-8 |
| **D-6** 新增 `make test-asan` + `ci.yml` 步骤；`et_config.h` 注释更新 | **接受** | 开关数量与取值零变化（apidump 无相关变更行）；注释更新是 HC-5「不新增开关」下对模块清单的如实描述 |
| **D-7** 用例数预估偏差（4723 → 474 实际） | **接受** | 纯计数，已按 HC-7 全链回刷；实际比预估多 4 例（master 按 AC 逐条覆盖到计划未列的边界）属正向偏差 |
| **D-8** CI 首轮红（`va_list` 跨 ABI）→ 修 + 加 `-Werror=incompatible-pointer-types` | **接受（处理优秀）** | 已独立核验完整链条：`35378026959`(71fde71) failure → `35379320313`(8651bd9) failure → `35380277870`(51a7848) success → `35380622040`(e44553c) success 七 job。修复方向正确（全链统一 `va_list *`）；**顺带补的持久门是本版最有价值的产出之一**（把本机盲区变成 Linux 侧硬失败）。中间失败如实保留，未掩盖 |
| **D-9** 计划 §6「快照 tag 无副作用」被实测证伪 | **接受（处置正确）** | 已独立核验：run `35378057777`（head = `b0fc5d8`）**conclusion = cancelled**，`gh release list` 最新仍为 v2.4 → **未产生任何 Release**。计划的事实错误由执行方实测发现并立即止损，且**未**给 `release.yml` 加对已存在 tag 无效的守卫（理由正确：tag 触发取该 tag 提交的 workflow 文件）。改进建议（快照 tag 去 `v` 前缀）正确，见 CO-12 |
| **D-10**（本记录新增）交付文档 §4 的 commit/diffstat 与实际 tree 不符 | **需返工（文档）** | 实测：`§4` 声称「`v2.5-start..HEAD` = 35 files, **4723** insertions」+「HEAD = `e44553c`」+「`origin/main` = `e44553c`」。实际：`4723` 恰是 **`51a7848`** 的读数（4768@`e44553c` / 4769@`e61ccd7`）；实际 HEAD 与 `origin/main` 均为 **`e61ccd7`**。即 §4 的 diffstat 取自回填前的提交、commit 声明滞后自身一个提交。属「自指滞后」同族（与 v2.1 已根治的 docsync 自指、N-5/N-8 同源），**且无门守护** |
| **D-11**（本记录新增）板侧归因数字微差 | **接受（记录在案）** | 独立复核：FLASH 38712 / RAM 5312 与声称**逐字节一致**；`nm` 证实 `et_modbus_master` 符号 **0**、`emit_number` 0x1AC=428B 一致；仅 `vformat` 实测 0x4A4=1188B vs 文档写 0x4A0=1184B（差 4B）。不影响结论，属转写/重链精度 |

---

## 5. 遗留问题清单

> 编号属 v2.5；「建议处置版本」= 本记录对下一版计划的硬性输入。`CO-1(v2.5)` 即返工项①，`CO-2~CO-5` 为返工项②。

| CO | 问题 | 来源 | 严重度 | 建议处置版本 |
|---|---|---|---|---|
| **CO-1(v2.5)** | **HC-10 未满足**：板上逐字节证据缺失（P2-2 主站命令组 / P2-3 真机走单含重发序列、异常码、广播写、`et_log` 域宽行 / P2-4 板上既有回归）。触发 = G474 板接入本机（走单脚本已就绪：`--slave` / `--slave-drop N` / `--slave-exc 0x02` / `--slave-regs`） | AC-21/AC-22、交付 §5.1/§6 | **高** | **v2.5-r2**（补齐后发 tag） |
| **CO-2(v2.5)** | 交付文档缺 §8：HC-11 的第三落点不存在，AC-2/AC-25 的证据指向空小节。需新增「CO 处置与候选池一致性」节，把计划 §1.1 / v3-candidates / 交付文档三处措辞并置对账 | HC-11、AC-2、AC-25 | 中 | **v2.5-r2**（纯文本） |
| **CO-3(v2.5)** | `docs/architecture.md:111` 仍「四例自检式示例」（本版 4→5）；HC-7 的 examples 数漏刷 | HC-7 | 中 | **v2.5-r2**（纯文本） |
| **CO-4(v2.5)** | `docs/architecture.md:112` 仍「docsync 210 断言」（实测 291）、`docs/API_GUIDE.md:1406/1419` 仍「398」（实测 474）；HC-7 的 docsync 计数/用例数漏刷，且**无门守护** | HC-7 | 中 | **v2.5-r2**（修订 + `docsync.sh` 增断言：architecture 机制门行的 docsync 计数 = 实测） |
| **CO-5(v2.5)** | 交付文档 §4 的 commit 声明与 diffstat 滞后自身提交（D-10）：声称 `e44553c`/4723，实际 `e61ccd7`/4769，`origin/main` 亦已前移。机制缺口：commit/diffstat 无门 | D-10 | 中 | **v2.5-r2**（回填）+ **v2.6 机制化**（§4 明确「记录范围 = `vX.Y-start..<最后一次代码提交>`」并加 docsync 断言 diffstat 与实测一致） |
| **CO-6(v2.5)** | **主站定长误判（本记录新发现，交付未记载）**：应答定长取自 `rx[2]` 字节数域且**先于 CRC 校验**被信任。噪声形如 `[addr][合法读功能码]`（**板侧单线半双工下主站自身请求回显即属此形**）时，解析器按噪声字节数定长 → 随后的真应答被当作 CRC 坏丢弃 → 事务只能靠超时+重发收场，`retry_max=0` 时直接 `ET_MB_TIMEOUT`；且 `crc_err` 计数被污染（计数了从未构成帧的字节）。实测：`feed([11 03])` → `feed(真应答13B)` 返回 **0**、`poll` 保持 BUSY、仅 `poll(+50ms)` 得 TIMEOUT，`crc_err=2`。既有用例 `mbm.garbage_resync` 用的噪声是 `{FF,00}`（fc 非法）→ **未覆盖此路径** | 本记录探针 | **中** | **v2.6**（修法：定长前先做 bc 合理性检查——与 `2*exp_qty` 或 `2*ET_MODBUS_RD_QTY_MAX` 比对，不符即 `drop_head` 重同步；并补用例） |
| **CO-7(v2.5)** | **`%c` 的域宽/精度被解析后丢弃**（本记录新发现）：实测 `et_log_raw("[%5c]",'A')` → `[A]`，host 为 `[    A]`；`[%-3c]` → `[A]` vs `[A  ]`；`%3.1c` 精度亦被忽略（消费正确、无错位）。而 `et_log.h` 与 API_GUIDE 的修饰符承诺是**全域**「语义与 C printf 一致」，且无用例覆盖 `%c` 修饰。HC-2（消费）未破，属文档承诺超出实现/实测覆盖 | 本记录探针 | 低 | **v2.6**（二选一：补 `%c` 域宽实现 + 用例，或把文档承诺收窄为「受支持规格的域宽/精度适用于 d/i/u/x/X/s/p」） |
| **CO-8(v2.5)** | 未知转换字符**不消费**（N-2）：`%jd`/`%tu`/`%Lf` 走「未知」分支 → 其后实参仍会错位，是「错位」类缺陷的残留口子。本版按 HC-3 例外放行且已在头注/API_GUIDE 明示（合规） | 交付 §5.2 N-2 | 中 | **v2.6**（把 `j`/`t`/`L` 纳入「已知不支持→消费」集合，或纳入修饰集） |
| **CO-9(v2.5)** | `data()` 的结果缓冲有效窗口是**隐含契约**（指向内部 RX 缓冲，有效至下一事务喂入新字节前），靠文档而非类型约束 | 交付 §5.2 N-1 | 中 | **v3 窗口**（改 `read_into(m, …, uint16_t *dst, uint16_t cap)` 形态，调用方提供结果区） |
| **CO-10(v2.5)** | selftest 维持 20 → 板侧冒烟对**主站与日志零覆盖**；host 侧 25+24 例承担 | 交付 §5.1 P3-2 | 低 | **v2.6**（与 CO-1 同批：板接入后扩至 21 并全链回刷） |
| **CO-11(v2.5)** | USART2 独立串口（原 CO-2）/ 共享口地址不符静默的板侧验证（原 CO-11）**维持暂缓** | 计划 §1.1 | 低 | **v2.6**（须重新声明一次处置结论，不得静默消失；触发 = 第二路 USB-TTL 或双板对连） |
| **CO-12(v2.5)** | 计划模板三处起草期缺陷：① 快照 tag 命名与 `release.yml` 的 `v*` 触发面冲突（N-8，实测已触发 Release workflow）；② P2-2 文件落点写成 `examples/stm32g474_demo.c`，实际板侧挂载点在库外私有 `Core/Src/et_demo.c`（N-6）；③ 「AC 与冻结 API 形状互洽」检查缺失（N-5，本版 `result()` 单值 vs AC-15 逐值即此例） | 交付 §5.2 N-5/N-6/N-8、D-9 | 中 | **v2.6 计划模板**（快照 tag 改 `snap-<ver>-start`；「无副作用」类断言起草期必须实测；板侧落点标注「库外私有文件」；模板核验清单加「AC↔冻结形状互洽」） |
| **CO-13(v2.5)** | 本机对 ABI/内存类缺陷是**盲区**：MinGW 无 libasan、clang ASan 运行时本机初始化失败、`va_list` 形态差异只在 Linux 报警（N-4/N-7）。本版已把两类门放 CI，但**本机仍无法前置检出** | 交付 §5.2 N-4/N-7 | 中 | **v2.6**（引入 WSL/MSYS2 作 ASan 执行环境，或把「host 侧零警告」纳入 CI 硬断言——当前仅 ARM 交叉编译有零警告门） |
| **CO-14(v2.5)** | bench 基线**无负载控制**，同机同日可复现性差：本记录复跑得主站组帧 **40.0** ns/op、一轮事务 **110.0** ns/op、modbus 帧处理 **110** ns/帧，与 `docs/bench.md` 的 60.0/175.0/160 及其所述抖动带（155~210）**均不重合**（差幅最大 59%）。文档已如实披露抖动原因，但逐版数值对比因此不具判别力 | 本记录复跑 | 低 | **v2.6**（bench 改为相对比值/固定 CPU 亲和与预热，或明确标注「仅供量级参考，不作回归判据」） |

### 5.1 遗留风险复评（对交付文档 §6）

| 风险 | 复评 |
|---|---|
| HC-10 未满足 | **仍成立且未缓解** —— 本次返工的唯一硬件门；缓解路径（三条 `--slave*` 命令）明确且低成本 |
| 板侧主站时序未在真实串口验证 | **仍成立** —— 本记录新增的 CO-6（回显形态噪声致定长误判）恰好落在「只有板上能证伪」的区域，建议随 CO-1 走单时**专门注入该形态** |
| `et_log` 加固改变输出与体积 | **已缓解（库内）**；板面日志行原样比对随 CO-1 挂账 |
| 本机基准受负载波动 | **成立，且本次复评量化了幅度**（见 CO-14），非回归 |
| CI 未核验（推送前） | **已消除** —— `gh` 实测七 job 全绿、ASan 步骤执行且绿 |
| `data()` 隐含窗口 | **成立（低）** → CO-9 |
| selftest 维持 20 → 板侧零覆盖 | **成立（低）** → CO-10 |

---

## 6. 下一版计划必须处理的输入

**A. v2.5-r2（返工轮，补齐后即可发 tag；无需二次开发）**

1. **CO-1(v2.5)**：板接入后执行 P2-2/P2-3/P2-4，按 HC-10 取得板上逐字节记录（请求/应答/重发序列/异常码/广播写/`et_log` 域宽行原样），落 `移植stm32实机记录.md` §12；**并加做 CO-6 的注入**（用主站自身请求回显形态的噪声喂入，观察是否退化为超时）。
2. **CO-2(v2.5)**：交付文档新增 §8（计划 §1.1 / `docs/v3-candidates.md` / 交付文档三处 CO 措辞并置对账），并修正 AC-2 的自指证据。
3. **CO-3 / CO-4(v2.5)**：`docs/architecture.md` 机制门行改「五例」+ docsync 计数改实测；`docs/API_GUIDE.md` 的「398」改 474；`tools/docsync.sh` 补这三处的断言（无门是这三处漏刷的根因）。
4. **CO-5(v2.5)**：交付文档 §4 按实际 `HEAD` 与实测 diffstat 回填，并写明「§4 记录范围」的定义，消除自指滞后。

**B. v2.6 计划（必含项，带 CO 号）**

5. **CO-6(v2.5)**（中）主站定长前 bc 合理性检查 + 用例；随板侧走单一并验证。
6. **CO-8(v2.5)**（中）`j`/`t`/`L` 长度修饰纳入「已知不支持→消费」，消除未知分支的错位残留。
7. **CO-13(v2.5)**（中）本机 ABI/内存缺陷前置检出能力（WSL/MSYS2 或 host 零警告门）。
8. **CO-12(v2.5)**（中）计划模板四项修订（快照 tag 命名 / 无副作用断言须实测 / 板侧落点标注 / AC↔冻结形状互洽检查）。
9. **CO-10(v2.5)**（低）selftest 20→21 与 CO-1 同批（板侧可复验是扩套件的前提）。
10. **CO-11(v2.5)**（低）CO-2/CO-11（原 v2.4 编号）**重新声明一次**处置结论。
11. **CO-7 / CO-14(v2.5)**（低）`%c` 修饰的文档收窄或补实现；bench 基线改为可判别形态。

**C. v3 窗口（不进 v2.6）**

12. **CO-9(v2.5)**：`data()` → `read_into()` 形态变更（触碰签名，属破坏性窗口）。

---

> 记录纪律：本记录每条结论均可由 §0 所列命令独立复现（探针源码形态已在 CO-6/CO-7 中给出关键调用）。**未核验项（板上走单）不得按通过计**，故结论为返工而非有条件通过。
