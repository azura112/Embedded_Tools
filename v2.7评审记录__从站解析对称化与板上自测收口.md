# v2.7 评审记录

> 评审对象：`v2.7开发交付__从站解析对称化与板上自测收口.md`（含其声称已完成的 **G0′ = `v2.6-r2` 返工轮**，提交 `724d089`）
> 判据来源：`v2.7开发计划__从站解析对称化与板上自测收口.md` §2（HC-1…HC-12）、§4（AC-1…AC-22）
> 基线：`snap-2.7-start` = `724d089`（= v2.6-r2 终态）；被评终态 `HEAD` = `e7c79f7`（`tag v2.7` = `e7c79f7`）
> 评审日期：2026-09-26 ｜ 评审方式：**先 diff 后文档**；核心「修复前为红」与关键门读数全部由评审方独立复现
> **编号体系声明**：本文 `CO-n(v2.7)` 为**本次评审新发现**，与 `CO-n(v2.5)` / `CO-n(v2.6)` **不是同一体系**（计划/交付内部亦有 `CO-n` 表 v2.4 遗留的混用），引用必须带版本后缀。

---

## 1. 结论 —— **有条件通过**

本版是三代以来证据质量最高的一次：核心修复（`CO-5(v2.6)` 从站解析对称化）的「修复前为红」由评审方**独立复现**（8 例红，含 E10 受害形态与 `crc_err` 污染的直接证据）；v2.6 的发布阻塞项（`apidump --check` 恒红、已发布物不实、交付文档自相矛盾、门设计空隙）**全部修复并经独立复跑证实**；板侧首次取得 **A/B 双固件对照**（修复前数据实得，非"不可得"）；`CO-14(v2.6)`（selftest 收口）以 **22 套件 + 板上 22/22** 关闭，且 20→22 的口径偏离按 HC-11 三处并置声明。

**"有条件"的三项条件（须随下一批提交落实，均为文本/治理，不动代码）：**

1. **§4 的 commit 清单须改指真实提交**：现清单列 `838edb1`，而 `snap-2.7-start..HEAD` 实际只含 **`e7c79f7`**（`838edb1`/`23c0d3f` 为同信息悬空提交）；CI 证据 run `36157294575` 的 commit 亦为悬空的 `23c0d3f`（与 HEAD 差 12/4 行、**纯交付文档**，交付已披露该事实）。→ `CO-1(v2.7)`。
2. **基线名表述统一**：§2 AC-20 写"对 **v2.6** 基线"，§7.1 实跑的是"对 **v2.5** 基线"（`docs/API_INVENTORY_v2.6.md` 不存在，HC-1 的字面要求不可满足）→ `CO-2(v2.7)`。
3. **§7.3 复跑预期数字修正**：交付预测"`pass=23 fail=10`（新增 10 例全红）"，评审实测 `proto/modbus pass=25 fail=8`、另 2 例红在 `debug/selftest`；10 例新用例中 **2 例在基线上即绿**（零判别力，属回归钉）→ `CO-3(v2.7)`。

**同时明确（避免误伤）**：本版无任何已发布产物含不实内容（Release v2.7 正文已核对）；四门在本机全部复现为绿；`tag v2.7` = `HEAD` 满足 HC-9。条件项均为证据链精度问题，不涉及库内代码与行为。

---

## 2. AC 核验表

复现环境：Windows 11 / Git Bash（PortableGit 1.2.0 usr/bin）；host gcc MinGW-w64 16.1.0；`make` → `mingw32-make`；隔离目录 `build/review_v27/`。

### 前置门 G0′（= v2.6-r2，提交 `724d089`）

| AC | 交付声称 | 核验结果 | 证据或缺口 |
|---|---|---|---|
| **AC-1**（G0′-1，HC-4） | 通过 | **通过（独立复现）** | `sh tools/apidump.sh --check` → **exit 0**「清单与头文件一致」（评审实测 5m35s；v2.6 时同命令 exit 1 / 4m39s——**发布阻塞项已消除**）；`docsync` → **`pass=304 fail=0` exit 0**（实测 9m47s）。修法 = 比对前双侧 `sed 's/\r$//'` 行尾归一后比对**完整内容**（判别力与字节比对等价，且不依赖工作树行尾）——评审读码确认未降低门的判别力 |
| **AC-2**（CO-13 耗时） | 通过 | **通过** | 交付载 4m24s/4m18s/4m25s（附 shell 与命令）；评审实测 `--check` 5m35s、`--diff` 5m25s —— 同量级，"21 秒"不可复现读数已从全部文档清除 |
| **AC-3**（G0′-2） | 通过 | **通过（独立核验远端）** | `grep "板侧项未执行\|亦未打" CHANGELOG.md` = **0**；`gh release view v2.6 --json body` → 虚假表述 **0 命中**，且含"板侧项已执行…`tag v2.5` 已打并已推送"的更正段（v2.6 的已发布物错误**已在公开侧更正**，未重打 tag） |
| **AC-4**（G0′-3） | 通过 | **通过** | v2.6 交付文档 §4 现为终态表述（"CI 已核验 run `35975885148` …原'待推送后核验'已随 `CO-3(v2.6)` 更正"）；残留的"不合格/待推送"字样均在**修订声明的历史引述**中（`grep` 核对，非终态声称） |
| **AC-5**（G0′-4） | 通过 | **通过（独立复现 + 装门）** | `README.md:237` → `docs/API_INVENTORY_v2.5.md`（并新增 docsync 交叉断言，评审在 docsync 输出中见到该断言 ok）；`port/stm32g474/README.md:84` → "已同步（…见实机记录 §14）"，`grep "未同步" port/` = 0；v2.6 交付文档 AC-18 行已限定为"**selftest 相关行**零改动"并附更正说明。**docsync 新增 4 条断言全部在案**（基线名交叉核对 / 无"未同步"残留 / 模板第 5 项 / README 与模板项数一致） |
| **AC-6**（G0′-5） | 通过 | **通过** | `docs/开发计划模板.md` §7 = **五项**（⑤"证据落点章节号 + 占位节清理"，附 v2.6 反例）；`移植stm32实机记录.md:420` §12 标题含"**已由 §13 承接**（历史占位节）" |
| **AC-7**（G0′-6） | 通过 | **通过（代码核验）** | `tools/docref.sh:78-88` 新增 `END_SHA != HEAD_SHA → FAIL`（读码确认）；本版 §4 终点写 `HEAD`，`docref` → **OK**（21/1288/104 与实测逐值相等）。**新空隙见 CO-1(v2.7)**：门核"范围 + 三个数字"，**不核 §4 的 commit 清单** |

### 从站侧解析（★ 核心）

| AC | 交付声称 | 核验结果 | 证据或缺口 |
|---|---|---|---|
| **AC-8**（P0-1/P0-2，HC-2） | 通过（host + 板上双证） | **通过（独立复现，含红证据）** | **E1**：仅把 `protocol/et_modbus.c` 回退到 `snap-2.7-start` → `proto/modbus pass=25 fail=8`：`mb.noise_fake_len_then_read` `feed expected=1 actual=0`（**E10 受害形态逐字复现**）、`bc_qty_mismatch_resync` `expected=0 actual=1`（旧行为仍回异常）、`crc_bad_foreign_not_crc_err` `crc_err expected=0 actual=1`（口径收紧的直接证据）、`sticky_bad_then_good` `len expected=7 actual=5`（真请求被吞）、`noise_never_frames` `discarded 7 vs 0` 等；新版 → **33/33**。**E10 形态在新实现下 host 与实现均绿**（评审读码 + 用例核对） |
| **AC-9**（P0-1/P0-2） | 通过（+10 例） | **通过（独立复现）** | 新增 **10 例**（≥8）全绿；`proto/modbus (33 cases) pass=33 fail=0`；既有从站用例唯一改写者 = `mb.exc_illegal_value_wr`（diff 核对属实）；`test` / `test-g4` **497 ALL PASS**（实测逐例计数 497/497，fail=0） |
| **AC-10**（P0-3） | 通过 | **通过** | §7.4 主从同构对拍表在案；两侧用例名经源码核对全部存在（`mbm.noise_*` ↔ `mb.noise_*` / `mb.crc_bad_silent` 等）；差异面（主站有 `exp_qty` 先验、从站无）如实写明并与 N-4 一致 |
| **AC-11**（P0-4，HC-3） | 通过 | **通过** | `API_GUIDE` 5.7 统计口径表在案（`crc_err`/`addr_mismatch`/`discarded` 逐条加一条件，含"广播帧不计""从未成帧不计"）；`et_modbus.h` 的 stats 注释与 5.7 **同措辞**；交付 §2 已登记"行为修正（语义面）"。评审读码（`frame_is_expected_shape` / `process_frame` / `feed` 重同步分支）与口径表逐条比对**差集为空** |

### 板上自测与板侧

| AC | 交付声称 | 核验结果 | 证据或缺口 |
|---|---|---|---|
| **AC-12**（P1-1/P1-2） | 通过 | **通过（host 侧独立复现）** | `g_suites[]` 含 `{"modbus", st_modbus, 0u}` / `{"log", st_log, 0u}`（diff 核对）；`test_selftest.c:98` = `ET_CHECK_U32_EQ(22u, base)`、`:152` = `23u`（21→23 同批）；实测 `debug/selftest pass=9 fail=0`；板上 `22/22` 属不可离线复验项（§15.2 证据在案） |
| **AC-13**（P1-3，HC-7/HC-12） | 通过 | **通过（逐行 grep 核验）** | 九处 + 同批扩展面全部命中：`smoke.sh:97` = `SELFTEST: 22/22 PASS`；`README.md:46/:170/:188/:193` = 22/22 套件；`API_GUIDE.md:1445/1446/1454` = 22 内建 / 497 用例 / 1K 498；`architecture.md` = 22 套件 ×3 + 连续十七版；`CHANGELOG` 新增 `[2.7.0]` 节；`docsync` 新增 `SELFTEST: 22/22` 断言且 304/0 |
| **AC-14**（P1-4，HC-9） | 通过 | **通过（附注）** | 实机记录 §15.1/§15.2 在案：A/B 双树 `diff -rq` 全 OK、0 warning、FLASH/RAM 逐值（A 75440/5960、B₁ 80852/6744、B₂ 81244/6768）、`SELFTEST: 22/22 PASS`、SELFSTOR/SIMUPGRADE/从站分流复跑。板上字节不可离线复验（沿 v2.6 附注口径） |
| **AC-15**（P1-4，HC-9） | 通过 | **通过（附注）** | §15.3 **A/B 对照实得**（修复前数据已取得，非"不可得"）：form2 下 A 无应答 + `crc=3`，B 正确应答 + `crc=0 disc=17`。**附注**：MBSLAVE 读数为**会话累计值**（form1 先行：A `crc=1` → form2 后 `crc=3`；B `disc=6` → `17`），表内以绝对值呈现、结论以增量表述——建议下版在表头注明"累计值"。host 侧基线行为（E1）与板上定性结论一致 |
| **AC-16**（P1-5，CO-8） | 通过（已定位） | **通过（附注）** | §15.4 机械定位：`AT+MBRAW` 首字节恒 `0x0A`（3/3），仅 CR/仅 LF 结束时 `disc=0` ⇒ 来源 = **命令行 `\n` 残留**；排除清单三条均有读数（`ore=0`、无 TX 回环、取值非随机）；已写入交付 §5.2（N-1）与 §6 双处。**性质裁定合理**：板侧私有接口残留、非库缺陷（评审读码 + E1 基线行为佐证："与在途不同形的首字节 → discarded、不污染 crc_err"正是 v2.7 新口径的正确行为） |

### 代码卫生与证据链

| AC | 交付声称 | 核验结果 | 证据或缺口 |
|---|---|---|---|
| **AC-17**（P2-1） | 通过 | **通过（读码 + 全量复跑）** | `handle_frame()` 的不可达分支已删、改 `ET_ASSERT((uint32_t)m->rx[2] == 2u*exp_qty)`（`et_config.h` 确认默认展开 `((void)0)`，零开销）；`is_expected_shape()` 恒真子句已删（现仅"地址 + 功能码"，与评审 E12 结论一致）；497/497 全绿 → 行为零变化成立 |
| **AC-18**（P2-2） | 通过 | **通过** | `et_log.h:29` 与 `API_GUIDE.md:1327/1329` 均含"`L` + 整数转换 = 未定义行为"限定，两处措辞一致，并附"x86-64 SysV memory-class 可能不推进整数实参槽"的机理说明 |
| **AC-19**（P2-3，D-7） | 通过 | **通过（独立复现）** | 评审方在 `git worktree`（v2.5 树 = `25f3316`）上用**当前** `tools/apidump.sh` 真生成清单 → sha256 **`b8ec7378…95fac` / 23200 B**，与归档件 HEAD blob **逐字节相同**（交付所报 sha 逐值一致）；worktree 已清理。v2.6 D-7 的"人工等价性论证"降级就此**机器复核闭环** |
| **AC-20**（全版） | 通过 | **通过（附注①②）** | `docsync` 304/0 ✓、`--check` exit 0 ✓、`--diff` 对 v2.5 基线 **0/0 exit 0**（实测 5m25s）✓、`sizecheck` **pass=3 fail=0**（23884/24/740、36340/24/3016、24260/28/740 逐值）✓、`docref` OK ✓。**附注①**：实跑基线为 **v2.5**（HC-1 字面的"v2.6 基线"不可满足，见 CO-2）；**附注②**：CI run `36157294575`（success，7 job）的 commit = `23c0d3f`，为**悬空提交**，与被评 HEAD 差 12/4 行且**纯交付文档**（`git diff 23c0d3f..HEAD` 核实）——交付已披露，但证据锚点不可达（CO-1） |
| **AC-21**（全版） | 通过 | **通过** | 实测逐例计数：`test` **497** / `test-g4` **497** / `test-tab` **504** / `test-1k` **498**（全部 fail=0）；`make ex` 五例 PASS；`WERROR=1 test` 编译诊断 **0** 行；覆盖率 **行 97.2%（10575/10877）/ 函数 99.3%（1066/1074）**——与声称逐值一致（分支计数见 CO-4）；`test-asan`/`test-asan-1k` 维持 CI 侧 + 本机边界声明 |
| **AC-22**（P3-1） | 不做（显式声明） | **通过** | §8.3 显式声明"不做 + 理由 + 再记 v2.8 + 触发条件"，非静默略过；`docs/bench.md` 口径声明仍生效 |

---

## 3. HC 核验（逐条结论）

| HC | 结论 | 依据 |
|---|---|---|
| **HC-1 契约零破坏** | **满足（实质）／字面不可满足** | `--diff` = 新增 0 / 修改删除 0（独立复现）；公开头文件仅注释与版本宏变化（`et_modbus.h`、`et_log.h` diff 核对）。**但**"对 v2.6 基线"不可满足——`docs/API_INVENTORY_v2.6.md` 不存在，实际按 v2.5 基线（→ CO-2(v2.7)，计划漏列基线滚动任务） |
| **HC-2 从站不得信任噪声字节** | **满足** | `expected_len()` 切片前校验 `b[6] == 2*qty`（`et_modbus.c` P0-1 diff）；`head_may_be_frame` + `drop_head` 逐字节重同步；`process_frame` 返回前进量、CRC 失败的非同形序列计 `discarded`。E1 独立证明旧形态红、新形态绿 |
| **HC-3 统计口径与文档一致** | **满足** | 5.7 口径表 + 头注 + 用例三处一致；评审读码比对差集为空；语义收紧已在交付 §2 登记区说明（与 v2.6 D-2 同形态） |
| **HC-4 前置门 G0′ 全绿** | **满足** | 七项逐一核验通过（AC-1…AC-7）；四门本机复现全绿；`snap-2.7-start` 在 r2 终态定着（`724d089`，远端一致） |
| **HC-5 §4 范围覆盖 HEAD** | **部分满足** | 范围 `snap-2.7-start..HEAD` ✓、三个 diffstat 数字与实测逐值一致 ✓、`docref` OK ✓；**但 §4 的 commit 清单列 `838edb1`（悬空提交），实际范围内唯一提交是 `e7c79f7`** —— "commit 清单与实测逐值一致"不满足（→ CO-1(v2.7)；门不核清单，属门空隙） |
| **HC-6 库不变式** | **满足** | 新增 selftest 缓冲全为文件级静态（`g_st_mb_rx[32]`/`g_st_mb_tx[64]`/`g_st_mbm_rx[ET_MODBUS_ADU_MAX]`）；无 `malloc/calloc/realloc`；无新增开关；`et_config.h` 仅版本宏；无结构体字段变更 |
| **HC-7 数字回刷同批** | **满足** | 九处 + 同批扩展面逐行核验命中（见 AC-13）；交付还主动回刷了计划未列的 4 处（D-8：getting-started 474、API_GUIDE 9.1 的 326、port 平台表 291、g474 的"17 套件待回填"）并同步了门内字面量（D-9） |
| **HC-8 板侧落点库外路径** | **满足** | §15 落点声明：`D:/code/STM32CubeMX/G474VET6_ET_TEST/Core/Src/et_demo.c`（新增 `AT+MBSLAVE`/`AT+MBRAW`）、`...\Core\et\`、走单脚本 `build/board_c05_probe.py`/`board_disc_probe.py`（不入库）——全为库外全路径 |
| **HC-9 真机证据与 tag 完整性** | **满足** | CO-5 板上注入对照与 selftest 22 同一板侧会话取得（§15）；`tag v2.7` = `e7c79f7` = `HEAD`（下一版代码未合入）；A/B 修复前数据**实得** |
| **HC-10 内存门** | **满足** | `test-asan`/`test-asan-1k` CI 常设（自报）；本机不可执行已声明、未声称本机已跑；新增 modbus/log 套件代码路径纳入 CI 的 ASan 矩阵（同一 `test/*.c` 全量编译） |
| **HC-11 CO 一致性三落点** | **满足** | 交付 §8.1 对账表 15+2 条逐条并置；`docs/v3-candidates.md:66`「v2.7 复评记录」在案；**差集为空**（含 21→22 裁定的三处声明） |
| **HC-12 selftest 扩套件全链** | **满足（22 分支）** | 同批九处回刷（AC-13）；板上 `AT+SELFTEST 22/22` 复跑在案；`test_selftest.c` 与 `smoke.sh` 两个硬断言同批更新（漏改即 CI 红）；未再援引"不扩"分支 |

---

## 4. 偏差裁决表

| 偏差 | 裁决 | 理由 |
|---|---|---|
| **D-1** `snap-2.7-start` 推送受阻后重试成功（远端=本地=`724d089`） | **接受** | 跨版已知网络现象；`gh api` 核验两端一致；无副作用用**探针 tag 实测**（`snap-probe-nosideeffect`，用后即删）——把"快照 tag 无副作用"从断言升级为实测，符合计划模板第 ①② 项 |
| **D-2** 既有用例行为修正：`bc`/`qty` 不自洽的 `0x10` 帧**不再回异常 0x03**（改逐字节重同步） | **接受（附条件）** | HC-2 明文要求与旧断言**互斥**，按计划附 B 通道登记；能力损失与**恢复路径**（CRC 通过才接受 `9+b[6]` 的双假设定长）已写明。**条件**：恢复路径记 v2.8 评估项（→ CO-7(v2.7)），不得静默消失 |
| **D-3** `crc_err` 门槛取"仅本站地址"（不含广播），偏离计划可裁量项 2 的建议基准 | **接受** | 理由成立（把 0x00 纳入同形会使重同步级联中的噪声 `0x00` 误计 `crc_err`，实测有据）；广播帧的成帧/执行语义不变；已在 5.7 口径表写明。属"更严"一侧且方向与主站一致 |
| **D-4** selftest `log` 套件以"输出字符数"断言（非 putc 捕获内容） | **接受（附条件）** | 捕获设施是 host 专有，板上捕获需给 `port.h` 增钩子 = **公开契约变更**（违反 HC-1）——不可行性成立。**条件**：板上 log 冒烟**不校字符内容**的边界须在 API_GUIDE 8.4 明示（→ CO-11(v2.7)）；内容级断言继续由 host 的 30 例承担 |
| **D-5** 板侧 demo 新增 `AT+MBSLAVE`/`AT+MBRAW` 两条诊断命令 | **接受** | 库外私有文件（HC-8 合规）；为 A/B 对照与 `disc` 定位所必需；体积增项已逐值记档 |
| **D-6** 新增用例数 10（计划 ≥8）、log 套件 21 条断言 | **接受** | 计划给的是下限；覆盖矩阵按三条线自然展开 |
| **D-7** 实机记录采用 A/B 双固件对照（而非"不可得"声明） | **接受（升级）** | 把 AC-15 从"替代证据"升级为**直接证据**，并额外发现 form1 被 4ms 静默路径兜住的边界（补全危害触发条件）——正向上偏差 |
| **D-8** 同族漏刷第 5~8 例的计划外回刷（getting-started/API_GUIDE 9.1/port 平台表/g474 待回填句） | **接受** | 属计划遗漏边界的**正向补齐**，且把 `getting-started`/README 用例数纳入 docsync 硬断言（门本身滞后的一并修正）；残留一处见 CO-5(v2.7) |
| **D-9** docsync 4 条字面量断言随版本同步取值 | **接受（记治理观察）** | 门与数字回刷耦合是既有设计；"改单一来源取值"记 v2.8（与 v2.6 跨文档计数门同构） |
| **CO-14 的 20→22 裁定（相对评审建议的 21 属"更严"）** | **接受** | 理由成立（套件 = `et_selftest_report_fn` 的失败归因单元，合并两模块会削弱回调设计价值）；已按 HC-11 在三处并置声明，供本轮裁决——本评审裁决为**通过该裁定**，后续版本以 22 为基准 |

---

## 5. 遗留问题清单

> 严重度：**高** = 影响发布物正确性/门不可用；**中** = 正确性相邻或证据链断裂；**低** = 代码卫生/表述。

| # | 问题 | 严重度 | 建议处置版本 |
|---|---|---|---|
| **CO-1(v2.7)** | **§4 的 commit 清单错指悬空提交**：清单列 `838edb1`，而 `snap-2.7-start..HEAD` 实际只含 **`e7c79f7`**（`838edb1`/`23c0d3f` 为同信息、不同树的悬空提交，与 HEAD 差 12~13 行、纯交付文档）；CI 证据 run `36157294575` 的 commit 亦为 `23c0d3f`（不可达）。**HC-5 的"commit 清单与实测逐值一致"不满足**（范围与三个数字满足）。**门空隙**：`docref` 只核范围与三个数字，不核清单——建议增"清单中每个 SHA 须为 `RANGE` 的可达提交"断言 | **中** | **随本轮修订**（一行）＋ v2.8 装门 |
| **CO-2(v2.7)** | **冻结面基线滞留 v2.5 已跨两版**：`docs/API_INVENTORY_v2.6.md` 从未归档（v2.6 发布时未做、v2.7 计划亦漏列该任务），HC-1 的"对 v2.6 基线"字面不可满足；交付 §2 AC-20 与 §7.1 的基线名互相矛盾（v2.6 vs v2.5）。本次 0/0 的结论**实质成立**（v2.6/v2.7 公开面确无增改），但这是事实巧合而非门保证——基线不滚动，门对"v2.6 以来新增的公开面"没有分辨力 | **中** | **v2.8 必办**：补归档 v2.6 清单（用 `git worktree` 于 `tag v2.6` 生成，沿 AC-19 手法）+ 发版时归档 v2.7 + 默认基线前滚；并把"基线滚动"写进 `docs/开发计划模板.md` 起草期清单 |
| **CO-3(v2.7)** | **§7.3 复跑预期数字不准**：交付预测"`pass=23 fail=10`（新增 10 例全红）"，实测 `proto/modbus pass=25 fail=8`、另 2 例红在 `debug/selftest`（新套件跑在基线从站上连锁失败）；且 10 例新用例中 **2 例（`mb.unknown_fc_unicast_retained`、`mb.wr_truncated_then_completed`）在基线上即绿** → 对本修复**零判别力**（属回归钉，仍有长期价值但须如实标注）。方向正确、数字不精确 | **低** | 随本轮修订（改写 §7.3 预期）；下一版交付的"红证据预期"须以实测为准 |
| **CO-4(v2.7)** | 覆盖率分支计数不一致：交付"分支 57.4%（**5618**/9780）"，实测 **5612**/9780（同为 57.4%） | **低** | 随本轮修订 |
| **CO-5(v2.7)** | `README.md:192`"API 冻结机检（v2.1）：…对 **v2.0** 基线"——同族漏刷又一处（checklist #8 已由门护住，此行没有）；易被读成"当前基线是 v2.0" | **低** | **v2.6-r2 式文本轮**或 v2.8 |
| **CO-6(v2.7)** | **`docref` 的 `END==HEAD` 副作用**：交付文档一经提交，其后**任何**提交（评审记录、下一版计划、r2 修订）都会使门变红，直到下一版窗口回填——即门在仓库历史的大部分时间是"设计性红"。反滞后意图正确，但建议改为：`<end>` 之后至 HEAD 的提交**不得触碰交付文档 §2/§4/§7/§8 与 CHANGELOG**（白名单式判定），兼顾反滞后与可用性 | **低-中** | **v2.8** |
| **CO-7(v2.7)** | **能力损失待恢复**：`bc`/`qty` 不自洽的 `0x10` 帧不再回异常 0x03（D-2）。恢复路径 = "双假设定长"（CRC 通过才接受 `9+b[6]`）——可在不违反 HC-2 的前提下恢复异常面 | **中** | **v2.8 评估项**（须与 CO-5(v2.6) 的判据并读，防止重新打开"长度域被信任"口子） |
| **CO-8(v2.7)** | **`item_lines` 的"抗行尾"依赖 GNU sed 剥 CR 的隐式行为**（交付自报 N-2）：换用不剥离的实现会静默返回 0 条 → `--diff` 误红。修法一行：显式 `tr -d '\r'` | **中** | **v2.8**（一行，零风险） |
| **CO-9(v2.7)** | **`apidump.sh` 的 `TMPD_REL` 回退静默 + 无参数运行无守卫**（交付自报 N-3，实测复现：v2.5 时代工具在 worktree 内生成 0 条目）。修法：回退时打印告警；无参数生成加"条目数 < 基线一半即拒绝写入"守卫 | **中** | **v2.8** |
| **CO-10(v2.7)** | **从站"读帧无长度域"的 8 字节窗口**（交付自报 N-4）：噪声伪造 `[addr][读 fc]` 2 字节即可让坏帧按整帧丢弃（可能吃掉真请求前缀）——主站侧边界的弱化版。已写明判据；"读帧 CRC 失败改逐字节重同步"的取舍须评估（代价：坏帧残字节可能重组成假帧 = 从站误写，风险更高） | **中** | **v2.8 评估项**（默认维持现状） |
| **CO-11(v2.7)** | 板上 `log` 套件**不校字符内容**（D-4 的边界）：板上修饰面冒烟只验"生效性"（字符数） | **低** | **v2.8**：在 API_GUIDE 8.4 明示该边界（或评估 `port.h` 增可选捕获钩子的代价，属公开契约变更须走破坏性窗口） |
| **CO-12(v2.7)** | 板侧 demo 的命令行 `\n` 残留未清（CO-8 已定位，保留证据原样是合理选择） | **低** | **v2.8 板侧待办**（库外一行级修法已在 §15.4 给出） |
| **CO-13(v2.7)** | §15.3 的 `MBSLAVE` 读数为**会话累计值**但表内以绝对值呈现（结论以增量表述正确）；建议表头注明口径，防误读 | **低** | 随本轮修订 |

---

## 6. 下一版计划必须处理的输入（从第 5 章精选）

### 6.1 随本轮修订（纯文本，与评审记录同批提交）

1. **CO-1(v2.7)**：v2.7 交付文档 §4 的 commit 清单改为 `e7c79f7`（并在 §4 注明 `838edb1`/`23c0d3f` 为修订前悬空提交、CI run `36157294575` 与 HEAD 的差异面 = 纯本文档 12/4 行）。
2. **CO-2(v2.7)**：§2 AC-20 与 §7.1 的基线名统一为"v2.5（v2.6 归档缺失，见 CO-2(v2.7)）"。
3. **CO-3(v2.7)**：§7.3 的复跑预期改为实测值（`proto/modbus pass=25 fail=8` + `debug/selftest` 2 例连锁红；`unknown_fc_unicast_retained`/`wr_truncated_then_completed` 基线即绿）。
4. **CO-4 / CO-13(v2.7)**：分支计数 5612；§15.3 表头注明"累计值"。

> 执行提示：上述修订会使 `docref`（END==HEAD）转红——**须与评审记录入仓同一提交完成**，并在提交前重跑 `docref` 至 OK；这本身即 CO-6(v2.7) 所述副作用的一次实证。

### 6.2 v2.8 必含项

1. **CO-2(v2.7)** 冻结面基线滚动：补归档 `API_INVENTORY_v2.6.md`（`git worktree` 于 `tag v2.6` 生成，沿 AC-19 手法）+ 归档 v2.7 + 默认基线前滚；"基线滚动"写入计划模板起草期清单。
2. **CO-8(v2.7)** `item_lines` 显式 `tr -d '\r'`（一行）。
3. **CO-9(v2.7)** `apidump.sh`：TMPD 回退告警 + 无参数生成守卫（条目数下限）。
4. **CO-1(v2.7)** `docref` 增"commit 清单 SHA 须为 RANGE 可达提交"断言；并裁决 CO-6 的 END==HEAD 副作用（白名单式判定）。
5. **CO-5(v2.7)** `README.md:192` 基线表述修正（或并入跨文档基线断言）。
6. **CO-7(v2.7)** 评估"双假设定长"恢复 `bc/qty` 不自洽的异常 0x03 面（须给出"长度域仍不被信任"的论证与用例）。
7. **CO-10(v2.7)** 裁决从站读帧 8 字节窗口的取舍（默认维持整帧丢弃，结论入 5.7）。

### 6.3 v2.8 可选

- **CO-11(v2.7)**：板上 log 冒烟的字符内容校验路径（公开契约评估）。
- **CO-12(v2.7)**：板侧 `\n` 残留清理（库外）。
- **CO-15(v2.6) / 交付 §8.3**：bench 相对比值化（触发条件未变）。
- **D-9**：docsync 字面量断言改"单一来源取值"。

---

## 附：评审方独立复现实验索引

**隔离目录**：`build/review_v27/`（`build/` 已 gitignore；产物不清理，作下一轮探针）

| 编号 | 目的 | 命令/手法 | 产物 | 结果 |
|---|---|---|---|---|
| **E1** | AC-8 红证据（仅回退从站实现） | `git show snap-2.7-start:protocol/et_modbus.c > build/review_v27/et_modbus_base.c`（自检：`frame_is_expected_shape`/`head_may_be_frame` 出现 0 次）→ 以**新版** `test/*.c` 链接基线从站编译运行（cwd=`build/review_v27/cwd`） | `t_base.exe` / `run_base.log` | `proto/modbus pass=25 fail=8`：`noise_fake_len_then_read feed expected=1 actual=0`（E10 形态）、`bc_qty_mismatch_resync expected=0 actual=1`、`crc_bad_foreign_not_crc_err crc_err 0→1`、`sticky_bad_then_good len 7→5`、`noise_never_frames discarded 7→0` 等；另 `debug/selftest` 2 例连锁红（新套件跑在基线从站上）。新版同矩阵 → `33/33`、全量 ALL PASS |
| **E2** | AC-1 / v2.6 阻塞项消除 | `sh tools/apidump.sh --check` | `gatesA.log` | **exit 0**（5m35s）；v2.6 同命令 exit 1 |
| **E3** | AC-20 docsync | `sh tools/docsync.sh` | `gatesB_docsync.log` | **`pass=304 fail=0` exit 0**（9m47s）；4 条新断言 ok 在案 |
| **E4** | AC-20 docref / sizecheck | `sh tools/docref.sh`；`sh tools/sizecheck.sh` | `gatesA.log` / `gatesD_sizecheck.log` | docref **OK**（21/1288/104 逐值）；sizecheck **pass=3 fail=0**（v2.7 三行逐值） |
| **E5** | AC-20 `--diff` | `sh tools/apidump.sh --diff`（默认基线 v2.5） | `gatesF_diff.log` | **新增 0 / 修改删除 0，exit 0**（5m25s） |
| **E6** | AC-21 全量回归 | `mingw32-make {test-g4,test-tab,test-1k,ex}`、`WERROR=1 test`、`coverage-build`+gcovr；逐例计数用 `build/et_tests*.exe` | `gatesC_tests.log` | 497/497/504/498 全 ALL PASS；五例 PASS；编译诊断 0 行；覆盖率 97.2%/99.3%（分支 5612/9780） |
| **E7** | AC-19 独立复现 | `git worktree add --detach build/wt-v25 v2.5` → 拷入当前 `tools/apidump.sh` → 生成 → sha256 → `worktree remove --force` | `gatesE_worktree.log` | 生成件 sha256 **`b8ec7378…95fac` / 23200 B** == 归档件 HEAD blob（与交付所报逐值一致）；worktree 已清理 |
| **E8** | AC-3 已发布物核验 | `gh release view v2.6/v2.7 --json body`、`gh release list` | `rel26_body.txt` | v2.6 正文虚假表述 0 命中 + 更正段在位；Release v2.7 = Latest、四附件、挂账段无不实内容 |
| **E9** | CO-1(v2.7) 取证 | `git log --oneline snap-2.7-start..HEAD`；`git rev-parse v2.7^{commit}`；`git diff --stat 838edb1..HEAD`；`gh run view 36157294575 --json headSha` | 命令输出 | 范围内唯一提交 = `e7c79f7`；`tag v2.7` = `e7c79f7` = HEAD；`838edb1`/`23c0d3f` 为悬空提交（与 HEAD 差 12~13 行、纯交付文档）；CI run headSha = `23c0d3f`（success） |

**工作区完好性复核（实验后）**：`git status --short` 仅 `?? .workbuddy/`；`git diff --stat HEAD` 为空；`git worktree list` 仅主工作树（`build/wt-v25` 已移除）；实验只写入 `build/review_v27/`。
