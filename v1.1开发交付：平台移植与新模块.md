# v1.1 开发交付：平台移植与三新模块

> 交付日期：2026-09-02 ｜ 基线：v1.0（`6cb8d5e`）｜ 定版：**tag v1.1**（`472a2a9`）
> 对应计划：[v1.1开发计划：平台移植与新模块.md](v1.1开发计划：平台移植与新模块.md)（状态行已记"已实施，范围无变更"）

---

## 1. 重点概况（TL;DR）

- **16 模块 / 150 用例全过**（v1.0 基线 13 模块 / 103 用例），host 构建与 ARM 交叉构建均 `-Wall -Wextra -pedantic` **零警告**；
- **P0 基建清理**全部完成：et_config.h 注释与实现对齐、版本宏（`ET_VERSION_STRING="1.1.0"` / `ET_VERSION=0x010100`）、Makefile/README Windows 无 make 构建说明；
- **P2 三新模块**按计划交付：`core/et_list`（16 例）、`algorithm/et_filter`（19 例，新目录纯算法层）、`drivers/et_spwm`（12 例）；
- **P1 STM32F103 移植**按降级方案交付：完整移植代码 + demo + arm-none-eabi-gcc 13.3 编译零警告（text 7976B / bss 216B），仿真与真机实测**顺延 v1.1.1**（本机无 Renode/QEMU、无硬件）；
- **P3 工程化**：GitHub Actions CI（ubuntu+windows 单测矩阵 / gcovr 覆盖率 / STM32 交叉编译）；
- **P2-4 et_kv** 按计划决策顺延 v1.2，flash 契约演进提案已落 `exp/kv` 分支评审文档；
- 开发中发现并修复 3 处 v1.0 遗留的可移植性问题（见 §5），未改动任何既有 API 语义。

## 2. 逐项验收对照

### P0 基建清理（M1，`ec170f2`）

| 计划任务 | 结果 |
|---|---|
| 1. 修正 9 处"(预留)"注释 | ✅ 13 个既有模块注释与 README 特性表逐一对应；顺带把模块开关补上 `#ifndef`（原注释声称可 `-D` 覆盖实际不可），裁剪方式经全模块烟测验证 |
| 2. 新增 `ET_VERSION_STRING` / `ET_VERSION` | ✅ 字符串由 MAJOR/MINOR/PATCH 三个分量自动拼出（单一事实来源），与 tag v1.1 一致 |
| 3. Makefile 补 Windows 无 make 构建说明 | ✅ mingw32-make 与纯 gcc 单行命令两套，注释中可直接复制；本机（make 不在 PATH）全程用该路径构建验证 |
| 4. README 头部声明版本并链接计划 | ✅ |

### P1 真机移植 port/stm32f103（M2，`e216dc4`）

| 计划任务 | 结果 |
|---|---|
| 1. PRIMASK 保存/恢复 + CPSID/CPSIE，可嵌套 | ✅ 保存/恢复 + 嵌套计数；计数在屏蔽区内天然无并发风险，正文推导见 `port_stm32f103.c` 头注释 |
| 2. SysTick 1ms 时基 | ✅ HCLK/8（HSI 8MHz）RELOAD=999 精确 1ms，ISR 仅累加，回绕由上层无符号减法消化 |
| 3. port_putc USART1 轮询 | ✅ TXE 等待阻塞发送，BRR=0x045 → 115200-8-N-1（+0.64% 误差） |
| 4. 最小真机 demo | ✅ `examples/stm32f103_demo.c`：日志横幅 + 连闪 3 次 + 心跳 + 按键短按闪烁/长按呼吸（et_led→et_spwm write 直连） |

**验收状态（降级方案）**：代码交付 ✅、`arm-none-eabi-gcc` 编译通过 ✅（零警告，向量表/链接布局经 objdump 核对）；Renode/QEMU 仿真 ⏳（本机无仿真器）；真机实测 ⏳（无硬件）。两项顺延 v1.1.1 补录，已在 `port/stm32f103/README.md` 与 `docs/API_GUIDE.md` §8.1 已验证平台表挂账。

### P2 新模块（M3/M4，`6c27f86`）

| 模块 | 交付 | 单测 | 计划要求覆盖 |
|---|---|---|---|
| `core/et_list` | 哨兵头双向链表，O(1) 插删；`prev==NULL` 判"不在链上"→ 重复 remove/push 防护 | 16 例 | 空表头尾插删 ✅ 遍历中自删 ✅ 删后继 ✅（删除时保留 next 指向原后继，一次性摘光整表亦安全）重复 remove ✅ |
| `algorithm/et_filter` | movavg（O(1) 环形均值，int64 累加）/ lpf1（Q15，`y+=k*(x-y)`，可移植地板除法）/ slew；禁 port.h | 19 例 | 稳态收敛（正阶跃残差 1 LSB、负阶跃精确收敛的手算序列）✅ 阶跃响应数值 ✅ Q15 饱和边界（k=0/32767 死区）✅ reset 语义 ✅ |
| `drivers/et_spwm` | 毫秒时基相位法，静态注册表 ET_SPWM_CH_MAX=4，输出缓存 | 12 例 | duty 0/255 边界恒电平 ✅ 虚拟时间注入翻转序列 ✅ 跨 tick 回绕 ✅ 多通道相位独立性 ✅ |

DoD 达成：`.c/.h` 成对 + 并发上下文标注（均为单上下文/仅主循环，头文件显式声明）；`ET_MODULE_*` 开关置 0 编译为空 TU（逐模块烟测）；接入 `test_main.c` 套件注册；API_GUIDE/README 同步（见 §4）；零警告、零 malloc、无隐藏全局状态。

### P3 工程化（M4/M5，`472a2a9`）

- `.github/workflows/ci.yml`：push/PR 触发；ubuntu `make test` + windows **纯 gcc 无 make 路径**（与 README 文档化命令完全一致，CI 即文档验证）；gcovr 覆盖率构建本地已验证、报告随 artifact 产出，暂不设门槛；另附 STM32F103 交叉编译 job 作为移植回归门。

### P2-4 et_kv（支线）

按计划决策条款（"若 P0~P3 进度顺利再合入 v1.1，否则定版 v1.2 首个特性"）：**定版 v1.2 首个特性**。`exp/kv` 分支已交付 `docs/proposals/et_kv_flash_contract.md`：port_flash 契约草案 + 5 条评审要点 + 页格式与 API 设计草案，v1.2 开工直接引用。

## 3. 质量数据

| 项 | v1.0 | v1.1 |
|---|---|---|
| 模块数 | 13（+port） | **16**（+algorithm 层；core 4 / algorithm 1 / sys 3 / protocol 3 / drivers 3 / debug 2） |
| 单元用例 | 103 | **150**（+47），ALL PASS |
| host 构建警告 | 0 | 0（gcc 16.1 / MinGW） |
| ARM 构建警告 | 未验证 | 0（arm-none-eabi-gcc 13.3） |
| demo 资源占用 | — | STM32F103C8T6：text 7976B（Flash 12%）/ bss 216B（RAM 1.1%），全模块启用 |
| CI | 无 | ubuntu+windows 单测 / gcov 覆盖率 / STM32 交叉编译 |

回归方式：`mingw32-make test`（或 Makefile 头部注释中的纯 gcc 命令）；`mingw32-make demo` 全栈演示。

## 4. 文档同步

- `docs/API_GUIDE.md`：章节重排（algorithm 层为第 3 章），新增 2.4 et_list / 3.1 et_filter / 6.3 et_spwm / 8.1 已验证平台 / 10.5 呼吸灯直连配方；配置项参考表补 `ET_SPWM_CH_MAX` 与版本宏；目录与交叉引用更新。
- `README.md`：版本声明 + 16 模块特性表 + 目录树（algorithm/、port/stm32f103/）+ 快速开始双路径 + 使用速查（链表 / GPIO 呼吸灯）+ 移植平台清单；历史遗留的"96 用例"陈旧数字一并修正。
- `port/stm32f103/README.md`（新）：文件清单、实现要点、构建/烧录步骤、已验证平台清单与顺延项说明。

## 5. 开发中发现并修复的问题（超出原计划范围）

1. **头文件不自包含**：newlib（ARM 工具链）下 `NULL` 不可用，10 个 v1.0 头文件缺 `<stddef.h>`——host MinGW 因传递包含侥幸可用，首次交叉编译即暴露。已全部补齐（新增 include，不涉及 API 变更）。
2. **模块开关 `-D` 覆盖失效**：et_config.h 声称"所有配置均带 #ifndef 保护"，模块开关实际未保护。已补齐并用"开关置 0 编译"烟测验证。
3. **et_log 三处恒假防御判断**：`(int)lv < ET_LOG_LEVEL_TRACE`（枚举恒非负）触发 ARM gcc `-Wtype-limits`；编译期裁剪相关的判断已用 `#if ET_LOG_MAX_LEVEL > TRACE` 门控，语义不变。

## 6. 范围外事项 / 顺延清单

| 事项 | 状态 | 去向 |
|---|---|---|
| et_kv 模块 | 契约评审文档就绪 | v1.2 首个特性（`exp/kv` 分支） |
| Renode/QEMU 仿真截图 | 本机无仿真器 | v1.1.1 |
| F103 真机实测记录 | 无硬件 | v1.1.1（`port/stm32f103/README.md` 表格挂账） |
| RTOS 适配 / 浮点 printf / 既有 API 变更 | 按 Non-goals 不做 | — |

## 7. 提交与版本

```
472a2a9  M4/M5 文档与工程化: API_GUIDE v1.1 章节重排, README 同步, GitHub Actions CI   ← tag: v1.1
e216dc4  P1 真机移植: port/stm32f103 (Cortex-M3/BluePill)
6c27f86  P2 新模块: core/et_list / algorithm/et_filter / drivers/et_spwm
ec170f2  P0 基建清理: et_config.h 注释修正/版本宏/新模块开关, README 版本声明
6cb8d5e  Initial commit: Embedded_Tools v1.0                                          ← tag: v1.0
02e48a1  (支线 exp/kv) et_kv 技术验证: port flash 契约演进提案
```

`et_config.h` 版本宏与 tag v1.1 已核对一致；计划文档按其维护约定归档。
