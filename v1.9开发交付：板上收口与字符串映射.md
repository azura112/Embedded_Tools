# v1.9 开发交付：板上收口与字符串映射

> 交付日期：2026-09-08 ｜ 版本：v1.9.0（`ET_VERSION = 0x010900`）｜ 基线：v1.8（`08ef770`）
> 计划文档：[v1.9 开发计划：板上收口与字符串映射](./v1.9开发计划：板上收口与字符串映射.md)（状态：✅ 已交付，挂账见 §5）
> 远程：https://github.com/azura112/Embedded_Tools（main @ `ff1c4c7` + 本交付提交）

## 1. 重点概况（TL;DR）

v1.9 兑现"**先还债，再扩容**"：连续三版悬置的板面验证一次性清偿（三 tag 按序补打），文档断裂类缺陷制度化根治（文档即测试），随后交付字符串键映射。

- **P0 板上收口（本版主线）**：G474 真机全自动会话（ST-Link + CubeProgrammer CLI 烧录/复位 + pyserial 控制台采集）走完 **v1.6 走单 1~4**（SIMUPGRADE even→attempt 1→2→`ROLLBACK: attempts exhausted`；odd→`CONFIRMED`→attempt 冻结不累计；**xmodem 真传** `1932 bytes in 16 blocks` 收发一致→verify→STAGED→CONFIRMED；**IWDG 真超时** `reset cause: IWDG` 原因寄存器 + boot#n+1），**v1.7 库化 selftest 板上记录**（`SELFTEST: 17/17 PASS` 59ms + `AT+SELFSTOR` kv/bootctl 实跑 PASS 且 demo kv 自动重建），**v1.8 板面验证**（tickless 深空闲 ≥20s 后敲 `AT+VER` 即时应答——RX 中断唤醒实证）。走单 5（LED 极性/按键手感）经用户 2026-09-09 现场目视确认（PC0 高有效、短按/长按全对）——**升级链走单 1~5 全部板上实证，v1.6~v1.9 板面零欠账**。
- **走单暴露 8 处缺陷，全部实机定位并修复**（实机记录 §6 问题 3~10）：demo 升级链从未真正通过（`verify/stage` 误传扇区号）、SIMUPGRADE 版本解析不归零、升级缺 `abandon()` 前置（状态机明示规则）、`xm_sink` 跨槽写踩邻接状态扇区、xmodem 成功判定误用 DONE 时已被清零的 `total` + DONE 动作未回收尾 ACK（协议二段 EOT）、**port_wdt 冷启动时序错误**（LSI 仅 START 后起振，旧序先等同步必然 guard 超时却已写 START——F103/G474/CubeMX 三 port 同病，改 HAL_IWDG_Init 参考序）、selftest bootctl 套件假设干净态（真实升级后 6 断言连锁误报→init 后强制 `abandon()`）、WDTEST 复位后 IWDG 不可停→demo 开机检测 IWDGRSTF **自愈喂狗**。"三版无人碰的升级链一上真机即全红"——P0 主线的价值即在此。
- **P1 缺陷清偿与文档可执行化**：F103 demo selftest 调用全 `#if ET_MODULE_SELFTEST` 守卫 + README 构建命令补 `storage/*.c`（文档命令**无宏原样**编译链接通过，text 16852）；三处滞后数字回刷（v1.8 交付 25180→25200、15/15→17/17、86→87）；**CI `docbuild` job**：port README ```` ```docbuild ```` 定界构建块提取原样执行（含 objcopy/产物存在），今后同类断裂 CI 直接红；发布 checklist 第 7 条"**数字回刷纪律**"入册；命令表计数改 `sizeof` 自适应（CubeMX 9u 漏改教训根治）。
- **P2 `et_smap`**（core/，第 27 模块）：定容字符串键映射——FNV-1a 32 位哈希 + **内嵌键存储池拷入语义**（原串可释放）+ 哈希缓存/键长预比较；探测/墓碑/满表拒绝与 et_map 同规则（文档交叉引用，防两套措辞漂移）；空键/超长/池满显式拒绝且不推进池水位（先定位后分配）；`ET_MODULE_SMAP`/`ET_SMAP_KEY_MAX`（默认 16）；**14 例全绿**（池治理/拷入/大小写/碰撞链同桶键对搜索/遍历中删安全/多实例/边界）。**键 0 评估决议落地**：et_map 维持键 0 保留，API_GUIDE FAQ 写明理由（空槽/墓碑是实现事实，改键域需重设计状态编码），应用侧走 `key+1` 偏移，不做库级变体。
- **P3 三项收编不堆积**：`ET_CRC_TABLE` 扩展 **CRC32/IEEE 查表**（1KB 表驻只读段同 SECTION 机制，表/位算法随机对拍逐字节一致，标准向量 CBF43926；bench 125→500 MB/s 与 ccitt 表同档）；bench v1.9 行（新增 map/smap 查找：**2 vs 8 ns/op**，字符串键的哈希+memcmp 成本可见）；smap 配方（API_GUIDE 11.9，atcmd 取参→smap 查对象两级解耦 + 池预算公式）。
- **治理断档修复**：CHANGELOG **v1.8 条目缺失**（写死"最新版本 1.7.0"断言抓不住缺条）→ 补齐 1.8.0/1.9.0 并把断言改为**版本链动态全覆盖**（1.0.0..当前逐个断言）。

## 2. 逐项验收对照

### P0 板上收口（★ 本版主线）

| # | 任务 | 验收结果 |
|---|---|---|
| 0 | pyserial 前置 | ✅ 已装；且额外打通 ST-Link+CubeProgrammer CLI v2.19.0（烧录/复位自动化），COM12（CH343） |
| 1 | v1.6 走单 5 条 + tag v1.6 | ✅ 走单 1~4 全实证（§5 日志逐行）；走单 5 经用户 2026-09-09 目视确认（PC0 高有效/PD15 行为全对）；实机记录落数 + docsync 占位符反向断言；**tag v1.6 补打** |
| 2 | v1.7 库化 selftest 板上记录 + tag v1.7 | ✅ `SELFTEST: 17/17 PASS`（15 PASS+2 SKIP，59ms）+ `SELFSTOR` 实跑 PASS + kv 重建；实机记录 §4.5；**tag v1.7 补打** |
| 3 | v1.8 板面验证 + tag v1.8 | ✅ tickless+RX 唤醒实测（§4.6）+ xmodem_tx 场景经回环库测覆盖 + 走单 3 真传；**tag v1.8 补打** |

### P1 缺陷清偿与文档可执行化

| # | 任务 | 验收结果 |
|---|---|---|
| 1 | selftest `#if` 守卫 | ✅ 文档命令无宏原样执行通过（text 16852）；带宏 25352 行为不变（CI/smoke 回归） |
| 2 | 三处数字回刷 | ✅ 25200 / 17、87 全表核对（v1.8 交付注记 + port README v1.8 行改实值）；新增数回刷纪律条款 |
| 3 | docbuild job | ✅ `tools/docbuild.sh` 提取 ```` ```docbuild ```` 块原样执行；CI 新 job 绿（run `34152880417`）；自证：删块→报"文档定界错误" |
| 4 | checklist 条款 | ✅ README 第 7 条（数字回刷纪律 + docbuild 守护）；docsync 含占位符反向断言 |

### P2/P3

| # | 任务 | 验收结果 |
|---|---|---|
| P2 | et_smap | ✅ 第 27 模块；14 例 ≥12；语义交叉引用（DoD #4）；键 0 FAQ 决议入 API_GUIDE |
| P3-1 | CRC32 查表 | ✅ 同开关扩展，1KB 表驻只读段；对拍逐字节一致 |
| P3-2 | bench v1.9 行 | ✅ map/smap 新增 + crc32 表档；环境注记纪律沿袭 |
| P3-3 | smap 配方 | ✅ API_GUIDE 11.9（P3 三项收编，无尾巴入 v2.0——沿"不再顺延"决议） |

## 3. 里程碑对照

| 里程碑 | 内容 | 提交 |
|---|---|---|
| M1 | P0 板上收口全套 + 走单暴露 8 缺陷修复 + 实机记录回填 + pack_image | `485035a` |
| M2 | et_smap 合入 + 版本 1.9.0 + CRC32 表 + bench | `eda1141` |
| M3 | docbuild 门 + 数字回刷治理 + 文档同步 + docsync 121 | `ff1c4c7` |
| M5 | 交付文档 | `98772ef` |
| 收口 | release.yml 门修复 + 四 tag 定着（见 §4 DoD#2） | `3e7cf81` |

## 4. DoD 对照（v1.9 §4）

| DoD | 状态 | 证据 |
|---|---|---|
| 1 沿用 v1.8 全部 DoD（docsync 全绿/双几何/量化+环境注记/覆盖率行） | ✅ | docsync **121/121**；340×2 几何全绿（1K 变体 341）；§6 复现表含覆盖率行 |
| 2 P0 三 tag 收口为 v1.9 tag 前置（顺序收口链清零） | ✅ | **定着点与定因**：v1.6→`b669823`（Release 自动✅）；v1.7→`64a1387`（交付 commit `aa4d9bc` 的 Makefile 双缺陷——TEST_SRC 缺条+续行反斜杠——修复在其后，tag 移至周期末全门通过态，Release 自动✅）；v1.8→`08ef770`（该树 demo 无守卫而门无宏，**历史树不可能追溯过门**——例外经人工 Release 发布，正文含失败 run `34154399570` 与工件注记）；v1.9→`3e7cf81`（release.yml 门补宏修复后，Release 自动✅） |
| 3 文档即测试：docbuild 门 + 新增 port 同批入清单 | ✅ | CI job 绿；`tools/docbuild.sh` 的 `PORTS` 清单即"同批"载体 |
| 4 et_smap 语义重叠交叉引用文档化 | ✅ | et_smap.h 头注 + API_GUIDE 2.6 双处指向 et_map 定义 |
| 5 数字回刷纪律生效 | ✅ | 本交付后无改动数字的后续提交；checklist 第 7 条 + docsync 体积行/覆盖率/用例数断言承载 |

## 5. 挂账与后续

1. ~~走单 5（目视项）~~ ✅ 2026-09-09 上板人现场目视确认：PC0 高电平点亮（3 连闪可见）、PD15 短按 blink/长按 breath 行为全对——已回填实机记录 §5/§7，板面挂账清零（本版交付闭环）。
2. F103 真机维持常设挂账（不排期，沿既有边界）；Renode G4 维持政策关闭。
3. **追补 tag 的门治理教训**（已固化）：release.yml/Makefile/ci.yml 三处显式构建清单各自为政是本次四 tag 中三处翻车的共因——v1.9 的 docbuild 门（README=文档即测试）是第一步；后续版本若再显式复制构建命令，优先改为调用 docbuild/Makefile 单一来源；
4. v2.0 候选仅记不排：字符串大小写折叠/通配（smap）、u32 全键域容器、shell Tab 补全（连续五版排除）。

## 6. 量化数据复现（环境注记）

| 项 | 复现命令 | 环境 |
|---|---|---|
| 单元全量 | `make test` / `make test-g4` → RESULT 行 **340/340 双几何**；1K 变体 341 | MinGW-w64 gcc 16.1.0（x86_64-win32-seh），Git Bash |
| 覆盖率 | CI `coverage (gcovr)` job 日志 → `lines: 97.0% (7067 out of 7289)`，门 ≥85% | ubuntu-latest 系统 gcc + gcovr（pip），run `34152880417` |
| docsync | `sh tools/docsync.sh` → **121/121** | Git Bash |
| ARM 体积 | 两 port README 构建命令（`docbuild` 同款），F103 默认 **16852** / selftest **25352**，G474 默认 **18088**；CubeMX 工程 v1.9 固件 FLASH **32100** B 零警告 | GNU Tools for STM32 **13.3.rel1**（CubeCLT 1.18.0）/ arm-none-eabi-size |
| 板面全套 | 实机记录 §8"v1.9 走单收口会话"命令组 | G474VET6 真机 + ST-Link V2J47 + CubeProgrammer **v2.19.0** + COM12@115200 + Python 3.12/pyserial，固件 v1.9.0(`0x10900`)，2026-09-08 |
| bench | `make bench` / `make bench-table` | 同 v1.7 基线环境注记（docs/bench.md） |
