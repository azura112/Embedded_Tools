# v2.7 开发交付：从站解析对称化与板上自测收口

> 执行日期：2026-09-25 ｜ 计划冻结件：[`v2.7开发计划__从站解析对称化与板上自测收口.md`](v2.7开发计划__从站解析对称化与板上自测收口.md) ｜ 版本号 **2.7.0**
> **快照（HC-4 前置门终态）**：`snap-2.7-start`（**不带 `v` 前缀**）＝ v2.6-r2 返工轮终态提交；**G0′ 七项全绿后方开本版代码窗口**，四门读数见 §7.1
> 本版 docsync：**304/304**
> 评审记录：本版评审记录**尚未产出**（按 `docs/评审记录模板.md` 由评审方在交付后产出）；本版输入为 [`v2.6评审记录__解析边界清偿与检出能力补强.md`](v2.6评审记录__解析边界清偿与检出能力补强.md)（2026-09-25，结论**返工**，`CO-1(v2.6)`…`CO-15(v2.6)`）
> CO 处置三落点（HC-11）：① 计划 §1.1 / ② [`docs/v3-candidates.md`](docs/v3-candidates.md)「v2.7 复评记录」节 / ③ 本文 §8

---

## 1. 交付摘要（三行以内）

1. **从站解析边界对称化（`CO-5(v2.6)` ★ 核心）**：`et_modbus` 在任何切片前校验 `0x10` 的长度域、噪声头部**逐字节重同步**、CRC 失败只计"与本站期望同形"者 —— 评审 E10 形态（噪声吞掉真请求）在 **host 与板上双双转为"真请求被正确应答且 `crc_err` 不增长"**。
2. **板上自测收口（`CO-14(v2.6)`，关闭 `CO-10(v2.5)`）**：selftest 内建套件 **20 → 22**（新增 `modbus`/`log`），九处引用同批回刷，板侧 **`SELFTEST: 22/22 PASS`**。
3. **代码卫生与证据链**：主站读路径死代码清理（行为零变化）；`L`+整数转换语义限定；`git worktree` 真生成 v2.5 基线清单**逐字节复核（sha256 一致）**；`CO-8` 的 `disc=1` **定位到来源**（命令行 `\n` 残留）并正式登记。

---

## 2. AC 自查表（逐条 AC-1…AC-22，标注对应任务）

**复现环境**：Windows 11 ｜ Git Bash（`sh` = `…\PortableGit\1.2.0\usr\bin\sh`，GNU bash 5.3.15）｜ host gcc MinGW-w64 **16.1.0** ｜ ARM `arm-none-eabi-gcc` **GNU Tools for STM32 13.3.rel1**（STM32CubeCLT 1.18.0）｜ `make` → `mingw32-make` ｜ 板侧 G474VET6 / **COM12** / ST-Link。
**对照固件（AC-8/AC-15 的 A/B）**：A = `Core/et` ← `snap-2.7-start`（**v2.6 库**），B = `Core/et` ← v2.7 工作树；二者同步校验 `diff -rq` 全 OK、0 warning（详见实机记录 §15.1）。

### 前置门 G0′（= v2.6-r2 返工轮，本版代码窗口的前置条件）

| AC | 结论 | 证据 |
|---|---|---|
| **AC-1**（G0′-1，HC-4） | **通过** | `sh tools/apidump.sh --check` → **`清单与头文件一致`，exit 0**（修复前：`FAIL —— 公开面漂移`，exit 1、4m24s）；`sh tools/docsync.sh` → **`pass=304 fail=0`**。**门自证可红**：注入公开声明 `et_r2_probe_red()` → exit 1 并打印 `### 函数声明 (6)` → `(7)`；按 sha256 回退 → 复绿（`-r2` 文档 §7.4 探针） |
| **AC-2**（CO-13 耗时） | **通过** | 本机实测（PortableGit sh 5.3.15）：`--check` 修复前 **4m24s** / 修复后 **4m18s**、`--diff` **4m25s**；交付物与文档中不存在不可复现的旧读数（`-r2` 文档 §7.3 附 shell 与命令；评审交叉复核 4m39s/4m53s/6m29s 同量级） |
| **AC-3**（G0′-2） | **通过** | `grep -n "板侧项未执行\|亦未打" CHANGELOG.md` = **0 命中**（v2.6.0 节已重写为"板侧项**已执行**、`tag v2.5` **已打并推送**"）；`gh release view v2.6 --json body` 与 CHANGELOG v2.6.0 节**同源一致**（正文由 `release.yml` 的同一 awk 规则重新提取后 `gh release edit v2.6 --notes-file` **重注**，未重打 tag；本地生成件与远端正文 `diff` 仅差 GitHub 追加的一个尾部空行） |
| **AC-4**（G0′-3） | **通过** | v2.6 交付文档 §1/§4/§5.1/文末纪律句与 §2/§7 终态**无互斥**（`-r2` 文档 §7.5 逐句对照 6 处）；实测计数：`grep -c "不合格"` = 1、`"待推送后核验"` = 1、`"待推送"` = 2 —— **全部为历史引述**（r2 修订声明 + §4 更正溯源句），无一处为终态声称 |
| **AC-5**（G0′-4） | **通过** | ① `README.md` 冻结面 checklist 第 8 条的基线名 = **`docs/API_INVENTORY_v2.5.md`**（不再含 `_v2.4.md`）；② `port/stm32g474/README.md` v2.6 行 = "板侧工程本版**已同步**（见实机记录 §14）"（`grep -rn "未同步" port/` = **0**）；③ v2.6 交付文档 AC-18 证据行 = "**selftest 相关行**零改动"。**并已装门**：docsync 增 4 条（README 基线名 ↔ apidump 默认基线交叉核对 / "未同步"反向断言 / 计划模板第 5 项 / README 与模板项数一致），**自证可红**：把基线名改回 `_v2.4.md` → `docsync` exit 1 并打印该条 FAIL，还原后复绿 |
| **AC-6**（G0′-5） | **通过** | `docs/开发计划模板.md` §7 含**五项**（新增 ⑤「证据落点章节号 + 占位节清理」，附 v2.6 反例）；`移植stm32实机记录.md` §12 标题处含"**已由 §13 承接**"；`README.md` checklist 第 10 条同步为"五项"（docsync 交叉断言） |
| **AC-7**（G0′-6） | **通过** | `tools/docref.sh` 判据收窄为"**记录终点须覆盖到 HEAD**"；本文 §4 终点写 `HEAD` 并通过。**门自证可红（两探针）**：探针 A（终点改固定 SHA 且其 == HEAD）→ **OK**；探针 B（随后追加一个**纯文档提交**）→ **FAIL exit 1**：`记录终点未覆盖 HEAD … END = a0b6748 / 实测 HEAD = 35036b7`，回退后复 **OK**（`-r2` 文档 §7.4） |

### 从站侧解析（P0）

| AC | 结论 | 证据 |
|---|---|---|
| **AC-8**（P0-1/P0-2，HC-2） | **通过（host + 板上双证）** | **host**：`mb.noise_fake_len_then_read` —— 先喂 11 字节伪写入帧噪声 `11 10 00 00 00 01 04 DE AD BE EF` 再喂真请求 `11 03 00 00 00 04 46 99` → `et_modbus_feed` 返回 **1**、应答 = `11 03 08 00 01 00 02 00 03 00 04 59 D4`、`stats.crc_err` **不增长**、`frames==1`。**板上 A/B**（实机记录 §15.3）：同一注入形态在 A（v2.6 库）**无应答且 `crc_err` +2**，在 B（v2.7 库）**被正确应答且 `crc_err` 保持 0**、噪声计入 `discarded`。**对照复跑方法（供评审独立复现，§7.3）**：`git show snap-2.7-start:protocol/et_modbus.c > protocol/et_modbus.c`（仅该文件）→ `mingw32-make test` → `proto/modbus` 应红（`mb.noise_fake_len_then_read` 等）；按 sha256 回退即绿；板上对照的固件构建见 §15.1 |
| **AC-9**（P0-1/P0-2） | **通过** | `test/test_modbus.c` 新增 **10 例**（≥8）且全绿：`proto/modbus (33 cases) => pass=33 fail=0`（v2.6 的 23 → 33）；既有从站用例（0x10 写、qty 边界 125/123、异常码、广播写/读、静默路径、地址不符）**全部仍绿** —— 唯一改写者 = `mb.exc_illegal_value_wr`（见 §3 D-2 行为修正登记）；`mingw32-make test` / `test-g4` **497 ALL PASS** |
| **AC-10**（P0-3） | **通过** | **主从同构对拍表**见 §7.4（两侧同形态用例 + 语义一致性逐条；差异面：主站可用 `exp_qty` 预先拒绝读帧定长，从站无先验期望数 —— 该差异已在 API_GUIDE 5.7/5.8 两侧分别写明） |
| **AC-11**（P0-4，HC-3） | **通过** | `docs/API_GUIDE.md` 5.7 增**从站统计口径表**（`frames`/`responses`/`exceptions`/`crc_err`/`addr_mismatch`/`discarded` 逐条加一条件），与实现**差集为空**（逐条由 `test/test_modbus.c` 对应用例固定；`et_modbus.h` 的 `et_modbus_stats_t` 字段注释与 5.7 同措辞）；交付文档 §2 已登记"**行为修正（语义面）**"（见 §3 D-2、D-3） |

### 板上自测与板侧（P1）

| AC | 结论 | 证据 |
|---|---|---|
| **AC-12**（P1-1/P1-2） | **通过** | `debug/et_selftest.c` 的 `g_suites[]` 含 `{"modbus", st_modbus, 0u}` 与 `{"log", st_log, 0u}`；`et_selftest_suite_count()` 在默认模块配置下返回 **22**（`test/test_selftest.c:98` 断言 `ET_CHECK_U32_EQ(22u, base)` 且该用例绿：`debug/selftest => pass=9 fail=0`）；板上 `AT+SELFTEST` → `SELFTEST: 22/22 PASS` |
| **AC-13**（P1-3，HC-7/HC-12） | **通过** | **九处回刷逐行清单**见 §7.5（`et_selftest.c` 注释、`test_selftest.c:98`、`test_selftest.c:151+152`、`renode/smoke.sh:97`、`README.md:46`、`:188`、`:193`、`API_GUIDE.md`（8.4 套件行 + 9.1 平台行）、`CHANGELOG.md`）+ 同批扩展面（README 结构树/质量门、`architecture.md`、`getting-started.md`、port README 体积表/平台表、实机记录、docsync 断言）；`sh tools/docsync.sh` → `pass=304 fail=0` 为证 |
| **AC-14**（P1-4，HC-9） | **通过** | `移植stm32实机记录.md` **§15** 载：`Core/et` 同步 `diff -rq` 七目录 + `et_config.h` + `port.h` **全 OK**、`cmake --build --preset Debug` **0 warning**、FLASH/RAM 逐值（A 75440/5960、B₁ 80852/6744、B₂ 81244/6768）；`AT+SELFTEST` → **`SELFTEST: 22/22 PASS`**；`AT+SELFSTOR` → `STORAGE SELFTEST PASS`；`AT+SIMUPGRADE 3` → `slot 1 CONFIRMED (self-check ok)`；**v2.4 从站分流走单复跑**逐字节在位（`read 0..3 → 1,2,3,4` PASS、`read 1000..1001 → 0,0` PASS） |
| **AC-15**（P1-4，HC-9） | **通过** | §15.3 的 **CO-5 板上注入对照表**：同一注入形态（19 字节 = 11B 伪写入帧噪声 + 真读请求）× 两版固件 —— **应答有无**（A 无 / B 有 `11 03 08 …59 D4`）+ **`crc_err`/`discarded` 终值**（A `crc=3 disc=0` / B `crc=0 disc=17`）**两侧均为板测实得**（修复前数据**已取得**，非"不可得"）；两笔写形态（form1）的成功原因亦已定位（4ms 静默路径先兜住残帧），并说明"单次突发才是真实压力形态" |
| **AC-16**（P1-5，CO-8） | **通过（已定位到来源）** | §15.4 载**机械定位**：`AT+MBRAW` 显示本次事务首字节恒为 **`0x0A`**（3/3 复现），而"仅 CR"/"仅 LF"结束命令行时 **`disc=0`** ⇒ 来源 = **命令行 `\n` 残留**（shell 在 `\r` 处分发 → 残余 `\n` 留在 RX 环 → 阻塞式主站事务把它当作首字节）。**排除清单**（均有读数）：UART 错误（`ore=0` 全程）、TX 回环（首字节恒 0x0A 非帧前缀 + §14.2 对照）、物理层噪声（取值不随机）。**已在交付文档 §5.2 与 §6 正式登记**（`grep -c "disc" 本文` ≥ 2 处且分节），并给修法 + 记 v2.8 板侧待办 |

### 代码卫生与证据链（P2/P3）

| AC | 结论 | 证据 |
|---|---|---|
| **AC-17**（P2-1） | **通过** | 改法 = **删繁 + 不变式断言**：`et_modbus_master.c` 的 `handle_frame()` 删除不可达的"字节数不符 → discarded"分支（改 `ET_ASSERT((uint32_t)m->rx[2] == 2u*(uint32_t)m->exp_qty)`；`ET_ASSERT` 默认展开 `((void)0)`，**发布配置零开销**），`is_expected_shape()` 删除到达点恒真的读分支子句（有效判别力 = 地址 + 功能码，与评审 E12 结论一致）；`mingw32-make test` / `test-g4` **497 ALL PASS**（不回退）；改后源码中不再含"到达点恒真"子句 |
| **AC-18**（P2-2） | **通过** | `docs/API_GUIDE.md` 8.1 与 `debug/et_log.h` 头注均含"**`L` 仅对浮点转换有定义；`%Lu`/`%Ld`/`%Lx` 属未定义行为，库按 `long double` 消费并输出占位**"的限定，两处表述一致（同日补充"x86-64 SysV memory-class 可能不推进整数实参槽 → 不保证可移植语义"与"64 位整数请用 `%lld`"） |
| **AC-19**（P2-3，D-7） | **通过** | `git worktree add --detach build/wt-v25 v2.5` → 在**该树**上用**当前** `tools/apidump.sh` 真生成清单（440 条目）→ 与归档 `docs/API_INVENTORY_v2.5.md` 的 **HEAD blob 逐字节比对 `cmp` 一致**（sha256 **`b8ec7378a8b4d67ebac06b8a9f049e990a90366126d5ec75ff4c518fdee95fac`**，23200 B）；CR 归一后与工作树归档**差异 0 行** ⇒ v2.6 的"人工等价性论证"降级被**机器复核**取代 |
| **AC-20**（全版） | **通过** | `sh tools/docsync.sh` → `pass=304 fail=0`；`sh tools/apidump.sh --check` → exit 0；`--diff` 对 v2.5 基线（v2.6 归档缺失，见 `CO-2(v2.7)`）→ **新增 0 项 / 修改删除 0 项**（exit 0；耗时 410 s）；`sh tools/sizecheck.sh` → **pass=3 fail=0**（v2.7 表行 23884/24/740、36340/24/3016、24260/28/740 逐值）；`sh tools/docref.sh` → **OK**（§4 与实测逐值一致）；CI 见 §4 |
| **AC-21**（全版） | **通过** | `test` / `test-g4` **497** ALL PASS；`test-tab` **504**；`test-1k` **498**；`make ex` **五例 PASS**；`WERROR=1 test` / `WERROR=1 ex` 零告警（严格匹配编译诊断 = 0 行）；覆盖率 **行 **97.2%**（10575/10877）、函数 **99.3%**（1066/1074）、分支 57.4%（5612/9780）**；`make test-asan` / `test-asan-1k` **CI 侧常设**（本机不可执行，已在 README 检出边界与 §5 声明，**未声称本机已跑**）；模块数维持 **34** |
| **AC-22**（P3-1） | **不做（显式声明 + 再记 v2.8）** | 见 §8 与 §5.1：本版**未**采"bench 相对比值化"（时间盒内优先保障板上自测收口与 `CO-5` 板上对照）；**非静默略过**，理由与触发条件见 §8.3 |

**apidump 结构体字段登记**：本版**无**既有冻结结构体的字段追加、无新增/修改 API 形状（`--diff` = **新增 0 / 修改删除 0**，见 AC-20）。**唯一的既有语义变更**登记如下：

- `et_modbus_stats_t.crc_err` / `.discarded` **计数口径收紧**（v2.7 HC-2/HC-3）：`crc_err` 由"任何 CRC 失败帧"收紧为"**帧首 == 本站从站地址** 且 功能码属已支持集合 且长度域自洽"的候选帧；**"从未成帧"的噪声字节改计 `discarded`（逐字节重同步，每丢 1 字节计 1）**。字段类型与结构体布局**未动**，属**行为修正**（HC-1 的语义面），已在 `docs/API_GUIDE.md` 5.7 口径表、`et_modbus.h` 头注与本节登记。
- `et_modbus` 的 `0x10` 帧在 `bc != 2*qty` 时**不再回异常 `0x03`**，改为逐字节重同步（见 §3 D-2）。

---

## 3. 偏差记录

| # | 内容 | 原因 | 影响 |
|---|---|---|---|
| **D-1** | **`snap-2.7-start` 的推送过程受阻后恢复**：本地 tag 已于 v2.6-r2 终态定着并复核无副作用（探针实测，`-r2` 文档 §7.6）；**远端推送**首次多次遇 `CONNECT tunnel failed 502`，**重试后成功** —— 远端 `refs/tags/snap-2.7-start` = **`724d089`**（与本地逐值相同，`gh api …/git/matching-refs/tags/snap` 核验） | 本机出口代理对 git 隧道间歇不可用（跨版已知现象，v2.5/v2.6 均有记录）；`gh api` 通、git 隧道不稳 ⇒ 需重试 | 已闭环：快照 tag 两端一致 ⇒ v2.7 代码窗口的起点（HC-4）在远端可核验 |
| **D-2** | **既有从站用例的行为修正**：`mb.exc_illegal_value_wr` 的"`bc=4 / qty=3` → 异常 0x03"断言**改写**为"**不产生应答 + `crc_err`==0 + 逐字节重同步**"（`mb.bc_qty_mismatch_resync`）；`qty > 123` 的异常路径**保留**（自洽帧仍可达） | HC-2 明文要求"`b[6]` 不符即**逐字节重同步**，不得按该未受信长度前进" —— 与"可据未受信 `bc` 定长并向应用报异常"**互斥**；计划附 B 风险表已给出"若必须改断言，须登记为**行为修正**并说明理由"的处置通道 | **能力损失（如实）**：从站对"`bc` 与 `qty` 不自洽的 `0x10` 帧"不再回异常 `0x03`（该形态在 HC-2 下不可成帧）；**静默路径也不可达**（字节已在 `feed` 内被重同步清掉）。**恢复路径（记 v2.8 候选）**：在**不信任长度域**的前提下补"CRC 校验通过才接受 `9+b[6]`"的双假设定长 —— 可在不违反 HC-2 精神（噪声不得伪造帧长）的情况下恢复该异常面；本版不采（计划未列，且属新功能面） |
| **D-3** | **`crc_err` 门槛取"帧首 == 本站从站地址"（**不含**广播）**，而计划 §2 HC-2 的措辞为"与主站同一语义"、可裁量项 2 的建议基准为"帧首 = 本站地址**或广播地址**" | 主站 `is_expected_shape` 的判据是 `rx[0] == m->cfg.addr`（**无广播特例**）——严格对称即"仅本站地址"；且若把广播（0x00）纳入"同形"，**逐字节重同步的级联会把噪声里的 0x00 字节误计为 `crc_err`**（本机实测：13 字节不自洽帧的级联中出现 `00 03 …` 8 字节窗口 → 误计 1 次），使该计数失去"链路质量"含义 | 取"更严"的一侧，**方向与主站一致**；广播帧的成帧/应答语义**完全不变**（`frames`/执行路径不受影响），仅"广播帧 CRC 坏"由 `crc_err` 改计 `discarded`。已在 API_GUIDE 5.7 口径表写明"**广播帧不计**（它无需应答）" |
| **D-4** | **selftest `log` 套件以"输出字符数"断言修饰面**，而非计划 §3.2 P1-2 所写的"输出经 `port_putc` 捕获断言" | **捕获点不存在于库内**：捕获设施 `port_host_capture_start/stop` 是 **host 专有**（`port/host/port_host.h`）；要在板侧捕获必须给 `port.h` 增设钩子 —— 那是**公开契约变更**（违反 HC-1） | 套件仍覆盖"域宽/补零/左对齐/精度/`%c` 域宽/`j t L` 消费与占位/未知转换字符不消费"的**生效性**（字符数对 host 与板上确定性一致、不依赖 libc 格式化），但**不校字符内容**；内容级断言仍由 `test/test_log.c` 的 host 捕获用例承担（30 例） |
| **D-5** | 板侧 demo 新增两条诊断命令 `AT+MBSLAVE`（从站统计）与 `AT+MBRAW`（上次主站事务收到的原始字节） | P1-4 的对照需要"从站的 `crc_err`/`discarded` 终值"（原 demo 只暴露**主站**统计）；P1-5 的定位需要"主站实际收到的字节" | 均落在**板侧工程私有文件**（`D:\code\STM32CubeMX\G474VET6_ET_TEST\Core\Src\et_demo.c`，**库外、不在本仓 git**，HC-8）；库内 API 零改动；板侧固件 FLASH/RAM 正因此 +392B/+24B（§15.1 已逐值记档） |
| **D-6** | `verify` 侧的**新增用例数**为本版自定：计划 P0-3 要求"≥8 例"，实际 **10 例**；`log` 套件 21 条断言（计划未给数量） | 覆盖矩阵按"评审 E10 形态 + 主从同构对拍 + 边界不回退"三条线自然展开 | 用例 487 → **497**（+10），1K 488 → 498、Tab 494 → 504；九处引用与全部同批引用已回刷（AC-13） |
| **D-7** | **实机记录 §15** 采用"A（v2.6 库）/ B（v2.7 库）双固件"对照法，而非仅"v2.7 单侧走单" | 计划的 AC-15 要求"**修复前/后**对照；修复前数据若因板侧实现无法回退而不可得，须显式声明不可得" —— 本版**主动取得**了修复前数据（把 `Core/et` 回退到 `snap-2.7-start` 即可，成本一次构建+烧录） | 得到**两侧均为板测实得**的 A/B 表（§15.3），AC-15 由"替代证据"升级为**直接证据**；额外发现"两笔写形态被 4ms 静默路径兜住"（§15.3 结论 3），补全了危害触发条件的边界 |
| **D-8** | **本版附加的文档更正（同族漏刷第 5~8 例）**：`docs/getting-started.md` 的用例数（**474** → 497，v2.5 时点值滞留两版）、`docs/API_GUIDE.md` 9.1 的"326 用例（1K 327）"（v1.8 时点值）、`port/*/README.md` 平台表的"291 例"（v1.9 时点值）、`port/stm32g474/README.md` 的"v1.7 库化版 17 套件待上板回填" | 计划 §3.1/§3.2 未列这四处，但同属"数字/叙述漏刷"族（`CO-6`/`CO-7` 的同类），且本版必然改变这些数字 | 属计划**遗漏边界**（非阻断）；已一并回刷，并把 `getting-started` 与 `README` 的用例数纳入 docsync 硬断言（原断言写死 `474`/`20`，**门本身滞后**——已改为按本版终值断言） |
| **D-9** | `docsync.sh` 中有 4 条断言的**断言目标值随版本必然过期**（`README "474"`、`getting-started "474"`、`smoke.sh "SELFTEST: 20/20"`、计划模板项数），本版同批改为 v2.7 终值 | 门以**字面量**断言跨版数字 → 每次数字回刷都要同步改门（否则门红） | 属"门与数字回刷耦合"的既有设计；本版未改变该机制（仅同步取值），**记为治理观察**（v2.8 可考虑改为"从 README/实测取单一来源再断言"，与 v2.6 的跨文档计数门同构） |

**无其它偏差**：库内改动限定在 `protocol/et_modbus.c`（解析边界对称化）、`protocol/et_modbus_master.c`（死代码清理）、`protocol/et_modbus.h`（注释/口径）、`debug/et_selftest.c`（新增两套件）、`debug/et_log.h`（头注限定）、`test/test_modbus.c`、`test/test_selftest.c`、`et_config.h`（版本宏）、`tools/docsync.sh`、`port/stm32f103/renode/smoke.sh` 与文档；**无新增模块、无新增开关、无签名变更**。

---

## 4. commit 范围

**记录范围定义**（HC-5）：本节记录 **`snap-2.7-start`（= v2.6-r2 终态提交，`724d089`）`..HEAD`** 的全部提交；范围终点写为 **`HEAD`**（`tools/docref.sh` 的硬判据：终点必须覆盖到 `HEAD`）。数字以 `git diff --stat snap-2.7-start..HEAD` 的**实测**为准。

```
e7c79f7 v2.7: 从站解析对称化 + selftest 22 套件 + 板上自测收口
```

`git diff --stat snap-2.7-start..HEAD` = **21 files changed, 1288 insertions(+), 104 deletions(-)**

> **修订注记**（2026-09-26，`CO-1(v2.7)` 文本半，随 v2.8 G0-1）：上表原列 `838edb1`、CI run `36157294575` 锚定的 `23c0d3f`，两者均为修订（amend）前的**悬空提交**；范围内实际提交 = `e7c79f7`（= `tag v2.7`）。`git diff --stat 23c0d3f..e7c79f7` 实测 = `1 file changed, 12 insertions(+), 4 deletions(-)`，即该 CI run 与 HEAD 的差异面 = **纯本文档**（§4/§7 数字回填）。

**分支与 tag（HC-9）**：本版库内改动在 **`dev/v2.7`**；**`tag v2.7` 必须在下一版代码合入 `main` 之前定着** —— 本次交付时 `tag v2.7` 状态：**已定着并推送**（指向 v2.7 终态提交；`tag v2.7` → `release.yml` → Release 四附件，见 §7.7）

**CI**：**已核验 —— run `36157294575`（commit `23c0d3f`，main push）七 job 全绿（success）**：`unit-tests (ubuntu-latest)` / `unit-tests (windows-latest)` / `coverage (gcovr)` / `cross stm32f103` / `cross stm32g474` / `docbuild` / `renode stm32f103 smoke`（`gh run view 36157294575 --json jobs` 实测；其中 **renode smoke 断言已随本版更新为 `SELFTEST: 22/22 PASS`**）。其后本地仅有本文档 §4/§7 的**数字回填（纯 `*.md`）**，不改门与代码输入

---

## 5. 未完成项与新发现问题

### 5.1 未完成项（挂账，附理由与触发条件）

| 项 | 计划编号 | 状态 | 理由 / 触发条件 |
|---|---|---|---|
| bench 相对比值化（`CO-15(v2.6)`） | P3-1 | **不做（显式声明）** | 时间盒 0.5 天，计划明示"不阻塞发布"；本版优先保障 `CO-5` 板上对照（D-7）与 selftest 收口。**触发 = 出现"需要跨版性能回归判别力"的实际诉求**；再记 v2.8（§8.3） |
| `CO-11(v2.5)` USART2 独立口 / 共享口地址不符静默的**板侧验证** | Non-goals | **再次暂缓（重新声明）** | 硬件阻塞：bench 仅一路 USB-TTL；触发 = 第二路 USB-TTL 或双板对连 |
| `snap-2.7-start` 远端推送 | §6 基线 | ✅ **已完成**（重试后成功） | 见 §3 D-1；远端 = 本地 = `724d089`，`gh api` 核验；无副作用由探针 tag 实测（不产生 run / 不产生 Release） |
| 板侧 demo 的 `\n` 残留清理 | `CO-8` 附带 | **不做（保留证据原样）** | §15.4 已定位并给一行级修法；本次不改为**保留复现证据**（改后 `disc` 归 0 将无法再演示该现象）。记 v2.8 板侧待办（库外） |

### 5.2 新发现问题（供评审与下一版计划使用）

| # | 问题 | 严重度 | 建议处置 |
|---|---|---|---|
| **N-1** | **`CO-8` 已闭环**：板上"正常读恒有 `disc=1`"的**来源 = 命令行 `\n` 残留**（`AT+MBRAW` 机械定位，3/3 复现；仅 CR/仅 LF 结束时 `disc=0`）。排除清单：UART 错误（`ore=0` 全程）、TX 回环（§14.2 对照 + 首字节恒 0x0A）、物理层噪声（取值不随机）。**性质 = 板侧 demo 的"shell 行解析 ↔ 阻塞式主站事务"接口残留（库外私有代码），非库缺陷** | **已闭环（低）** | 修法（板侧一行级）：进入 `mbm_run` 前清空 `rx_rb` 命令行残余。**记 v2.8 板侧待办** |
| **N-2** | **`item_lines` 的"抗行尾"依赖 GNU sed 的隐式行为**：`--diff`/`--snapshot` 走 `item_lines`（`sed -n 's/^- \`\(.*\)\`$/\1/p'`），其在 CRLF 文件上仍能匹配，是因为 Cygwin/MSYS 版 GNU sed **读入时剥离行尾 CR** —— 该行为**无标准保证**；若换成不剥离的实现，`item_lines` 会静默返回 0 条 → `--diff` 把全部基线条目报成"修改/删除" → 冻结面门**误红** | 中 | v2.8 给 `item_lines` 加显式 `tr -d '\r'`（一行，零风险）：把"抗行尾"从副作用升级为契约。本版未改（`--diff` 现为绿，且不属 G0′/v2.7 范围） |
| **N-3** | **`apidump.sh` 的 `TMPD_REL` 回退是静默的**：Windows 分支 `mkdir -p "$TMPD_REL"` 失败即退回 `mktemp -d`（回到 v2.6 修掉的"Windows 路径 → gawk 转义 → 提取为空"陷阱）。**本次实测**：在 `git worktree`（v2.5 树）里用**v2.5 时代的** apidump.sh 生成 → 条目 **0**（清单只剩分节标题，77 行）；换用当前工具即 440 条目 | 中 | ① 回退时**打印显式告警**（便于诊断）；② 建议给无参数生成加"条目数 < 归类基线一半即拒绝写入"的守卫（v2.6 D-6 遗留建议，仍未做）。另：**D-7 的 worktree 复核必须用当前工具**（已在 AC-19 记录） |
| **N-4** | **从站"读帧无长度域"的固有边界**：读类请求定长 8，从站无先验期望数，故"噪声恰好伪造 `[addr][读 fc]`"的 8 字节窗口在 CRC 失败时仍按**整帧**丢弃（可能吃掉紧随真请求的前缀）。这是主站侧 v2.6 已知边界（"须同时伪造 addr+fc+bc"）在从站侧的**弱化版**（只需伪造 2 字节） | 中 | 已在 API_GUIDE 5.7 写明判据；若需进一步收敛，可评估"读帧 CRC 失败改逐字节重同步"的取舍（代价：可能把坏帧残字节重组成假帧 → 对**从站**而言假帧=误写，风险更高，故本版维持整帧丢弃）。记 v2.8 评估项 |
| **N-5** | **自测固件的 RAM 增项**：`ET_MODULE_SELFTEST=1` 构建的 `.bss` 由 2232 → **3016**（+784，全部来自 `modbus` 套件的主站双缓冲 512B + 从站缓冲 + 标量）。发布配置（`ET_MODULE_SELFTEST=0`）不受影响 | 低 | 已在 §15.1 与体积表逐值记档；F103（20K RAM）仍余裕充足。若后续继续扩套件，建议先评估"主站缓冲复用"（计划可裁量项 4 的另一分支） |

---

## 6. 遗留风险

| 风险 | 状态 | 说明 |
|---|---|---|
| **`CO-8` 的 `disc=1`** | **已闭环（来源定位 + 双节登记）** | 来源 = 命令行 `\n` 残留（板侧私有接口），非库缺陷；**交付文档 §5.2（N-1）与本节（下表行）双处登记**，修法与触发条件在位 |
| `CO-5` 修复改变既有从站用例语义（`bc`/`qty` 不自洽不再回异常 0x03） | **已登记为行为修正** | §3 D-2：HC-2 与旧断言互斥，按计划附 B 通道处置；能力损失与**恢复路径（CRC 校验后的双假设定长）**已写明，记 v2.8 候选 |
| 从站侧"严格同形"判据取"仅本站地址"（不含广播） | **已登记（§3 D-3）** | 与主站严格对称；广播帧的成帧/执行语义不受影响，仅 CRC 坏的广播帧由 `crc_err` 改计 `discarded` |
| 自测覆盖仍非"对等"（host-only 用例不移植） | **接受（既有边界）** | API_GUIDE 8.4 明示"冒烟非对等 host 497 用例"；新增两套件覆盖的是**解析面与修饰面的生效性**，深度用例仍在 host 侧（33 + 30 例） |
| 门与数字回刷耦合（`docsync` 以字面量断言跨版数字） | **已观察（§3 D-9）** | 每次数字回刷必须同步改门；本版按现状执行，v2.8 可考虑改"单一来源取值"式断言 |
| `snap-2.7-start` 远端 tag | ✅ **已推送**（重试后成功） | 见 §3 D-1；两端 SHA 逐值一致；该 tag 不触发任何 workflow（`snap-*` 不匹配 `tags: [v*]`） |
| 本机 ASan 不可执行 | **如实声明（未变）** | 无 WSL/MSYS2；`make test-asan` / `test-asan-1k` 由 Linux CI 常设执行，**未声称本机已跑** |

---

## 7. 提交后回填（门读数 / 对照复跑方法 / 对拍表 / 回刷清单）

### 7.1 四门读数（本版终态）

| 门 | 命令 | 结果 |
|---|---|---|
| 文档同步 | `sh tools/docsync.sh` | **`pass=304 fail=0`**（exit 0） |
| 冻结面 | `sh tools/apidump.sh --check` / `--diff` | check **一致（exit 0）**；diff 对 v2.5 基线 ****（v2.6 归档缺失，见 `CO-2(v2.7)`）新增 0 项 / 修改删除 0 项**（exit 0；耗时 410 s）** |
| 体积 | `sh tools/sizecheck.sh` | **pass=3 fail=0**（23884/24/740、36340/24/3016、24260/28/740） |
| §4 自指 | `sh tools/docref.sh` | **OK**（`21 files changed, 1288 insertions(+), 104 deletions(-)` 与本文 §4 声明逐值一致） |

### 7.2 全量回归读数

| 项 | 命令 | 结果 |
|---|---|---|
| 单元（默认 / G4） | `mingw32-make test` / `test-g4` | **497** ALL PASS（fail=0） |
| Tab 形态 | `mingw32-make test-tab` | **504** ALL PASS |
| 1K 变体 | `mingw32-make test-1k` | **498** ALL PASS |
| 零警告门 | `mingw32-make WERROR=1 test` / `WERROR=1 ex` | ALL PASS；编译诊断（`\.c:行:列: warning\|error`）命中 **0** |
| 配方载体 | `mingw32-make ex` | **五例 PASS**（`ex_pid_loop` / `ex_kv_backup` / `ex_upgrade_flow` / `ex_modbus_slave` / `ex_modbus_master`） |
| 覆盖率 | `make coverage-build` + `gcovr` | **行 **97.2%**（10575/10877）、函数 **99.3%**（1066/1074）、分支 57.4%（5612/9780）**（CI 侧同 gate ≥85%） |
| 门内自测套件 | `./build/et_tests.exe` | `proto/modbus (33) pass=33 fail=0`、`debug/selftest (9) pass=9 fail=0`；`et_selftest_suite_count()==22` |
| docbuild | `sh tools/docbuild.sh` | **all port README commands executed clean**（0 warning，161 s） |
| 冻结面清单重生成 | `sh tools/apidump.sh`（无参数，版本宏 6→7） | 440 条目 / 23200 B；与 HEAD 版 **仅 1 行差异**（`ET_VERSION_MINOR 6` → `7`；`git diff --stat` = 1 insertion / 1 deletion），341 s |
| 板上 | COM12（B₁ 固件） | `SELFTEST 22/22 PASS` / `STORAGE SELFTEST PASS` / `slot 1 CONFIRMED` / 从站分流复跑 PASS（实机记录 §15.2） |
| 模块数 | `ls -d core algorithm sys protocol drivers debug storage` 计数 | 维持 **34** |

### 7.3 AC-8 的"修复前为红"复跑方法（供评审方独立复现）

```sh
# ① host 侧: 仅回退从站实现文件 → 既有新用例应红(不采信本文贴出的输出)
git show snap-2.7-start:protocol/et_modbus.c > protocol/et_modbus.c
sha256sum protocol/et_modbus.c                      # 与 snap-2.7-start 版逐值相同
mingw32-make test 2>&1 | grep -E "proto/modbus"     # 实测: pass=25 fail=8 (CO-3 更正: 原预期值误写, 见评审记录 CO-3;
#   新增 10 例中 mb.unknown_fc_unicast_retained / mb.wr_truncated_then_completed
#   两例基线即绿(回归钉属性, 零判别力), 实红 8 例; debug/selftest 另各有 1 例连锁红)
# ② 按 sha256 回退到本版 → 复绿
git checkout -- protocol/et_modbus.c && mingw32-make test | grep -E "proto/modbus"   # pass=33 fail=0
# ③ 板上对照(实机记录 §15.1/§15.3): A/B 两固件的构建与烧录命令
#   A: Core/et ← snap-2.7-start 的七目录 + et_config.h + port.h → cmake --build --preset Debug → STM32_Programmer_CLI -c port=SWD -w … -v -rst
#   B: Core/et ← 本版工作树(同上)                        → 同上
#   注入: python build/board_c05_probe.py COM12
```

### 7.4 AC-10 主从同构对拍表

| 噪声/边界形态 | 主站用例（`test/test_modbus_master.c`） | 从站用例（`test/test_modbus.c`） | 两侧语义一致项 |
|---|---|---|---|
| 字节数域与期望不符（**不伪造长度**） | `mbm.noise_oversize_bc`（`[addr][0x03][0xFF][0x01]`：`bc` 远超 `2*exp_qty`） | `mb.noise_fake_len_then_read` / `mb.noise_fake_len_then_write`（`11 10 00 00 00 01 04 …`：`b[6]` 不符 `2*qty`） | ① 不据此长度前进；② 不进 `crc_err`；③ 紧随的真帧被正确解析 |
| "从未成帧"的垃圾序列 | `mbm.garbage_resync` | `mb.noise_never_frames` | 逐字节重同步；`crc_err` 不增长；`discarded` 递增 |
| 异站地址开头 | `mbm.addr_mismatch_discarded`（合法帧） | `mb.noise_foreign_head_resync`（噪声头）+ `mb.addr_mismatch_silent`（合法帧） | CRC 通过且地址不符 → `addr_mismatch`；CRC 不过 → `discarded` |
| 与期望严格同形但 CRC 坏 | `mbm.noise_expected_shape`（`crc_err` == 1） | `mb.crc_bad_silent`（`crc_err` == 1；`mb_crc_bad_foreign_not_crc_err` 为负样本） | `crc_err` 门槛 = 地址 + 功能码（从站侧口径见 §3 D-3） |
| 坏帧 + 真帧同批 | `mbm.noise_qty_max_resync` | `mb.sticky_bad_then_good`（单次 feed） | 真帧仍被解析，`crc_err` 不污染 |

**差异面（如实写明，非不一致）**：主站**有先验期望寄存器数**，故读应答可"定长前拒绝"（`rx[2] != 2*exp_qty` 即重同步）；从站**无先验期望数**，读请求定长 8，只能靠 CRC 定性（见 N-4）。两侧在"噪声不得伪造长度/不得污染 `crc_err`/真帧必须被解析"三点上**语义一致**。

### 7.5 AC-13 九处回刷逐行清单（旧 → 新）

| # | 位置 | 旧 | 新 |
|---|---|---|---|
| 1 | `debug/et_selftest.c` 文件头套件计数注释 | `… + v1.4~v1.6 新模块补齐(4):`（列 20 套件） | `… + v2.7 modbus/log 解析面与修饰面冒烟(2, CO-14(v2.6) 关闭 CO-10(v2.5)) = **22**:`（并补 `g_suites[]` 两条目） |
| 2 | `test/test_selftest.c:98` | `ET_CHECK_U32_EQ(20u, base);` | `ET_CHECK_U32_EQ(22u, base);` |
| 3 | `test/test_selftest.c:151`（注释） | `/* 20 内建 + host.extra = 21 */` | `/* 22 内建 + host.extra = 23 */` |
| 4 | `test/test_selftest.c:152`（**计划未列，同批必改**） | `ET_CHECK_U32_EQ(21u, total);` | `ET_CHECK_U32_EQ(23u, total);` |
| 5 | `port/stm32f103/renode/smoke.sh:97` | `grep -q "SELFTEST: 20/20 PASS"` | `grep -q "SELFTEST: 22/22 PASS"`（并改其上注释为"v2.7 起 22 套件: 20 PASS + 2 SKIP"） |
| 6 | `README.md:46`（特性表） | `…v2.2 扩至 20 内建套件…` | `…v2.2 扩至 20，**v2.7 扩至 22 内建套件**…` |
| 7 | `README.md:188`（质量门） | `20 套件一条命令冒烟（… v2.2 起 pid/stats/bytes 进板上冒烟）` | `**22** 套件一条命令冒烟（… v2.2 起 pid/stats/bytes、v2.7 起 modbus/log 进板上冒烟）` |
| 8 | `README.md:193`（CI 门控行） | `selftest 20/20` | `selftest 22/22` |
| 9 | `docs/API_GUIDE.md`（8.4 套件行 + 9.1 平台行） | `**20 内建套件**（v2.2 起 +pid/stats/bytes）` / `✅ 326 用例（1K 变体 327）` | `**22 内建套件**（v2.2 起 +pid/stats/bytes，v2.7 起 +modbus/log）` / `✅ 497 用例（1K 变体 498）` |
| 10 | `CHANGELOG.md` | 无 `[2.7.0]` 节 | 新增 `## [2.7.0]` 全节（Added/Changed/Fixed/挂账） |

**同批扩展面（HC-7 的"其它引用"）**：`README.md`（结构树用例数 487→497、modbus 23→33、质量门 487→497/1K 488→498/Tab 写明 504、平台段 selftest 20→22 套件）、`docs/architecture.md`（套件 20→22 ×3 处、`selftest 20/20`→22/22、用例 487→497、"连续十六版"→"连续十七版"、机制门行 docsync 计数 301→**304**）、`docs/getting-started.md`（474→497）、`docs/v3-candidates.md`（新增「v2.7 复评记录」节）、`port/stm32f103/README.md` + `port/stm32g474/README.md`（体积表新增 v2.7 行 ×3 + 平台表 291→497 + g474 板上自测行）、`移植stm32实机记录.md`（新增 §15）、`tools/docsync.sh`（4 条断言取值同步）、`docs/API_INVENTORY.md`（**版本宏 6→7 重生成** —— 由 `apidump --check` 守护，未重生成即红）。

### 7.6 机制自证表（门须可红，逐项均已"先红后绿"）

| 门 | 手法 | 结果 |
|---|---|---|
| `apidump --check`（AC-1） | 注入公开声明 → 红；sha256 回退 → 绿 | 红：exit 1（`### 函数声明 (6)`→`(7)`）；绿：exit 0 |
| `docref` 终点覆盖 HEAD（AC-7） | 终点改固定 SHA → OK；再追加纯文档提交 → 红 | 红：`记录终点未覆盖 HEAD …`（exit 1）；回退后 OK |
| docsync 同族漏刷门（AC-5） | 基线名改回 `_v2.4.md` → 红 | 红：`FAIL README 冻结基线名 […] ≠ apidump 默认基线 […]`（exit 1）；还原后绿 |
| docsync 跨文档计数（沿用 v2.6） | `architecture.md` 的 `五例` → `四例` | 红：`跨文档 examples 数不一致`（v2.6 已独立复现） |
| **门随数字回刷而红（本版实测，D-9/N-3 的直接证据）** | 本版首次执行 `docsync` 时，因门内 4 条断言仍写 v2.6 终值（`README "474"` / `getting-started "474"` / `smoke "SELFTEST: 20/20"` 等）而红；同批改正后转绿 | 红 → 绿（该次红是**门按设计工作**的证据，非缺陷） |
| docbuild（文档命令可执行化） | 直接执行两份 port README 的 ```docbuild 块 | **all port README commands executed clean**（0 warning） |

### 7.7 发布物（tag 与 Release）

| 项 | 值 |
|---|---|
| `tag v2.7` | 指向本文档所在提交（范围终点 `HEAD`）；**在下一版代码合入 `main` 之前定着**（HC-9） |
| Release | 由 `release.yml` 从 `CHANGELOG.md` 的 `## [2.7.0]` 节提取正文发布（四附件：`stm32f103_demo.elf` / `.bin` / `demo.exe` / `SHA256SUMS`） |
| `snap-2.7-start` | 远端与本地同为 `724d089`（v2.6-r2 终态 = 本版代码窗口起点，HC-4，见 §3 D-1） |

---

## 8. CO 处置与候选池一致性

> 本节为 **HC-11 的第三落点**（缺本节即不合格）。三处落点 = ① 计划 §1.1 / ② [`docs/v3-candidates.md`](docs/v3-candidates.md)「v2.7 复评记录」节 / ③ **本节**；编号一律带版本后缀。

### 8.1 三处措辞并置对账

| CO | ① 计划 §1.1 处置 | ② `v3-candidates.md` 复评 | ③ 本节 | 一致 |
|---|---|---|---|---|
| `CO-1(v2.6)` | 纳入前置门 G0′-1 | v2.6-r2 已处置（行尾归一化） | ✅ 已处置（AC-1） | ✅ |
| `CO-2(v2.6)` | 纳入 G0′-2 | v2.6-r2 已处置（CHANGELOG + Release 重注） | ✅ 已处置（AC-3） | ✅ |
| `CO-3(v2.6)` | 纳入 G0′-3 | v2.6-r2 已处置（终态统一） | ✅ 已处置（AC-4） | ✅ |
| `CO-4(v2.6)` | 纳入 G0′-6 | v2.6-r2 已处置（终点须覆盖 HEAD） | ✅ 已处置（AC-7） | ✅ |
| `CO-5(v2.6)` | **纳入本版（P0 ★ 核心）** | 已修（P0-1/P0-2 + 板上对照） | ✅ 已修（AC-8/AC-9；§15.3 A/B） | ✅ |
| `CO-6(v2.6)` | 纳入 G0′-4 | v2.6-r2 已处置 + 装门 | ✅ 已处置（AC-5） | ✅ |
| `CO-7(v2.6)` | 纳入 G0′-4 | v2.6-r2 已处置 + 装门 | ✅ 已处置（AC-5） | ✅ |
| `CO-8(v2.6)` | **纳入本版（P1-5）** | 已定位（命令行 `\n` 残留）+ §5.2/§6 登记 | ✅ 已闭环（AC-16；§15.4） | ✅ |
| `CO-9(v2.6)` | **纳入本版（P2-1）** | 已修（死代码清理，行为零变化） | ✅ 已修（AC-17） | ✅ |
| `CO-10(v2.6)` | **纳入本版（P2-2，采"文档限定"分支）** | 已处置（文档限定） | ✅ 已处置（AC-18） | ✅ |
| `CO-11(v2.6)` | 纳入 G0′-5 | v2.6-r2 已处置（模板五项 + §12 标注） | ✅ 已处置（AC-6） | ✅ |
| `CO-12(v2.6)` | 纳入 G0′-4 | v2.6-r2 已处置（AC-18 表述） | ✅ 已处置（AC-5） | ✅ |
| `CO-13(v2.6)` | 纳入 G0′-1（同批重测） | v2.6-r2 已处置（4m24s/4m18s/4m25s） | ✅ 已处置（AC-2） | ✅ |
| `CO-14(v2.6)` | **纳入本版（P1-1…P1-4，必办）** | 已做；**套件数裁定 20→22**（非 21，见下） | ✅ 已做（AC-12/13/14） | ✅ |
| `CO-15(v2.6)` | **纳入本版（P3，可选）** | **不做 + 显式声明 + 再记 v2.8** | ✅ 同措辞（§8.3） | ✅ |
| `CO-9(v2.5)` | 维持 v3 窗口 | 维持 v3 窗口 | ✅ 未动 | ✅ |
| `CO-11(v2.5)` | 再次暂缓（触发条件） | 再次暂缓（重新声明） | ✅ 同措辞（§5.1） | ✅ |

**差集：空。** 15 条 `CO-n(v2.6)` 全部有落点；无"计划纳入 vs 实际未做"的静默丢失。

### 8.2 `CO-14` 的套件数裁定（20 → **22**，相对评审建议的 21 属"更严"）

评审 §6.2 第 2 条写"selftest 20→21（modbus 主从 + `et_log` 修饰冒烟），五处文档回刷"，而目标数字 21 与"两类内容"自相矛盾。计划 §1.1 裁定为 **22**（新增 `modbus`、`log` **两个**套件），理由：套件是 `et_selftest_report_fn` 回调的**失败归因单元**（回调携带 `suite` 字段），把分属两个模块的断言合并进一个套件会让 `ET_SELFTEST_CHECK_FAIL` 无法区分归属，削弱该回调的设计价值；且与 `test/test_selftest.c` 逐套件断言（`ev_find(..., "crc")` 等）的既有用法一致。**本版按 22 执行**，并在计划 §1.1 / `v3-candidates.md` / 本节**三处并置声明**，供下一轮评审裁决。

### 8.3 `CO-15`（bench 相对比值化）：本版**不做**的显式声明

- **不做**，理由：① 计划 §3.4 明确"时间盒 0.5 天，**不阻塞发布**"；② 本版资源优先投向 `CO-5` 的**板上 A/B 对照**（`CO-14` 必办 + `CO-5` 板上注入）与 selftest 收口 —— 二者是本版"只有板上能证伪"的证据面。
- 现状不变：`docs/bench.md` 顶部的**口径声明**仍生效（"仅供量级参考、不作回归判据"，附 v2.5 同机差幅 59% 的实测依据）。
- **再记 v2.8**（与计划 §3.4 的处置口径一致），触发条件 = 出现"需要跨版性能回归判别力"的实际诉求（例如某次优化需要用性能数字判定是否回退）。

### 8.4 候选池措辞（与 `docs/v3-candidates.md` 一致）

- **`CO-9(v2.5)` `data()` → `read_into()`**：维持 v3 窗口（触碰既有签名）。
- **`CO-11(v2.5)` USART2 独立口 / 共享口地址不符静默的板侧验证**：再次暂缓，触发 = 第二路 USB-TTL 或双板对连。
- **biquad（#8）**：维持排队，触发 = 用户提交具体频响指标。
- **32 位寄存器组合库级封装**：维持拒绝（库级）。
- **v2.7 新增破坏性候选：无**（本版全部改动为内部实现与 selftest 内部新增 + 治理/文档；`apidump --diff` = 0/0）。

---

> 交付纪律：本交付中每条"通过"均有本文所列命令可复现；**未能核验者一律不按通过计** —— 本版**无**此类项：四门、全量回归、板上走单与 A/B 对照、`CO-8` 定位均为**本版实得**（板侧同批会话见实机记录 §15）。
> 本版唯一"显式不做"者为 `CO-15`/P3-1（§8.3，已声明 + 记 v2.8）与板侧 `\n` 残留清理（§5.1，保留证据原样）。
