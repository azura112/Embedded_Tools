# v1.4 开发交付：命令交互与传输生态

> 交付日期：2026-09-03 ｜ 版本：v1.4.0（`ET_VERSION = 0x010400`）｜ 基线：v1.3（`5d4d82b`）
> 计划文档：[v1.4 开发计划：命令交互与传输生态](./v1.4开发计划：命令交互与传输生态.md)（状态：✅ 功能交付完成）
> ✅ **tag v1.4 已补打并推送**：账号侧就绪后 P0 销账——远程 `azura112/Embedded_Tools` 建立，CI 首跑 3 连绿（`4e98a2b`/`d13bba2`/`6958578`，Renode 门在内 5/5 job），release.yml verify→release 实跑通过，GitHub Release v1.4 附件齐全（ELF/BIN/demo.exe），见 §6。

## 1. 重点概况（TL;DR）

v1.4 把库从"组件齐"推向"**设备可交互、固件可更新**"：

- **新模块 et_xmodem**（protocol/，第 20 模块）：XMODEM-CRC 接收器，bootloader 拉取固件镜像的传输协议。逐字节喂入返回应答动作、CRC 复用 `et_crc16_ccitt_update`（流式种子 0x0000，零新增 CRC 变体）、超时由调用方注入 `now`（host 虚拟时间全速测试）、`ET_XM_1K=1` 可选 1024B 大块。**17 例对端行为矩阵全绿**（1K 开关双变体均过）。
- **新模块 et_shell**（debug/，第 21 模块）：et_atcmd 之上的薄交互壳——回显/退格擦写（`\b \x1b[K`）/提示符/help 自动生成（命令表追加 help 字段，只增不改旧语义）。**15 例全绿**。
- **增量 API et_kv_iter**（v1.4）：只读枚举活跃页有效 key（诊断导出/批量读取），快照语义 + 去重 + tombstone 跳过。**7 例全绿**。
- **P0 一致性收尾**：README 补齐、发布 checklist 强化（新增"量化声明附复现命令 + 环境注记"）、版本宏 1.4.0、port README 体积表注明测量环境；`exp/kv` 分支已删。**远程建立与 CI 实跑因账号侧阻断挂起**（见 §6）——tag 按计划 DoD #5 暂缓。

## 2. 逐项验收对照

### P0 工程收尾（外部依赖部分挂起）

| # | 任务 | 验收结果 |
|---|---|---|
| 1 | 远程仓库 + CI 实跑 | ✅ **已销账**：账号侧就绪后建远程 `https://github.com/azura112/Embedded_Tools`（公开），main + v1.0~v1.4 全推送；**CI 首跑经历 3 轮修复全绿**（/opt 权限→$HOME、linux 包可执行名 `renode` 无 .exe、擦除态填充被 `*.bin` 规则挡住改为 smoke.sh 现场生成），**3 连绿 run：`4e98a2b`/`d13bba2`/`6958578`**（unit 矩阵×2 + coverage 门 + ARM 交叉 + Renode smoke 全过）；README/checklist/1.4.0/exp/kv 删除同批完成 |
| 2 | release.yml 验证 | ✅ **已销账**：v1.4 tag 触发两段式（verify=全量测试+Renode smoke → release），run 结论 success，GitHub Release v1.4 附件 `stm32f103_demo.elf` / `stm32f103_demo.bin` / `demo.exe` 齐全 |
| 3 | v1.3 验收保留项补正 | ✅ port README ARM 体积条目注明测量环境（GNU Tools for STM32 13.3.rel1 + Git Bash + `arm-none-eabi-size`），v1.1~v1.4 逐版本列行 |
| 4 | 版本宏 1.4.0 | ✅ `ET_VERSION=0x010400`，与计划 tag 名对齐 |

### P1 et_xmodem（M2，`7e5384f`）

- **API**：`et_xmodem_rx_init / et_xmodem_rx / et_xmodem_rx_tick`，sink 回调出口数据。
- **状态机**：SOH/STX 分块、块号+补码校验、序号回绕（255→0→1）、CRC 坏 NAK 重传、重复块 ACK 不重复 sink、EOT 两段确认、CAN×2 中止、10s 静默会话复位重臂、失序致命、sink 拒绝中止。
- **测试**：17 例对端行为矩阵（host 虚拟时间驱动），ET_XM_1K=0/1 双变体均 ALL PASS；`test/test_xmodem.c` 头注载明矩阵清单（DoD #3）。
- **bootloader 数据通路图**已入 API_GUIDE §5.4 与头注。

### P2-1 et_shell（M3，`1e0e27d`）

- **定位**：atcmd 之上的薄交互层，不做解析重复；行编辑语义由底层 atcmd 决定，shell 只决定回显什么。
- **功能**：输入回显 + 退格擦写（`\b \x1b[K`，可关）+ 提示符（可定制可关）+ help 自动生成（扫描命令表，help 字段 NULL 只出名称）。
- **兼容性**：atcmd 命令表尾部追加 `help` 字段，位置初始化器仍兼容（C99）；atcmd 仍可独立使用。
- **测试**：15 例（回显开关/CRLF 对/退格擦写/行首边界/超长行/help 逐行比对/help 经命令/提示符自动补打与隐藏/未知命令与 args 透传/控制字节不回显）。

### P2-2 et_kv_iter（M3，`c59b3bc`）

- **API**：`et_kv_iter_init(kv, &it)` / `et_kv_iter_next(kv, &it, &key, &len)`，只读、不触碰页状态。
- **语义**：快照（init 固定活跃页，迭代期间压实换页不影响快照页可读性）；只出 CRC 有效 + 非 tombstone + 无更新版本（去重）的记录。
- **测试**：7 例（多 key 完整性/tombstone 跳过/多版本最新/空库与参数边界/迭代中 set 追加可见/跨 commit 快照/坏 CRC 记录跳过）。

### P3 可选项

| # | 任务 | 结果 |
|---|---|---|
| 1 | `ET_CRC_TABLE=1` 查表优化 | ✅ 落地（`316c85f`）：CRC16-CCITT 256 项静态表驻只读段，`ET_CRC_TABLE_SECTION` 可指定段；双变体回归全绿，xmodem 直接受益 |
| 2 | API_GUIDE 配方 11.6 | ✅ 落地：断言故障现场落 kv（et_assert 钩子 × et_kv），debug×storage 协同 |
| 3 | demo 串联 | 🟡 经评估列为 v1.5 候选（计划已记录变更） |

## 3. 质量数据（每项附复现命令 + 环境注记，DoD #2）

| 项 | 数值 | 复现命令 / 环境注记 |
|---|---|---|
| 模块 | 21（+protocol/et_xmodem、debug/et_shell） | `ls protocol/ debug/` |
| 主机单测 | **245 例 ALL PASS** | `mingw32-make test`（MinGW gcc 16.1，Git Bash） |
| 1K 双变体 | xmodem 套件双变体均 ALL PASS | `gcc -DET_XM_1K=1 ...`（命令见 v1.4 计划与 smoke 脚本） |
| CRC 双变体 | 位/表两路径均 ALL PASS | `gcc -DET_CRC_TABLE=1 ...` |
| 行覆盖率 | **97.1%**（4841/4988），函数 99.4%，门槛 85% | `gcovr --print-summary`（MinGW gcc 16.1 + `--coverage`） |
| ARM 编译 | 零警告，`text=12276 data=4 bss=276`（Flash 17%） | GNU Tools for STM32 13.3.rel1，`-Wall -Wextra -pedantic -Os -g` |
| Renode smoke | 5/5 断言通过（本机 1.16.1） | `sh port/stm32f103/renode/smoke.sh ...` |
| 内存 | 依旧零动态分配 | `grep -rn malloc core/ algorithm/ sys/ protocol/ storage/ drivers/ debug/` 无命中 |

## 4. 文档同步

- `docs/API_GUIDE.md`：新增 §5.4 et_xmodem（协议表/API/bootloader 通路图）、§8.3 et_shell（API/示例）、§6.1 末尾 iter 说明、配方 11.6（故障现场落 kv）、配置表（新开关 `ET_MODULE_XMODEM/SHELL`、`ET_XM_1K`、`ET_CRC_TABLE`、版本 1.4.0）、平台表 245 用例。
- `README.md`：v1.4.0 版本行、特性表两行（xmodem/shell）、245 用例、checklist 强化条目（DoD #2 环境注记）。
- `port/stm32f103/README.md`：ARM 体积表注明测量环境并加 v1.4 行；平台表 245 用例。
- 计划文档状态行与里程碑结果已归档（tag 暂缓说明写入）。

## 5. 开发中发现并修复的问题（超出原计划范围）

1. **【严重】XMODEM 线级字节与枚举影子冲突**（`et_xmodem.c`）：`ch == ET_XM_CAN` 误用枚举值（2）而非线级字节 0x18，导致 payload 中值为 2 的字节被当成"取消"——对端行为矩阵当场揪出。修复：块内一切字节皆数据（仅连续 CAN×2 才中止），块边界清 can_cnt 防跨块误连击。
2. **测试侧修复**：补码错用例的线级长度写成 `4+plen`（应为 5）；feed 末字节断言改为按首字节断言（feed 串的"末动作"语义）；1K 使能时容量契约（cap≥1028）在 init 即终态。
3. **shell 测试**：退格/控制字节与 atcmd 行语义联动（控制字节会进行缓冲，先回车冲掉）。

## 6. 范围外事项 / 挂账（账号侧外部依赖）

| 项 | 状态 | 去向 |
|---|---|---|
| ~~建立 git remote + CI 首跑~~ | ✅ **已销账**（2026-09-05）：`azura112/Embedded_Tools` + CI 3 连绿 + tag v1.4 补打 | — |
| ~~release.yml 实跑验证~~ | ✅ **已销账**：Release v1.4 两段式 success + 3 附件 | — |
| F103 真机实测 | ⏳ 常设挂账（不阻塞发布） | 硬件到位后按 checklist 补录 |
| demo 串联（shell + xmodem 演示） | 🟡 v1.5 候选 | 时间盒外 |
| 磨损均衡 v2 / 加密 / YMODEM-ZMODEM | Non-goal | — |

## 7. 提交与版本

| 里程碑 | 提交 | 说明 |
|---|---|---|
| M2 | `7e5384f` | et_xmodem 接收器 + 17 例对端行为矩阵（双变体） |
| M3 | `1e0e27d` / `c59b3bc` | et_shell 15 例 + kv_iter 7 例 |
| M4 | `316c85f` / `f2342fe` | CRC 查表 / 文档同步 + 计划归档 |
| M5 | 本文本 / `git tag v1.4` | 回归 245 全绿 + 交付文档 + **tag v1.4 已推送，Release 两段式实跑通过** |

版本钉：`et_config.h` → 1.4.0 / `0x010400`；tag **v1.4**（三方一致：宏/tag/交付文档版本行）。
