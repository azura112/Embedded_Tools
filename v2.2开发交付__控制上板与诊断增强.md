# v2.2 开发交付：控制上板与诊断增强

> 交付日期：2026-09-12 ｜ 版本：v2.2.0（`ET_VERSION = 0x020200`，冻结下纯增量 MINOR）｜ 基线：v2.1（tag `v2.1` → `410b96b`，Release 已发布）
> 计划文档：[v2.2 开发计划__控制上板与诊断增强](./v2.2开发计划__控制上板与诊断增强.md)（状态：✅ 已交付归档）
> 远程：https://github.com/azura112/Embedded_Tools（**本工作区尚未提交/打 tag**；`v2.2` tag 由发布 checklist 核对后执行）
> 本版 docsync：**210/210**（`sh tools/docsync.sh` 实测,含本自指断言;机制与自证见 §7）

## 1. 重点概况（TL;DR）

v2.2 的主题一句话：**让 v2.1 的控制链路真正上板**。pid/stats/bytes 三个模块从"PC 全测、板上被 GC"的零足迹状态，变成板上 `AT+SELFTEST` 可见可跑的 20 套件之一，并在真机上用 shell 命令组跑通了"测量→滤波→控制→输出"的完整闭环——三组参数的响应摘要与 host 定点仿真**逐值一致**。算法面补上中值滤波（尖峰去除）与调度可观测性（任务耗时统计）两块拼图；治理面完成 apidump 基线首次滚动，并让"结构体字段追加须人工登记"的冻结附则完成了第一次实战（`et_task_t`）。全程 `apidump --diff` 对 v2.1 基线 **新增 10 项 / 修改删除 0 项**，零破坏性变更。

- **P0 治理小件与基线滚动**：`--snapshot docs/API_INVENTORY_v2.1.md` 固化新基线（374 项比较单元），`--diff` 默认基线切至"最近已发布 MINOR"（滚动规则入 API_STABILITY §5），v2.0 归档只读保留；checklist 第 5 条交付文档命名放宽为"全角冒号或 `__` 均可"（v2.1 遗留①，docsync 无冒号硬编码无需放宽）；版本宏 2.2.0。
- **P1 selftest 17→20（★ 数字回刷全链）**：pid（固定增益阶跃 5 步 + 输出钳位）、stats（10 样本 min/max/mean/方差对拍）、bytes（BE/LE 往返 + 越界拒绝）三套件进 `et_selftest`（`#if ET_MODULE_*` 门控，随模块开关自动增减）。回刷清单逐处核对（DoD #2，见 §4）；板上 `AT+SELFTEST` **20/20 PASS** 实证，smoke.sh 断言同步 `20/20`（Renode 以 CI run 为准，本机无 Renode，沿既有政策）。
- **P2 板上闭环 demo（★ 真机走单）**：CubeMX 板侧 `et_demo.c` 新增 `AT+PIDSET <sp> <kp> <ki> <kd> [k] [imax]` / `AT+PIDRUN <ms>` / `AT+PIDOUT` 命令组，模拟被控对象 `y += k(u−y)`（10ms 节拍，文档明示无真实被控对象）。三组参数真机走单：**A 保守**（peak=496/ov 0%/settle 1110ms）、**B 激进**（peak=610/ov 22%）、**C 积分钳位对照**（imax=50 → 稳态钳在 410，全程未进 ±5% 带）——与 host 同款定点公式仿真预计算**逐值一致**，顺带实证了 et_pid 积分钳位的真机行为。顺带修复：demo 命令表计数手改 9u → `sizeof` 自适应。记录入实机记录 §10。
- **P3 `et_medfilt`（第 31 模块）**：奇数窗（3~15）中值滤波，偶数/超长窗 init 拒绝（消除"上/下中位"歧义），窗满前"下中位"语义文档化，小窗插入排序零分配。单测 10 例（单点尖峰抑制/阶跃延迟窗/2/窗满前后语义等）；API_GUIDE 3.5 含与 movavg 取舍对比表 + `medfilt → lpf1` 两级配方。
- **P4 `et_sched` 任务耗时统计（附则首次实战）**：`et_sched_task_stats/reset` 两 API，poll_once 内注入前后时基差（不引入新时基依赖，亚毫秒任务报 0）；**`et_task_t` 尾部追加 `last_ms`/`max_ms` = API_STABILITY §5.1 登记区第一条**：登记理由 + 破坏半径（调用方重编译即兼容、既有字段偏移不变）+ `--diff` 保持绿，三证齐全。单测 6 例。
- **P5 可选全做**：① kv 参数备份/恢复配方（纯文档，API_GUIDE 11.11）；② bench v2.2 行（medfilt push 11.0 / sched poll_once 5.0 ns/op）；③ CRC16-MODBUS 查表（`ET_CRC_TABLE` 第三表 512B，bench 142.9→500.0 MB/s，表路径与位算法 200 例随机对拍 + 标准向量双验证——查表构建下全量 398 用例亦全绿）。
- **过程自省**：MODBUS 查表首版把表放在了使用点之后（表构建编译失败即暴露），复排后与 ccitt 同构——位算法/查表双构建本地跑通后再合入的顺序值得沿用。

## 2. 逐项验收对照（v2.2 计划 §3）

| # | 任务 | 验收结果 |
|---|---|---|
| P0-1 | apidump 基线滚动 | ✅ v2.1 基线归档（374 项）；`--diff` 默认对 v2.1 基线 **0/0 绿**（发版前）/ 新增 10/0（本版交付态）；v2.0 归档保留；滚动规则入 API_STABILITY §5 |
| P0-2 | checklist 第 5 条 | ✅ 命名放宽"全角冒号或 `__` 均可"；docsync 自指断言本就以 glob 匹配（无冒号硬编码），自证不回归 |
| P0-3 | 版本宏 2.2.0 | ✅ `0x020200`；板面横幅 `Embedded_Tools v2.2.0 (0x20200)`；README/API_GUIDE/CHANGELOG 对齐 |
| P1 | selftest 17→20 | ✅ 三套件进表（门控式）；板上 **20/20**；smoke.sh/README/API_GUIDE/实机记录/port README 体积表/交付文档全链回刷（§4 清单） |
| P2 | 板上闭环 demo | ✅ 命令组 + 模拟对象上板；三组参数走单与 host 仿真逐值一致（§5）；只做真机记录未进 smoke 断言（政策沿袭）；实机记录新增 §10 |
| P3 | et_medfilt | ✅ 第 31 模块，10 例；init 校验（偶数/超长/NULL/过短）+ 手算中值序列 |
| P4 | sched 任务耗时统计 | ✅ 两 API + `et_task_t` 字段追加，附则人工登记完成 + `--diff` 绿 + 破坏半径声明（§3）；6 例；未触发降级条款 |
| P5 | 时间盒三项 | ✅ 全做：kv 备份配方（11.11）/ bench v2.2 行 / MODBUS 查表 |

## 3. 冻结证明与附则实战

`sh tools/apidump.sh --diff`（默认基线 `docs/API_INVENTORY_v2.1.md` ↔ 当前头文件）：

```
新增     10 项  (只追加 = OK)
修改/删除 0 项  (冻结面破坏 = 红)
apidump: OK —— 纯新增 (10 项, 修改/删除 0)
```

**diff 摘要：新增 10 项，修改 0 项。** 函数 7（`et_medfilt_*` ×5、`et_sched_task_stats*` ×2）；类型 1（`et_medfilt_t`）；宏 2（`ET_MODULE_MEDFILT`、`ET_MEDFILT_WIN_MAX`）。

**结构体字段追加登记区（--diff 附则要求）——本版 1 项（附则流程首次实战）**：

| 版本 | 结构体 | 追加字段 | 理由 | 破坏半径评估 |
|---|---|---|---|---|
| v2.2 | `et_task_t`(sys/et_sched.h) | `last_ms`/`max_ms`（尾部追加） | 任务耗时统计（P4）：板上诊断抖动/超时任务的第一手数据 | **调用方重编译即兼容**：字段在结构体**尾部**追加，既有字段偏移不变；实例均为调用方静态分配，库以源码分发无 ABI 边界；唯一可见影响 = `sizeof(et_task_t)` 增大 8B（32 位），静态数组/嵌入结构自动获得新大小；`--diff` 保持绿（类型名未变，apidump 不登记字段） |

登记区同时落档于 [docs/API_STABILITY.md](docs/API_STABILITY.md) §5.1（逐版累积）。清单口径：`docs/API_INVENTORY.md` 共 **391 项**（`--diff` 比较单元 384 项 = v2.1 基线 374 + 10）。

## 4. DoD 对照（v2.2 计划 §4）

| DoD | 状态 | 证据 |
|---|---|---|
| 1 沿用 v2.1 全部 DoD | ✅ | docsync **210/210**；398×双几何 + 1K 399 + test-tab 全绿；docbuild 原样执行 0 warning；sizecheck 3/3 |
| 2 **数字回刷全链**（selftest 17→20 每一处引用） | ✅ | ① `port/stm32f103/renode/smoke.sh` 断言 `SELFTEST: 20/20` ② README 4 处（特性表/平台段/质量门/CI 行）③ API_GUIDE 8.4（20 内建套件 + 覆盖边界数）④ 实机记录 §10 板上记录 ⑤ port README 体积表 selftest 行 29500/2232 ⑥ `test/test_selftest.c` 计数断言（20 内建/21 总）⑦ 本交付文档——docsync 断言 20/20 双向把关 |
| 3 基线滚动生效 | ✅ | `--diff` 默认对 v2.1；v2.0 归档只读保留（docsync 断言两基线文件在档） |
| 4 板上闭环记录 §10 + selftest 上板复验 | ✅ | 实机记录 §10：20/20 + 三组 PID 走单 + SELFSTOR + 升级链 confirm/rollback |
| 5 `et_task_t` 字段追加登记 | ✅ | §3 登记区 + API_STABILITY §5.1 + `--diff` 绿 + 破坏半径声明 |

## 5. 板上记录摘要（G474VET6_ET_TEST，全记录见实机记录 §10）

```
开机横幅  : Embedded_Tools v2.2.0 (0x20200)     （kv/bootctl 持久化跨固件替换保持, boot #8→#9）
AT+SELFTEST: 20/20 PASS                          （pid/stats/bytes 首次上板即 PASS; kv/bootctl SKIP）
AT+SELFSTOR: kv PASS + bootctl PASS
PID 走单(模拟对象 y += k(u-y), 10ms 节拍; 期望 = host 同款定点公式仿真):
  A 保守  PIDSET 500 32768 131072 0 13107       → peak=496 ov=0%  settle=1110ms（仿真 496/0%/1110 一致）
  B 激进  PIDSET 500 131072 262144 0 13107 1000 → peak=610 ov=22% settle=2000ms（仿真 610/22% 一致）
  C 钳位  PIDSET 500 131072 262144 0 13107 50   → peak=420 ov=0%  settle=2000ms
            （imax=50 → 积分权限钳死 → 输出上限 ~410 → 稳态误差 18%: 钳位法"积分权限"语义真机实证）
升级链回归: SIMUPGRADE 3 → CONFIRMED; 4 → self-check FAILED → 复位 → ROLLBACK: attempts exhausted
板侧构建  : 0 warning; FLASH 36088 B（v2.1 32100 → +3988 = 新模块被调用链入 + selftest 扩充 + PID 命令组, 预期激活）
```

## 6. 里程碑对照

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 | P0 基线滚动 + checklist 修正 + 2.2.0 | ✅ |
| M2 | P1 selftest 扩充 + 板同步 + smoke/数字全链回刷 | ✅ |
| M3 | P3 medfilt 合入 | ✅ |
| M4 | P2 板上闭环 demo + P4 时间盒（未降级） | ✅ |
| M5 | 全量回归 + 板面终验 | ✅（tag `v2.2` 待发布 checklist 执行） |

## 7. 机制自证记录（v2.2 新增/滚动的红绿双向验证）

| 机制 | 绿向 | 红向（故意破坏） |
|---|---|---|
| 基线滚动 `--diff` | 对 v2.1 基线 0/0（发版点）；对当前头文件新增 10/0 | 沿 v2.1 自证（故意改签名 → 1 增 1 删红）；滚动后规则与忽略集（`ET_VERSION*`）不变 |
| docsync 自指断言 | 交付文档行首声明"本版 docsync 210/210" → `ok`，`pass=210 fail=0` | 把该行计数手改 → `FAIL [自指] ≠ 实测` → exit 1；改回即绿（同 v2.1 记录） |
| MODBUS 表路径 | `-DET_CRC_TABLE=1` 构建下全量 398 用例全绿（向量断言过表路径） | ——（表生成脚本对位算法 200 例随机对拍一致） |

## 8. 挂账与后续

1. **tag `v2.2` 待执行**：本工作区未提交、未推送；发布 checklist 核对后打 tag，`release.yml` 注入 CHANGELOG 2.2.0 节。**Renode smoke 断言已同步 20/20，本机无 Renode——以发布 CI run 为准**（若 CI 红：断言行与 F103 demo selftest 输出格式核对，属两分钟级修复面）。
2. **v3.0 候选池不变**：`docs/v3-candidates.md` 四项维持"需求触发才评"；本版零破坏。
3. **Non-goals 维持**：无浮点 PID/统计、无 FOC/自动整定、无真实被控对象硬件接入、无自适应窗口/加权中值、无 i2c/spi/RTOS/动态内存（连续十版）。
4. **板侧 demo 即 API_GUIDE 11.10 的板上实例**：`et_demo.c`（工程私有，不入库）已与 11.10 互链；若后续要进库，归入 examples/ 增量评估。
5. **medfilt 未进 selftest**（沿 v2.1 决议模式）：host 10 例覆盖，selftest 计数维持在计划口径 20。

## 9. 量化数据复现（环境注记）

| 项 | 复现命令 | 结果 | 环境 |
|---|---|---|---|
| 单元全量 | `mingw32-make test` / `make test-g4` RESULT 行 | **398×2** 全绿；1K 变体（`-DET_XM_1K=1`）**399**；`make test-tab` 同套件数 | MinGW-w64 gcc **16.1.0** / Windows 11 Git Bash；CI ubuntu make 同绿 |
| 查表变体 | `gcc ... -DET_CRC_TABLE=1 ... && ./et_tests_tbl` | 全量 398 全绿（MODBUS 表路径过标准向量） | 同上 |
| 覆盖率 | `mingw32-make coverage-build` + `gcovr --print-summary` | **lines 97.0%（8122 / 8371）**，门 ≥85%；新模块 et_medfilt 94% / et_sched 96% / et_selftest 97% | gcovr **8.6** + MinGW gcc 16.1（`--coverage`） |
| docsync | `sh tools/docsync.sh` | **pass=210 fail=0**（含 apidump `--check` 与自指断言） | Git Bash |
| API 清单/冻结 | `sh tools/apidump.sh --check`；`sh tools/apidump.sh --diff` | 清单 391 项（比较单元 384）；**新增 10 / 修改删除 0** | 同上；脚本内锁 `LC_ALL=C` |
| ARM 体积 | `sh tools/docbuild.sh` + `arm-none-eabi-size`；`sh tools/sizecheck.sh` | F103 **19892**(默认)/**29500**(selftest)、G474 **20264**；sizecheck **3/3** | GNU Tools for STM32 **13.3.rel1**（STM32CubeCLT 1.18.0），`-Os -g` 零警告 |
| CubeMX 板侧 | `cmake --preset Release && cmake --build --preset Release`；`STM32_Programmer_CLI` + COM12 | 0 warning；FLASH **36088 B**；板上 20/20 + PID 三组走单 + 升级链双路径 | 同上工具链 + ST-LINK(SWD) + CH343 115200-8-N-1 |
| bench | `make bench` / `make bench-table` | `medfilt push` **11.0**、`sched poll_once` **5.0** ns/op；crc16-modbus 位 142.9 → 表 **500.0** MB/s | 同 v1.7 基线注记（Ryzen 9 7945HX / gcc 16.1 / `-O2`） |

---

> 维护约定：本文档随 v2.2 交付定稿；后续范围变更不再回改，走下一版计划。
