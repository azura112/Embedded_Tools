# v1.6 开发交付：G4 入库、双几何回归与 tickless

> 交付日期：2026-09-06 ｜ 版本：v1.6.0（`ET_VERSION = 0x010600`）｜ 基线：v1.5（tag `a88ebff`）
> 计划文档：[v1.6 开发计划：升级链真机闭环与双平台回归](./v1.6开发计划：升级链真机闭环与双平台回归.md)（状态：✅ 已交付，走单回填挂账见 §5）
> 远程：https://github.com/azura112/Embedded_Tools（main @ `94e7362`）

## 1. 重点概况（TL;DR）

v1.6 把"组件可用"收敛为"**链路可证**"：G4 成果正式入库，8B 槽适配的教训固化为**双几何回归制度**，并交付 tickless 休眠的库侧支撑：

- **P0 G4 入库**（`530bed1`）：8B 槽适配（et_kv/et_bootctl/测试×2/docsync/port.h）+ `port/stm32g474/` 裸机移植 + `stm32g474_demo` + G474 实机记录，共 9 改动 + 5 新增入库，CI 全绿；版本宏升至 **1.6.0**。
- **P2 双几何回归制度化**：`et_kv.h` 公开容量宏 **`ET_KV_VAL_MAX`**（容量经 API 计算），test_kv 三处几何硬编码用例化修正（其一为 2KB 几何下**栈越界段错误**），`make test-g4` 变体目标 + CI `cross-stm32g474` job（含 `#error` 守卫负向验证）。**291×2（F1/G4）双几何全绿**——本版即制度首次完整执行。
- **P2-2 tickless 增量 API**：`et_sched_next_due()` / `et_stimer_next_due()` + `PORT_TICK_WAIT_FOREVER`，语义=相对毫秒（0=已到期应立即 poll）、跨回绕无符号减法、ISR 启停后下次调用重算；**12 例单测**；API_GUIDE 新增配方 11.8 并把 **"WFI 前必须使能 RX 中断等唤醒源"** 写成必查项（G474 RX 丢字节教训的制度化，DoD #4）。
- **P1 升级链走单工具链**：`tools/xmodem_send.py`（参数化串口/文件/1K/verbose）+ `tools/xmodem_host_recv.c`，**host 端到端双块型闭环**（128B/1K，1000B 镜像逐字节一致）；板端新增 `AT+WDTEST`（IWDG 真超时演示）与开机复位原因日志；走单 5 条叙事 runbook 入实机记录 §5。
- **顺带修复**：G474 demo AT 命令计数 6u 未随表扩容——`AT+HELP` 一直不可达（恰在第 7 项），SELFTEST 在前 6 项内故未暴露。→ 8u。

## 2. 逐项验收对照

### P0 G4 成果入库（M1，`530bed1`）

| # | 任务 | 结果 |
|---|---|---|
| 1 | 提交工作区 G4 适配 + 推送 | ✅ 9 改动 + 5 新增；push `fd63543..94e7362`，CI run `34009520054` |
| 2 | port README 体积行 15468（适配后复测值） | ✅ 随 M1 入库，docsync 断言绿 |
| 3 | 版本宏 1.6.0 | ✅ `0x010600`，与 README/API_GUIDE 版本行三方一致（docsync 动态断言） |

### P2 双几何回归制度化（M3）

| # | 任务 | 结果 |
|---|---|---|
| 1 | host 几何变体双跑 | ✅ F1（1024×16 默认）与 G4（2048×16/40ms `-D`）各 **291 例 ALL PASS**；发现并修 3 处用例几何硬编码（见下），**未放宽任何产品代码** |
| 2 | CI G474 job | ✅ `cross-stm32g474`：`-mcpu=cortex-m4` + `-D` 三宏全量零警告编译 + **漏配负向验证**（无 `-D` 编译必须失败，否则 job 红） |
| 3 | Makefile/README/docsync | ✅ `make test-g4`；docsync 增 10 断言至 **59 条全绿** |

用例几何硬编码三例（"发现即修用例"实录）：

1. `kv.set_validation`：以 1 字节栈变量 + 长度 1001 触发拒绝路径——2KB 几何下 1001 < `ET_KV_VAL_MAX`(2024) 变成**合法写入，栈越界段崩溃**。改为 `ET_KV_VAL_MAX+1` + 真实缓冲；
2. `kv.overlong_rejected`：`v[1024]`/1000B"恰好填满整页"为 F1 专属语义 → `ET_KV_VAL_MAX` 相对化（G4 下 2032B 槽恰满页，页容量-页头-记录头的边界语义保持）；
3. `pcut.erase_half_sector`：30 条×24B=720B、`memset(mem+SZ/2, …, 736-SZ/2)` 在 2KB 下**长度为负**（size_t 巨值 memset）→ 记录数/篡改区间/校验循环全部按 `SZ/2` 相对化，掉电注入语义不变。

### P2-2 tickless 增量 API（M4）

| 项 | 结果 |
|---|---|
| API | `et_sched_next_due()` / `et_stimer_next_due()`：相对毫秒，0=已到期应立即 poll，空表/无运行者返回 `PORT_TICK_WAIT_FOREVER`（port.h 哨兵）；到期判定与各自 poll 同一算法（sched 无符号 elapsed / stimer int32 差值），**纯增量，不改既有语义** |
| 约束文档 | 两头文件 + API_GUIDE 均注明 **WFI 配对约束**（RX 中断=唤醒源，引实机记录问题 1 为反例）——DoD #4 |
| 单测 | **12 例全绿**：空表/单任务起算/多任务取最近/到期 0/注销重算/与 poll 重锚定一致/uint32 回绕 ×2（sched 6 + stimer 6） |
| 文档 | API_GUIDE §4.1/§4.2 API 表 + **配方 11.8**（tickless 主循环骨架 + RX 唤醒投喂流程） |

### P1 升级链真机闭环（工具与固件就绪，走单待上板）

| # | 项 | 结果 |
|---|---|---|
| 工具 | `tools/xmodem_send.py`：`--port/--baud/--file/--xmodem-1k/--emit/--timeout/--verbose` 全参数化；NAK/'C' 起步握手、CRC16-CCITT 高字节在前、EOT 两段确认，与 et_xmodem 协议逐项对齐 | ✅ 入库 |
| 自测 | host 端到端：`--emit` 字节流 → `tools/xmodem_host_recv.c` → et_xmodem 接收 → 比对。**128B 与 1K 双块型，1000B 随机镜像逐字节一致**（接收端含 0x1A 填充至块粒度，bootctl 按头 img_size 校验不受影响） | ✅ 2026-09-06 实跑 |
| 固件 | 板端（CubeMX 工程）：`AT+WDTEST`（IWDG 200ms 真超时）+ 开机复位原因日志（IWDG/SOFT/PIN，`__HAL_RCC_CLEAR_RESET_FLAGS`）| ✅ 零警告，Release 29196B |
| 走单 | 5 条叙事 runbook 入 `移植stm32实机记录.md` §5（回滚/confirm/xmodem 真传/IWDG/LED 按键） | ⏳ **待上板执行回填**（不阻塞代码交付，阻塞 v1.6 收口声明） |

### P3 可选（本版不做，如实记录）

| # | 项 | 状态 |
|---|---|---|
| 1 | `tools/bench.c` host 基准 | 未做（时间盒外，不阻塞发布；下版按需） |
| 2 | Renode G4 模型 | 未做（本机无 Renode；维持挂账） |
| 3 | F103 真机 | 维持常设挂账（按计划不再单独排期） |

## 3. 里程碑对照

| 里程碑 | 提交/Run | 内容 |
|---|---|---|
| M1 | `530bed1` | P0 G4 入库 + 实机记录 + v1.6 计划 |
| M2-M4 | `94e7362` | 双几何 + CI G474 + tickless + 升级链工具 + 版本 1.6.0 |
| M5 | 本次交付提交 | 交付文档 + 计划状态归档；**tag v1.6 挂账**（见 §5） |

## 4. DoD 对照（v1.6 §4）

| # | 条款 | 状态 |
|---|---|---|
| 1 | v1.5 全部 DoD（docsync 全绿、量化声明附复现命令与环境注记） | ✅ docsync **59/59**；本文档所有数字可复现（命令见 §6） |
| 2 | 双几何变体全绿为发布硬性项 | ✅ 291×2；制度化落 Makefile/CI/docsync 三处 |
| 3 | 升级链走单记录 + docsync 断言 + 脚本可复现 | ✅ 脚本与 runbook/锚点断言就绪；⏳ 走单日志待回填 |
| 4 | tickless 头文件注明 WFI/RX 唤醒配对约束 | ✅ et_sched.h / et_stimer.h / port.h / API_GUIDE 11.8 |
| 5 | CI G474 job 连续 3 run 绿 | ⏳ 第 1 run 进行中（`34009520054`），**tag v1.6 待 3 连绿后打** |

## 5. 挂账与后续

1. **升级链走单回填**（§2-P1 表 5 项）——板与上位机在手即可执行，回填后实机记录 §5 的结果表落数；
2. **tag v1.6**：待 CI `cross-stm32g474` 连续 3 run 绿（本批 push 为第 1 run）+ 走单回填后，按发布 checklist 走 `release.yml`；
3. P3-1 bench / P3-2 Renode G4：顺延下版按需。

## 6. 量化数据复现（环境注记）

| 声明 | 复现命令 | 环境 |
|---|---|---|
| host 291 例 × 2 几何 ALL PASS | `make test` 与 `make test-g4`（或 Makefile 注释中纯 gcc 命令 + `-D` 三宏） | MinGW gcc **16.1.0**（x86_64-win32-seh）/ Git Bash |
| ARM 零警告双平台 | F103：README 构建命令（text **15576**）；G474：port/stm32g474/README 构建命令（text **15944**），`arm-none-eabi-size` 读数 | GNU Tools for STM32 **13.3.rel1**（STM32CubeCLT 1.18.0），`-Wall -Wextra -pedantic` 零输出 |
| 板端固件（CubeMX 集成，含自测+WDTEST） | `cmake --preset Release && cmake --build build/Release --clean-first`（text **29196**） | 同上工具链 + CMake/Ninja |
| docsync | `sh tools/docsync.sh` → **59/59** | Git Bash |
| xmodem 端到端 | `python tools/xmodem_send.py --emit --file 镜像 > stream` → `build/xmrecv out < stream` → `head -c <镜像大小> out \| cmp - 镜像`（1K 型加 `--xmodem-1k` 与 `-DET_XM_1K=1`） | Python **3.12.4**（无第三方依赖；串口模式需 pyserial） |
